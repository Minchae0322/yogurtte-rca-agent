# T3-E 게스트 스와이프 폭주 테스트

> **결론:** 300 VU 무인증 스와이프에서 실패율 1.61%·p99 1.26초로, 같은 행 update 경합이 락(T3-D)보다 훨씬 완만하게 나타났다. Case D 후보였으나 실측상 병목은 약했다.

| 구분 | 내용 |
|---|---|
| 상태 | 실측 완료 · 2026-08-18 |
| 상황 | 비로그인 사용자의 스와이프 투표 몰림 |
| 부하 | 최대 300 VU · 4.5분 · gid 쿠키 게스트 |
| 대상 | `POST /battles/{id}/swipe` (같은 행 update 경합) |
| 판정 | **약한 경합** — 임계 소폭 초과, 붕괴는 아님 |

## 시험 설계

배틀 22(스와이프 아이템 6개)를 대상으로 gid 쿠키 게스트가 `POST battles/{id}/swipe`를
연속 호출했다. 좁은 아이템 집합에 300 VU가 몰려 같은 행 update 경합을 유도했다.

## 핵심 결과

| 항목 | 실측 | 임계값 |
|---|---:|---:|
| 처리량 | 502 rps (135,851 요청) | - |
| p95 / p99 | 993ms / **1.26s** | p99 <1000ms (소폭 초과) |
| 실패율 | **1.61%** (2,195건) | <5% (통과) |
| 중앙값 | 483ms | - |

## 해석과 다음 조치

T3-D(리액션 락)가 처리량을 3배 떨어뜨린 것에 비하면, 스와이프의 같은 행 경합은 훨씬
완만하다 - 502 rps를 소화하며 실패는 1.6%에 그쳤다. 경합 경로이되 락 획득이 짧거나
행 분산(아이템 6개)이 어느 정도 작동한 것으로 보인다.

미측정: 실패 2,195건의 유형(409 conflict vs deadlock vs 5xx)은 상태코드 태그로 분해하지
못했다. deadlock이면 T3-D와 같은 락 계열 개선 대상이고, 단순 409면 정상 경합 처리다 -
다음 실행에서 분해한다. 서버측 정점 지표(DB 락·풀)는 이 시험에서 동시 관측하지 못했다.

원본: [output](2026-08-18-t3e-output.txt) · [summary](2026-08-18-t3e-summary.json)

---

## 재실행 (2026-08-23 밤 · 2차 diet 구성 · nginx 직결) - 875 rps에 실패 소멸 - 1차의 1.61%는 경합이 아니라 용량 문제였다

> **결론:** **875 rps(1차 502)** 를 소화하며 실패 **2건(0.001%, 1차 2,195건·1.61%)** ·
> med 159ms(483) · p99 1.45s(1.26). 1차에서 미분해로 남겼던 실패 2,195건의 정체가
> 간접 판정됐다 - 경합(deadlock/409)이었다면 처리량이 1.74배로 늘었을 때 실패도 늘었어야
> 하는데 **소멸**했다. 즉 1차 실패는 행 경합이 아니라 당시 용량 한계(CPU·CF 타임아웃)의
> 꼬리였다. "약한 경합" 판정은 "**측정 가능한 경합 없음**"으로 강화.

### 결과 비교

| 항목 | 1차 | **2차** |
|---|---|---|
| 처리량 | 502 rps (135,851) | **875 rps (236,571)** |
| med / p95 / p99 | 483 / 993 / 1,260ms | **159 / 912 / 1,450ms** |
| 실패 | 1.61% (2,195건, 유형 미분해) | **0.001% (2건)** |
| content CPU / 노드 | (미관측) | **합 4.3코어 · 노드 68~79% (여유)** |

같은 행 update 경합 경로가 875 rps에서도 실패 없이 돈다 - 스와이프의 행 분산(아이템 6개)과
짧은 락 점유가 이 처리량에서도 유효함이 실측됐다. p99 1.45s 임계(1s) 소폭 초과는 잔존.

원본: [output](2026-08-23-t3e-diet-output.txt) · [summary](2026-08-23-t3e-diet-summary.json) · [timestamps](2026-08-23-t3e-diet-timestamps.txt)
