# T3-F 서비스 간 전파 테스트

> **결론:** auth가 97% 실패로 붕괴하는 동안 content의 피드 조회는 실패 0%·p99 1.37초로 버텼다. 3초 타임아웃과 익명 폴백이 실제로 작동해 장애 전파를 차단했다.

| 구분 | 내용 |
|---|---|
| 상태 | 실측 완료 · 2026-08-18 |
| 상황 | auth 포화가 content로 전파되는가 (MSA 장애 격리) |
| 부하 | auth 300 VU(폭주) + content 50 VU(읽기) 동시 · 4분 |
| 검증 대상 | content→auth 호출의 3초 타임아웃 · 익명 폴백 |
| 판정 | **격리 성공** — 폴백이 전파를 막음 |

## 시험 설계

auth를 300 VU로 포화시키는 동안 content 읽기(`GET /feeds/scroll`)를 50 VU로 병행했다.
content는 피드 작성자 정보를 auth에서 가져오는데, 실패 시 예외 대신 익명으로 폴백하고
별도 카운터를 올린다. 이 폴백이 정상 상태에서는 캐시 히트로 발동하지 않으므로,
auth가 느릴 때만 경로가 드러난다.

## 핵심 결과

| 항목 | 실측 | 판정 |
|---|---:|---|
| auth 전체 실패율 | **97.2%** (585,913 / 602,787) | auth 자체는 붕괴 |
| GET /feeds/scroll 실패율 | **0.00%** (0 / 16,640) | content 격리 유지 |
| GET /feeds/scroll p99 | 1.37s | 임계 <3s 통과 |
| "3초 타임아웃 미만" 체크 | 100% | 통과 |
| "scroll 200 (폴백 포함)" 체크 | 100% | 통과 |

## 서버 관측 — 전파의 흔적은 있었다

정점(auth 300 VU) 동시 스냅샷: auth CPU 0.57 · **auth 풀 pending 141** 로 포화되는 동안,
content에도 **풀 pending이 0 → 3으로 번졌다** (content CPU는 0.28~0.40으로 여유).
즉 auth 지연이 content→auth 호출 경로를 통해 content 커넥션을 잠깐 붙잡는 전파가
실측됐으나, **3초 타임아웃 + 익명 폴백이 그 이상 번지는 것을 차단**했다.

## 해석과 다음 조치

**장애 격리 설계가 실제로 작동함을 실증했다** - "auth가 죽어도 content가 버티도록"이라는
의도가 auth 97% 붕괴라는 극한에서도 content 실패 0%로 성립했다. 서사상 이 결과는
"모든 병목을 스케일링으로 푸는 게 아니라, 이미 구조로 막아둔 것도 있다"는 대조 사례다.

미측정: 폴백 카운터(익명 대체 발생 건수)의 절대값은 Mimir에서 확인하지 못했다 -
전파의 강도(몇 %가 폴백을 탔는지)를 수치화하려면 그 카운터가 필요하다. content pending
3은 전파가 "감지될 정도"였음을 보여주지만 "얼마나"는 다음 실행 과제.

원본: [output](2026-08-18-t3f-output.txt) · [summary](2026-08-18-t3f-summary.json)
