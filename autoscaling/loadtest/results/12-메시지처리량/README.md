# WS-B 메시지 처리량 + Kafka Consumer 테스트 (단일 방 + 다중 방 대조군)

> **결론:** 200 연결에 초당 ~500건을 흘리자 chat CPU가 0.99로 포화하고 신규 연결이 62% 실패했다. 저장 컨슈머(db-writer)의 lag이 2만 건 넘게 쌓였고, 부하 종료 후에도 줄지 않았다. 1차 판정은 단일 파티션 hot-spotting이었으나, **다중 방 대조군(08-19)이 이를 절반만 확증했다** — lag 분산은 실제로 일어났지만(9/12 파티션, 최대 점유 23%) 총 소비 속도는 1~5 msg/s 그대로였다. **병렬성의 상한은 파티션이 아니라 chat 파드 CPU였다** (db-writer가 chat 파드 안에서 돌아 팬아웃과 CPU를 나눠 쓴다).

| 구분 | 내용 |
|---|---|
| 상태 | 실측 완료 · 단일 방 2026-08-18 · **다중 방 대조군 2026-08-19** |
| 목적 | 연결 아닌 **메시지 처리량**의 한계 + CPU vs Kafka lag 관계 → 대조군: 파티션 가설 검증 |
| 부하 | 200 WS 연결 고정 · 메시지 간격 400ms (목표 ~500 msg/s) · 6분 (두 실행 동일) |
| 판정 | **메시지 팬아웃 = chat의 진짜 병목** · hot-spot은 실재하나 **소비 제약의 상류는 CPU** |

## 시험 설계

T3-J(연결 1,000개에 CPU 31%)와 T2-C(200연결·5초 간격)의 중간을 메운다. 연결 수는 200으로
고정하고 **메시지 전송 간격만 400ms로 낮춰**(c-chat-ws.js의 `MSG_INTERVAL_MS` 신규 파라미터)
메시지 처리량을 끌어올렸다. chat이 메시지를 Kafka `chat.messages`로 보내면 `db-writer`
컨슈머 그룹이 소비해 저장하므로, 같은 부하로 Kafka Consumer 시험도 겸한다.

## 핵심 결과 — 클라이언트

| 항목 | 실측 | 판정 |
|---|---:|---|
| ws 101 | 100% | 통과 |
| **STOMP CONNECTED** | **38% (343/897)** | 실패 62% - 메시지 부하 중 신규 연결 밀림 |
| ws_connecting p95 | 199ms (max 2.49s) | 꼬리 악화 |
| 메시지 송신 | 30,419 (77/s) | 목표 500엔 못 미침 - 서버가 못 받아서 |
| 메시지 수신 | 42,479 (107/s) | |

## 핵심 결과 — 서버 (유지 구간 정점)

| 지표 | 값 | 해석 |
|---|---:|---|
| chat CPU | **0.99 (포화)** | 메시지 팬아웃이 천장. 연결(T3-J 31%)과 정반대 |
| chat 파드 CPU | 297~299m | limit 근처 |
| **db-writer Kafka lag** | **9,030 → 20,404 → 25,999 (계속 증가)** | 저장이 유입을 못 따라감 |
| chat threads | 193 | |

## 원인 분석 — 단일 파티션 hot-spotting

부하 종료 후 5분이 지나도 lag이 줄기는커녕 26,001까지 늘었다. 파티션별로 보니
**전량이 `chat.messages` 파티션 6번 하나에 몰려 있었다** (다른 파티션 정상).

원인: 시험이 **한 채팅방(roomId 1개)** 에만 메시지를 보냈고, Kafka 파티션 키가 roomId라
전부 같은 파티션으로 라우팅됐다. 파티션 하나 = 컨슈머 스레드 하나이므로, chat이 CPU를
더 써도 그 파티션의 소비 속도를 못 올린다. **파티션 단위 병렬성이 무력화된 것.**

## 해석과 다음 조치

- **메시지 처리량이 chat의 진짜 병목**임이 확정됐다 (연결 아님 - T3-J와의 대비).
- **Kafka Case 판정**: CPU 0.99 + lag 증가 = 설계의 **Case A** (CPU도 lag도 높음).
  단순 CPU 기반 확장으로 consumer를 늘리는 게 방향이지만 - **파티션 수가 병렬성 상한**이라
  파티션 6에 몰린 이상 consumer replica를 늘려도 그 파티션은 한 컨슈머가 잡는다.
  즉 KEDA/HPA 이전에 **파티셔닝 키·파티션 수 설계가 선행**돼야 한다는 것이 이 시험의 교훈.
- 이 발견은 서사 Chapter 3(KEDA)의 전제를 바꾼다: "CPU vs lag"만이 아니라
  "파티션 병렬성"이 이벤트 스케일링의 진짜 축이다.

미측정: lag 완전 소진 시간(회복 곡선)은 부하 종료 후에도 CPU가 포화라 관측 창 안에
안 끝났다 - 백그라운드 소진 중. 여러 방으로 분산한 재시험이 파티션 가설을 확증한다.

---

## 다중 방 대조군 (2026-08-19 23:19~23:26 KST) - 파티션 가설의 절반만 살아남았다

### 설계 - 팬아웃을 통제 변수로 고정한다

방을 나누면 방당 구독자도 줄어 **팬아웃 부하(CPU)와 파티션 분산이 동시에 변한다.**
그래서 스크립트를 "각 VU가 전 방(20개)을 구독하되 전송은 자기 방으로만"으로 짰다
(c-chat-ws.js `ROOM_COUNT` 신설) - 메시지당 수신자 수는 단일 방과 동일하게 유지되고
바뀌는 것은 Kafka 파티션 키(roomId)의 분산뿐이다. 나머지 조건은 08-18과 동일
(200연결·400ms·6분). 사전 확인: 이전 lag 2.6만 전량 소진 · `chat.messages` 파티션 12개.

### 결과 - 두 실행 대조

| 지표 | 단일 방 (08-18) | 다중 방 20개 (08-19) |
|---|---|---|
| 송신 (통제 확인) | 30,419 (77/s) | 29,539 (74/s) - 동등 |
| chat CPU (통제 확인) | 0.99 포화 | **1.00 포화 - 동등, 통제 성립** |
| **lag 분포** | **파티션 6 하나에 전량 (100%)** | **9/12개 분산 · 최대 파티션 점유 23%** |
| lag 정점 | 26,001 (종료 후에도 증가) | 13,077+ (종료 8분 후에도 증가) |
| **db-writer 소비 속도** | (미계측) | **1~5 msg/s** (유입 74/s의 3~7%) |
| STOMP CONNECTED | 38% | 32% (재현) |
| 메시지 수신 | 42,479 (이론치의 7%) | **277 (사실상 0)** - 아래 미해명 |

### 판정 - hot-spot은 실재, 그러나 소비 제약의 상류는 CPU

1. **파티션 hot-spot 가설 자체는 확증됐다.** roomId 분산만으로 lag이 파티션 하나(100%)에서
   9개(최대 23%)로 퍼졌다 - 파티션 키 설계가 lag 분포를 지배한다는 것은 사실.
2. **그러나 "파티셔닝이 선행"이라는 1차 결론의 처방은 반증됐다.** 파티션 병렬성이 9배로
   열렸는데 총 소비 속도는 1~5 msg/s 그대로다. 이유는 배치 구조다 - **db-writer는 별도
   배포가 아니라 chat 파드 안의 컨슈머 스레드**이고(클러스터 배포 3개: auth·content·chat 실측),
   그 파드 CPU를 팬아웃이 다 먹는다(1.00). 파티션을 아무리 나눠도 소비할 CPU가 없다.
3. **결론 개정: 순서는 파티셔닝 → 스케일링이 아니라 CPU 분리 → 파티셔닝 → 스케일링.**
   저장 컨슈머를 chat 파드에서 분리(또는 chat CPU 확보)하지 않으면 KEDA로 무엇을 늘려도
   같은 CPU를 나눠 먹는다. [11-커넥션풀](../11-커넥션풀/README.md)과 동형의 발견 -
   풀/파티션 지표는 하류 증상이고 상류는 CPU다. 서사 Chapter 3 전제 2차 수정.
4. lag이 종료 8분 후에도 증가(9,133 → 13,077)한 것은 chat 프로듀서 측 백로그가 계속
   flush되고 있다는 뜻 - 종료 후에도 CPU 1.00이 유지된 것과 부합한다.

미해명 1건: 메시지 수신이 277건(단일 방 42,479 대비 사실상 0)으로 오히려 악화됐다.
가설은 ① 세션당 구독 20개 등록이 브로커 팬아웃 경로를 추가로 태움 ② CPU 포화 심화로
배달 스레드 기아. 판정에는 영향 없음(팬아웃 붕괴 방향은 동일)이나 별도 재현 필요.

원본: [output](2026-08-18-wsb-output.txt) · [summary](2026-08-18-wsb-summary.json) ·
대조군 [output](2026-08-19-wsb-multiroom-output.txt) · [summary](2026-08-19-wsb-multiroom-summary.json) ·
[timestamps](2026-08-19-wsb-multiroom-timestamps.txt)

---

## 재해석 (2026-08-21 · MEM-3 폭주 검증 창에서 발견) - 이 문서의 결론에 큰 별표가 붙는다

> **발견:** WS-B 전 실행의 저장 소비 바닥(1~5 msg/s)·lag 적체는 CPU도 파티션도 아니라
> **시험 설계 결함**이 만든 것이었다. 시험이 쓰는 방 `loadtest-room`은 실존하지 않고,
> db-writer는 저장(insert) 성공 후 `updateLastMessage`에서 `CHAT_ROOM_NOT_FOUND`로
> **전량 예외 → retry 토픽 루프**를 돌았다 (`ChatRoomService.java:80` orElseThrow 실측,
> retry-1000 소비가 본토픽과 1:1 동률·Mongo insert 카운터와 로그로 확인).

기전: 본토픽에서 saveMessage 성공 → updateLastMessage 예외 → retry-1000 발행 →
1초 백오프 뒤 retry에서 existsByMessageId 중복 감지 → 조기 return = "성공".
메시지당 예외 1회 + 토픽 홉 1회 + 파티션 직렬 = 소비 ~2 msg/s 고정.
**chat CPU 0.28/1.0(여유) 상태에서도 소비 1.9/s** - "소비 상한 = chat CPU" 결론이 반증됐다.

| 기존 결론 | 재해석 |
|---|---|
| 단일 파티션 hot-spot이 소비 병목 | 분산 확증은 유효하나, 소비 바닥의 원인 아님 |
| CPU 분리가 소비의 선행 조건 | **반증** - CPU 여유에서도 동일. 원인은 실패 루프 |
| lag 2.6만·"백그라운드 소진 중" | poison 시나리오 측정값. **데이터 유실은 없음**(insert는 본토픽에서 성공) |

**MEM-3 창의 확정 사실** (chat limit 1000m·힙 384 첫 폭주):

- **STOMP CONNECTED 38% → 100%** · 핸드셰이크 p99 28.8s → **152ms** - 연결 축은 CPU-2가 해소
- chat 정점 CPU 1.0(limit 도달) · **힙 237/384Mi·워킹셋 700/896Mi - MEM-3 통과**
- 부수 발견: **운영 3서비스 profile=dev** - chat이 메시지당 Mongo DEBUG 로그 수 KB
  (CPU 포화 지분 의심, 프로파일 정리 = 공짜 CPU 후보)

다음: **실존 방(chat_rooms+participants Mongo 직접 생성)으로 재시험**해야 진짜 소비 상한과
팬아웃 한계가 나온다. 그 전까지 이 문서의 처리량 수치는 인용하지 않는다.
원본: [mem3 output](2026-08-21-wsb-mem3-output.txt) · [timestamps](2026-08-21-wsb-mem3-timestamps.txt)

---

## 재실행 (2026-08-23 밤 · 2차 diet 구성 · nginx 직결) - 시험 불성립, 대신 실장애를 만들었다: WS 메모리 계단 → 코어 노드 사망

> **결론:** 메시지 처리량 측정은 **불성립**이다 - 12만 연결 시도 중 ws 101 성공 836뿐
> (99.3% 거부), 세션 수명 med 28ms. 그런데 이 실패가 이번 배치 최대의 발견이 됐다:
> **WS 시험마다 chat 워킹셋이 계단식으로 쌓이다가(T2-C +84Mi → T3-J +58Mi → WS-B에서
> 809Mi 도달) 코어 노드 메모리를 고갈시켜 NodeNotReady - auth·chat 실서비스 전체가
> 다운**됐다. 1차 결론 "chat의 병목 = 메시지 팬아웃 CPU"에 새 상위 항목이 생겼다:
> **그 전에 WS off-heap 메모리가 노드를 죽인다.**

### 무슨 일이 일어났나 - 타임라인 (전부 실측)

| 시각 (KST) | 관측 | 출처 |
|---|---|---|
| 22:10~16 (T2-C) | chat 워킹셋 611 → 695Mi (**+84Mi**, 종료 후 미반납) | ktop 60s |
| 22:37~44 (T3-J) | 692 → 750Mi (**+58Mi**, 미반납) | ktop |
| 22:56 (WS-B 시작) | 초반 연결 836개만 성립, 이후 **전량 거부**(101 아님 응답) | k6 |
| 22:56~23:02 | k6 재연결 폭풍 **122,486 세션 시도** (세션 med 28ms) · 서버 chat CPU 33m = 부하가 chat에 닿지도 않음 | k6+ktop |
| ~23:0x | chat RSS **809MB** 실측(Xmx 384 - off-heap 부풀음) · 노드 available **72MB** · load 9.6 | node 직접 접속 |
| 23:0x | worker1 **NodeNotReady** → auth·chat 엔드포인트 제거 → **nginx 503 = 실서비스 다운** | kubectl events |
| 23:0x~23:4x | kubelet 기아로 파드 삭제 교착 → **노드에서 chat JVM 직접 kill** → 메모리 512MB 회복 → auth 엔드포인트 복귀(로그인 200/153ms) → chat 새 파드 ready | 수동 복구 |

거부의 기전: chat 워킹셋 팽창 → 노드 메모리 압박 → readiness probe 타임아웃 →
엔드포인트 이탈 → nginx가 ws 업그레이드에 503 → k6가 즉시 재시도(백오프 없음) →
초당 수백 연결 시도가 다시 nginx·노드를 두들김 → kubelet까지 아사. **관측된 "12만 세션"은
부하가 아니라 장애 증폭 루프의 산물**이다.

### 1차와의 비교 - 실패 모드가 다르다

| | 1차 (08-18 · CF) | MEM-3 재실행 (08-21) | **2차 (08-23 · 직결)** |
|---|---|---|---|
| STOMP CONNECTED | 38% (343/897) | 100% (899/899) | **0.3% (325/122,157)** |
| 실패 기전 | chat CPU 0.99 포화로 연결 밀림 | 통과 | **메모리 → probe 이탈 → nginx 503** |
| Kafka lag | 2.6만 적체 | - | **측정 불가** (수집 정지) |

### 구조적 원인 셋 (재발 방지 대상)

1. **WS off-heap 무상한**: Xmx 384인데 RSS 809MB - 세션 버퍼·다이렉트 버퍼가 힙 밖에서
   자라고 세션 해제 후에도 OS에 반납되지 않는다(계단식 단조 증가 실측). `MaxDirectMemorySize`
   등 상한 미설정.
2. **코어 노드 메모리 오버커밋**: auth 768 + chat 896 = limit 합 1,664Mi vs 노드 1,906MB -
   시스템 몫을 치면 limit을 안 넘고도 노드가 먼저 죽는 배치. chat RSS 809MB는 자기 limit 안이었다.
3. **kubeReserved 미설정**: kubelet·시스템 몫 예약이 없어 마지막 100MB까지 파드가 먹을 수
   있고, 그 순간 복구 수단(kubelet)이 함께 죽는다 - 파드 삭제 교착의 원인.

### 관측 유실 (정직 기록)

- chat 파드 로그: 복구 과정에서 파드 재시작으로 유실 - 거부가 nginx 503인지 chat 자체
  거절인지의 최종 확정은 불가 (엔드포인트 이탈 실측으로 nginx 503이 유력).
- Kafka lag·chat 앱 메트릭: 같은 시간대 관측 노드(t3.micro)도 죽어 Mimir 수집 정지.
  이 관측 노드 사망은 별개 사건으로 [요약](../2026-08-23-2회차-diet-요약.md)에 기록.

### 다음 회차로 넘기는 것

WS-B 재시험의 선행 조건: ① chat off-heap 상한 설정 ② 코어 노드 limit 재배치(오버커밋
해소) 또는 chat 전용 노드 ③ kubeReserved ④ k6 WS 재연결 백오프 ⑤ 시험 전 chat 재시작
(워킹셋 계단 리셋) - 이것 없이 돌리면 측정이 아니라 장애 재현이 된다.

원본: [output](2026-08-23-wsb-diet-output.txt) · [summary](2026-08-23-wsb-diet-summary.json) · [timestamps](2026-08-23-wsb-diet-timestamps.txt)

## 모델 정렬 회차 (2026-08-31 00:26~00:45 KST) - 진짜 방에서의 발화: 두 티어 모두 SLO 통과, 그리고 "배달 0"의 해부

> **결론:** 부하모델-chat.md의 발화 모델 두 티어(평시 2 msg/s·스파이크 10 msg/s)를
> **실제 1:1 방** 위에서 시험해 모두 통과 - 배달 지연 **평시 med 33ms/p95 60ms ·
> 스파이크 med 30ms/p95 47ms/max 526ms**, 체크 100%, SLO(배달 p99<1s) 여유 충족.
> 전제 작업이 본편이었다: 기존 c-chat-ws의 가상 방(loadtest-room-N)은 **배달 0건**이
> 됐는데, 원인은 chat 재배포에 실린 relay의 참여자 검증
> (`roomMemberIds.isEmpty() → return`, log.debug라 무음)이었다. 프레임 덤프 →
> Kafka 오프셋 증분 실험(producer 생존 확인) → consumer lag 0 확인 → 소스 추적으로
> 특정했고, **setup에서 REST로 실제 방을 만들어 시험하는 [ws-chat-real.js](../../ws-chat-real.js)를
> 신설**했다. 시험이 저장(Mongo)·참여자 조회·발신자 조회까지 실경로 전체를 타게 된 부수 개선.

### 조건

[ws-chat-real.js](../../ws-chat-real.js) · 직결 경로 · 인그레스 한시 상향 후 원복 ·
1:1 방 setup 생성(POST /v1/chat/rooms/chat, participantIds는 JWT userId 클레임에서 추출) ·
세션 60s 순환 · 배달 지연은 **상대 세션 수신만** 집계(자기 에코 제외).

| 티어 | 구성 | 목표 인입 | 실측 |
|---|---|---|---|
| 평시 (읽기 83rps 시간대) | 30쌍·60세션·간격 30s | 2 msg/s | 배달 292건 · **med 33ms · p95 60ms · max 158ms** · 체크 100% |
| 스파이크 (읽기 400rps 시간대) | 150쌍·300세션·간격 30s | 10 msg/s | 배달 3,109건 · **med 30ms · p95 47ms · max 526ms** · 체크 2,698/2,698 |

### 서버 (ktop 60s)

chat CPU: 평시 ~80-110m · 스파이크 최대 **459m** (300세션 핸드셰이크+발화) - CPU limit
1000m의 절반 이하. 워킹셋 **778Mi(87%/896)** - 단 이 값은 직전 1,250 연결 시험의
미회수분(730Mi) 위에 얹힌 것이라 발화 시험 자체의 몫은 작다. 워킹셋 미회수 추세는
09-WS연결폭주 관찰 항목과 동일 건. (Mimir 소급은 obs 노드 재고착으로 불가 - ktop만.)

### 판정

1. **발화 모델 두 티어 모두 현재 구성(1 replica)이 SLO 여유로 수용** - 모델치가 기존
   WS-B 한계 실측의 1/50 수준이라는 예측과 정합.
2. **"저부하인데 배달 p99>1s면 경로 결함" 반증 조건은 발동 안 함** - 대신 그 전 단계에서
   "배달 0"이라는 더 근본적인 경로 결함(가상 방 스킵)을 발견·해부·우회했다. 기존
   c-chat-ws 기반 회차와의 비교 시 이 스크립트 차이를 명시할 것.
3. 부하시험 인프라 교훈: 배포가 시험 스크립트의 전제(멤버십 무검증)를 소리 없이
   무효화할 수 있다 - 시험 자산도 앱과 함께 버전 관리가 필요하다.

원본: 평시 [output](2026-08-31-wsmodel-normal-output.txt) · [summary](2026-08-31-wsmodel-normal-summary.json) ·
[timestamps](2026-08-31-wsmodel-normal-timestamps.txt) / 스파이크 [output](2026-08-31-wsmodel-spike-output.txt) ·
[summary](2026-08-31-wsmodel-spike-summary.json) · [timestamps](2026-08-31-wsmodel-spike-timestamps.txt) /
[ktop](2026-08-31-wsmodel-ktop.txt) · 폐기: 가상 방 1차(2026-08-30-wsmodel-normal-*, 배달 0 - 해부의 증거로 보존)

## 발화량 한계 탐색 (2026-08-31 23:26~23:42 KST) - 최대 인입 ~50 msg/s, 벽은 chat CPU 1코어

> **결론:** 연결 200세션(1:1 방 100개·팬아웃 2)을 고정하고 발화율만 계단식으로 올려
> 한계를 특정했다 - **인입 50 msg/s가 SLO(배달 p99<1s) 경계**이고, 70에서 붕괴,
> 100에서 완전 붕괴(배달 중앙값 35초). 병목은 **chat 파드 CPU limit 1000m**으로,
> 전 계단에서 900~985m 포화가 실측됐다. 연결 축(3,000 통과·한계 미도달)과 달리
> 발화 축은 명확한 벽이 있다.

### 계단 실측 (각 45s 램프 + 2m 유지)

| 인입 목표 | 배달 med | p90 | p95 | max | 판정 |
|---:|---:|---:|---:|---:|---|
| **10** (모델 스파이크, 08-31 앞선 회차) | 30ms | 37ms | 47ms | 526ms | 통과 - 여유 |
| **50** | **83ms** | 566ms | **1.02s** | 3.4s | **경계** - p95가 SLO 1s에 접촉 |
| **70** | 1.65s | 16.1s | 21.2s | 34.1s | **붕괴** - 대기열 누적 시작 |
| **100** | 34.8s | 73.3s | 78.4s | 123s | 완전 붕괴 |

전 계단에서 연결·STOMP 체크는 100%(1,000/1,000) - **끊기지 않고 느려지는** 형태의 붕괴다.
배달 지연은 k6 수신 큐 대기를 포함하므로 서버 지연의 상한값으로 읽는다.

### 병목 - chat CPU 포화 (ktop 30s)

| 구간 | chat CPU | 워킹셋 |
|---|---:|---:|
| 50~100 msg/s 전 구간 | **864~985m / limit 1000m** | 793~800Mi |

요청당 비용 환산: 50 msg/s 인입 + 100 msg/s 배달을 ~0.95코어로 처리 = **인입 1건당 약 19ms
CPU**. 메시지 하나가 Mongo 저장 + 참여자 조회 + 발신자 정보 조회 + Kafka 왕복 + STOMP
팬아웃을 거치므로 읽기 경로(12.6ms/요청)보다 비싸다.

### 모델 대비 여유와 해석

- 부하모델-chat.md의 평시 인입은 **2 msg/s**, 스파이크는 **10 msg/s** - 한계 50 대비
  각각 **25배 · 5배 여유**.
- 역산하면 50 msg/s는 활성 채터 1,500명(분당 2건)에 해당하고, 모델의 활성률 5%를 적용하면
  **동접 3만 명 상당**이다. 반면 연결 축은 3,000에서 아직 한계 미도달 - 즉 이 서비스의
  현실 조합에서는 **연결이 먼저 걸리고 발화는 한참 여유**다.
- 발화 한계를 올리려면 CPU limit 상향(1000m → 노드 여유만큼) 또는 replica 확장이지만,
  모델상 필요가 없으므로 **현재는 조치 대상이 아니다**. 필요 신호는 "활성률 5% 가정의 상향"
  또는 "동접 1만 이상"이다.

### 부수 - obs 노드 3차 사망, limit 처방은 부분 성공에 그쳤다

같은 시험 창(23:37경)에 obs 노드가 또 NotReady. 직전에 적용한 limit 총합 축소
(1,180 → 640Mi)로 노드 메모리는 **74% → 70%(641~665Mi)** 로 내려갔고 **컨테이너 OOMKill은
0건**이었지만, 노드는 여전히 죽었다. 실측:

| 지표 | 값 | 판독 |
|---|---|---|
| 크레딧 잔고 | 97.6 → 98.4 (상승) | 크레딧 아님 (3회 연속 반증) |
| CPU 사용률 | 4~11% | CPU 아님 |
| 노드 메모리 | 641~665Mi / 914 (70%) | limit은 지켜졌다 |
| **EBS ReadOps** | 5분당 3.2만 → **11.7만 (390 IOPS)** | **페이지 캐시 스래싱 지속** |

해석: 파드 실사용(~420Mi)에 OS·k3s(~230Mi)를 더하면 665Mi이고, 남는 250Mi로는 **Alloy 3개
인스턴스의 거대한 Go 바이너리 텍스트 페이지**를 캐시하기에 부족하다. 커널이 실행 코드를
축출·재적재하며 시스템이 디스크 속도로 떨어진다. **limit은 컨테이너 폭주를 막지만
"상주 워킹셋 + 페이지 캐시"가 물리 메모리를 넘는 구조 자체는 못 고친다.**

→ 남은 수단: ① t3.small(2GB) 승격(+$3/월) ② 트레이스 수집 중단/샘플링 축소로 **Alloy
인스턴스 수를 3→2로** 줄이기(receiver 제거) ③ 현상 유지 + 시험 때마다 리부트.
`kubectl top`의 70%는 "여유 있음"이 아니었다는 것이 이 회차의 교훈이다.

원본: [50](2026-08-31-msgmax-iv4000-output.txt) · [70](2026-08-31-msgmax-iv2857-output.txt) ·
[100](2026-08-31-msgmax-iv2000-output.txt) · [ktop](2026-08-31-msgmax-ktop.txt) ·
setup 타임아웃으로 중단된 200 msg/s 시도(2026-08-31-msgmax-iv1000-*)는 실패 기록으로 보존
