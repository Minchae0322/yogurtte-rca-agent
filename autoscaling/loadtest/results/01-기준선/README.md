# T1 기준선 테스트

> **결론:** 실패는 없었지만 로그인과 채팅방 조회가 SLO를 초과했다. 특히 chat이 첫 병목 후보로 드러났다.

| 구분 | 내용 |
|---|---|
| 상태 | 실측 완료 · 2026-08-16 |
| 목적 | 평상시 고정 부하에서 경로별 SLO 기준선 확인 |
| 부하 | 55 VU · 10분 · 32,694 요청(52.8 rps) |
| 판정 | **부분 통과** — 오류 0%, 지연 기준 3건 초과 |

## 시험 설계

읽기 30 VU(`scroll`·상세, 1초 간격), 로그인 5 VU(3초 간격), 채팅 20 VU(방 목록 조회,
WS 30초 유지·5초마다 STOMP SEND)를 동시에 실행했다.

## 핵심 결과

| 경로 | p95 실측 | 임계값 | 판정 |
|---|---|---:|---|
| GET /feeds/{id} | 109ms | <200ms | 통과 |
| GET /feeds/scroll | 133ms (p99 500.1ms) | p95<300 · p99<500 | p95 통과 · p99 경계 초과 |
| POST /login | **673ms** (평균 306ms) | <400ms | 초과 |
| GET /chat/rooms | **1.8s** (평균 466ms · max 3.3s) | <300ms | 크게 초과 |
| WS 연결 | p95 208ms · 400세션 성립 | - | 정상 |

## 해석과 후속 조치

1. **최대 병목은 `GET /chat/rooms`** - 다른 읽기가 100ms대일 때 혼자 1.8s.
   → [소급 관측](../99-소급관측/README.md)에서 원인 확정: chat 단일 레플리카 CPU 0.99 포화.
2. **로그인 p95 673ms - 동시 5 VU만으로 임계값 초과.** BCrypt 비용이면 T2-A(300 VU)에서
   크게 무너질 예고편 → [T2-A](../03-로그인폭주/README.md)에서 실증됨.
3. **채팅 메시지가 되돌아오지 않았다 - "실패한 줄 모른 채 통과" 실증.** 송신 2,800 대
   수신 400(CONNECTED 프레임뿐). 존재하지 않는 `loadtest-room`으로 보내 서버가 조용히
   버림 → [T2-C](../05-채팅동시접속/README.md) 전에 실제 방 생성으로 해소.
   T1의 채팅 수치는 "연결 유지 부하"만 잰 것이다.

서비스별 극한 시험(T2)을 진행한다. 우선순위는 auth와 chat이다. 서사상 위치는
[Chapter 1](../../../서사.md)의 "어디가 먼저 제한되는가"를 확인하는 첫 데이터다.

원본: [output](2026-08-16-baseline-output.txt) · [summary](2026-08-16-baseline-summary.json)
