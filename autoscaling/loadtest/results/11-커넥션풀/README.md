# Connection Pool 한계 곡선 테스트

> **결론:** pending이 0을 벗어나는 지점은 **200 VU**다(파드1 pending 10 · active 20=풀 상한). 단 그 앞 100 VU에서 파드 CPU가 이미 0.83까지 차올랐고, 커넥션 점유 시간이 0.2s → 2.0s로 늘어난 뒤에 풀이 고갈됐다 - **풀 고갈은 원인이 아니라 CPU 포화의 하류 증상**이다. acquire 타임아웃 0건 · 실패 0%로, 쓰기 경로에서도 T2-B와 같은 "우아한 포화" 패턴을 재확인했다.

| 구분 | 내용 |
|---|---|
| 상태 | **실측 완료** · 2026-08-19 (1차 부분 측정 2026-08-18) |
| 목적 | 분산 리액션 쓰기로 Hikari pending이 0을 벗어나는 VU 지점 탐색 |
| 부하 | 20→50→100→200→400 VU 계단 (각 2분) · CloudFront 경유 · 인그레스 상향(2000/2000/120000) |
| 판정 | **고갈 지점 200 VU - 단 원인 계층은 풀이 아니라 CPU** (Phase 1 해석 규칙 적용) |

## 시험 경과

**1차 시도(08-18)는 요청 0건으로 실패했다.** mixed-breakpoint 직후 곧바로 돌렸는데, auth가 아직
포화(로그인 pending 165·응답 5초) 상태라 setup의 토큰 발급 50회가 타임아웃에 걸려
시험 시간 전체를 setup에서 소진했다. → **설계 원칙 "시나리오 사이 안정 시간 확보"의 실전 근거.**
재실행(08-18)은 100 VU 초반에서 사용자 요청으로 중단됐다(그 구간 pending 0·active 2 - 여유).

**완주(08-19 22:36:38~22:47:59 KST)** 는 auth 회복 스모크(로그인 200 · 394ms) 확인 후 실행했다.
크레딧은 사전 소급으로 미소진·unlimited 확정 상태([99-소급관측](../99-소급관측/README.md)) -
CPU 수치에 크레딧 스로틀 오염 없음.

## 핵심 결과 - 계단별 곡선 (k6 + Mimir 동시각 소급, content 파드 2개)

| 계단 | 처리량(rps) | active max (파드1/2) | **pending max** | acquire max | 점유(usage) max | CPU max |
|---|---:|---|---:|---:|---:|---:|
| 20 VU | 31.0 | 0 / 0 | 0 | 0.01s | 0.26s | 0.03 |
| 50 VU | 112.3 | 1 / 2 | 0 | 0.16s | 0.31s | 0.40 |
| 100 VU | 158.1 | 10 / 5 | 0 | 0.41s | 1.41s | **0.83** |
| **200 VU** | 164.8 | **20** / 5 | **10** / 0 | 0.82s | 2.03s | 0.97 |
| 400 VU | 170.2 | 15 / **18** | 0 / **18** | **1.19s** | 2.03s | 0.99 |

- 풀 크기 20/파드 (`hikaricp_connections_max`) · acquire 타임아웃 **0건** (`timeout_total` 무증가)
- k6 전체: 83,647 요청 · **실패 0%** · med 263ms · p95 2.42s · p99 2.9s (임계 3s 턱걸이 통과)
- 파드별 고갈이 번갈아 나타난 것(200 VU는 파드1만, 400 VU는 파드2만)은 인그레스 분배 편중으로 보임

## 해석 - 설계 3-3 판정 분기 적용

세 갈래(Pool 부족 / DB 포화 / 쿼리 느림) 중 어디에도 깨끗이 떨어지지 않고, **Phase 1 해석
규칙("CPU가 낮은데 pending이 오르면 풀 계층")의 대우**가 적용된다: CPU가 먼저 포화했다.

1. **인과 순서가 명확하다.** 100 VU에서 CPU 0.83(pending 0) → 커넥션 점유 시간이
   0.31s → 1.41s로 4.5배 → 200 VU에서 풀 상한 도달·pending 이탈. 처리 CPU가 모자라
   커넥션을 오래 물고 있는 것이 고갈의 기전이다.
2. **따라서 "풀 상향(20→?)"은 근본 대응이 아니다.** CPU가 그대로면 풀을 키워도 점유 시간이
   길어 pending이 다시 쌓인다. 대응은 CPU 계층([개선/cpu개선.md](../../../개선/cpu개선.md)
   CPU-1~5: 요청당 16.9ms CPU 절감·limit 재설정)이 먼저고, 풀은 그 뒤 재측정.
3. **처리량 정체가 이를 뒷받침한다.** 100→400 VU에서 VU 4배에 처리량 +7.7%(158→170 rps) -
   늘어난 VU는 전부 대기(acquire 최대 1.19s + 응답 p95 2.42s)로 갔다.
4. **DB 계층은 판정 보류.** MySQL 익스포터 부재(정리 항목 기존 등재)로 DB CPU·커넥션을
   못 봤다. 단 점유 시간 증가가 파드 CPU 포화와 동기화돼 있어 DB 원인 가설은 약하다.

T2-A(auth 풀 10에 pending 190)·T3-D(락이 풀 고갈)와 함께 놓으면: **같은 "풀 고갈"이라도
원인 계층이 셋 다 다르다** - auth는 BCrypt CPU, T3-D는 락 직렬화, 여기는 처리 CPU 부족.
풀 지표만 보고 풀을 키우는 대응이 왜 틀리는지의 3중 실증.

원본: [output](2026-08-19-connpool-output.txt) · [summary](2026-08-19-connpool-summary.json) ·
[timestamps](2026-08-19-connpool-timestamps.txt) · [1차 부분 output](2026-08-18-connpool-partial-output.txt)

---

## 재실행 (2026-08-23 밤 · 2차 diet 구성 · nginx 직결) - 고갈 지점이 사라졌다 - 400 VU에서 pending 0, C-3의 커넥션 회전 효과 실증

> **결론:** 1차에서 "pending 이탈 200 VU"였던 것이 **관측 상한(400 VU)까지 고갈이 아예
> 나타나지 않았다** - Mimir가 커버한 400 VU 구간(22:46~54)에서 **Hikari active max 5/12 ·
> pending 0(세 파드 전부) · acquire max 0.5s**. 처리량 228 rps(1차 170) · 실패 0 ·
> acquire 타임아웃 0. **C-3 쿼리 다이어트(커넥션을 쥔 채 돌던 Redis 순차 20왕복 제거 +
> levelTable 캐시)가 커넥션 점유 시간을 줄인 직접 증거**다.

### 결과 비교

| 항목 | 1차 (풀 20/파드·replica 2) | **2차 (풀 12/파드·replica 3·diet)** |
|---|---|---|
| 처리량 (전체) | 83,647 (129.8 rps) · 400VU 구간 170.2 | **144,960 (228.1 rps)** |
| med / p95 / p99 | 263ms / 2.42s / 2.9s | **55.2ms / 2.02s / 3.49s** |
| pending 이탈 지점 | **200 VU** (pending 10 · active 20/20) | **관측 안 됨** (400 VU에서 pending 0 · active 5/12) |
| acquire | max 1.19s · 타임아웃 0 | **max 0.5s · 타임아웃 0** |
| 실패율 | 0% | **0%** |
| content CPU | 0.83(100VU)→0.99 | 합 5.0코어 · 노드 90% (풀가동) |

### 판정 - 1차 결론이 한 단계 진화

1차: "풀 고갈은 CPU 포화의 하류 증상" → 2차: **"점유 시간을 줄이면 하류 증상 자체가
사라진다."** 풀이 20→12로 줄었는데도 고갈이 안 나타난 것이 결정적 - 풀 크기가 아니라
점유 시간이 변수였음의 최종 실증. 남은 임계 초과(p99 3.49s)는 순수 CPU 포화 몫
(content 5.0코어 풀가동)이고, DB 계층 판정 보류(MySQL 익스포터 부재)는 그대로다.

관측 한계: 20~100 VU 초반 계단은 Mimir 공백(관측 노드 플래핑) - 위 풀 수치는 400 VU
구간(최악 지점) 실측이라 판정에는 충분하다.

원본: [output](2026-08-23-connpool-diet-output.txt) · [summary](2026-08-23-connpool-diet-summary.json) · [timestamps](2026-08-23-connpool-diet-timestamps.txt)
