# Connection Pool 한계 곡선 테스트 (부분 · 중단)

> **결론:** 100 VU 초반 구간까지만 측정됐고 그 구간에서 content 풀은 pending 0·active 2로 여유였다. 고갈 지점에 도달하기 전 사용자 요청으로 중단해, 한계 곡선은 미완이다. 또한 첫 시도는 직전 auth 포화로 setup이 실패해, "시험 간 회복 대기"의 필요성을 실증했다.

| 구분 | 내용 |
|---|---|
| 상태 | **부분 측정 · 중단** · 2026-08-18 |
| 목적 | 분산 리액션 쓰기로 Hikari pending이 0을 벗어나는 VU 지점 탐색 |
| 부하 | 20→50→100→200→400 VU 계단 설계 · **실제로는 100 VU 초반에서 중단** |
| 판정 | **미완** — 측정 구간에서 풀 고갈 미발현 |

## 실행에서 배운 것 (기록 가치 있는 실패)

**1차 시도는 요청 0건으로 실패했다.** mixed-breakpoint 직후 곧바로 돌렸는데, auth가 아직
포화(로그인 pending 165·응답 5초) 상태라 setup의 토큰 발급 50회가 타임아웃에 걸려
시험 시간 전체를 setup에서 소진했다. → **설계 원칙 "시나리오 사이 안정 시간 확보"의
실전 근거.** auth 회복(pending 0·로그인 192ms)을 확인한 뒤 재실행했다.

## 측정된 부분 결과

| 항목 | 실측 (100 VU 초반까지) |
|---|---|
| 처리량 | 26.6 rps (2,830 요청) |
| 리액션 응답 | 2xx (정상) |
| **content Hikari pending** | **0** (여유) |
| content Hikari active | 2 |
| content CPU | 0.24 |

풀 pending이 0이라는 것은 **이 구간(≤100 VU)에서는 커넥션이 병목이 아니었다**는 뜻이다.
설계상 200·400 VU에서 pending이 오르기 시작할 것으로 예상했으나, 그 구간에 도달하기 전
중단해 고갈 지점은 미확인이다.

## 남은 것

- 200·400 VU 구간 재측정 필요 (분산 리액션이 실제 좋아요 데이터를 계속 쌓으므로,
  재실행 시 데이터 정리 또는 별도 쓰기 경로 고려).
- 이미 있는 방증: T2-A에서 auth 풀 10에 pending 190, T3-D에서 락이 풀을 고갈시켰다 -
  풀 병목 자체는 실측된 바 있고, 이 시험은 그것을 content 읽기·쓰기 경로의 **곡선**으로
  만들려던 것이다.

원본: [partial output](2026-08-18-connpool-partial-output.txt)
