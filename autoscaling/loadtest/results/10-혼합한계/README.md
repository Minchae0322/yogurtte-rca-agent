# 혼합 Breakpoint 테스트 - 동시 사용자 한계는 auth가 정한다

> **결론:** 실제 트래픽 비율(읽기 80/로그인 15/챗 5)로 1,200 VU까지 올렸을 때, 읽기는 p99 250ms로 멀쩡했고 로그인만 붕괴했다. "시스템 한계"는 전체가 아니라 auth 한 서비스가 정하며, 그마저도 노드 CPU가 아니라 파드 CPU limit에서 걸렸다.

| 구분 | 내용 |
|---|---|
| 상태 | 실측 완료 · 2026-08-18 |
| 목적 | 실제 비율 혼합 부하의 "동시 N명" 한계를 발견 |
| 부하 | 읽기 80% / 로그인 15% / 챗 5% · 100→300→600→1000→1200 VU 계단 |
| 판정 | **auth가 한계를 정함** — 읽기는 1200 VU 통과, 로그인만 붕괴 |

## 시험 설계와 재실행 경위

각 VU가 iteration마다 가중치로 행동을 고른다(읽기 80·로그인 15·챗 5). 실제 트래픽
프로파일(80% content)을 시나리오에 그대로 반영한 것 자체가 설계의 일부다.

**1차 실행은 38 VU에서 조기 중단됐다** - `p99<1s abort` 기준이 문제였다. 로그인(BCrypt)이
15% 섞여 p99를 지배하는데, 그 꼬리가 1초를 넘자마자 멈춰 "시스템 한계"가 아니라
"로그인 꼬리"를 잰 셈이 됐다(중앙값은 59ms로 정상이었다 - 크레딧 고갈이 아니라 꼬리 문제).
그래서 **abort 기준을 읽기 경로 p99<3s + 전체 실패율로 바꿔** 재실행했다.
(원본 보존: `2026-08-18-mixed-v1-aborted-output.txt` - 이 실패 자체가 시험 설계 교훈이다.)

## 핵심 결과 (재실행, 1,200 VU까지)

| 경로 | p99 | 실패 | 판정 |
|---|---:|---:|---|
| GET /feeds/scroll | **252ms** | 0% | 1,200 VU까지 통과 |
| GET /feeds/{id} | **230ms** | 0% | 1,200 VU까지 통과 |
| POST /login | **60s (타임아웃 상한)** | **44%** (1,039/2,388) | 붕괴 |
| 전체 | - | 5.05% | 로그인이 전부 |

전체 처리량 48.9 rps, 읽기 중앙값 60ms.

## 서버 관측 — 파드 CPU limit이 먼저 걸린 지점 (551 VU)

| 노드 | CPU | 그 위 파드 CPU |
|---|---:|---|
| worker1 (t3.medium) | **62%** | auth **500m** · content 489m · chat 66m |
| worker2 (t3.small) | 30% | content 555m |

**auth 파드가 자기 CPU limit(500m) 상한에 정확히 붙었다.** 노드는 62%로 여유가 있는데도
auth만 스로틀링된 것 - 즉 이 한계는 **노드 capacity가 아니라 auth의 파드 limit + 단일
replica** 때문이다. content는 limit 1500m에 대해 489~555m로 여유라 1,200 VU를 견뎠다.

## 해석과 다음 조치

**"동시 사용자 N명 한계"는 시스템 전체값이 아니라 auth가 정하는 값이다.**
- 읽기 관점: 이 클러스터는 1,200 VU(≈49rps 혼합)에서도 content 읽기가 p99 250ms로 건강하다.
- 로그인 관점: auth는 파드 CPU limit 500m + replica 1개 때문에 훨씬 이른 지점에서 무너진다.

이것이 설계의 계층 분리가 증명한 것이다 - 노드 CPU(62%)를 보고 "여유 있다"고 하면 틀리고,
파드 CPU(auth 500m=limit)를 봐야 진짜 병목이 보인다. 개선 방향은 노드 증설이 아니라
**auth의 limit 상향 + replica scale-out**(T2-A의 풀 병목과 함께)이다.

미측정: 800·1000·1200 VU 각 단계의 노드/파드 스냅샷은 551 VU 1점만 확보. 계단별 곡선은
Mimir range 쿼리로 소급 가능. CPUCreditBalance는 AWS CLI 미설치로 미확인.

원본: [output](2026-08-18-mixed-output.txt) · [summary](2026-08-18-mixed-summary.json) · [1차 중단본](2026-08-18-mixed-v1-aborted-output.txt)

---

## mixed-v2 (2026-08-23 14:06~14:18 KST) - 로그인 10% 재보정: 처리량 3.2배, 그래도 한계는 auth

> **결론:** 로그인 비중을 15% → 10%로 재보정하고([mixed-v2.js](../../mixed-v2.js) 신설,
> 읽기 85/로그인 10/챗 5) 같은 계단(100→…→1,200 VU)을 돌리자 **전체 처리량 48.9 → 157.1 rps
> (3.2배)로 완주**했다. 읽기는 1,200 VU에서 p99 549/588ms·실패 0으로 통과, **로그인만
> 실패 20.2%·med 29.1s로 붕괴** - "한계는 가장 약한 서비스(auth)가 정한다"는 v1 결론이
> limit 3배·replica 3 토폴로지에서도 유지됐다. 벽의 위치만 500m → 1500m으로 이동했다.

### 조건 - v1과 무엇이 같고 무엇이 다른가

- **단일 변수(스크립트):** 비율 하나만 변경 (읽기 80→85 / 로그인 15→10 / 챗 5 유지).
  계단·abort 기준·sleep 0.3s·CloudFront 경유 전부 v1과 동일.
- **단 토폴로지가 v1(08-18)과 다르다:** auth limit 500m→1500m·힙 384(CPU-2/MEM-3) ·
  content replica 3(전용 스팟 노드·limit 없음·풀 12·캐시·톰캣 스레드 200 명시) - 그래서
  v1과의 처리량 비교는 "비율 효과"가 아니라 **"비율 + 개선 누적" 복합**이다.
- 웜업 11,208요청(med 45ms) 후 실행. 인그레스 상향(2000/2000/120000) 상태.

### 실패한 두 시도 (원본 보존 - 시험 절차 교훈)

| 시도 | 파일 | 무엇이 있었나 |
|---|---|---|
| try1 (13:43) | `try1-contaminated-*` | 시험 도중 13:46 content 배포 롤아웃(`SERVER_TOMCAT_THREADS_MAX=200` 적용)이 겹침 - 파드 드레인+콜드 스타트가 읽기 p99 4.17s를 만들어 345 VU에서 abort. **"시험 창 안에 배포 금지"의 실증** |
| try2 (13:55) | `try2-sigterm-*` | 동시에 돌던 다른 시험과 충돌 - k6가 외부 SIGTERM으로 100초 만에 종료. 그 창에 content 파드 readiness 실패도 관측됨 |

### 핵심 결과 - 클라이언트 (k6, 완주 12m19s)

| 항목 | v1 (로그인 15%, 08-18) | **v2 (로그인 10%, 08-23)** |
|---|---|---|
| 전체 처리량 | 48.9 rps (20,536 요청) | **157.1 rps (109,313 요청) - 3.2배** |
| 읽기 p99 (scroll/detail) | 252 / 230ms | 549 / 588ms (임계 3s 통과 · **읽기 실패 0**) |
| 읽기 med | ~60ms | 63.6 / 60.9ms |
| 로그인 | 실패 **44%** (1,039/2,388) · p99 60s | 실패 **20.2%** (2,082/10,318) · med 29.1s · p99 60s |
| 전체 실패율 | 5.05% | **1.90%** (전부 로그인) |
| 완주 | 1,200 VU | 1,200 VU |

읽기 p99가 v1보다 높은 것(252→549ms)은 악화가 아니라 부하 차이다 - 읽기 rps 자체가
~39 → ~134로 3.4배인 상태의 꼬리이고, med는 동일(60ms대)하다.

### 서버 관측 (시험 창 max, Mimir 소급 + kubectl top 60s 로거)

| 계층 | 실측 | 판정 |
|---|---|---|
| **auth 파드 CPU** | **1.481 / limit 1.5 - 14:10부터 종료까지 벽에 상주** (ktop 1437~1497m) | **한계를 정하는 지점** |
| auth Hikari | **active 10/10 (상한) · pending 190** | T2-A의 이중 병목(CPU+풀) 형태가 혼합에서 재현 |
| auth 서버 rps | max 17.6/s (성공 로그인 평균 ~12/s) | 로그인 단가 ~106ms CPU × 1.5코어 = 상한 ~14/s와 부합 |
| content 파드 CPU | 0.86 / 0.91 / 1.03 (limit 없음) | 절반 수준 - 여유 |
| content Hikari pending | **0 (세 파드 전부)** | 풀 12로도 이 부하는 무압박 |
| content 서버 rps | max 172.5/s | T2-B 극한(314)의 55% 수준 |
| 노드 CPU | content 노드 0.96~1.30/2.0 (48~65%, 순간 76%) · **core(auth+chat) 1.77/2.0 (89%)** | 노드는 어디도 포화 안 함 |
| chat | 파드 0.156 | 무관 |

v1의 미측정이던 계단별 스냅샷은 이번엔 60초 간격 [ktop 로거](2026-08-23-mixed-v2-login10-ktop.txt)로 확보.

### 판정

1. **"동시 N명 한계 = auth의 값"은 구조적 결론이다.** limit을 3배 올리고 로그인 비중을
   줄여도, 1,200 VU 혼합에서 무너지는 것은 여전히 로그인뿐이다. 벽의 높이만 바뀐다
   (실패 44→20%, 전체 실패 5.05→1.90%).
2. **auth의 다음 병목 순서도 T2-A와 동일** - CPU 1.5 벽 + 풀 10 상한/pending 190.
   남은 수는 auth replica 스케일아웃(단 RDS 커넥션 예산 59/60이 선결) 또는 로그인 경로
   자체의 단가 절감. limit 추가 상향은 core 노드 89%라 여유가 ~0.2코어뿐이다.
3. **읽기 계층은 이 비율에서 한계와 무관** - 1,200 VU에서 content CPU 절반, 풀 pending 0.
   읽기의 극한은 T2-B(단일축 500 VU)가 따로 잰다.

### 로그인 붕괴의 3단계 (Mimir 1분 소급 + k6 로그 실측)

| 단계 | 시점 (VU) | 실측 | 상태 |
|---|---|---|---|
| 1. 포화 | 14:08 (~60–100 VU) | 유입이 처리 상한 ~15건/초(106ms×1.5코어) 초과, 풀 pending 10 발생 | 지연 시작, 실패 0 |
| 2. 고착 | 14:09–14:10 (~220–290 VU) | auth CPU 1.5 벽 도달 · pending 75→**190(상한)** 이후 종료까지 고정 | 느리지만 완료 (med 29s의 기원) |
| 3. 실패 | 14:14:58 (~1,000 VU) | 첫 60s 클라 타임아웃 - 이후 타임아웃 1,669건 + 서버 500(풀 획득 30s 초과) 1.33건/초 | 실패 2,082건 |

붕괴 내내 서버 처리율은 /login 200 기준 **14.97건/초로 일정** - 무너진 것은 처리가 아니라
대기열이다. 종료 후 pending은 1분 내(14:19) 0으로 소진, auth CPU 0.005로 회복.

미측정: 로그인 실패 2,082건의 상태 분해(CF 504 vs 클라 타임아웃)는 이번에도 못 했다
(CloudFront 경유 유지 - 직결 전환은 비율과 변수를 섞지 않기 위해 보류). 챗 경로는 5%
(5,437건) 전부 성공(rooms 200 100%).

원본: [output](2026-08-23-mixed-v2-login10-output.txt) · [summary](2026-08-23-mixed-v2-login10-summary.json) ·
[timestamps](2026-08-23-mixed-v2-login10-timestamps.txt) · [ktop](2026-08-23-mixed-v2-login10-ktop.txt)
