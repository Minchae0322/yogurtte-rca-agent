# 관측 데이터 활용 구조 개선

> **이 문서는 기록입니다.** 통합본이 SoT입니다 → [관측개선-통합.md](관측개선-통합.md)
> (탐색 LLM 앞/뒤로 나눠 네 문서를 합쳤고, B-45 결론은 통합본이 최신입니다.)

> **구현 상태가 다섯으로 갈립니다** — §1(로그 스택트레이스)은 **적용 완료**,
> §2(Trace 호출 그래프)는 **검증·구현·테스트 고정까지 완료 (2026-07-31) · 점수 효과 미측정**,
> §3(로그 기록 체계 통합)은 **규칙 제정 + 일부 코드 반영·미배포**,
> §4(익명 폴백 관측 보강)는 **코드 적용 완료 (2026-08-02) · 미배포·효과 미측정**입니다.
> §5(탐색 후보의 판별력)는 **B-44 적용·재생 검증 완료 / B-45 코드 적용·기본 꺼짐·효과 없음 실측 (2026-08-10)** 입니다.
> §2의 상세 스펙·검증 기록은 [service-graph-spec.md](service-graph-spec.md),
> §5의 단계별 설명은 [incident-clustering-walkthrough.md](incident-clustering-walkthrough.md),
> 대기열 원문은 [round-6/README.md](../round-6/README.md). 자세한 상태는 맨 아래 표.

RCA 에이전트를 검증하는 과정에서 장애 원인에 필요한 정보가 Loki와 Tempo에는 존재했지만, 현재 수집·가공 방식에서는 일부 정보가 분석 컨텍스트에 포함되지 않거나 관계 정보가 활용되지 않는 문제를 확인했습니다. 로그는 스택트레이스가 필터링되어 예외 원인이 누락되고 있었고, Trace는 Span 관계를 해석하지 않아 서비스 간 호출 구조를 복원하지 못했습니다. 이를 해결하기 위해 로그 수집 규칙과 Trace 표현 방식을 수정하여 기존 관측 데이터를 보다 완전한 Evidence로 활용할 수 있도록 개선했습니다.

---

## 1. 로그 스택트레이스 수집 개선

### 문제

팔로우 목록 NPE(AP-2)는 실제 원인이 `FollowCondition.java:25`에 있었지만 기존 LogQL은 `ERROR|WARN`이 포함된 헤더 라인만 조회하고 있었습니다.

```logql
{service_name=~"..."} |~ "ERROR|WARN"
```

동일한 예외 로그는 다음과 같이 저장됩니다.

```
ERROR FollowService : 팔로우 목록 조회 실패      ← 조회됨
java.lang.NullPointerException ...             ← 제외됨
    at FollowCondition.java:25                 ← 제외됨 (실제 원인)
```

또한 traceId 기반 조회 역시 스택 라인에는 traceId가 기록되지 않아 동일한 문제가 발생했습니다. 즉 예외 로그는 수집되었지만 실제 원인을 담고 있는 스택 정보는 분석 컨텍스트에 포함되지 않았습니다.

### 개선

분석 단계의 LogQL을 다음과 같이 변경했습니다.

```logql
{service_name=~"..."} |~ `ERROR|WARN|Exception|Caused by|\.java:[0-9]+\)`
```

추가된 패턴은 다음 정보를 함께 수집합니다.

| 패턴 | 목적 |
|------|------|
| `Exception` | 예외 클래스 |
| `Caused by` | 중첩 예외 |
| `\.java:[0-9]+\)` | 실제 코드 위치 |

반면 장애 탐색 단계에서 사용하는 발생률 집계(Query Range)는 그대로 유지했습니다. 스택 라인까지 집계에 포함하면 하나의 예외가 수십 줄로 계산되어 장애 발생량이 왜곡되기 때문입니다.

수집량 증가는 **예외 발생 건수에 비례**합니다. 레벨 필터를 아예 없애 INFO까지 받는 것과는
성질이 다릅니다.

| | 증가 |
|---|---|
| INFO 전량 수집 | 1시간 2,300줄 중 ERROR/WARN 8줄 → **약 287배** |
| 스택 라인 추가 | 예외 8건 × 스택 20~50줄 → **8줄 → 200~400줄** |

스택 라인은 정상 요청에는 아예 존재하지 않습니다.

> **근본 해결은 따로 있습니다.** 수집기에서 **multiline 스티칭**으로 스택을 하나의 엔트리로
> 묶으면 traceId가 함께 붙어 기존 쿼리가 그대로 작동합니다. 다만 관측 파이프라인 변경이고,
> **과거 데이터는 이미 줄 단위로 저장돼 있어 재조사로 검증되지 않습니다**(새로 주입해야 한다).
> 그래서 지금은 라인 필터 확장을 택했습니다 — AP-2 재조사가 주입 없이 가능합니다.

---

## 2. Trace 관계 정보 활용 개선

### 문제

Tempo에는 `parentSpanId`, `db.system`, `messaging.system` 등 서비스 호출 관계를 복원할 수 있는 정보가 이미 저장되어 있었지만 기존 구현은 Span을 단순 목록으로만 사용하고 있었습니다.

그 결과

- 부모-자식 호출 관계를 알 수 없고
- 서비스 간 의존성을 시간 순서로만 추론해야 했으며
- Redis, MySQL, Kafka와 같은 공통 리소스를 하나의 장애로 연결하지 못했습니다.

즉 필요한 데이터는 존재했지만 분석 과정에서 활용되지 못하고 있었습니다.

### 개선

Trace를 단순 Span 목록이 아닌 호출 그래프로 변환하도록 구조를 변경했습니다.
Span마다 아래 판별 순서를 위에서부터 적용해 "이 Span은 어떤 호출인가"를 정하고,
같은 (종류, 출발, 도착) 엣지에 속한 Span들을 한 줄로 집약합니다.

```
① messaging.system                     → 메시징 (publish: 서비스→토픽 / receive: 토픽→서비스)
② db.system                            → 데이터베이스 (redis 등)
③ jdbc.datasource.*                    → 데이터베이스 (mysql/<풀 이름>)
④ client.name (아웃바운드 HTTP)         → 서비스 경계 — 상대 span이 없어도 나온다 (검증에서 추가)
⑤ parentSpanId (부모의 service.name 비교) → 서비스 경계
⑥ peer.service 만 있다                  → 미분류로 남긴다. 신뢰할 수 없기 때문이다
```

**`peer.service`를 마지막에 두는 것이 이 순서의 핵심입니다.** 같은 Span에 이렇게 들어옵니다.

```json
{"key":"jdbc.datasource.name","value":{"stringValue":"content"}},
{"key":"peer.service",        "value":{"stringValue":"content"}}     ← MySQL DB 이름이다
```

`peer.service=content`는 `content-service`가 아니라 **MySQL 데이터베이스 `content`** 입니다.
저장된 트레이스에서 288건이 이 형태였습니다. 표준 키보다 먼저 보면
**`content-service → content-service` 자기 참조 Edge**가 생기고, `-service` 접미를 떼어
비교하는 방식도 같은 함정에 빠집니다.

그래서 이름을 **시스템/대상** 형태로 짓습니다 — `mysql/content` · `redis` ·
`kafka/user.notifications`. 서비스와 DB가 문자열로 구별됩니다.

JDBC 세부 Span(query·result-set·connection)은 제거하지 않고 하나의 Edge로 집계하여
SQL 오류나 Kafka Publish 실패와 같은 핵심 정보는 유지하면서 노이즈만 줄입니다.

### 검증에서 배운 것 — 설계 그대로는 두 곳이 틀렸다

구현 전에 `reports/raw/`의 저장된 트레이스 **17개 전부**에 추출 규칙을 돌려 확인했습니다
(2026-07-31 · 주입/배포 불필요). 원안이 그대로 통과하지 못했고, 그 자체가 수확입니다.

| 발견 | 무엇이 틀렸었나 | 어떻게 고쳤나 |
|---|---|---|
| **죽은 서비스는 트레이스에 합류하지 못한다** | auth-service 다운 장애(AU-4)의 트레이스는 **66개 Span 전부가 content-service**다. 호출이 Connection refused로 끝나면 상대는 Span을 남길 수 없어, "부모 서비스 비교"(⑤)만으로는 이 엣지가 **조용히 사라진다** — 검증에서 실제로 사라졌다 | 판별 ④ 신설 — 부른 쪽 CLIENT Span에 남는 `client.name`으로 엣지를 만든다. 결과: `content-service → auth-service` 엣지에 `Connection refused` 오류까지 실린다 |
| **receive의 토픽명은 다른 키에 온다** | 원안의 `messaging.destination.name`으로는 receive 엣지가 `kafka/?`가 된다 | receive는 `messaging.source.name`을 읽는다 (CH-1 실측) |
| **MongoDB는 Span을 만들지 않는다** (반증) | "`db.system=mongodb`로 올 것"이라던 구두 확인이 틀렸다 — 17개 트레이스에 mongodb Span **0건** | Mongo 장애는 Kafka receive Span의 `error` 속성 텍스트로 온다. 엣지에 error를 붙이는 집약 설계 덕에 지문은 전달되지만, `chat → mongodb` 엣지는 관측상 만들 수 없다고 한계를 명시 |

### 실제 출력 — CH-1 트레이스: Span 28개 → 엣지 6줄

알림 미발송 장애의 저장된 트레이스에서 실제로 나온 출력입니다. **여섯 줄이 장애 서사
전체를 담습니다** — 발행은 정상(16.6ms), 소비가 30초씩 걸리며 그 Span에 Mongo 접속 거부
에러가 붙어 있고, 결국 DLQ로 갔다는 것까지.

```
content-service --messaging--> kafka/user.notifications   1회  최대 16.6ms  [publish]
kafka/user.notifications --messaging--> chat-service      4회  최대 30108.2ms  [receive]
    error: Timed out ... MongoSocketOpenException ... Connection refused ...:27017
chat-service --messaging--> kafka/user.notifications.dlq  1회  최대 1300.6ms  [publish]
chat-service --jdbc--> mysql/content (HikariPool-1)       4회  최대 30021.5ms
    events: acquired, rollback
content-service --jdbc--> mysql/content (HikariPool-1)    8회  최대 45.3ms
content-service --db--> redis                             1회  최대 0.6ms  [GET]
```

이전에는 모델이 Span 28개를 시각으로 짜맞춰야 나오던 결론입니다.

**부수 실측** — 위 넷째 줄이 뜻밖의 발견입니다. chat의 도메인 저장소는 MongoDB인데,
트레이스상 **chat의 JVM에 MySQL 풀(`HikariPool-1`, datasource명 `content`)이 실재**하고
알림 처리가 그 JDBC 트랜잭션 안에서 30초 점유 후 rollback합니다
(`receive → connection → process-notification` 부모 사슬로 확인). 메시지 1건당 MySQL
커넥션 30초 점유는 NF-10(커넥션 점유) 계열의 앱 쪽 관찰이며, toy 쪽 설정 확인 대상입니다.
그래프를 "chat은 원래 Mongo"라는 정적 지식으로 고치지 않습니다 — 관측이 말하는 것을
그대로 싣는 것이 이 설계의 원칙이고, 실제로 그 원칙이 이 발견을 만들었습니다.

### 구현 — 어디에 무엇이 들어갔나

| 층 | 파일 | 내용 |
|---|---|---|
| 파싱 | `collector/TraceSpans.java` | `spanId` · `parentSpanId` · `attributes` · `eventNames` 4필드 추가 |
| 추출 | `analyzer/ServiceGraphExtractor.java` (신규) | 판별 ①~⑥ + 엣지 집약 |
| 모델 | `report/ServiceGraph.java` (신규) | 엣지 목록 + 텍스트 렌더링 한 곳 — 컨텍스트와 리포트가 같은 서술을 본다 |
| 소비 1 | `analyzer/ContextAssembler.java` | LLM 컨텍스트에 "호출 그래프" 절 — **원본 트레이스는 그대로**(요약 추가이지 대체가 아니다) |
| 소비 2 | `report/ReportMarkdown.java` | 리포트 관측 증거 절에 기록 — 없으면 그래프의 효과를 채점에서 귀속 못 한다 |
| 프롬프트 | `prompts/*.md` × 3 (+ resources 복사본) | 손으로 적었던 토폴로지 문장 제거 — 같은 회차 원칙 |

검증 때 쓴 트레이스 3건을 `src/test/resources/traces/`에 픽스처로 복사하고, 검증 실측값을
**테스트 5건으로 박제**했습니다(`ServiceGraphExtractorTest`) — 접힘·자기참조 0건·죽은 서비스
엣지·Kafka 방향·빈 그래프. 기대값이 바뀌면 추출 규칙이 회귀한 것입니다. `./gradlew test` 통과.

### 파이프라인에서의 위치 — 탐색은 그대로, 분석이 바뀐다

전체 흐름에서 이 변경이 어디에 들어가는지입니다.

```
[1] 자연어 질문
      ↓  TimeExpressionParser
[2] 시간 창 계산
      ↓  Surveyor — Loki/Tempo/Mimir를 집계값으로 스윕
[3] Signal → Incident 후보 → LLM ①이 선택 (탐색)          ← 변경 없음
      ↓  창·서비스·traceId가 좁혀짐
[4] Collector — 그 범위의 트레이스 원본 + 로그 + 메트릭      ← 변경 없음
      ↓
[5] ContextAssembler — 컨텍스트 조립                        ← ★ 호출 그래프 절 추가
      ↓
[6] LLM ② — 원인 분석                                      ← ★ 영향을 받는 곳
      ↓
[7] 리포트 (관측 증거)                                      ← ★ 그래프 기록 추가
```

**[1]~[3] 탐색 단계는 이번 변경과 무관합니다.** 탐색은 집계값(에러율·트레이스 검색
결과·메트릭 이상)만 받고 트레이스 원본이 없어서, 그래프를 만들 재료 자체가 없습니다.

영향은 세 가지입니다.

1. **분석 단계 LLM의 입력 질이 바뀝니다.** 지금까지 [6]의 모델은 평평한 Span 목록에서
   *"Redis GET 직후 0.7ms 뒤에 auth 호출 — 아마 캐시 미스 보충"* 같은 **시각 기반 정황
   추론**을 해야 했고, AU-4에서 정답을 맞히고도 그 추론이 정황이라 확신도를 낮춰 감점됐습니다.
   이제 컨텍스트에 관측 사실 몇 줄이 먼저 옵니다 — **추론해야 했던 것이 읽으면 되는 것**이 됩니다.
2. **트레이스에 없던 신호가 생겼습니다.** 죽은 서비스는 Span을 못 남기므로 기존 목록에서는
   "auth가 호출됐다는 것" 자체가 안 보였습니다. 판별 ④(`client.name`) 덕에 *"auth를 불렀는데
   거부됐다"* 가 엣지 한 줄로 나옵니다.
3. **시스템 지식의 출처가 프롬프트에서 관측으로 바뀌었습니다 — 구조적으로 가장 큰 변화입니다.**
   토폴로지가 사람이 박아둔 문장이던 동안에는 (a) 문장에 없는 인프라(Redis·MySQL·Mongo)는
   모델이 알 수 없었고, (b) 점수가 움직여도 그래프 덕인지 문장 덕인지 가릴 수 없었습니다.
   문장을 지우고 관측 유도로 바꾸면서 둘 다 해소됩니다. 새 서비스·인프라가 붙어도
   프롬프트를 고칠 필요가 없어진 것은 부수 효과입니다.

**리스크 하나를 함께 적어둡니다.** 탐색 프롬프트([3])에서도 토폴로지 문장을 뺐는데,
탐색 단계는 그래프를 **받지 못하므로** 거기는 대체물 없이 지식만 줄었습니다.
탐색 점수가 떨어지면 이것이 첫 번째 용의자이고, 아래 반증 조건의 "프롬프트 문장 제거"
행이 이 경우를 잡습니다.

### 예상되는 성과 — 실측과 예측을 구분한다

**실측된 사실 (지금 말할 수 있는 것):**

| 사실 | 수치 |
|---|---|
| 집약 효과 | AP-3 트레이스 Span 23개 → 엣지 **1줄** (오류·rollback 유지) · CH-1 Span 28개 → **6줄** |
| 함정 회피 | `peer.service=content` Span 트레이스당 최대 60개 전부 정분류 — 자기참조 **0건** · 미분류 **0건** |
| 죽은 서비스 도달 | AU-4 트레이스에서 `content → auth-service` 엣지 + `Connection refused` 오류 — **이전에는 아예 없던 신호** |
| 컨텍스트 크기 | 그래프 절 추가분은 트레이스당 **수백 자** — 원본을 대체하지 않으므로 컨텍스트는 오히려 약간 커진다. **토큰 절감을 주장하지 않는다** |

**예측 (재조사 후 검증 — 미측정):**

- **AU-4 원인 항목 +10 회복.** 회차 2에서 에이전트는 read-through 패턴을 **정확히 맞히고도**
  근거가 "시각이 붙어 있고 사용자 ID가 같다"는 정황 추론이라 확신도 낮음(후보 3)에 뒀고
  원인에서 −10을 받았다. 그래프는 그 정황을 **관측 사실**(엣지 + 오류)로 바꾼다.
  주입 불필요 — 기존 traceId 재조사로 잰다.
- **프롬프트 문장 제거 후 점수 유지.** 그래프가 손으로 적은 문장을 대체할 수 있다는
  주장의 검증 조건이다. 유지되면 성공, 떨어지면 그래프가 그 문장만큼도 못 한 것이다.
- **IN-1 수렴 근거는 이번 범위 밖.** 한 Trace는 한 요청의 경로만 담으므로
  "세 서비스가 모두 Redis를 쓴다"는 `B-9`(후보 N건 수집) 후 여러 Trace 누적에서 나온다.
  여기서 미리 성과를 주장하지 않는다.

---

## 3. 로그 기록 체계 통합 (앱 계측 — 변경군 A)

한 일은 두 가지입니다.

| | 무엇 | 상태 |
|---|---|---|
| ① | 세 서비스의 **HTTP 요청 로그를 "실패한 것만 남기기"로 통일** | content·chat·**auth(toy-user)** **코드 반영 완료** (미배포) |
| ② | 세 서비스의 **로그 기록 형식 규칙을 문서로 제정** ([logging-format.md](../../../toy-content/docs/observability/logging-format.md)) | 규칙만 제정, **코드 적용은 안 함** |

### ① 요청 로그 통일 — content는 끄고, chat은 켰다

지금까지 서비스마다 제각각이었습니다.

- **content**: 성공한 요청까지 **전부** 로그로 남김 → 1시간에 2,300줄인데 그중
  ERROR/WARN은 8줄. 나머지는 Tempo 트레이스에 이미 있는 정보의 중복
- **chat**: 요청 로그가 **아예 없음** → 401이든 500이든 로그에 한 줄도 안 남음
- **auth (toy-user)**: 요청 로그 없음 + JWT 검증 실패 사유가 빈 catch에 삼켜짐 +
  `RestApiException`이 ControllerAdvice에서 무로그 처리

이걸 하나의 기준으로 맞췄습니다: **실패(4xx·5xx)와 느린 요청(1초 초과)만 남긴다.
성공한 요청은 트레이스가 이미 전부 기록하므로 로그에는 안 남긴다.**

```
[변경 전 content]                          [변경 후 content·chat 공통]
[HTTP] GET /api/feeds 200 - 12ms   INFO    (안 남김 — 트레이스에 있음)
[HTTP] GET /api/users 401 - 3ms    WARN    [HTTP] GET /api/users 401 - 3ms    WARN
[HTTP] POST /api/feeds 500 - 74ms  ERROR   [HTTP] POST /api/feeds 500 - 74ms  ERROR
[HTTP] GET /api/feeds 200 - 1840ms WARN    [HTTP-SLOW] GET /api/feeds 200 - 1840ms WARN
```

실제 코드 변경:

1. content `RequestLoggingFilter` — 성공 요청 INFO 한 줄 **삭제**
2. chat — 같은 필터를 **신설** (chat의 401·500이 처음으로 로그에 잡힘)
3. chat 설정 오타 수정 — `logging.level` 키가 존재하지 않는 패키지(`com.example.chat`)를
   가리켜 **헛돌고 있었음** → `com.example.toychat`으로 정정
4. auth(toy-user) — 같은 필터 **신설** + 무로그 실패 경로 셋 보강:
   JWT 검증 실패 사유(빈 catch → A-0과 동일한 사유별 WARN),
   JwtFilter의 "토큰 유효·사용자 없음" 500 경로,
   ControllerAdvice의 `RestApiException` 무로그·`handleAllException` WARN→ERROR.
   JWT 예외는 필터에서 나므로 ControllerAdvice에 **구조적으로 안 잡힌다**는 확인 포함
   (상세: [jwt-exception-handling-spec.md §7-b](jwt-exception-handling-spec.md) — 통합 필터 A-5는 보류 유지)

### ② 형식 규칙 제정 — 나중에 로그를 JSON으로 바꾸기 위한 준비

지금 로그는 평문이라 스택트레이스가 여러 줄로 찢어져 저장되고, 그래서 §1의 라인 필터
우회가 필요했습니다. 규칙 문서의 핵심은 **"로그 한 건 = JSON 한 줄"로 바꾸면
스택이 traceId와 같은 줄에 담겨 §1 우회 자체가 필요 없어진다**는 것입니다 —
§1 말미의 *"근본 해결은 따로 있습니다"* 가 이것입니다. 다만 **아직 규칙만 있고
코드는 안 바꿨습니다.**

### 성과 — 말할 수 있는 것과 없는 것

**실측된 사실 (지금 말할 수 있는 것):**

| 사실 | 수치 |
|---|---|
| content 로그의 구성비 | 1시간 2,300줄 중 ERROR/WARN **8줄** — 나머지 99.7%가 성공 INFO |
| 그 성공 INFO의 정보 가치 | Tempo(샘플링 1.0)와 **완전 중복** — 지워도 정보 손실은 userId 하나 |
| chat의 관측 공백 | 요청 로그 **0줄** (401·500 포함) — 필터 신설로 실패가 처음 기록됨 |
| 앵커 안전성 | `[HTTP]` INFO에 의존하는 채점 문항 0건 (전수 확인) |

**예측 (배포 후 검증 — 미측정):**

- 요청 로그 적재량 **약 1/287** (2,300줄 → 8줄+α). 실측 구성비에서 산술로 나오는
  예측이고, 배포 후 `count_over_time` 전/후 비교로 확정한다.
- Grafana Cloud Free 한도(로그 50GB/월) 여유 확보, 사람이 Grafana에서 실패만 보게 됨.

**과장 금지 — "토큰을 줄였다"는 그대로 쓰면 틀린다.** 에이전트의 에러 조회는
원래부터 `ERROR|WARN` 라인 필터라 **성공 INFO를 읽지 않았다.** 지운 2,292줄은
대부분 에이전트 컨텍스트에 들어가지도 않던 줄이다. 토큰 효과가 실재하는 경로는
traceId 전량 조회(요청당 `[HTTP]` 1줄 감소)뿐이라 크지 않고, 배포 후에나 잴 수 있다.

**그래서 포트폴리오 서사는 토큰이 아니라 이것이다:**

> 같은 사실이 로그와 트레이스에 이중 기록되고 있었다. **채널별 SoT를 정했다** —
> 성공 요청의 기록은 트레이스(전 요청 샘플링), 실패의 기록은 로그, 집계는 메트릭.
> 그 결과 로그 적재량의 99.7%를 차지하던 중복을 제거하면서 **정보 손실은
> userId 하나로 좁혀 명시했고**(span 태그로 복원 계획), 반대로 실패조차 기록되지
> 않던 서비스(chat)에는 같은 규칙으로 기록을 **추가**했다. 줄인 쪽과 늘린 쪽이
> 같은 원칙("실패와 저속만 로그에")에서 나온다.

### 왜 배포·적용을 미루나

- 이건 앱 쪽 변경(A군)이라, 조사 도구 변경(§1·§2, B군)과 같은 회차에 섞으면
  점수가 움직여도 뭐 덕분인지 못 가립니다. **배포는 회차 경계에서.**
- content의 성공 INFO를 지워도 되는지 확인했습니다 — 채점 앵커에 `[HTTP]` 줄을
  요구하는 문항이 없고, 과거 리포트가 인용한 `[HTTP]` 줄은 전부 500 ERROR라
  변경 후에도 그대로 남습니다.

---

## 4. 익명 폴백 저하 관측 보강 (앱 계측 — 변경군 A · NF-09)

### 문제

auth-service 22분 다운(AU-4)에서 content-service는 외부 사용자 조회 실패를 익명
대체(fallback)로 흡수했습니다. 요청이 전부 HTTP 200으로 끝나 실패율 지표는 움직이지
않았고, 사용자 10명이 익명으로 저하됐는데 이를 사후에 특정할 채널이 하나도 없었습니다.
([NF-09](../findings/nf-09-user-fallback-no-traceid.md))

**§1·§2와는 문제의 성질이 다릅니다.** 거기는 데이터가 Loki·Tempo에 있는데 읽지 못하는
문제였고, 여기는 **찾을 수 있는 신호가 애초에 만들어지지 않는** 문제입니다. 그래서 고치는
곳도 도구(rca-agent)가 아니라 앱(toy-content `external/user` 패키지)입니다.

관측 공백은 세 겹이었습니다.

| 채널 | 무엇이 막혔나 | 왜 |
|---|---|---|
| 로그 검색 | 하네스 프로브가 fallback **0건으로 오보고** — 그 오보고가 앵커에 들어가 채점 불가로 이어짐 | 실패 로그 29개가 접두사 3종(없음 / `[외부사용자 조회]` / 이모지) 혼용. 검색이 메시지 본문 문구에 결합돼, 문구가 어긋나면 조용히 깨짐 |
| traceId 귀속 | traceId 조회(`\|= "<traceId>"`)로 이 로그에 도달 불가 | `.doOnError` 콜백이 Reactor Netty 이벤트 루프 스레드에서 실행 → 요청 스레드의 MDC 미전파 |
| 알람 | 익명 10명이 발생해도 알람 불가 | 집계 지표 부재 — `user_fallback` 계열 시리즈 없음, `http_client_requests_seconds_bucket{application="content-service"}`도 부재 |

실측 로그:

```
07:23:40.346Z  ERROR  [reactor-http-epoll-1]  [traceId=NONE,spanId=NONE,userId=NONE]
```

**예측 하나는 적용 전에 반증됐습니다.** *"셀렉터를 고쳐도(B-1) 이 로그에는 도달 불가"*
는 틀렸습니다 — `errorWarnQuery`는 traceId 없이 레벨로 거르는 라인 필터라 로그 자체는
도달합니다. 따라서 A-2의 가치는 도달 가능성이 아니라 **귀속 정확도**(어느 요청의
저하였는지를 traceId로 잇는 것)입니다.

### 개선 — 구멍마다 하나씩 (2026-08-02 코드 적용)

**A-1. 도메인 접두사 규약** — 검색을 메시지 문구가 아니라 접두사에 결합시킵니다.

| 위치 | 기존 | 변경 후 |
|---|---|---|
| 목록 조회 `doOnError` | `사용자 목록 조회 실패: userIds={}` | `[user-fallback] auth 목록 조회 실패 → {}명 익명 대체: userIds={}` |
| 단건 조회 `doOnError` | `사용자 정보 조회 실패: userId={}` | `[user-fallback] auth 단건 조회 실패: userId={}` |
| 단건·일괄 catch | `외부 서비스 (일괄) 호출 중 예외` | `[user-fallback] auth (일괄) 호출 예외` |
| 단건 폴백 catch | `[외부사용자 조회] 외부 API 호출 실패` | `[user-fallback] auth 단건 조회 실패 → 익명 대체` |
| `UserCacheStore` 25건 | 이모지 접두 또는 없음 | `[user-cache] ...` (이모지 제거) |

문구가 바뀌어도 `\|= "[user-fallback]"` 은 깨지지 않습니다 — round-2 A-4b가 하네스
프로브를 이 접두사로 전환합니다. 비실패 INFO 2건(유효하지 않은 userId · 캐시 미스)은
**의도적으로 제외**했습니다 — 접두사를 붙이면 프로브가 정상 동작을 폴백으로 집계합니다.

**A-2. 호출 시점 MDC 캡처 · 콜백 복원** — `traceId=NONE`을 해소합니다.

```java
Map<String, String> callerMdc = MDC.getCopyOfContextMap();   // 요청 스레드에서 캡처
...
.doOnError(error -> logWithCallerMdc(callerMdc, () ->
    log.error("[user-fallback] auth 단건 조회 실패: userId={}", userId, error)))
```

이벤트 루프 스레드의 MDC를 백업하고 캡처분을 복원한 뒤 로그를 남기고 원상 복구합니다.
로그 패턴의 `traceId=%X{traceId:-NONE}` 자리에 실제 값이 찍히므로 포맷 변경 없이
traceId 조회가 이 로그를 잡습니다. 전역 `Hooks.enableAutomaticContextPropagation()`
대신 클래스 국소 방식을 택했습니다 — 전역 훅은 앱 전체 로그 동작을 한 번에 바꿔
회차 2 델타에서 이 변경의 몫을 가릴 수 없습니다.

**A-3. fallback 카운터** — 사후 조사가 아니라 지표로 감지합니다.

```java
long fallbackCount = userIds.stream().distinct()
    .filter(userId -> !result.containsKey(userId)).count();
...
meterRegistry.counter("user.fallback").increment(fallbackCount);   // → user_fallback_total
```

`rate(user_fallback_total[5m]) > 0` 알람이 성립합니다. 목록 조회가 에러 없이 일부만
반환하는 부분 실패도 — 로그는 안 남지만 — 카운터에는 잡힙니다.

### 성과 — 말할 수 있는 것과 없는 것

**실측된 사실 (지금 말할 수 있는 것):**

| 사실 | 수치 |
|---|---|
| 접두사 혼용의 실해 | AU-4 하네스 프로브 `user_fallback` **0건 오보고** → 그 값이 앵커에 들어가 채점 불가 |
| `doOnError`의 traceId | 실측 `traceId=NONE` — 스레드 `reactor-http-epoll-1` |
| 적용 범위 | `[user-fallback]` 5건 · `[user-cache]` 25건 · 카운터 1개 — `compileJava` · 전체 테스트 통과 |

**예측 (배포 + AU-4 회차 2 주입 후 검증 — 미측정):**

| 항목 | 회차 1 (실측) | 회차 2 기대 | 반증 조건 |
|---|---|---|---|
| 하네스 `user_fallback` 프로브 | 0건 (문구 불일치) | 실제 발생 건수 | 여전히 0건 → 수집 경로 문제 |
| 로그의 traceId | `NONE` | 실제 traceId | `NONE` 유지 → 전파 지점 오판 |
| `user_fallback_total` | 시리즈 없음 | 익명 인원수만큼 증가 | 안 오르면 진입 지점 오판 |
| 조사 리포트 근거 | trace 단일 채널 | trace + 로그 양쪽 | 변화 없으면 병목은 로그가 아님 |

이 기준은 적용 전에 [round-2 README](../round-2/README.md)에 박제해 두었습니다.

### 왜 발견 즉시 고치지 않았나

결함 발견은 회차 1(07-27, NF-09)이지만 적용은 지금입니다. 회차 1을 baseline으로
고정해야 전후 델타가 성립하고, 도구 변경군(§1·§2, B군)과 같은 회차에 섞으면 점수가
움직여도 무엇 덕분인지 못 가리기 때문입니다. §1·§2와 달리 **이 변경만은 재조사로
검증되지 않습니다** — 신호가 새로 만들어지는 것이라 과거 데이터에는 존재하지 않고,
배포 후 AU-4 회차 2 주입이 필요합니다.

### 확산 — 같은 규약을 세 서비스 전 로그로 (2026-08-02)

처음 `external/user` 한정이었던 접두사 규약을 **세 서비스의 나머지 로그 전체로
확산**했습니다. 회차 2 측정 대상(`external/user`)이 이미 적용된 뒤라, 확산분은
AU-4 델타 해석과 분리됩니다.

- **규칙 먼저**: 접두사 체계를 [logging-format.md **R10**](../../../toy-content/docs/observability/logging-format.md)으로
  박제한 뒤 코드에 반영했습니다 (R8 규율 — 문서 먼저, 세 레포 같이).
- **규모**: content 88건(선행분 30건 외) · chat 225건 · auth(toy-auth-user-region) 99건 —
  **총 412개 로그 지점, 98개 파일.** 기존 대괄호 접두사 20여 종(`[Kafka]` `[FCM]`
  `[STOMP ...]` `[비번 재설정]` 등)과 이모지 접두를 R10 표의 도메인 접두사로 통일.
- **본문 불변 원칙**: 접두사 추가·이모지 제거만 하고 메시지 본문은 바꾸지 않았습니다.
  하네스 프로브가 아직 본문 문구(`알림 발행` · `DLQ` · `알림 처리 실패` ·
  `JWT 서명 검증 실패` 등)를 grep하기 때문입니다 — Loki `\|=`는 부분 문자열 매칭이라
  접두사 추가는 안전하고, 보존 여부는 전후 grep 개수 대조로 확인했습니다(chat 71건 동일 등).
  `[Kafka]` → `[notify]` 교체로 메시지에서 `Kafka`가 빠진 3개 라인은 로그 패턴의
  로거 이름(`KafkaNotificationProducer`)이 같은 라인에 찍혀 `\|= "Kafka"` 프로브가
  유지됩니다(패턴 확인, 배포 후 실측 대상).
- **검증**: 세 레포 `compileJava` · 전체 테스트 통과. 남은 이모지는 로그가 아닌
  컨텍스트(푸시 템플릿·주석·Swagger example)뿐이며, 지우면 사용자 노출 문자열이
  바뀌므로 보존했습니다.
- **미배포**입니다. 배포는 회차 경계에서, 프로브의 접두사 전환(A-4b류)과 함께 갑니다.

---

## 5. 탐색 후보의 판별력 — 필드는 응답에 있었고, 라벨은 필터를 못 넘었다

**변경군 B (조사 도구).** §1·§2가 **분석** 단계를 고쳤다면 이 절은 **탐색** 단계다.
후보 하나를 잘못 고르면 분석이 아무리 좋아도 끝이라는 것이 CH-3에서 실증됐다
(세 회차 연속 4점 — 조사 창이 주입 1초 전에서 끊겼다).

### 문제

탐색 LLM은 로그 원문도 span 원문도 받지 않는다. **후보 목록**과 채널별 도달 요약,
무신호 목록만 본다(`include-raw=false`, 2026-08-04 기본화 · 45,991자 → 7,697자).
그 후보의 군집 키가 `채널 | 리소스 | 지문`인데 두 채널에서 판별력이 무너져 있었다.

| 채널 | 후보가 들고 있던 것 | 문제 |
|---|---|---|
| Tempo | **루트 서비스·루트 span 이름뿐** | `content → kafka → chat` 트레이스도 `content` 하나로 보인다. 상류·하류가 안 보인다 |
| Loki | 서비스명 · 건수 · 5분 구간 | **지문이 항상 `ERROR/WARN`**. 한 서비스의 성격 다른 예외가 후보 하나로 뭉친다 |

Loki 쪽은 *"후보가 부실하면 원본 JSON이 메운다"* 던 첫 판단이 실측으로 반증됐다.
채널별로 재 보니 원본이 후보보다 더 주는 것은 Mimir 시리즈 라벨(32 KB)과 파손된 트레이스
행(1.4 KB) 둘뿐이고 **Loki는 0**이었다. Loki 원본 4,223 B 중 실제 데이터는 146 B이고
그 146 B가 곧 후보에 적힌 건수다. **후보 포맷 문제가 아니라 스윕 쿼리의 `sum by`가 내용을
지운 결과**다.

### 개선 ① — 지나간 서비스 (B-44 · 적용)

*"상류를 보려면 span을 받아야 하고 그건 탐색 단계에 너무 비싸다"* 고 봤는데, **저장 응답을
열어 보니 이미 들어 있었다.**

```json
"serviceStats": { "auth-service":    {"spanCount": 4},
                  "chat-service":    {"spanCount": 14},
                  "content-service": {"spanCount": 28} }
```

`reports/raw/*tempo-search*` 전수 주사 — **트레이스 645건 중 52건이 2개 이상.**
새 쿼리도 추가 호출도 추가 토큰도 없이 파싱만 추가했습니다.

**지문(군집 키)에는 넣지 않습니다.** 키에 넣으면 같은 엔드포인트가 상류 조합마다 다른
후보로 흩어집니다 — Mimir 시리즈 라벨을 키에서 뺀 것과 같은 이유입니다(라벨을 키에 넣으면
44개 시리즈가 44개 후보가 된다). `errorCount`가 0이면 적지 않습니다. 전부 적으면
**어느 서비스에서 에러가 났는가**라는 신호가 묻힙니다.

**검증 — 저장 스윕 재생 (주입·배포 불필요).** CH-1 회차 5b의 스윕 원본 8개를 지금 코드로
다시 태웠습니다. 그때 나온 후보 13개는 `reports/rounds/ch-1-round5b.json`에 박제돼 있습니다.

| 항목 | 기대 | 결과 |
|---|---|---|
| 후보 개수 | 불변(13) — 변하면 지문에 샌 것 | **13 = 13** |
| 신호 개수 | 불변(42) | **42 = 42** |
| 설명 증가분 | 몇백 자 | **124자 / 후보 2건** (탐색 컨텍스트 약 +1.6%) |

새로 보이게 된 줄입니다.

```
INC-8  content-service | http post /battles/{battleId}/items/{itemId}/comments
- 31,370ms (error 채널)  [지나간 서비스: chat-service 35 (err 10) · content-service 15]
                                        ↑ 에러 10건은 chat 에서 났다
```

**31초짜리 실패의 에러가 실제로는 chat에서 났다는 사실이 탐색 단계에 처음 도달했습니다.**
그 회차 리포트는 INC-8을 *"댓글 쓰기 자체가 실패"* 로 읽었는데, 다르게 읽혔을 여지가
있습니다 — **그것은 미측정입니다.**

> **재생 중에 잡은 하네스 함정.** 첫 재생이 후보 14개를 냈습니다. B-44 때문이 아니었습니다.
> `reports/raw/`의 파일명은 쿼리를 파일명으로 쓸 수 있게 소독한 형태(`min_over_time_mongodb_up_5m__`)라
> 그대로 키로 넘기면 `metricNameOf()`가 그것을 지표명으로 읽어 `zero-is-abnormal` 목록과
> 어긋나고 **"0이었다" 신호가 사라집니다.** 그러면 구멍이 60초를 넘어 후보가 갈립니다.
> **저장 원본으로 회차를 재생할 때는 쿼리 문자열을 반드시 복원해야 합니다.**

**남는 한계**: `serviceStats`는 서비스별 span 수·에러 수만 주고 호출 **방향**은 없습니다.
방향은 `parentSpanId`가 필요하고 그건 span 원문이라 §2의 호출 그래프 몫입니다.

### 개선 ② — 로그 지문 (B-45 · 코드 적용 · 기본 꺼짐 · **효과 없음이 실측됐다**)

`Series.logSignature()`는 이미 `exc` 라벨을 읽고 있었습니다. **라벨을 만드는 쿼리가
없었을 뿐**이라, 기존 총 건수 곡선은 그대로 두고 예외별 곡선을 하나 더 던지도록 했습니다
(설정이 비면 안 던지고, 응답이 비면 총 건수 곡선으로 자동 복귀).

**§3.2대로 켜기 전에 "이 신호가 실제로 도달하는가"를 먼저 쟀고, 예측이 반증됐습니다.**
라이브 Loki에 쿼리를 직접 던져 두 창에서 측정했습니다(2026-08-10).

| 확인 항목 | 결과 |
|---|---|
| ① 시리즈가 0이 아닌가 | **통과.** 매칭 실패 라인은 떨어지지 않고 `exc` 없는 시리즈로 남아 총 건수가 보존됩니다 (chat 2·4·24·37·4 + 2·5 = 원래 2·4·26·42·4) |
| ② 예외 클래스로 뽑히는가 | 🔴 **부분 실패.** 164건 중 **9건(5.5%)**, 진짜 예외 클래스는 **7건(4.3%)**. 다른 창은 552건 전부 **0%** |
| ③ 과분할하는가 | **안 합니다.** chat 2개 · auth 2개 · content 1개 |

**②가 실패한 이유는 §1과 같은 구조입니다.**

```
Loki 저장 모습
  14:23:11 ERROR [traceId=…] MongoRepository - 알림 저장 실패      ← 헤더 줄. "ERROR" 있음
  org.springframework.dao.QueryTimeoutException: Redis command…   ← 클래스는 여기. "ERROR" 없음

스윕 필터 |~ "ERROR|WARN"  →  헤더 줄만 통과  →  클래스 줄은 regexp 에 닿지도 못한다
```

§1에서 **분석 단계**는 라인 필터를 넓혀 해결했지만, **탐색 집계는 건수 왜곡 때문에 일부러
넓히지 않았습니다** — 예외 하나가 스택 30줄로 세어지면 발생량 곡선이 31배로 튑니다.
그래서 예외 클래스는 지금 구조에서 **구조적으로 얻을 수 없습니다.** 잡힌 9건 중 2건은
Logback이 축약한 **로거 이름**(`mc.e.t.a.c.e.GlobalException`)이라 오탐이기도 합니다.

**그래서 켜지 않습니다.** 판별력이 안 생기는데 후보 구조만 흔듭니다. 남은 갈래 셋 중
어느 것도 착수하지 않았습니다.

| 갈래 | 무엇 | 제약 |
|---|---|---|
| ⓐ | 앱이 헤더 줄에 예외 클래스를 찍게 한다 | **변경군 A** — B와 같은 회차에 섞지 않는다 |
| ⓑ | Alloy multiline 스티칭으로 스택을 한 엔트리로 | 관측 파이프라인 변경 · **과거 데이터로 검증 불가**(§1과 같은 제약) |
| ⓒ | 스윕에 라인 조회를 따로 건다 | 응답 크기 미측정 · `include-raw=false`로 -83%를 만든 직후다 |

### 함께 확인된 것 — 채널 간 병합 근거는 아직 없다

지금 채널을 잇는 결정적 근거는 traceId 하나뿐이고 **Tempo 신호에만** 있습니다.
로그 줄에는 `[traceId=…]`가 있지만 스윕이 `count_over_time`으로 개수만 세므로 줄이 없고,
줄이 없으니 traceId도 없습니다. 그래서 Mimir·Loki 후보는 시간이 겹쳐도
`같은 시각의 다른 후보`로 **표시만** 되고, 묶는 판단은 LLM이 합니다.

이것을 **의도적으로 남겨 둡니다.** `mongodb_up`이 0이 된 것과 `lag`이 밀린 것을 코드가
합치려면 *"chat이 Mongo를 쓴다"* 는 토폴로지가 필요한데, 그건 **채점 대상인 정답 구조를
코드에 심는 것**입니다(§2에서 프롬프트의 토폴로지 문장을 지운 것과 같은 원칙).

### 구현 — 어디에 무엇이 들어갔나

| 층 | 파일 | 내용 |
|---|---|---|
| 모델 | `report/Evidence.java` | `TraceHit.serviceStats` 추가 + `crossServiceText()` |
| 파싱 | `triage/survey/SurveyResult.java` | `serviceStats` 파싱 · `logSignatureRatesJson` 필드 |
| 신호 | `triage/incident/SignalExtractor.java` | 설명에 지나간 서비스 · 지문 곡선 우선/폴백 |
| 쿼리 | `triage/SurveyProperties.java` · `survey/Surveyor.java` | `log-signature-query`(기본 빈 값) |
| 설정 | `resources/application.yml` | 켜기 전 확인 항목을 주석으로 박제 |
| 테스트 | `CrossServiceSignalTest`(5) · `LogSignatureSignalTest`(4) | 픽스처는 저장 응답 원본 |
| 재생 | `tools/Ch1Round5bReplayTool` | 저장 스윕으로 회차 5b 재현 (`-Drca.tools=true`) |

`./gradlew test` 전체 통과.


AU-4 회차 2 주입이 답합니다.

---

## 결과

§1·§2는 새로운 데이터를 추가한 것이 아니라 **기존 Loki와 Tempo에 존재하던 관측 데이터를 분석 가능한 형태로 전달하는 것**에 목적을 두었습니다. §4만 방향이 반대입니다 — 존재하지 않던 신호(검색 가능한 접두사 · traceId 귀속 · fallback 카운터)를 앱에서 새로 만들었습니다.

```
관측 데이터
        ↓
수집·표현 방식 개선
        ↓
분석 가능한 Evidence 생성
        ↓
LLM 기반 RCA 분석
```

로그에서는 예외 원인과 스택 정보를 분석 컨텍스트까지 전달할 수 있게 되었고, Trace에서는
서비스 간 호출 관계가 엣지 단위 그래프로 컨텍스트와 리포트에 실리게 되었으며,
앱에서는 조용한 저하(익명 폴백)가 로그·traceId·지표 세 채널 모두에 신호를 남기게
되었습니다. **점수 효과는 전부 미측정**입니다 — 도구(§1·§2)는 재조사가, 앱 계측(§4)은
배포 후 AU-4 회차 2 주입이 답합니다.

---

## 구현 상태

| | 상태 | 검증 |
|---|---|---|
| **§1 로그 스택트레이스** | **적용 완료** (2026-07-30) | 테스트 통과 — 헤더·예외줄·스택프레임·`Caused by` 포착, 평범한 INFO 는 미포착. **점수 효과는 미측정** (AP-2 재조사 필요 · 주입 불필요) |
| **§2 Trace 호출 그래프** | **검증·구현 완료** (2026-07-31) | 저장 트레이스 17개로 추출 규칙 검증(규칙 2곳 수정·MongoDB 반증) → 픽스처 3건·테스트 5건으로 박제, `./gradlew test` 통과. **점수 효과는 미측정** (AU-4 재조사 필요 · 주입 불필요) |
| **§3 로그 기록 체계 통합** | 규칙 제정 완료 · **R9 코드 반영(content·chat)·미배포** · R1 미적용 · auth 보류 | compileJava 통과. 배포 후 도달 검증 4종은 [logging-format.md §6](../../../toy-content/docs/observability/logging-format.md) — **미측정** |
| **§4 익명 폴백 관측 보강 (A-1·A-2·A-3)** | **코드 적용 완료** (2026-08-02) · **미배포** | `compileJava` · 전체 테스트 통과. A-4b(프로브 접두사 전환)는 배포 후. **점수 효과는 미측정 — 재조사로는 검증 불가, AU-4 회차 2 주입 필요** |
| **§5 지나간 서비스 (B-44)** | **적용 완료** (2026-08-10) | 저장 스윕 재생으로 검증 — CH-1 회차 5b 재현 시 신호 42 · 후보 13으로 **박제값과 일치**(후보 개수 불변 = 지문에 안 샘), 설명 증가분 124자. 테스트 5건. **점수 효과는 미측정** |
| **§5 로그 지문 (B-45)** | **코드 적용 · 기본 꺼짐** (2026-08-10) | 라이브 Loki에 쿼리 직접 측정 — 시리즈는 나오지만 `exc` **부착률 5.5%(진짜 예외 4.3%) · 다른 창 0%**. 원인은 스윕 필터가 헤더 줄만 통과시키는 구조. **효과 없음이 실측돼 켜지 않는다.** 테스트 4건 |
| **§4 확산 — R10 접두사 세 서비스 전 로그** | **코드 적용 완료** (2026-08-02) · **미배포** | content 88 · chat 225 · auth 99개 로그 지점, 본문 불변(하네스 grep 문구 전후 개수 동일) · 이모지 제거. 세 레포 `compileJava` · 테스트 통과 |

§2에서 **남은 것은 하나**입니다.

- ~~**MongoDB 키**~~ — **해소(반증으로 확정, 2026-07-31).** `db.system=mongodb` Span은
  저장분에 존재하지 않는다. Mongo 장애는 receive Span의 `error` 속성으로만 온다 (§2 검증 표)
- **여러 Trace 누적** — 한 Trace 는 그 요청이 지난 경로만 담습니다. AU-4(auth → content 캐시)는
  한 Trace 로 되지만, **IN-1(Redis → 3서비스)은 세 요청이 필요**하므로 `B-9`(Trace N건 수집)이
  선행입니다

## 하지 않기로 한 것

- **정적 토폴로지 맵을 코드에 넣지 않습니다.** `"Redis는 content·auth·chat이 공유한다"` 를
  박으면 IN-1의 정답을 심는 것이 됩니다. **관측에서 유도하는 것만** 합니다
- **탐색 단계 집계 쿼리에 스택 패턴을 넣지 않습니다**(§1에서 서술한 이유)
- **프롬프트의 토폴로지 문장**(`content → Kafka → chat`)은 그래프 구현과 **같은 날 제거했습니다**
  (2026-07-31, 세 프롬프트 전부). 안 빼면 그래프 덕인지 문장 덕인지 귀속되지 않습니다
- ~~접두사 규약의 전 코드베이스 일괄 적용을 하지 않습니다~~ — **2026-08-02 확산 적용으로
  변경.** 회차 2 측정 대상(`external/user`)이 먼저 적용·분리된 뒤라 델타 해석과
  충돌하지 않습니다 (§4 확산). 단 **본문 재작성(R7 전면 적용)은 여전히 하지 않습니다** —
  본문을 바꾸면 하네스 프로브가 깨지므로, 본문 전환은 프로브 개정과 함께 회차 경계에서
- **전역 컨텍스트 전파 훅(`Hooks.enableAutomaticContextPropagation`)을 켜지 않습니다** —
  앱 전체 로그 동작이 한 번에 바뀌어 변경의 몫을 가릴 수 없습니다 (§4)

## 반증 조건

| 변경 | 개선이 없으면 |
|---|---|
| 로그 스택 수집 | 스택이 도달했는데도 AP-2 가 안 오르면 병목은 신호가 아니라 **추론**이다 |
| Trace 호출 그래프 | AU-4 가 안 오르면 병목은 관계 정보가 아니라 **배치**다 |
| 프롬프트 문장 제거 | **분석** 점수가 떨어지면 그래프가 그 문장만큼도 못 한 것이다. **탐색** 점수가 떨어지면 성질이 다르다 — 탐색은 그래프를 받지 못하므로 대체물 없는 제거였고, 탐색 프롬프트에 한해 문장 복원을 검토한다 (§2 파이프라인 절의 리스크) |
| R1 JSON 전환 (§3) | 전환 후에도 스택이 traceId 쿼리에 안 오면 원인은 줄 분리가 아니라 **수집기(Alloy) 단**이다 |
| 폴백 접두사 A-1 (§4) | 배포 후에도 프로브가 0건이면 병목은 문구가 아니라 **수집 경로**다 |
| MDC 캡처 A-2 (§4) | `traceId=NONE`이 유지되면 **전파 지점 오판** — 캡처 시점에 이미 MDC가 빈 경로가 있다 |
| fallback 카운터 A-3 (§4) | `user_fallback_total`이 안 오르면 **진입 지점 오판** — 익명 대체가 다른 메서드 경로로 일어난다 |
| 지나간 서비스 B-44 (§5) | 후보 **개수**가 변하면 지문에 샌 것이다. 상류가 보이는데도 탐색 선택이 안 바뀌면 병목은 정보량이 아니라 **프롬프트**다 |
| 로그 지문 B-45 (§5) | **이미 반증됐다** — 라벨 부착률 5.5%/0%. 켜도 후보가 한두 개 갈릴 뿐이라 착수 대상은 쿼리가 아니라 위 ⓐ~ⓒ다 |

**주장 범위**: 로그는 *"고쳤고 효과는 아직 재지 않았다"*, Trace 는 *"데이터에 관계 정보가
있는데 읽지 않고 있다는 것을 실측으로 확인하고, 저장된 원본 17개로 추출 규칙을 검증해
두 곳을 고친 뒤, 구현·테스트 박제까지 마쳤다"* 까지입니다. **두 쪽 다 점수 효과는
재조사(AP-2 · AU-4)가 답하며, 그 전에 효과를 주장하지 않습니다.** 앱 계측(§4)은
*"없던 신호를 만들었고 컴파일·테스트까지 확인했다"* 까지이며, 효과는 배포 후
