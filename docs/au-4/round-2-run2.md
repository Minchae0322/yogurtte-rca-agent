# AU-4 회차 2 · **2차 실행** — **83 / 100** · 🏆 **§8.1 N=2 성립(85.5 ± 2.5)** · 원인 항목이 **세 번째로 같은 값**

> **편차만 보려고 돌린 실험이다.** 문항·문안·앵커·루브릭·도구·모델을 고정하고 같은 조사를
> 한 번 더 돌렸다. AU-4는 §8.1이 요구하는 (문항 × 자 × 입력 모델) N≥2를 **처음으로 채웠다**.

## 한눈 요약

| | |
|---|---|
| **실제 원인** | `kubectl scale deploy/auth-service --replicas=0` **12분 54초 유지**(11:34:49~11:47:43Z) + user 캐시(TTL 10분) 만료 → content가 auth 직행 → **Connection refused** → 익명 fallback. 붕괴 아닌 **저하** |
| **입력** | **traceId 없음.** 실행 1과 **문자 그대로 같은 문안** — *"최근 2시간 안에 피드에 작성자 이름이 이상하게 나온다는 제보가 있다. 원인을 조사해줘"* |
| **탐색 결과** | 2시간 창 → **11:32:00~11:48:43Z(16분 43초)** · 대상 `content-service` + `auth-service` · traceId `6a69e7fe1224083c021ae372bcec4cc0` **자력 선정(후보 7건 중 Top-1)** |
| **에이전트 파악 원인** | 1순위(확신도 **높음**) *"auth-service 인스턴스 부재로 `GET /api/external/users` **커넥션 거부** → 작성자 이름 조회 실패"*. 2순위(중간) 실패를 삼키는 폴백이 대체값 노출. 3순위(**낮음**) 폴백 값의 Redis 잔존 — **자력 반증** |
| **§8 채점** | **83 / 100** — 원인 30 · 근거 25 · 탐색 15 · **영향 5** ⚠️ · 오귀인 3 · 조치 5. 앵커 [anchors-v2.md AU-4 (갈래 A)](../../../toy-content/docs/chaos/anchors-v2.md) — **실행 1과 동일 버전** |
| **모델** | `claude-opus-5` · `num_turns=1` · 라벨 정상 |
| **비용·시간** | 탐색 $0.5568 · 71.4s + 분석 $1.2355 · 159.8s = **$1.7923 · E2E 232.7s** |
| **에이전트 보고서 전문** | [round-2-run2-rca-report.md](round-2-run2-rca-report.md) |

> ## 🏆 이 회차의 핵심 — **AU-4가 §8.1을 충족했고, 갈린 항목이 또 그 항목이다**
>
> **평균 85.5 ± 최대편차 2.5 → ±10 이내 → 인용 가능.**
> 다섯 항목(원인 30 · 근거 25 · 탐색 15 · 오귀인 3 · 조치 5)이 **편차 0**이고,
> 갈린 하나가 **4) 영향 판정**이다 — **AP-3 쌍과 구조가 완전히 같다.**
>
> 그리고 실행 1의 4)는 그때도 ⚠️ 판정이었고 *"반대 판정(5)이면 총점 83"* 이라고 적혀 있었다.
> **판정을 어느 쪽으로 통일하든 밴드는 83~88이고, 통일하면 편차는 0이 된다.**
> → 분산의 출처가 **에이전트가 아니라 앵커의 중간 구간 부재**라는 [round-3 §5 ㄷ](../round-3/README.md)의
> 주장이 **두 번째 문항에서 재현**됐다.

## 🔴 그리고 원인 항목 30이 **세 번째**다

| | 회차 1 (07-28) | 회차 2 실행 1 (07-29 02:44) | **실행 2 (07-29 11:34)** |
|---|---|---|---|
| 입력 모델 | traceId 직접 | 자연어 | 자연어 |
| 자 | v3 앵커 4항목 | anchors-v2 6항목 | anchors-v2 6항목 |
| 분석 `out` | 2,980 | 7,982 | **10,342** |
| **1) 원인** | **30** | **30** | **30** |
| 사유 | *"캐시 만료 발현 조건 미언급"* | **같은 문언** | **같은 문언** |

**입력 모델·자·출력 분량(3.5배 폭)이 전부 바뀌었는데 같은 실패가 세 번 났다.**
회차 1의 진단(*"간결본 누락 — 분량이면 풀린다"*)은 실행 1에서 반증됐고, 이번에 **출력이 또
30% 늘었는데도(7,982 → 10,342) 그대로**여서 두 번 반증됐다. **남은 레버는 프롬프트(변경군 C)뿐이다.**

## 회차 2 실행 1 ↔ 실행 2 — 항목 대조

**같은 앵커·같은 자·같은 도구.** `src/`·`prompts/` 마지막 변경은 `7827b77`(2026-07-28T16:08:56Z)이고
두 실행 모두 그 이후이며 워킹트리 무수정이다.

| # | 항목 | 배점 | 실행 1 | **실행 2** | 편차 |
|---|---|---:|---:|---:|:-:|
| 1 | 원인 적중 | 40 | 30 | **30** | **0** |
| 2 | 근거 | 25 | 25 | **25** | **0** |
| 3 | 탐색 | 15 | 15 | **15** | **0** |
| **4** | **영향 판정** | 10 | 10 ⚠️ | **5** ⚠️ | **5** |
| 5 | 오귀인 | 5 | 3 | **3** | **0** |
| 6 | 조치 | 5 | 5 | **5** | **0** |
| | **합계** | **100** | **88** | **83** | **5** |

## 채점 — 앵커 v2 (anchors-v2.md AU-4 갈래 A · 루브릭 v3 6항목)

**채록 전 박제 확인**: 앵커 커밋 `add5aff` = 2026-07-28T16:09:54Z, 주입 = 2026-07-29T11:34:49Z.
**19시간 25분 앞선다.** toy-content 워킹트리 무수정.

> 항목마다 **① 채점 기준 → ② 이번 리포트 대조 → ③ 결론** 순이다.
> 인용문은 전부 [이 회차 리포트](round-2-run2-rca-report.md) 원문이다.

---

### 1) 원인 적중 — **30 / 40**

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **40** | auth 전면 다운 + **user 캐시 만료**로 직행 → fallback 발동. **두 조건의 결합**까지 |
| 30 | auth 다운 확정, **캐시 만료라는 발현 조건** 미언급 |
| 20 | "auth 이상"까지 |
| 0 | content·Redis 지목 |

**이번 리포트**

| 요건 | | 리포트가 쓴 것 |
|---|:-:|---|
| auth 전면 다운 확정 | ✅ | 1순위 · **확신도 높음** — *"auth-service 인스턴스 부재/재기동으로 인한 `GET /api/external/users` 커넥션 거부"*. *"약 **11분 45초간 스크레이프 대상 자체가 없음**"* |
| 이름의 출처가 auth임을 논증 | ✅ | **문언 이상** — *"content DB 쿼리는 `tb_feed`(21행) … **이름 컬럼을 가져오는 쿼리가 없음**"* |
| **캐시 만료를 발현 조건으로** | ❌ | 없음. Redis `GET` 5건을 *"cache-aside 패턴 정황"* 으로 읽었지만 **후보 3(폴백 값 잔존, 확신도 낮음)** 아래 뒀고, 후보 1은 **auth 부재만으로** 구성했다 |
| content·Redis **지목** | — | 아니다 — 1순위가 auth이고 후보 2·3은 *증상 노출 경로*·*지속 요인*으로 성격 한정 → 0점 사유 아님 |

**→ 30점.** 40과 갈리는 지점이 *"두 조건의 결합"* 인데 **캐시 만료를 원인 구성에 넣지 않았다.**
실행 1·회차 1과 **문언까지 같은 사유**다.

> **채점자 실측 — 캐시 만료의 직접 증거는 창 안에 있었다.**
> baseline 트레이스 `6a69e54f…`(11:34:39Z, 주입 **10초 전**)에 Redis **`SET ×4`** 가 있다(캐시 적재).
> TTL 10분 → **11:44:39 만료** → 첫 증상 트레이스가 **11:45:52**다. 1분 13초 뒤다.
> 즉 *"왜 주입 11분 뒤에야 증상이 나왔나"* 의 답이 같은 창의 두 트레이스 대비로 나온다.
> **에이전트는 그 baseline 트레이스를 받지 못했다**(결함 ④ — 선정 1건만 수집).

---

### 2) 근거 — **25 / 25**

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **25** | ⓐ **실패 방식 구별**(즉시 거절 vs 3s timeout) + ⓑ **fallback 로그**(`사용자 목록 조회 실패`) |
| 18 | 둘 중 하나 |
| 10 | 500·익명만 인용 |
| 0 | 근거 없음 |

**이번 리포트**

| 요건 | | 리포트가 쓴 것 |
|---|:-:|---|
| ⓐ 실패 방식 구별 | ✅ | *"**7.76ms 만에 즉시 실패, 타임아웃이 아니라 TCP 레벨 거부**"* + `error = finishConnect(..) failed: Connection refused: …10.43.13.21:8081` 축자 인용 |
| ⓐ 의미 해석 | ✅ | *"DNS는 정상 해석됨 … **ClusterIP에 Ready 엔드포인트가 없어 REJECT된 형태**"* |
| ⓑ fallback 로그 | ✅ | `ExternalUserApiClient - 사용자 목록 조회 실패: userIds=[1, 7, 9, 56]` 원문 + **두 파드 5건 연속** 열거 |
| 대안 배제 | ✅ | Kafka·DB풀·인프라·GC·401을 각각 지표로 배제. auth 과부하 가설도 `hikaricp=0`·GC 0으로 **자력 기각** |

**→ 25점.** 두 요건을 모두 충족했고 배제 논증까지 붙였다 — 실행 1과 같다.

---

### 3) 탐색 — **15 / 15**

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **15** | **S = `http get` client span `STATUS_CODE_ERROR`** + B(**정상 구간 트레이스의 auth 호출 span과 대조**) |
| — | T(시간창)는 [루브릭 §3](../scoring/rubric-v3.md) 공통 기준 |

**이번 리포트**

| 요건 | | 탐색이 한 것 |
|---|:-:|---|
| T 시간창 | ✅ | 좁힌 창 11:32:00~11:48:43Z가 `INJECT 11:34:49` ~ `REVERT 11:47:56`을 **전부 포함** |
| **S 대상 선정** | ✅ | 후보 **7건 중 Top-1**로 증상 트레이스 자력 선정. 선정 이유가 *"피드 조회가 **부분 실패**(스팬 1개만 error) … `spanCount 26~29` 대비 `errorCount=1`"* — **팬아웃 부분 실패 패턴을 정확히 짚었다** |
| 대상 서비스 | ✅ | `content-service` + `auth-service` **둘 다**(상·하류 동시 스코프) |
| B 대조군 | ⚠️ | 앵커가 예시한 **정상 트레이스 대조는 미수행**(결함 ④로 못 받음). 대신 *"content-service(2개 파드)와 chat-service는 전 구간 `up=1`로 연속"* + `hikaricp`·GC 정상으로 확보 |

**→ 15점.** 실행 1과 **같은 판정·같은 사유**다 — B는 §3의 택일 요건(정상 구간 **또는** 부재 신호)을
부재 신호 + 인프라 대조군으로 충족했다.

> **부수: 창 안의 무관 에러 트레이스 2건을 자력 기각했다.** 스윕 후보 7건 중 `/feeds/following`
> 2건(`6a69e01b…` 11:12:27 · `6a69e3aa…` 11:27:38)은 이 장애와 무관한데 고르지 않았다.
> **AU-2 회차 2 실행 2에서는 바로 그 `6a69e01b…`를 골라 1순위가 뒤집혔다** — 같은 도구가
> 같은 날 두 문항에서 반대 결과를 냈다. 차이는 **증상 트레이스가 창 안에 존재했는가**다.

---

### 4) 영향 판정 — **5 / 10** ⚠️ 채점자 판정

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **10** | **저하(degraded)이지 붕괴(broken)가 아님** — 200 유지 + **익명 전환**. fallback이 의도대로 버팀 |
| 5 | 유보 |
| 0 | 붕괴로 오판 · 또는 "정상"으로 판정 |

**이번 리포트**

| 요건 | | 리포트가 쓴 것 |
|---|:-:|---|
| 200 유지 · 저하≠붕괴 | ✅ | *"최상위 span은 `status=200`, `outcome=SUCCESS`, `exception=none`, 총 58ms로 **정상 종료** → 사용자 요청 자체는 실패하지 않음"* |
| 폴백 경로 인지 | ✅ | 후보 2 — *"제보 형태('에러가 난다'가 아니라 '이름이 이상하게 나온다')와 정확히 일치"* |
| **익명 전환 확정** | ❌ | *"**폴백이 실제로 어떤 값을 넣는지 관측 데이터에 없음** — null / 빈 문자열 / '알 수 없음' / userId 노출 / 다른 사용자 이름 혼입 중 무엇인지 판별 불가 … **데이터 부족**"* |
| 도달 범위 정량 | ❌ | 없음. 실행 1은 *"userIds 4명뿐이고 피드는 11건 → **전체가 아니라 일부 작성자만**"* 으로 정량화했다 |
| 지속 시간 | ❌ | *"조회창이 16분뿐이라 '최근 2시간 제보 전체의 원인'이라는 점까지는 **중간**"* |
| 0점 사유 | — | 붕괴 오판·"정상" 판정 **둘 다 피했다** |

**→ 5점.** 저하-붕괴 축은 확정했으나 **익명 전환과 도달 범위를 둘 다 유보**했다.
*"판별 불가"* · *"데이터 부족"* 이라고 쓴 대상이 앵커 10점 문언의 **`익명 전환`** 그 자체다.

> **⚠️ 판정 근거와 반대 판정.** 앵커 구간이 10/5/0뿐이라 *"저하는 확정, 범위·내용은 유보"* 가
> 문언 사이에 떨어진다. **AP-3 회차 2 실행 2에서 같은 성격을 5로 판정한 선례**를 따랐다
> (*"유보하며 쓴 단어가 앵커 5점 문언과 같다"*). **반대 판정(10) 시 총점 88 · 편차 0.**
>
> **편차를 0으로 만들려고 10을 주지 않았다** — §8.1은 안정성을 확인하는 절차이지 만드는 절차가 아니다.
> 실행 1이 10을 받은 근거 두 줄 중 하나(**도달 범위를 4/11명으로 정량화**)가 이번엔 없다.

**채점자 실측 — 익명 전환은 실제로 일어났다.** 하네스 symptom 채록 `t2-feed.json`:

```
baseline (11:34:38)  "nickname":"운영자"
symptom  (11:45:49)  "nickname":"사용자1"   ← 익명 fallback
```

**신호는 API 응답 본문에만 있고 관측 채널(trace·log·metric) 어디에도 없다.**
에이전트가 *"응답 본문 로그도, 폴백 카운터 메트릭도 없다"* 고 정확히 지적한 그대로다.
→ **[round-3 ㅂ](../round-3/README.md)(관측이 우연히 존재해야만 달성 가능한 요건) 계열이 확인됐다.**

---

### 5) 오귀인 — **3 / 5**

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **5** | content·Redis **미지목** |
| 3 | 후보로만 (자력 반증 + 확신도 낮게 유지) |
| 0 | content 탓 |

**이번 리포트**

| 요건 | | 리포트가 쓴 것 |
|---|:-:|---|
| Redis 미지목 | ❌ | 후보 3 — *"조회 실패/폴백 값이 Redis 사용자 캐시에 잔존"* |
| 자력 반증 | ✅ | *"trace에 Redis `SET`/`SETEX` span이 **하나도 없음** → 캐시에 쓴다는 직접 증거 없음"* · `up{job="redis"}=1` · 확신도 **낮음** |
| content 오귀인 | — | 후보 2(폴백)는 **정답 구조의 일부**이고 증상 노출 경로로 다뤘다 → 오귀인 아님 |

**→ 3점.** 실행 1과 **같은 판정·같은 사유**(Redis를 후보로 올리되 자력 반증).

---

### 6) 조치 — **5 / 5**

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **5** | auth 복구 + **캐시 TTL·워밍 검토** |
| 3 | 복구만 |
| 0 | 무관 조치 |

**이번 리포트**

| 요건 | | 리포트가 쓴 것 |
|---|:-:|---|
| auth 축 | ✅ | 조치 6 — *"`replicas ≥ 2` + **PodDisruptionBudget** — 단일 파드가 SPOF이며 재기동 전체 구간이 곧 장애 구간"* |
| **캐시 TTL** | ✅ | 조치 4 — *"폴백 값을 Redis에 캐싱하는지 확인. 캐싱한다면 **해당 키 TTL 확인 후 무효화**(`SCAN` → 오염 키 삭제)"* |
| 워밍 등가 | ✅ | 조치 5 — *"실패 결과는 캐시 금지 + 실패 시 **'마지막 정상값 유지'**"*(§8.2 충분 근거로 워밍 계열과 등가) |
| 재발 방지·관측 보강 | ✅ | 조치 7(`up` 결측 알림) · 조치 8(Reactor context-propagation으로 `traceId=NONE` 해소) |

**→ 5점.** 실행 1과 동일.

---

## ⚠️ 조건 차이 — 실행 1과 같지 않은 것 (3건, 전부 밝힌다)

| | 실행 1 (02:44Z) | **실행 2 (11:34Z)** |
|---|---|---|
| 다운 유지 | 11분(available 13분) | **12분 54초**(available 13분) — 사실상 동일 |
| **창 안 타 문항 신호** | 없음 | **`/feeds/following` 에러 2건** — 11:12:27(AU-3 하네스 게이트 프로브) · **11:27:38(채점자 실험)** |
| 실행 플랫폼 | Windows | **macOS**(앱·CLI·프롬프트 동일, `C` 26,576 → **21,309**) |

> ### 🔴 채점자 자신이 관측 데이터를 오염시켰다 — 밝히고 넘어간다
>
> `6a69e3aa…`(11:27:38Z)는 **AU-2 실행 2를 채점하며 내가 돌린 반증 실험**이다
> (서명을 1자 훼손한 토큰 → `/feeds/following` 500). 그것이 이 회차 스윕 후보 7건 중 1건으로 들어왔다.
>
> **결과에는 영향이 없었다** — 좁힌 창(11:32:00~)이 그 트레이스(11:27:38)를 배제했고
> 에이전트도 고르지 않았다. 그러나 **채점자의 검증 행위가 다음 회차의 입력이 된다**는 사실이
> 실측으로 확인됐다. 반증 실험은 **조사 창 밖에서 하거나, 하면 반드시 기록**해야 한다.
> → [round-3](../round-3/README.md) 절차 항목으로 등재.

## 장애 상황 (채점자 재구성)

| 시각(UTC) | 사건 | 근거 |
|---|---|---|
| 11:34:38 | 로그인 **200**(221ms) · 피드 **200**, 작성자 **"운영자"** | baseline 채록 |
| 11:34:39 | 피드 트레이스 `6a69e54f…` — **36 span** · auth 호출 `http get` **88.26ms 성공(200)** · Redis **`GET ×4` + `SET ×4`** | **캐시 적재 시점** |
| **11:34:49** | **`INJECT AU-4` — `scale --replicas=0`** | `timeline.log` |
| 11:35~ | `status_replicas_available` **1 → 0** | kube-state-metrics |
| 11:36:00 | 구 파드 `glc4w` `up` 마지막 샘플 | Mimir |
| **~11:44:39** | **user 캐시 TTL(10분) 만료** — 적재 11:34:39 + 600s | `UserCacheStore.DEFAULT_TTL` |
| 11:45:52 | 첫 증상 — `/feeds/scroll` **200 · 62ms**, `http get` **refused**, fallback 로그 | `6a69e7f0…` |
| 11:45:49 | 증상 채록 — `t2-feed.json` 작성자 **"사용자1"**(익명 전환) | evidence |
| 11:45:52~11:46:06 | 증상 트레이스 **5건**(`/feeds/scroll` 4 · `/feeds/{feedId}` 1) · fallback 로그 **5건**(두 파드) | Tempo · Loki |
| ~11:45:53 | 원복 `scale --replicas=1` → 신 파드 `vpkqw` | |
| 11:47:43 | `Started ToyAuthApplication in 101.003 seconds` | Loki |
| **11:47:56** | **`REVERT AU-4`** — 로그인 200 복귀 확인 | `timeline.log` |
| 11:48:00 | 신 파드 `vpkqw` `up` 최초 샘플 | Mimir |
| 11:48:43 | 조사 스윕 시작 | |

- **auth 프로세스 부재 12분 54초** — user 캐시 TTL 10분을 **2분 54초 초과**해 갈래 A(만료)가 성립
- 증상 트레이스의 client span: `Connection refused … 10.43.13.21:8081` · **4.81~7.76ms** ·
  `WebClientRequestException` · `STATUS_CODE_ERROR` — **3s timeout이 아니다**(AU-1과의 변별점)
- fallback 로그는 여전히 `traceId=NONE`(NF-09 · A-2 미적용). 에이전트는 이번에도 **밀리초 시각
  상관**으로 귀속을 복원했다 — 실행 1과 같은 우회

## 토큰

| 단계 | 총 `in` | 문자 | ① `in − 21,309` | ② `chars × 0.524` | 격차 |
|---|---|---|---|---|---|
| 탐색 | 54,310 | 59,373 (컨텍스트 58,177 + 프롬프트 1,196) | **33,001** | **31,111** | +6.1% |
| 분석 | 108,141 | 155,302 (컨텍스트 154,727 + 프롬프트 575) | **86,832** | **81,378** | +6.7% |
| **합계** | **162,451** | **214,675** | **≈ 119,833** | **≈ 112,490** | **+6.5%** |

- **`█ 실측`** — `coverage.overheadTokens = 21,309`. **AU-2 실행 2와 같은 값**(29분 간격, 같은 macOS 실행).
  Windows 회차의 `26,576`과 갈리는 축이 플랫폼일 가능성이 두 표본으로 늘었다 — **여전히 가설이다.**
- **비용 $1.9451 → $1.7923(−7.9%).** 창이 25분 → **16분 43초**로 줄어 분석 컨텍스트가
  203,219 → **154,727자(−23.9%)** 가 됐다. **창 크기가 비용을 지배한다**는 실행 1의 관찰이 역방향으로 재확인됐다.
- **탐색이 비쌌다** — `out` 2,615 → **5,326(2배)**, 시간 42.4s → **71.4s**. 후보가 1건 → **7건**이라
  랭킹 서술이 길어졌다. 그 대가로 **Top-1을 정확히 골랐다.**
- 분석 `out` 7,982 → **10,342(+30%)** 인데 1) 원인은 그대로 30이다 — **분량 가설 네 번째 반례.**

## 스크린샷용 traceId

| 용도 | traceId |
|---|---|
| **증상 — 에이전트가 고른 트레이스** (29 span · `http get` **refused 7.76ms** · 루트 **200**) | `6a69e7fe1224083c021ae372bcec4cc0` |
| **대조군 — 주입 10초 전 baseline** (36 span · auth 호출 **88.26ms 성공** · Redis **SET ×4**) | `6a69e54f055f8d9188d81945cf7b19fa` |
| 증상 첫 트레이스 (11:45:52 · 28 span) | `6a69e7f00e63c377af1b51ebea9ca59d` |
| 무관 — 채점자 실험(11:27:38) · AU-3 게이트 프로브(11:12:27) | `6a69e3aacb0cb1b86516674f2cd52e45` · `6a69e01bc0116147bba4b75907e9d57e` |

**두 트레이스를 나란히 놓는 것이 이 문항의 대표 그림이다** — 같은 `/feeds/scroll`인데
한쪽은 `http get` **88ms 성공 + SET ×4**, 다른 쪽은 **refused 7.76ms + SET 없음**.

## 다음 회차로 넘기는 것

1. **🏆 §8.1 N=2 성립 — 그런데 편차 전량이 앵커에서 나왔다.** AP-3에 이어 **두 번째 문항**이고,
   두 문항 다 **4) 영향**에서 갈렸다. [round-3 §5 ㄷ](../round-3/README.md)(중간 구간 신설)의
   근거가 이제 문항 두 개다.
2. **🔴 1) 원인 30이 N=3 재현.** 입력 모델·자·분량이 전부 바뀌어도 **캐시 만료 결합**을 못 세운다.
   **변경군 C(프롬프트)의 단독 측정 지점으로 이 문항이 가장 적합하다** — 다른 다섯 항목이
   이미 편차 0으로 안정돼 있어 프롬프트 델타가 원인 항목에 그대로 드러난다.
3. **🔴 결함 ④ — 이번엔 "캐시 만료"의 직접 증거를 막았다.** baseline 트레이스(`SET ×4`, 주입 10초 전)가
   **좁힌 창 안에 실재**했는데 선정 1건 제한으로 오지 않았다. **B-9가 이 문항의 원인 10점에 직결된다.**
4. **익명 전환 신호가 관측 채널 밖이다** — API 응답 본문에만 있다. 4) 영향 만점 요건이
   구조적으로 도달 곤란(CH-1의 결함 11과 같은 계열) → **round-3 ㅂ에 병합**.
5. **🔴 채점자 실험이 관측 데이터를 오염시킨다** — 반증 실험은 조사 창 밖에서 하거나 반드시 기록한다.
6. `401 rate` 메트릭 no series — 또 실패.
