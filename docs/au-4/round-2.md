# AU-4 회차 2 — 자연어 진입 · **88 / 100** · 회차 1의 실패가 **N=2로 재현**됐다

## 한눈 요약

| | |
|---|---|
| **실제 원인** | `kubectl scale deploy/auth-service --replicas=0` **11분 유지**(02:44:40~02:55:40Z) + user 캐시(TTL 10분) 만료 → content가 auth 직행 → **Connection refused** → 익명 fallback. 붕괴 아닌 **저하** |
| **입력** | **traceId 없음.** 자연어 한 줄 — "최근 2시간 안에 피드에 작성자 이름이 이상하게 나온다는 제보가 있다. 원인을 조사해줘" |
| **탐색 결과** | 2시간 창 → **02:40:00~03:05:00Z(25분)** · 대상 **`content-service` + `auth-service` 둘 다** · traceId `6a696b6e4373bd66c1dc571d8ff6a03a` **자력 선정** |
| **에이전트 파악 원인** | 1순위(확신도 **높음**) "auth 파드 부재 구간에 content의 사용자 조회가 **TCP 레벨에서 실패**". 2순위(중간) content 폴백이 예외를 삼키고 200 반환 — **결합 필요를 명시**. 3순위(낮음) Redis 캐시 잔존 — **자력 반증** |
| **§8 채점** | **88 / 100** — 원인 30 · 근거 25 · 탐색 15 · 영향 10 · 오귀인 3 · 조치 5. 앵커 [anchors-v2.md AU-4](../../../toy-content/docs/chaos/anchors-v2.md) (채록 **10시간 35분 전** 커밋) |
| **모델** | `claude-opus-5` · `num_turns=1` · 라벨 정상 |
| **비용·시간** | 탐색 $0.4596 · 42.4s + 분석 $1.4855 · 128.9s = **$1.9451 · E2E 172.7s** — **또 최고 경신**(직전 AU-2 $1.3892) |
| **에이전트 보고서 전문** | [round-2-rca-report.md](round-2-rca-report.md) |

> ### 입력에서 바꾼 것 하나 — "최근 1시간" → **"최근 2시간"**
>
> 주입이 조사 시점보다 **1시간 4분 앞서** 있어 1시간 창으로는 덮이지 않는다.
> anchors-v2 문안 템플릿의 `<시각>` 자리에 해당하므로 규격 안이고, rubric §3도
> *"과도하게 넓은 창은 그 자체로 감점하지 않는다"* 고 규정한다. **나머지 문안 골격은
> 앞선 자연어 4회와 동일**하다. 회차 간 비교 시 이 사실을 함께 밝힌다.

## 채점 — 앵커 v2 (anchors-v2.md AU-4 갈래 A · 루브릭 v3 6항목)

**채록 전 박제 확인**: 앵커 커밋 `add5aff` = 2026-07-28T16:09:54Z, 주입 = 2026-07-29T02:44:40Z.
**10시간 35분 앞선다.**

| # | 항목 | 배점 | 점수 | 판정 근거 (앵커 문언 대조) |
|---|---|---|---|---|
| 1 | 원인 적중 | 40 | **30** | 만점 = "auth 전면 다운 + **user 캐시 만료**로 직행 → fallback 발동. **두 조건의 결합**까지". auth 부재는 3소스 교차로 확정(*"약 12분 15초 동안 auth 인스턴스가 하나도 관측되지 않음"*). **캐시 만료를 발현 조건으로 세우지 못했다** — Redis GET 4개 → auth 4명 조회를 관측하고 *"캐시 미스분만 원격 조회하는 read-through 패턴"* 이라 해석했으나, 그것을 **후보 3(캐시에 비정상 값 잔존, 확신도 낮음)** 아래 두었고 후보 1은 auth 부재만으로 구성했다 → 30("auth 다운 확정, **캐시 만료라는 발현 조건** 미언급") 문언에 정확히 해당. 0점 사유(content·Redis **지목**)에는 걸리지 않음 — 1순위가 auth이고 content는 증폭 층위로 명시 |
| 2 | 근거 | 25 | **25** | 만점 = "ⓐ **실패 방식 구별** + ⓑ **fallback 로그**" 2요건. ⓐ *"에러 유형이 timeout이나 UnknownHost가 아니라 **Connection refused (ClusterIP 10.43.13.21까지 도달)** → DNS/네트워크 문제가 아니라 **Service 뒤에 Ready 엔드포인트가 없었던 상태**"* — 실패 방식을 구별하고 그 의미까지 읽었다. ⓑ `ExternalUserApiClient - 사용자 목록 조회 실패: userIds=[1, 3, 7, 9]` 원문 인용 + **트레이스와 밀리초 일치 확인**. **⚠️ 앵커 개정이 섞여 있다** — 아래 |
| 3 | 탐색 | 15 | **15** | 만점 = "S = **`http get` client span `STATUS_CODE_ERROR`** + B". S 정확 적중(그 트레이스를 자력 선정). **T**: 2시간 창이 주입 전체 포함, 좁힌 창 02:40~03:05도 주입(02:44:40~02:57:40) 전체 포함 ✅. **대상을 `content-service` + `auth-service` 둘 다** 잡았다 — **AU-2에서 못 한 상·하류 동시 스코프를 여기선 했다**. B는 §3 택일 요건을 부재 신호 + 인프라 대조군 명시로 충족. **단 앵커가 예시한 정상 트레이스 대조는 미수행** — 아래 |
| 4 | 영향 판정 | 10 | **10** | **⚠️ 채점자 판정 1건** — 아래 |
| 5 | 오귀인 | 5 | **3** | 만점 = "content·Redis **미지목**". 3 = "후보로만". **Redis를 후보 3으로 올렸다** → 3. 단 확신도 **낮음** + **자력 반증**(*"관측된 Redis span은 GET 4건뿐이고 SET/SETEX span은 없음 → 실패 결과를 캐시에 기록한 흔적이 없음"*, `up{job="redis"}=1`). **AP-2 회차 2 선례와 동일 처리**(DB 계열 후보 + 자력 반증 → 3). content는 오귀인이 아니다 — **폴백은 정답 구조의 일부**이고 에이전트도 원인이 아닌 증폭 층위로 다뤘다 |
| 6 | 조치 | 5 | **5** | 만점 = "auth 복구 + **캐시 TTL·워밍 검토**". auth 축은 복구보다 강하게(조치 10 = `replicas ≥ 2` + PDB로 **재기동 중 단절 제거**). 캐시 축도 셋 — 조치 8 `TTL`로 문제 userId 캐시 값 확인·삭제로 즉시 완화, 조치 9 negative caching 제거, **조치 11② "캐시된 마지막 정상 값 사용"**(§8.2 충분 근거로 워밍 계열과 등가). **회차 1이 5/10이던 사유(캐시 조치 부재)가 해소됐다** |
| | **합계** | **100** | **88** | |

### ⚠️ 채점자 판정 1건 — 4) 영향 판정을 10으로 본 근거

앵커의 영향 배점이 **10 / 5 / 0** 세 단계뿐이고, 행동이 그 사이에 있다.
**AP-3 회차 2와 같은 구조의 판정이며 같은 기준을 적용했다**(일관성).

| 앵커 문언 | 실제 | |
|---|---|---|
| 10: "**저하이지 붕괴가 아님** — 200 유지" | *"부모 SERVER span은 `outcome=SUCCESS`, `status=200`, `exception=none`, 79ms 정상 종료… **예외를 상위로 전파하지 않고 삼킨 뒤 응답을 구성**"* + Loki `[HTTP] GET /api/feeds/scroll 200 - 79ms` | 충족 |
| 10: 도달 범위 | *"조회 실패 대상은 `userIds=[1,3,7,9]` **4명**뿐이고 피드는 11건 … **전체가 아니라 일부 작성자만** 비정상"* | 충족 — **정량까지** |
| 0: "붕괴로 오판 · 또는 '정상'으로 판정" | 둘 다 피함 | 해당 없음 |
| 5: "유보" | **지속 시간**은 유보 — *"조회 창이 제보 범위의 약 1/5(25분)만 커버. 영향 범위·지속 시간은 판단 불가"* | 부분 |

**10으로 판정한 근거**: rubric v3 §4의 세 축(유실/지연 · **저하/붕괴** · 도달 범위)을 모두 맞혔다.
저하-붕괴 판정을 span status로 논증했고, 도달 범위를 **4/11명으로 정량화**했다.
유보한 것은 *지속 시간*이고 그건 창이 25분이라는 도구 사실에서 온다.
**AP-3 회차 2에서 같은 성격의 판정을 10으로 준 선례를 따른다.**

**반대 판정(5)이면 총점 83.**

> 부수: 에이전트는 이 폴백을 **부정적으로** 평가했다(조치 11 *"실패를 삼키지 말고"*).
> 앵커의 *"fallback이 의도대로 버팀"* 과 톤은 반대이나, **사실 판정(200 유지·부분 저하)** 은
> 일치한다. 앵커가 재는 것은 톤이 아니라 판정이므로 감점 사유로 보지 않았다.

### ⚠️ 2) 근거 25점 만점에는 **앵커 개정이 섞여 있다 — 델타로 인용 금지**

회차 1은 근거 **20/30**이었고 회차 2는 **25/25**다. **그 차이의 일부는 자가 바뀐 것이다.**

| | 회차 1 앵커 (v3) | **회차 2 앵커 (anchors-v2)** |
|---|---|---|
| 요건 수 | **3종** — 실패 방식 · 익명 전환 · **baseline 대조** | **2종** — 실패 방식 · **fallback 로그** |
| 배점 | 30 | 25 |
| 변경 사유 | | ③ 익명 전환 **제거**(유형 E — 질문 문안의 증상을 근거로 요구하면 순환논증) · ② baseline 대조를 **3) 탐색으로 이동** · fallback 로그 **복원** |

**회차 2 대기열의 인용 규율 그대로다** — *"③ 앵커 수정으로 오른 점수는 개선이 아니다."*
근거 항목의 상승은 **인용하지 않는다.**

**다만 fallback 로그 도달 자체는 도구 개선(변경군 B)의 실증이다.**
[round-2 대기열](../round-2/README.md)이 *"`log.error`라 `errorWarnQuery`로 도달 가능 —
A-2(리액티브 traceId 전파) 없이도 로그 자체는 회차 2에서 보인다. **A-2의 값어치는
도달 가능성이 아니라 귀속 정확도에 있다**"* 고 예측했는데, **문자 그대로 맞았다.**

```
2026-07-29 11:54:38.498 [reactor-http-epoll-1] ERROR [traceId=NONE,spanId=NONE,userId=NONE]
  c.e.t.e.u.s.ExternalUserApiClient - 사용자 목록 조회 실패: userIds=[1, 3, 7, 9]
```

**`traceId=NONE`이 그대로다**(NF-09 · A-2 미적용). 그런데 에이전트가
*"트레이스와 **밀리초 단위로 일치**"* 로 시각 상관을 걸어 **귀속을 스스로 복원했다.**

## 🔴 회차 1의 실패가 **N=2로 재현됐다** — 이 프로젝트에서 처음

| | 회차 1 (07-28) | 회차 2 (07-29) |
|---|---|---|
| 입력 | traceId 직접 | **자연어** |
| 자 | v3 앵커 · **4항목**(근본40·근거30·오귀20·조치10) | anchors-v2 · **6항목** |
| 총점 | **75** | **88** |
| **원인/근본** | **30** — *"캐시 만료 발현 조건 미언급"* | **30** — **같은 문언, 같은 사유** |
| 조치 | 5/10 — 캐시 조치 없음 | **5/5** — 캐시 조치 3건 |
| 출력 | out 2,980 (간결본) | out 7,982 |

**총점 75 → 88을 "개선"으로 인용하면 안 된다** — 자가 4항목에서 6항목으로 바뀌었다.

**그러나 원인 항목의 30은 두 회차가 같은 자(부분점 문언 동일)로 같은 값을 냈고,
사유도 동일하다.** 입력 모델·출력 분량(2,980 → 7,982, **2.7배**)이 모두 바뀌었는데도
**캐시 만료 발현 조건을 못 세우는 실패가 그대로 재현**됐다.

> **회차 1의 예측이 맞았다.** 그 기록은 *"근본 −10·조치 −5(캐시 만료 미식별)는 도구 결함이
> 아니라 간결본 누락이라 **프롬프트 튜닝/N≥2 평균으로만** 오른다"* 고 했다.
> **분량이 2.7배가 됐는데도 근본은 그대로였다** — 즉 *간결본 누락*이라는 진단은 틀렸고,
> **분량으로 해결되지 않는 구조적 실패**다. 남은 레버는 **프롬프트(변경군 C)** 뿐이다.

**AU-4는 §8.1 N≥2에 가장 가까운 문항이지만 아직 아니다** — 자와 입력 모델이 함께 바뀌었다.
다만 **항목 단위(원인 30)로는 재현이 성립**하고, 이것이 §8.1이 요구하는 "재현되는 실패"의
첫 사례다.

## 🔴 결함 ④ — 세 문항 연속 재현, 그리고 이번엔 **원인 항목**을 막았다

**채점자 실측** (Tempo `{ name = "http get /feeds/scroll" }`, 스윕 창 안):

| | 시각 | auth 호출 span | span | Redis GET |
|---|---|---|---|---|
| **baseline** | 02:43:24.469 | `http get` **446.44ms 성공** | 36 | **4** |
| **symptom** | 02:54:38.461 | `http get` **25.73ms · `Connection refused`** | 28 | **4** |

**앵커의 B가 요구한 "정상 구간 트레이스의 auth 호출 span과 대조(성공 ↔ refused)"가
스윕 창 안에 실재했다** — 이 회차 값으로는 **446.44ms ↔ 25.73ms**다.
에이전트는 못 받았다(선정한 traceId 1건만 심층 수집).

**그리고 이번엔 손실이 탐색이 아니라 원인 항목에 걸렸다.**

두 트레이스 모두 **Redis GET ×4 뒤에 auth를 호출**한다 → **캐시는 주입 전부터 이미 만료돼
있었다.** baseline이 있으면 *"평시에도 auth를 부른다 = 캐시가 만료된 상태"* 가 직접 보이고,
거기서 **"auth 다운 + 캐시 만료"의 결합**이 논증된다. AU-2(01:29:57Z)의 캐시 히트 트레이스가
**auth 호출 span 0건**이었던 것과의 대조가 이 문항의 정체다.

> **즉 결함 ④가 막은 것이 세 문항에서 각각 다른 항목이었다.**
>
> | 문항 | 결함 ④가 막은 것 | 걸린 항목 |
> |---|---|---|
> | AP-2 | 정상 트레이스 전량(에러 트레이스 0건) | 근거 · 원인 |
> | AP-3 | 정상 피드 생성 대조 | 탐색 B(메트릭으로 대체 성공) |
> | **AU-4** | **baseline auth 호출 span** | **원인(캐시 만료 결합)** |
> | AU-2 | 장애 중 정상 피드 트레이스 | 원인 · 근거 · 영향 · 조치 |

**[round-3 대기열 B-9](../round-3/README.md)의 우선순위를 다시 확인시켜 준다.**

## 탐색 단계 (15/15) — **AU-2에서 못 한 것을 여기선 했다**

| 항목 | 값 | 판정 |
|---|---|---|
| 시간창 해석 (T) | "최근 2시간" → `02:02:37Z ~ 04:02:37Z` | ✅ 주입(02:44:40~02:57:40) 전체 포함 |
| 스윕 | Tempo · Loki · Mimir · **실패 0** (survey 2,507ms) | ✅ |
| 대상 선정 (S) | **`content-service` + `auth-service` 둘 다**, 창 `02:40~03:05Z` | ✅ **상·하류 동시 스코프** |
| traceId | `6a696b6e4373bd66c1dc571d8ff6a03a` | ✅ **client span `STATUS_CODE_ERROR` 보유 트레이스** |
| 대조군 (B) | 인프라 4종 + chat 연속성을 **"대조군"이라 명시**하며 확보 | ✅ (§3 택일) — 단 앵커 예시(정상 트레이스)는 미수행 |

**선정 이유가 두 신호를 엮었다**: *"auth-service가 사라진 02:42~02:57 공백과 content-service
/feeds/scroll 에러(02:54:38Z)가 **정확히 겹치므로**"*.
**AU-2에서 auth만 보고 content를 안 본 것과 대조된다** — 증상 문안이 content 쪽("작성자 이름")을
가리킨 것이 스코프 확장을 유도했을 개연성이 크다.

> **이 관찰이 변경군 C(프롬프트)의 설계 근거를 보강한다.** 증상이 하류를 가리키면 상·하류를
> 함께 보고, 상류를 가리키면(AU-2의 "로그인") 상류만 본다. **질문에 없는 쪽을 스스로 보러
> 가지는 않는다** — [round-3 C-1](../round-3/README.md)이 메우려는 지점이다.

## 장애 상황 (채점자 재구성)

| 시각(UTC) | 사건 | 근거 |
|---|---|---|
| 02:43:24.469 | 피드 스크롤 **200 · 515ms** — auth 호출 **446ms 성공** | baseline `6a6968cc…` |
| **02:44:40** | **`spec_replicas` 1 → 0** (주입) · `available` 1 → 0 | kube-state-metrics |
| 02:45:15 | `up{auth-service}` 마지막 스크레이프(pod `wf6c7`) | Mimir |
| **02:54:38.461** | 피드 스크롤 **200 · 81ms** — auth 호출 **25.73ms `Connection refused`** → 익명 fallback | symptom `6a696b6e…` |
| 02:54:38.498 | `ExternalUserApiClient - 사용자 목록 조회 실패: userIds=[1, 3, 7, 9]` (`traceId=NONE`) | Loki |
| 02:54:38.541 | `[HTTP] GET /api/feeds/scroll **200** - 79ms` | Loki |
| **02:55:40** | `spec_replicas` 0 → 1 (원복) — **11분 유지** | |
| 02:57:30 | 새 pod `pr892` 첫 스크레이프 · `available` 0 → 1 — **13분 불가용** | |
| 04:02:37 | 조사 스윕 시작 (주입 **약 1시간 18분 후**) | |

- **갈래 A 성립**: auth 호출 refused인데 **응답은 200** — 에러율·지연 어느 쪽에도 안 잡히는 "성공한 장애"
- 캐시 TTL 10분 < 유지 11분 → **만료 조건 충족**. baseline(02:43)도 이미 auth를 호출하고 있었다
- 수집 누락 1건: `401 rate` 메트릭 no series — **5회 연속**

## 토큰 — 오버헤드는 이 회차에도 실측했다

| 단계 | 총 `in` | 문자 | ① `in − 26,576` | ② `chars × 0.524` | 격차 |
|---|---|---|---|---|---|
| 탐색 | 56,615 | 53,949 (컨텍스트 52,718 + 프롬프트 1,231) | **30,039** | **28,269** | +6.3% |
| 분석 | 141,810 | 203,816 (컨텍스트 203,219 + 프롬프트 597) | **115,234** | **106,800** | +7.9% |
| **합계** | **198,425** | **257,765** | **≈ 145,273** | **≈ 135,069** | **+7.6%** |

- **`█ 실측`** — `C = 26,576`으로 AP-3·AU-2 회차 2와 **세 번 연속 동일**(01:07Z · 01:42Z · 04:05Z).
  **`C`의 진동은 시간 단위이고, 3시간 구간에서는 안정적**이라는 관찰이 굳었다.
- **컨텍스트가 압도적으로 크다** — 203,219자로 AU-2(111,688)의 **1.8배**. 원인은 창이다:
  좁힌 창이 25분(1,500s)이라 메트릭만 **167,780B**다. AU-2는 813s / 103,094B였다.
- **비용 $1.9451로 또 최고 경신.** **자연어 5회가 전부 직전 최고를 갈아치웠다** —
  $1.1146 → $1.1353 → $1.3628 → $1.3892 → **$1.9451**. 창을 넓히면 비용이 초선형으로 는다.
- 분석 out 7,982 — AU-2(11,775)보다 **−32%**인데 점수는 더 높다(76 → 88).
  **분량과 점수의 단조 관계가 이 두 회차에서 깨졌다.**

## 스크린샷용 traceId

| 용도 | traceId |
|---|---|
| **증상** — auth 호출 `Connection refused` 25.73ms + 응답 200 (28 span) | `6a696b6e4373bd66c1dc571d8ff6a03a` |
| **대조** — 주입 전 정상, auth 호출 **446.44ms 성공** (36 span) | `6a6968cc6c1797f21914c37b8158815c` |

**두 트레이스를 나란히 여는 것이 이 문항의 대표 그림이다** — 같은 엔드포인트·같은 `userIds`,
auth 호출 span 하나만 성공↔refused로 갈리고 span 수가 36 → 28로 준다.
메트릭은 `up{job="auth-service"}` + `kube_deployment_spec_replicas{deployment="auth-service"}`,
KST **11:40~12:00**.

## 다음 회차로 넘기는 것

> 실행 계획과 변경군 분류는 **[round-3 대기열](../round-3/README.md)** 이 SoT다. 여기엔 발견만 적는다.

1. **결함 ④가 이번엔 원인 항목을 막았다** — baseline auth 호출 span(446ms 성공)이 창 안에
   실재했고, 그것이 "캐시 만료 결합"의 직접 증거다. **B-9의 근거가 하나 더 늘었다**
2. **캐시 만료 미식별이 N=2로 재현** — 분량 2.7배로도 안 풀렸다. **변경군 C(프롬프트)만 남았다**
3. **탐색이 증상 문안을 따라 스코프를 정한다** — 하류를 가리키면 상·하류를 함께 보고(AU-4),
   상류를 가리키면 상류만 본다(AU-2). **C-1 설계에 반영**
4. **창 크기가 비용을 지배한다** — 25분 창에 컨텍스트 203K자·$1.95. 창 축소 휴리스틱이 필요
5. `401 rate` **5회 연속** no series
