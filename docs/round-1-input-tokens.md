# 조사 1회의 입력 토큰 — 회차 1 기준선

AI 장애원인분석 에이전트(`rca-agent`)가 조사 한 번에 읽어들이는 입력의 크기를 실측한 기록이다.
**개선 전 기준선(baseline)이며, 아직 전후 비교가 아니다.**

측정 기준의 정의는 [measurement.md](measurement.md), 정확도 점수는 [scoring/](scoring/README.md).

> ## 🔴 먼저 읽을 것 — 이 문서는 두 층이다
>
> | 층 | 범위 | 상태 |
> |---|---|---|
> | **§측정 기준 ~ §5** | 회차 1까지 10회 · 오버헤드를 **26,626으로 가정**해 사후 추정 | **역사 기록.** 그대로 둔다 |
> | **[§2.1](#21-재측정-2026-07-29--오버헤드는-상수가-아니다)·[§2.2](#22-회차-자체-측정-2026-07-29-구현--다음-회차부터-가-이-된다)** | 회차 2부터 · 오버헤드를 **회차마다 프로브로 실측** | **현행 방식** |
>
> **26,626은 더 이상 쓰지 않는다.** 오버헤드는 상수가 아니고(하루 만에 −20%), 지난 회차의
> 값은 소급해서 알 수 없다. 아래 §1·§2·§5에 남아 있는 26,626은 **그때의 가정**이지 지금의
> 기준이 아니다. 새 조사의 수치를 적을 때는 **§2.2와 [CLAUDE.md](../CLAUDE.md)의 규칙**을 따른다.

---

## 측정 기준

**측정 대상** — 조사 1회에 **에이전트가 조립해 넘긴 입력**의 토큰 수 (시스템 프롬프트 + 관측 데이터
컨텍스트). 보고된 `input_tokens` 총량이 아니다. 거기엔 Claude CLI 자신의 고정 오버헤드가 섞여 있다.

**조사 입력은 두 개다.** `traceId`가 수집 범위(Tempo·Loki·Mimir 조회와 시간창)를 정하고,
**자연어 질문**이 과업을 정한다. 재현하려면 둘 다 고정해야 한다.

**측정 방법** — 짝지은 두 실행의 차분(paired difference). 오버헤드가 무엇이든 상쇄된다.
*(회차 2부터는 이 사후 차분 대신 **조사 중 프로브**로 오버헤드를 직접 잰다 — [§2.2](#22-회차-자체-측정-2026-07-29-구현--다음-회차부터-가-이-된다).
아래는 그 장치가 없던 10회를 사후 복원한 방법이다.)*

```
A: 꼬리말만            → in_A = C + tail
B: 컨텍스트 + 꼬리말   → in_B = C + context + tail
──────────────────────────────────────────────
B − A                 = context 토큰   (C 소거)
```

**왜 차분인가** — 오버헤드 상수 `C`를 회귀로 *추정*하는 방식은 폐기했다. 6회 기록에 적합하면
95% 신뢰구간이 `[965, 23,345]`로 벌어지고, 한 점만 제거해도 추정치가 2.6배 흔들렸다.
차분은 추정이 아니라 소거다.

**통제 변수** — 모델 `claude-opus-5` 고정(`--model` 명시) · `num_turns = 1` 4회 전부 확인 ·
레포 밖 임시 디렉터리에서 실행(프로젝트 문서 컨텍스트 차단).

**입력 복원** — 과거 조사의 컨텍스트를 원본 API 응답 덤프에서 재구성했다. 재구성 길이가 그때
기록된 `contextChars`와 **4/4 바이트 단위로 일치**한다.

**측정 검증** — 재현한 `in`이 그때 기록과 격리 이후 회차에서 **±200 토큰(0.3%)** 내로 일치한다.
어긋난 1건은 원인이 규명됐다(아래 §4).

**반복 횟수** — **문항당 1회.** 토큰화는 결정적이므로(같은 입력 → 같은 토큰 수) 이 축에서는
N=1이 유효하다. 비결정성이 문제가 되는 *정확도 점수*는 별도 축이며 N≥2를 요구한다.

---

## 핵심 수치

| | |
|---|---|
| **CLI 고정 오버헤드** | ⚠️ **상수가 아니다** — 07-28 측정 **26,626**(당시 2회, 편차 0)이 07-29에 **21,247**로 −20% 이동([§2.1](#21-재측정-2026-07-29--오버헤드는-상수가-아니다)). 아래 표들은 26,626을 가정한 **당시 기록**이다 |
| **토큰 / 문자 실측** | **0.524** — 코드가 쓰던 `chars/4`(0.250)는 **2.1배 과소**였다 (해당 필드 제거) |
| **재현 정확도** | **±0.3%** — 격리 이후 3회차 기준 |
| **검출된 컨텍스트 오염** | **3,407 tok** — 격리 전 회차에만 섞여 있던 프로젝트 문서 |

---

## 1. 조사 1회 입력 토큰

개선 지표는 `█` 하나다. `░`는 Claude CLI가 자기 시스템 프롬프트·툴 정의를 매 호출 싣는 고정
몫이라 이쪽에서 줄일 수 없다.

완주한 조사 **10회 전부**. 오버헤드는 **26,626으로 고정**했다 — 원본 덤프가 남아 있는 4회는
실측(`█`), 나머지 6회는 기록된 `in`에서 그 값을 뺀 추정(`▓`)이다. 추정의 한계는 바로 아래.

```
 tok   █ 컨텍스트 실측 (4회) ← 개선 대상
       ▓ 컨텍스트 추정 (6회 · 기록 in − 26,626)
       ░ CLI 고정 오버헤드 26,626 (빈 프롬프트를 보내도 소모)

 65k ┤
 60k ┤                          ████  ████
 55k ┤                          ████  ████  ▓▓▓▓
 50k ┤        ▓▓▓▓              ████  ████  ▓▓▓▓
 45k ┤  ▓▓▓▓  ▓▓▓▓  ▓▓▓▓        ████  ████  ▓▓▓▓              ████
 40k ┤  ▓▓▓▓  ▓▓▓▓  ▓▓▓▓  ▓▓▓▓  ████  ████  ▓▓▓▓        ████  ████
 35k ┤  ▓▓▓▓  ▓▓▓▓  ▓▓▓▓  ▓▓▓▓  ████  ████  ▓▓▓▓  ▓▓▓▓  ████  ████
 30k ┤  ▓▓▓▓  ▓▓▓▓  ▓▓▓▓  ▓▓▓▓  ████  ████  ▓▓▓▓  ▓▓▓▓  ████  ████
 25k ┤  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░
 20k ┤  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░
 15k ┤  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░
 10k ┤  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░
  5k ┤  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░
   0 ┼────────────────────────────────────────────────────────────────
       CH1a  CH1b  CH2   IN2   AU2   AU4p  AU4   AP1   AP2   AP3
```

| 열 | 회차 | 채록일 | 총 `in` | 컨텍스트 토큰 | |
|---|---|---|---|---|---|
| CH1a | CH-1 회차 1 | 07-26 | 44,798 | 18,172 | 추정 |
| CH1b | CH-1 회차 2 | 07-26 | 47,503 | 20,877 | 추정 |
| CH2 | CH-2 회차 1 | 07-26 | 45,935 | 19,309 | 추정 |
| IN2 | IN-2 회차 1 | 07-27 | 37,932 | 11,306 | 추정 |
| AU2 | AU-2 회차 1 | 07-27 | 62,007 | **35,381** | **실측** |
| AU4p | AU-4 파일럿 (채점만 폐기 — 아래 주) | 07-27 | 61,713 | **35,087** | **실측** |
| AU4 | AU-4 회차 1 | 07-28 | 56,325 | 29,699 | 추정 |
| AP1 | AP-1 회차 1 | 07-27 | 34,935 | 8,309 | 추정 |
| AP2 | AP-2 회차 1 | 07-28 | 38,526 | **11,900** | **실측** |
| AP3 | AP-3 회차 1 | 07-28 | 45,885 | **19,125** | **실측** |

실측 4회의 총 `in`은 이번 재현값, 나머지는 그때 기록값이다.

> **AU4p가 왜 "채점만 폐기"인가.** 이 회차는 §8 앵커가 사실과 다른 상태에서 채록됐다 —
> 만점 요건이 "3s timeout"과 "fallback 로그"를 요구했는데, 실측은 **23.5ms connection refused**
> 였고(`.timeout(3s)`는 대기 상한이라 TCP RST에는 발동하지 않는다), fallback 로그는 존재하나
> `traceId=NONE`이라 traceId 기반 조사로 도달할 수 없다. 3개 요건 중 2개가 물리적으로 달성
> 불가라 채점이 성립하지 않았다([채점 대장](scoring/README.md) 유형 C).
>
> **에이전트 성능 문제가 아니다** — 오히려 에이전트가 "타임아웃이 아니라 TCP RST"라고 앵커를
> 정정했고, 채점 가능했던 3항목은 70/70 전부 만점이었다. 앵커를 v3로 고친 뒤 07-28에
> 재주입·재실행한 것이 유효 회차 1(AU4)이다.
>
> **채점 무효 ≠ 실행 무효.** 실제로 돌아간 조사이고 원본 덤프가 남아 있어 토큰 축에서는
> 정상 데이터 포인트다. 단 AU-4는 이 표에 두 번(AU4p·AU4) 나오므로, **점수 축에서는 AU4만
> 유효**하다는 점에 주의한다.

### 추정 6회의 한계 — 약 4,600 토큰 과소다

오버헤드를 오늘의 값(26,626)으로 고정한 편의적 처리이고, 회차 문서에 남아 있는
`contextChars`로 검산하면 **그 가정이 정확하지 않다**:

| 회차 | 기록 `in` | 기록 chars | C=26,626 가정 시 tok/char | 비율 0.524 가정 시 C |
|---|---|---|---|---|
| CH-1 회차1 | 44,798 | 43,387 | **0.419** | 22,063 |
| CH-1 회차2 | 47,503 | 49,404 | **0.423** | 21,615 |
| CH-2 회차1 | 45,935 | 45,434 | **0.425** | 22,128 |
| IN-2 구회차 | 39,202 | 32,221 | **0.390** | 22,318 |
| AU-4 회차1 | 56,325 | 65,353 | **0.454** | 22,080 |
| **측정된 4회** | | | **0.511 ~ 0.532** | **26,626 (실측)** |

26,626을 적용하면 tok/char가 **0.39~0.45**로 떨어진다. 실측 4회는 0.511~0.532 안에 4% 폭으로
모여 있으므로, 20~25% 이탈은 측정 노이즈가 아니다.

반대로 실측 비율을 고정하면 **C가 21,615~22,318로 다섯 회차 모두 3% 폭 안에 모인다.**

> **그때 오버헤드는 ~22,000이었고, 지금은 26,626이다.** 다섯 회차가 독립적으로 같은 값을
> 가리키므로 우연이 아니다. 원인은 CLI 버전 변경(툴 정의 증가) 또는 모델 변경인데,
> **그 회차들은 모델이 기록되지 않아 확정할 수 없다.**
>
> 이것이 `--model` 고정과 `llmModel`·`num_turns` 기록을 도입한 직접적 이유다 —
> 지금 겪는 것이 정확히 그 부재의 대가다.

**따라서 `▓` 막대는 컨텍스트를 약 4,600 토큰씩 과소평가한다** — 오버헤드를 실제보다
크게(26,626 대신 ~22,000이어야 할 것을) 빼기 때문이다. 예를 들어 CH-1 회차1은 표가 18,172을
가리키지만 실제는 22,700 근처였을 것이다.

**회차 1은 러프한 기준선으로 남는다.**

> **정정 (07-29)**: 초안은 *"회차 2부터 전 문항을 실측해 `▓`를 전부 `█`로 대체한다"* 고 적었으나
> **이 10회의 `▓`는 영구히 `▓`다.** 원본 덤프가 없는 6회는 복원이 불가능하고, 무엇보다
> **그때의 오버헤드를 소급해서 알 수 없다**(§2.1). 새 회차만 §2.2의 프로브로 `█`이 된다.

---

## 2. 회색 칸의 내역 — 오버헤드 26,621 토큰은 무엇인가

> ⚠️ **07-28 시점의 분해다.** 07-29에 총량이 21,247로, 툴 정의 몫이 10,762로 줄었다([§2.1](#21-재측정-2026-07-29--오버헤드는-상수가-아니다)).
> **구성 비율의 근거로는 유효하고, 절대값으로는 인용하지 않는다.**

Claude CLI에 빈 프롬프트를 주고 기능을 하나씩 끄며 잰 차분:

| 실행 | `in` | 차이가 말하는 것 |
|---|---|---|
| 현재 (전체) | 26,621 | — |
| `--tools ""` | 3,420 | 툴 정의 = **23,201** |
| `--system-prompt "…"` | 23,334 | 시스템 프롬프트 = **3,287** |
| 둘 다 | 182 | 잔여 = **182** |

```
 tok
 26k ┤   ░░░░░░░░   ← ▓ 메시지 프레이밍     182  ( 1%)
 24k ┤   ░░░░░░░░   ← ░ CLI 시스템 프롬프트 3,238 (12%)
 22k ┤   ████████
 20k ┤   ████████
 18k ┤   ████████
 16k ┤   ████████
 14k ┤   ████████
 12k ┤   ████████   ← █ 툴 정의 스키마      23,201 (87%)
 10k ┤   ████████        에이전트가 쓰지 않음
  8k ┤   ████████
  6k ┤   ████████
  4k ┤   ████████
  2k ┤   ████████
   0 ┼─────────────
       오버헤드 26,621
```

**87%가 이 에이전트가 한 번도 호출하지 않는 툴의 설명서다.** `Bash`·`Read`·`Edit`·`Grep` 같은
코딩 도구의 JSON 스키마와 사용법이 매 호출 실린다. rca-agent는 텍스트를 넣고 텍스트를 받을
뿐이고, 네 회차 모두 `num_turns = 1`로 툴을 쓰지 않았음이 확인됐다.

크기 감각: **AP-2 회차의 실제 관측 데이터가 11,900 토큰인데, 쓰지 않는 툴 설명서가 그 두 배다.**

> `--tools ""` 한 줄이면 **모든 회차에서 23,201 토큰**이 사라지고 컨텍스트는 한 글자도 바뀌지
> 않는다 — AP-2 기준 총 입력의 60%다. 다만 입력이 바뀌면 답도 바뀔 수 있으므로,
> **같은 traceId + 같은 질문으로 재조사 → 정확도 점수 불변 확인**을 거친 뒤에 채택한다.
> **아직 적용하지 않았다.**

---

## 2.1 재측정 (2026-07-29) — **오버헤드는 상수가 아니다**

같은 방법(격리 cwd · `-p --model claude-opus-5 --output-format json` · 1자 프롬프트)으로
하루 뒤 다시 쟀다. **값이 움직였다.**

| 측정일 | CLI | 오버헤드 `C` | 툴 정의 몫 |
|---|---|---|---|
| 07-26~27 (검산 역산) | — | **~22,000** | 미측정 |
| **07-28 (문서 §1·§2)** | — | **26,626** | 23,201 |
| **07-29 (재측정)** | **2.1.220** | **21,247 / 21,567** | **10,762** |

**표본 11회 · 두 값만 나온다.**

```
21,247 ██████████        5회
21,567 ████████████      6회
                         Δ = 320 (1.5%)  · 그 외 값 없음
```

- 전부 `num_turns=1` · `modelUsage`에 `claude-opus-5` 단독 확인
- 툴 정의 분해: `--tools ""` → **10,485** (2회 동일) → 툴 몫 = 21,247 − 10,485 = **10,762**

**07-28 대비 −5,379 (−20%)이고, 감소분 대부분이 툴 정의다** (23,201 → 10,762, −12,439).
CLI가 툴 스키마를 줄인 것으로 보인다.

> ### 이 재측정이 바꾸는 것
>
> **`C`를 상수로 박아둔 §1의 `▓` 추정 6회는 재계산 대상이다.** 문서는 이미 검산으로
> *"그때 오버헤드는 ~22,000이었고 지금은 26,626"* 이라 적어 `C`가 움직인다는 것을 알고
> 있었는데, 이번 측정이 **세 번째 값**을 만들었다.
>
> **회차마다 그때의 `C`를 함께 기록해야 한다.** 조사 시점에 프로브를 한 번 돌려 리포트
> 옆에 남기는 것이 유일하게 성립하는 방법이다 — 나중에 소급 추정할 수 없다.
> **→ 구현했다 (아래 §2.2).**
>
> **검산이 새 `C`를 지지한다.** AP-1 회차 2(chars 90,775 · in 71,071)를 두 경로로 대조하면:
>
> | `C` | ① `in − C` | ② `chars × 0.524` | 격차 |
> |---|---|---|---|
> | 26,626 (07-28) | 44,445 | 47,566 | **−6.6%** |
> | **21,247 (07-29)** | **49,824** | 47,566 | **+4.7%** |
>
> 새 값이 독립 경로와 더 가깝다.

### 이번에 재측정하지 **못한** 것 — tok/char 비율

2점 차분(20,000자 / 40,000자 실제 컨텍스트 투입)으로 비율을 다시 재려 했으나 **실패했다.**
원시 JSON을 지시문 없이 던지자 **CLI가 도구를 쓰기 시작해 `num_turns=4`** 가 됐고,
`usage`가 턴 누적이라 20,000자에 42,196 토큰(= 2.11 tok/char)이라는 불가능한 값이 나왔다.

- **0.524는 07-28 값을 그대로 쓴다.** 재확인 전까지 이 비율은 미갱신 상태다.
- 부수 확인: **실제 조사는 안전하다** — AP-1 회차 2가 90,200자 컨텍스트로도 `num_turns=1`이었다.
  시스템 프롬프트가 과업을 지정하기 때문이고, **지시문 없는 대용량 원문만 에이전틱으로 튄다.**
- 다음 재측정은 도구를 막은 상태(`--tools`)로 해야 하는데, `--tools ""`와 `--system-prompt`를
  **함께** 주면 값이 78,000대로 튀어 조합 프로브 자체가 재현되지 않는다. 분해는 툴 항목만 유효하다.

---

## 2.2 회차 자체 측정 (2026-07-29 구현) — **다음 회차부터 `▓`가 `█`이 된다**

**장애 분석 리포트를 낼 때** 에이전트가 **1자 프롬프트를 한 번 던져 그 회차의 `C`를 직접 잰다.**
본 호출과 같은 조건(같은 명령·`--model`·격리 샌드박스)이고, 본 LLM 호출 **직후**에 실행된다.

**프로브는 리포트 1건당 1회다 — LLM 호출마다가 아니다.** `/diagnose`는 LLM을 두 번
(탐색·분석) 부르지만 프로브는 한 번만 돌고, 그 값을 **두 단계에서 각각 뺀다.** 오버헤드는
호출마다 같은 크기로 얹히고(§2), `C`가 움직이는 단위는 CLI 버전(일 단위)이라 같은 조사
안의 두 호출은 같은 값을 쓴다.

```
overheadTokens = 그 회차·그 시각·그 CLI 버전의 실측     ← LlmClient.overheadTokens()
contextTokens  = in − overheadTokens                    ← 회차 안에서 닫힌 계산
```

리포트가 단계별로 렌더한다.

```
### 토큰 축 (개선 지표)
| 단계 | 총 in | chars | 컨텍스트 토큰 |
| 탐색 | 43,025 | 33,700 | 21,778 |
| 분석 | 42,651 | 42,181 | 21,404 |
| **합계** | **85,676** | | **43,182** |

- **overheadTokens 21,247 tok — 이 회차에 실측**(1자 프롬프트 프로브, 같은 명령·모델·샌드박스).
```

**이 축이 성립하는 이유**는 이 문서가 이미 증명해 뒀다 — 오버헤드는 툴 정의 + CLI 시스템
프롬프트 + 프레이밍이고 **입력 크기와 무관하게 고정**이므로(§2), 호출마다 한 번씩만 얹힌다.
탐색·분석이 별도 호출이라 **각 단계에서 한 번씩 뺀다.**

| | 값 | 비고 |
|---|---|---|
| 설정 | `rca.llm.claude-cli.probe-overhead` (`RCA_CLI_PROBE_OVERHEAD`) | 기본 on |
| 비용 | 약 **$0.02 · 1초** / 조사 | |
| 남는 오차 | **±320 tok (1.5%)** | 프로브 11회가 21,247/21,567 두 값으로 갈렸다. 캐시로 설명되지 않는 변동 |
| 프로브 페이로드 | 약 **6 tok** 과대 | `"."` + 구분자. 위 오차의 2% 수준이라 보정하지 않는다 |
| 실패 시 | `-1` 기록 + 리포트에 **`▓ 추정`임을 명시** | 조사는 중단하지 않는다 |
| `num_turns > 1`이면 | `-1` | usage가 턴 누적이라 값을 쓸 수 없다 |

**이 문서의 `▓` 10회는 그대로 남는다.** 소급해서 `C`를 알 방법이 없기 때문이고,
그 사실이 이 장치를 만든 이유다.

---

## 3. 컨텍스트는 무엇으로 차 있나

문자 수 기준 구성.

```
  자      █ 트레이스 (Tempo)    ▓ 로그 (Loki)    ░ 메트릭 + 헤더

        65,958  65,366
          ▼       ▼      36,087
 65k ┤  ░░░░    ░░░░       ▼     22,665
 60k ┤  ░░░░    ░░░░               ▼
 55k ┤  ▓▓▓▓    ▓▓▓▓
 50k ┤  ▓▓▓▓    ████
 45k ┤  ████    ████
 40k ┤  ████    ████
 35k ┤  ████    ████    ░░░░
 30k ┤  ████    ████    ░░░░
 25k ┤  ████    ████    ▓▓▓▓    ░░░░
 20k ┤  ████    ████    ▓▓▓▓    ░░░░
 15k ┤  ████    ████    ████    ░░░░
 10k ┤  ████    ████    ████    ▓▓▓▓
  5k ┤  ████    ████    ████    ████
   0 ┼──────────────────────────────────
        AU-2    AU-4    AP-3    AP-2

 █ 트레이스   49,266  50,180  18,907   5,475
              74.7%   76.8%   52.4%   24.2%
 ▓ 로그        7,828   7,923   7,915   7,913
 ░ 메트릭+헤더 8,864   7,263   9,265   9,277
```

큰 조사일수록 트레이스 JSON이 압도한다 — AU-4에서 **76.8%**, span당 760바이트다. 압축
렌더러(`TraceSpans.topByDuration`)는 이미 코드에 있으나 크기 한도 초과 시에만 동작해 이
회차들에는 적용되지 않았다.

> **span 개수를 잘라내면 안 된다.** 결정적 근거가 짧은 span 또는 *없는 span*이었던 사례가 있다
> (CH-2·AU-2). 줄일 것은 **표현 형식**이지 span 개수가 아니다.

---

## 4. 측정 검증 — 재현한 입력이 그때 기록과 얼마나 맞나

복원한 컨텍스트로 다시 잰 `in`에서 그때 기록된 `in`을 뺀 값. 0에 붙을수록 복원과 측정이
정확하다는 뜻이다.

| 문항 | 그때 기록 | 이번 재현 | 차이 | 격리 |
|---|---|---|---|---|
| AU-2 회차 1 | 65,414 | 62,007 | **−3,407** | **적용 전** |
| AU-4 파일럿 | 61,528 | 61,713 | +185 | 적용 후 |
| AP-3 회차 1 | 45,885 | 45,751 | −134 | 적용 후 |
| AP-2 회차 1 | 38,662 | 38,526 | −136 | 적용 후 |

격리 이후 3회는 ±200 토큰 안이다. **어긋난 1건은 딱 그 회차만 격리 적용 전**이고, 차이
3,407 토큰은 그때 컨텍스트에 딸려 들어간 프로젝트 문서의 크기다.

독립 경로로도 같은 값이 나온다 — 빈 프롬프트 프로브(29,987) − 차분 실측(26,626) = **3,361**.
두 방법이 서로를 확인한다. 오염의 존재는 이전에 지적됐지만, 크기가 숫자로 확정된 것은 여기서다.

---

## 5. 측정값 전체

문자 수는 측정에 쓴 프롬프트 기준이라 리포트의 `contextChars`보다 시스템 프롬프트·꼬리말만큼 크다.

| 문항 | traceId | 질문 | 문자 | **컨텍스트 토큰** | tok/char | + 오버헤드 = 총 in | turns |
|---|---|---|---|---|---|---|---|
| AU-2 회차 1 | `6a66b2c4…` | 왜 로그인이 안 돼? | 66,562 | **35,381** | 0.532 | 62,007 | 1 |
| AU-4 파일럿 | `6a67077c…` | 피드에 작성자가 '사용자123' 같은 익명으로 보인다는 제보가 있어 | 65,970 | **35,087** | 0.532 | 61,713 | 1 |
| AP-3 회차 1 | `6a6804f0…` | 피드 등록이 실패했다는 제보가 있다. 원인을 조사해줘 | 36,691 | **19,125** | 0.521 | 45,751 | 1 |
| AP-2 회차 1 | `6a680b56…` | 팔로우 목록 조회가 실패했다는 제보가 있다. 원인을 조사해줘 | 23,269 | **11,900** | 0.511 | 38,526 | 1 |

---

## 이 측정이 말하지 않는 것

- **개선 전후 비교가 아니다.** 네 조사는 서로 다른 장애 문항이라 서로 비교 대상이 아니다.
  이 표는 압축 개선을 적용하기 *전*의 기준선이고, 델타는 같은 문항을 다시 재야 나온다.
- **10회 중 4회만 토큰이 실측이다.** 나머지 6회(`▓`)는 원본 API 응답 덤프가 없어 복원이
  불가능해, 오버헤드를 26,626으로 **가정**하고 기록된 `in`에서 뺀 값이다. 검산상 그때
  오버헤드는 ~22,000이었으므로 **약 4,600 토큰씩 과소평가**다.
- **이 10회는 영구히 `▓`다.** 오버헤드가 상수가 아니어서(§2.1) 그때 값을 소급해 알 수 없다.
  `█`이 되는 것은 §2.2의 프로브가 붙은 **회차 2 이후**뿐이다.
- **시스템 프롬프트는 현재 버전을 썼다.** 조사 당시 버전이 보존돼 있지 않다. 전체의 1%
  미만(597자)이며, 재현 오차 ±200 토큰의 대부분이 이것으로 설명된다.
- **비용은 실제 청구액이 아니다.** 이 프로젝트는 구독 계정으로 CLI를 호출하므로 달러 청구가
  없다. 리포트의 `total_cost_usd`는 API 환산 추정치이며, 그렇게 표기해야 한다.
- **정확도는 여기 없다.** 원인 적중 점수는 별도 루브릭으로 채점되며 문항당 N≥2를 요구한다.
  이 문서는 토큰 축만 다룬다.

---

**§측정 기준 ~ §5** — 측정일 2026-07-28 · `model=claude-opus-5`(고정) · `num_turns=1` ·
차분 기준선 N=2, 편차 0 · 입력 복원 `ContextRebuildTool`(contextChars 4/4 일치) ·
측정 `scripts/measure-contexts.ps1`

**§2.1·§2.2 (현행)** — 측정일 2026-07-29 · CLI 2.1.220 · 프로브 표본 11회(21,247/21,567) ·
구현 `LlmClient.overheadTokens()` · 리포트 작성 시 적용 규칙은 [CLAUDE.md](../CLAUDE.md)

기준 정의 [measurement.md](measurement.md)
