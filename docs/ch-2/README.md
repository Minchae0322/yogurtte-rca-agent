# CH-2 — chat 컨슈머 정지 × lag 누적 (회차별 기록)

toy-content `docs/chaos/` RUNBOOK의 CH-2 문항. IN-2와 대비쌍 — 같은 "알림 안 와요"인데 발행측은 무결하고 **소비자만 전멸**하면, 에러가 아니라 **lag 누적이라는 메트릭**만이 신호가 되고, 복구 시 밀린 알림이 몰아서 도착한다(유실 0). 폴더 구성 방식은 [`../ch-1/README.md`](../ch-1/README.md)와 동일.

## 회차 인덱스

| 회차 | 일시(UTC) | 입력 | 다운 | 결말 | RCA 조사 | 문서 |
|---|---|---|---|---|---|---|
| 1 | 07-26 09:43:51~09:52:56 (9분 5초) | traceId 직접 | `scale --replicas=0` | lag 25건 누적 → 복구 후 일괄 소비, 지연 4~10분·유실 0 | O | [round-1.md](round-1.md) |
| 2 | 07-29 05:05:15~05:15:15 (10분) | **자연어** | `scale --replicas=0` | lag 25건 누적 → 05:15:30 일괄 소비. **단 6분 전에 Mongo 다운(CH-1 성격)이 겹쳤다** | O | [round-2.md](round-2.md) |
| 2 · **2차 실행** | 07-29 12:07:25~12:16:55 (9분 30초) | **자연어** (동일 문안) | `scale --replicas=0` | lag **25건** 누적 → **12:18:00 일괄 소진**. **8분 전 Mongo 다운(CH-1)이 또 겹쳤다 — 같은 시퀀스 재현** | O | [round-2-run2.md](round-2-run2.md) |

> **회차 2 결과 (2026-07-29): 100 / 100** — anchors-v2 · 루브릭 v3 6항목.
> **CH-2 최초의 실제 점수**다(회차 1은 앵커 부적합으로 채점 불가 → 델타 불성립).
>
> **조사 창 안에 원인이 둘이었다** — ① `mongodb_up` 0 (04:58:45~05:03:45, 5분)
> ② chat `scale 0` (05:05:15~05:15:15). 에이전트는 섞지 않고 **근본(Mongo) → 확대(컨슈머 부재 9분)
> → 증폭(DLQ도 같은 Mongo 의존)** 으로 층을 세웠고, **CH-2 앵커의 여섯 요건을 전부 충족**했다.
> **CH-2 단독보다 어려운 문항이었고 통과했다.**
>
> ⚠️ **회차 1과 N=2로 묶지 않는다** — 자(v1 4항목 → v3 6항목)·입력 모델·**창 안 원인 개수**가
> 모두 다르다. 다음 회차를 N=2로 쓰려면 조건을 맞춰야 한다.
>
> **채점자 정정**: 최초에 "채점 불가(문항 격리 실패)"로 판정했다가 뒤집었다.
> 앵커의 *산문*(*"이상 트레이스가 존재하지 않는다"*)을 *채점 문언*으로 오독한 것이고,
> 실제 요건(*"S = lag 상승 구간 **또는** WS 급락 구간"* 택일)은 충족돼 있었다.
> 경위는 [round-2.md](round-2.md).

> **🏆 회차 2 · 2차 실행 결과 (2026-07-29): 100 / 100 — §8.1 N=2 성립, 평균 100 ± 0.**
> 여섯 항목 전부 재현됐고 **두 실행 다 채점자 판정 0건**이다. **만점 쌍은 이것이 처음**이다.
>
> **위 ⚠️가 요구한 조건 맞추기가 실제로 성립했다** — 이번에도 **8분 전에 Mongo 다운(CH-1,
> 11:54:10~11:59:14)** 이 겹쳤다. 선행 주입·간격·랙 정점(25건)·선정 traceId(둘 다 **CH-1 것**)까지
> 같은 시퀀스가 재현됐다.
>
> **가장 어려운 문항이 가장 안정적이다.** 앵커가 *"메트릭 채널 없이는 3)에서 0점"* 이라 못박은
> 문항인데 두 번 다 lag 채널로 도달했다. **AP-2 가설("도구가 막은 문항은 편차가 0")의 반대
> 사례** — 여기서는 신호가 전부 도달했고 그래서 0이다.
>
> 부수: `websocket_active_users`가 **두 실행 모두 전 구간 0**이라 앵커 ⓑ의 "급락"이 물리적으로
> 불성립이다(앵커 ㅅ 확정). 창에 AU-4 잔여 신호 5건 + 채점자 실험 1건이 섞였으나
> **좁힌 창으로 전부 배제**했다 — 같은 날 AU-2 실행 2가 오염에 걸린 것과 정반대다.
> 상세는 [round-2-run2.md](round-2-run2.md).

## 스크린샷 공통 팁

- Grafana 시간대 KST. 주입 창 = **KST 18:43:51 ~ 18:52:56**, 소진 완료 ≈ 18:54:05.
- 이 문항의 그림 세 장: ① Tempo 장애 트레이스 — publish와 consume 사이 **4분의 빈 구간**이 한 트레이스 안에 보이는 워터폴, ② `kafka_consumergroup_lag{consumergroup="notification-processors"}` — 계단식 상승 후 수직 낙하 (KST 18:43~18:56), ③ Loki 복구 직후 `알림 처리 완료` 몰림 (KST 18:53~18:55).
