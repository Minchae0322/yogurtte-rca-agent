# 프로젝트 현황판 (STATUS)

> **여기서 시작하세요.** 뭘 봐야 할지 기억 안 나면 이 파일 하나만 열면 됩니다.
> 유지 규칙: 작업 세션이 끝날 때마다 이 파일을 갱신한다 (① 지금 위치 체크박스,
> ② 다음 할 일 큐, ③ 활동 로그에 날짜 항목 추가).

## ① 지금 어디까지 왔나 (전략 Phase 기준)

전체 계획은 [strategy.md](strategy.md) — 아래는 그 실행 현황이다.

- [x] **Phase 0 — 관측성 기반**: Grafana Cloud + exporter 구축, Brave 유지 결정(ADR-001)
  - [x] (보강) Mongo/Redis/JDBC/@Observed 계측 추가 — toy-chat 배포됨 (2026-07-25)
  - [x] (보강) 컨슈머 예외 삼킴 제거 → 재시도/DLQ 경로 활성화 + 관측 로그
  - [x] (보강) 하트비트 span 노이즈 필터 (`0ba282b`) — **배포 확인 대기**
  - [ ] `service.version=$GIT_SHA` 트레이스 태깅 (Phase 4c에서)
- [x] **Phase 1 — v0 에이전트 + 측정 하네스**: rca/review 2모드, 토큰·비용·coverage 기록,
  실전 조사 1건 (dispatch 995ms 특정)
- [ ] **Phase 2 — 평가 하네스** ← **지금 여기**
  - [x] 서버에 chaos 하네스 구축 (`~/chaos`, baseline→주입→증상→원복 + 블라인드 채점)
  - [x] CH-1 재실행 완료 — 회차1(73s, 지연 흡수) / 회차3(4.5분, DLQ 복구). 블라인드 조사 [AE-01/02](findings/README.md)
  - [x] CH-2(컨슈머 정지)·IN-2(Kafka 유실) 실행 + 조사 [AE-03/04]
  - [x] **§8 블라인드 채점 실시** (2026-07-27) — [채점 대장](scoring/README.md).
        CH-1 회차2 **80** · IN-2 회차1 **80** / CH-1 회차1·CH-2 회차1 **채점 불가**(앵커 부적합).
        **조사 4회 중 유효 채점 2회, 그마저 N=1이라 §8.1상 전부 인용 불가** —
        "v0 baseline 4문항 확보"는 **실행** 4회를 뜻하며 채점 완료가 아니다.
        절반이 앵커 부적합으로 떨어진 것은 에이전트가 아니라 **자(앵커)의 문제**
  - [x] **AU-2 재실험 회차 1 완주** (2026-07-27 01:20Z, 2분 24초 — 캐시 히트로 흡수) [au-2/](au-2/README.md).
        소급 채록(Grafana 3채널) → 블라인드 조사 → 채점. **트레이스 무신호에서 메트릭 단절만으로
        auth 다운 도달** [AE-05](findings/ae-05-rca-v0-au2-blind-eval.md). §8 채점 불가(앵커 부적합)
  - [x] **AU-4 구 회차 1 폐기 → 07-28 재실행·채점 75** [au-4/](au-4/README.md). 구 회차는
        "앵커가 틀리고 에이전트가 맞은" 첫 사례([AE-06](findings/ae-06-rca-v0-au4-blind-eval.md))로
        앵커 정정("3s timeout"→"refused")의 근거. 재실행(간결본)은 **캐시 만료 미식별로 75** —
        폐기 장황본이면 ~100. **같은 v0 두 실행 25점 차 = §8.1 N≥2 필요의 실증**
  - [x] **AP-1 회차 1 완주·채점 75** (2026-07-27, 250자 댓글 → varchar 위반 500)
        [ap-1/](ap-1/README.md). 채점자 판정 2건이 **충분 근거 원칙(§8.2)으로 일반화**됨
  - [x] **IN-2 회차 1을 CLI 격리 재실험으로 덮어씀·재채점 80** (2026-07-28) [in-2/](in-2/README.md).
        구 회차(07-26)는 격리 전이라 폐기. 유실 재현(60초 metadata 타임아웃), **토픽 부재 오선정
        N=2 재현**. 부가: 유실/지연은 **다운 5분(메타데이터 만료) 초과 여부로 갈림**
  - [x] **AP-3 회차 1 완주·채점 100** (2026-07-28, 중복 해시태그 → `uk_feed_hashtag` 위반 500)
        [ap-3/](ap-3/README.md). **최초의 만점이자 절차 개정 후 앵커 부적합 0의 첫 회차**.
        단 N=1 · out 11,070 **장황본** · 원인이 예외 지문으로 자백된 문항이라 인용 불가
  - [x] **AP-2 회차 1 완주·채점 유보(앵커 미박제 — 신규 유형 D)** (2026-07-28, `size` 미지정
        → 언박싱 NPE → 500) [ap-2/](ap-2/README.md). v1 **60** / v2 65 —
        **절대 점수는 인용 불가지만 [델타 baseline으로는 유효](scoring/README.md#채점-불가와-채점-유보를-나눈다-2026-07-28-신설)**
        (v1이 조사 실행 전 실측만으로 작성돼 답안에 무오염). **정답이 로그에만 있는 첫 문항** —
        v0.1 도구 델타 **+25점**이 전량 Loki 셀렉터+`logfmt` 두 결함에 걸려 있다.
        **AP-1(+25, 코드 인지)과 서로의 대조군**
  - [x] **채점 규칙 개정** (2026-07-28): 시간 분리 폐지 · **충분 근거 원칙**(신호 개수 아닌
        증거력, 동치 대체 인정) · 앵커 작성 체크리스트(코드 독해≠실측) ·
        **도구 제약 면제 폐지**(캡 개념 폐기) · 신규 결함 **유형 E**(증상을 근거로 요구)
  - [x] **앵커 전수 감사** (2026-07-28) [anchors.md](scoring/anchors.md) — 실행 대상 **12문항 전부 성립**.
        IN-1·AU-1은 앵커가 아니라 **감사 판정이 틀렸던 것**으로 확인. AU-1은 **재현성 부족으로 문항 보류**
  - [x] **[루브릭 v3](scoring/rubric-v3.md) 제정** (2026-07-28) — 원인 40 / 근거 25 /
        **탐색 15(신설)** / **영향 10(근본 40에서 분리)** / 오귀인 5 / 조치 5.
        v1 결함 둘이 실측 확인됨: `근본 40`이 세 가지를 담고 있고(CH-1 감점 20 전액이 영향 오판),
        `오귀인 20`은 9회 전부 만점이라 척도가 아니다.
        [rubric-v2](scoring/rubric-v2.md)(소항목 14개)는 **보류** — 병목(N=1)을 악화시킨다
  - [x] **[대외용 보고서](scoring/report.md)** — 장애별 상황·채점 항목·결과 + **제품 경계 명시**
  - [x] **🔴 앵커 v2 신설 — 자연어 입력** (2026-07-28) [anchors-v2.md](../../toy-content/docs/chaos/anchors-v2.md) ·
        SoT `chaos/anchors-v2.md` **12문항**. v3 6항목 재배치 + **질문 문안 박제**
        + 문항별 탐색 요건(T·S·B). **기존 `answer.md`·anchors.md는 무수정 보존**(§8.2)
  - [x] **AP-2 회차 2 완주·채점 53** (2026-07-29, 자연어 입력 · **루브릭 v3 첫 적용**)
        [ap-2/round-2.md](ap-2/round-2.md). **v0.1 델타 예측 +25가 반증됐다** — B-1·B-2를
        고쳤는데 같은 자로 **−10**. 원인은 **셋째·넷째 결함**(라인 필터가 스택 배제 ·
        traceId 미선정 시 Tempo skip)이고 둘 다 실측으로 잡았다. 탐색은 **정답**
        (auth-service · traceId 미선정 · AP-1 주입 오답을 자력 기각)
  - [x] **AP-3 회차 2 완주·채점 100** (2026-07-29, 자연어 입력 · 루브릭 v3)
        [ap-3/round-2.md](ap-3/round-2.md). **전 항목 만점** — 탐색이 주입 트레이스를 **자력
        선정**(Top-1 적중)하고 `Duplicate entry … uk_feed_hashtag`를 span·Loki 양쪽에서 축자 인용,
        동시성 오귀인을 **구조적으로 반증**. **회차 1(100)과 자가 달라 비교 불가**(v1 4항목 vs
        v3 6항목 — 탐색 15·영향 10이 회차 2에만 있다). **계측 성과**: `C`가 같은 CLI 버전에서
        26,626 → 21,3xx → **26,6xx로 진동**함이 앱 프로브 + 독립 프로브로 확정
  - [x] **AU-2 회차 2 완주·채점 76** (2026-07-29, 자연어 입력 · 루브릭 v3) [au-2/round-2.md](au-2/round-2.md).
        **AU-2 최초의 실제 점수**(회차 1은 채점 불가 → 델타 불성립). **탐색 15/15 — 메트릭 채널
        첫 실증**(traceId 미선정 정답 · AP-3 주입 트레이스 자력 기각). **🔴 결함 ④ 예측 적중** —
        감점 24가 전부 content 축이고 그 신호가 **Tempo에 실재함을 채점자가 확인**했다(오를 자리 24점).
        부수: 5) 오귀인 **5/5인데 원인을 틀렸다**(재시작 루프 오판) → 앵커 요건 개정 후보
  - [x] **AU-4 회차 2 완주·채점 88** (2026-07-29, 자연어 입력 · 루브릭 v3) [au-4/round-2.md](au-4/round-2.md).
        **🔴 이 프로젝트 최초의 항목 단위 재현** — 회차 1·2 모두 **원인 30**, 사유도 동일
        (캐시 만료 발현 조건 미언급). 출력 2.7배로도 안 풀렸다 → **분량 가설 반증, 프롬프트만 남음**.
        ⚠️ 총점 75 → 88은 **자가 바뀐 것**(4항목 → 6항목, 근거 요건 3종 → 2종)이라 인용 금지.
        ✅ round-2의 "fallback 로그는 A-2 없이도 도달" 예측 **적중**
  - [x] **CH-2 회차 2 완주·채점 100** (2026-07-29, 자연어 입력 · 루브릭 v3) [ch-2/round-2.md](ch-2/round-2.md).
        **CH-2 최초의 실제 점수.** 조사 창에 **원인이 둘**(Mongo 다운 → 6분 뒤 chat scale 0)인
        **복합 창**인데 근본·확대·증폭으로 계층 분리해 **여섯 요건 전부 충족**.
        **채점자 정정 1건** — "채점 불가(유형 F)"로 판정했다가 철회했다(앵커의 산문을 채점
        문언으로 오독). 부수: 스윕이 깨진 행 반환(결함 9) · 파드 소멸 오판 4회 연속
  - [x] **CH-1 회차 2 완주·채점 100** (2026-07-29, 자연어 입력 · 루브릭 v3) [ch-1/round-2.md](ch-1/round-2.md).
        **CH-1 최초 만점이자, 이 프로젝트에서 처음으로 실패가 재현되지 않고 해소된 사례** —
        회차 1의 감점 20 **전액이 영향 오판("유실")** 이었는데 회차 2는 *"영구 유실은 근거가 약하다"* 로
        뒤집고 재처리 성공 시각을 **오차 2초**로 짚었다. **round-2 대기열(B-1·B-2·B-3)의 첫 양(+) 델타** —
        AP-2에서 −10을 냈던 같은 수정군이다. **🔴 결함 11 신규**: 도착 근거(`재처리 성공`)가 **INFO라
        수집 채널 밖** → 4) 영향 만점 요건이 구조적으로 도달 곤란(B-16).
        ⚠️ CH-2 회차 2와 **같은 창**(문안만 다름)이라 독립 표본이 아니다
  - [x] **🔴 CH-1 회차 재번호 (갈래 B 기준)** (2026-07-29) — 73초 회차가 [CH-3](ch-3/README.md) 사료로
        이관되면서 구 회차 1 → **이관 X**, 구 회차 2 → **회차 1**, 이번 자연어 조사 → **회차 2**.
        `anchors-v2.md`가 이미 80점 회차를 *"회차 1(v0, 80점)"* 로 부르고 있어 SoT와 일치시킨 것이다.
        **점수·본문 무수정 보존**(§8.2), 파일명·링크만 정리
  - [x] **🔴 CH-3 회차 1·2 완주 — 각 4 / 100 · 🏆 §8.1 첫 완전 충족 N=2** (2026-07-29, 자연어 입력)
        [ch-3/round-1.md](ch-3/round-1.md). 주입은 **사양대로**(Mongo 23초 · 공백 22.29초 ·
        에러 0건 · 재시도 미발동) 재현됐는데 **탐색이 그 구간에 도달할 채널이 없었다** —
        좁힌 창 끝이 주입 **1초 앞**(05:22:00 ↔ 05:22:01)이다.
        탐색 원본 실측: Tempo `{status=error}` **05:22 없음** · Loki `ERROR|WARN` 05:25 버킷 **1**
        (05:05은 54) · Mimir `mongodb_up` **step 5m에 한 샘플도 안 걸림**.
        **창이 아니라 해상도가 막았다.**
        **🔴 결함 ⑫(지연 채널 부재)·⑬(메트릭 해상도)·⑭(단일 패스) 신규** →
        [B-17·B-18·B-19](round-3/README.md), **주입 없이 재조사 가능**.
        **도구 결함은 점수의 원인이지 면제 사유가 아니다** — 최초에 "채점 불가"로 적었다가
        **앵커 밖 잣대임을 확인하고 뒤집었다**([채점 대장 §정정](scoring/README.md))
  - [x] **IN-1 회차 1 완주·채점 98** (2026-07-29, 자연어 입력 · 루브릭 v3) [in-1/round-1.md](in-1/round-1.md).
        **IN-1 첫 실행 — 문항 커버리지 10개로.** 증상 3건을 **Redis 하나로 수렴**시키고
        **auth 오귀인 함정을 회피**했다(앵커가 *"탐색 실패가 오귀인으로 번지는 유일한 문항"*
        이라 못박은 그것). 교차 서비스성을 **밀리초로 입증**하고 지연을 **호출당 2초 × N회**로 정량화.
        판정 2건(영향·조치, 범위 93~100). 감별 메트릭 `redis_up` 미수집이 **다섯 번째**
  - [ ] **정상 문항(오탐 대조군) 1건 실행** — 주입 불필요.
        F10("없는 원인 날조")을 푸는 유일한 방법이고 아직 **0회**다.
        **단 "오귀인 변별력 0"은 부분 해소됐다** — v3의 중간 구간이 AP-2 회차 2에서
        12회 만에 첫 비만점(3/5)을 만들었다. 자를 바꾼 효과이지 문항 세트가 바뀐 것은 아니다
  - [ ] **v0.1 도구 결함 수정 후 traceId 재조사** (델타 측정) ← 다음 우선순위, 아래 ② 참조.
        **주의**: AU-4 캡은 둘로 갈림 — 근거경로 −10(baseline 대조)은 v0.1 도구 델타,
        근본·조치 −15(캐시 만료 미식별)는 프롬프트/N≥2 델타
  - [ ] N1 정답지로 리뷰 모드 재현율 측정 (정답지는 [findings/](findings/README.md) 확정 완료)
  - [ ] C-트랙 나머지 7문항 (아래 ①-b) + 문항당 N≥2 반복
- [ ] **Phase 3 — 프롬프트·컨텍스트 최적화** (baseline 후)
- [ ] **Phase 4 — 코드 인지 RCA** (게이트: (c)유형 실패 실측 — 이미 1건 확보)
- [ ] **🔴 Phase 4.5 — 장애 탐색(triage)** 🆕 (2026-07-28 신설) —
      **여기까지 오기 전 v0는 RCA 에이전트가 아니라 RCA 설명기다.** 입력을 traceId에서
      **자연어만**으로. **채점 쪽 준비는 끝났고(앵커 v2·루브릭 v3) 이제 이것이 회차 2의
      단일 차단 요인이다** — `RcaController`가 아직 `@NotBlank traceId`를 요구한다.
      **3채널 필수**(Tempo error · Loki 광역 · Mimir) — 에러 검색만으로는 12문항 중 6문항을
      원리적으로 못 찾는다. **후단에 넘기는 것은 traceId가 아니라 `(시간창+서비스+신호 종류)`** —
      CH-2·AU-2는 넘길 traceId가 존재하지 않는다

### ①-b. 숫자로 본 현황 (2026-07-29 기준)

| | |
|---|---|
| 조사 완주 | **21회** — Ⅰ(traceId 입력) 9회: CH-1×2 · CH-2 · **IN-2 격리 재실험** · AU-2 · AU-4재실행 · AP-1 · AP-3 · AP-2 / **Ⅱ(자연어 입력) 12회: AP-1(+**실행2**) · AP-2 · AP-3 · AU-2 · AU-4 · CH-2 · CH-1 회차2 + **CH-3 회차1·2** + **IN-2 회차2 · IN-1 회차1****. + AU-4·IN-2 구회차 폐기 + IN-2 지연 갈래 탐색 3회 |
| 문항 커버리지 | **12개** 중 **10개** (**IN-1 첫 실행** — 2026-07-29). 남은 것: AU-3 · 정상 문항(C7) |
| **유효 채점** | **17회** — Ⅰ 5회(**AP-3 100**, CH-1 회차1 80, **IN-2 재실험 80**, AP-1 75, AU-4 75) · **Ⅱ 12회(AP-1 90(+**실행2 90**) · AP-2 53 · AP-3 100 · AU-2 76 · AU-4 88 · CH-2 100 · CH-1 100 · CH-3 회차1 4 · 회차2 4 · **IN-2 100 · IN-1 98**)** |
| **§8.1 평균에 못 드는 회차** | **4회** — **채점 불가** 3회(앵커 부적합: CH-1 회차1 · CH-2 회차1 · AU-2 회차1, baseline까지 잃음) + **채점 유보** 1회(앵커 미박제: AP-2 회차1, **델타 baseline은 유효**) |
| **인용 가능한 점수** | **🏆 2개 — CH-3 4 ± 0 · AP-1 회차2 구성 90 ± 0.** 둘 다 §8.1의 (문항 × 자 × 입력 모델) N=2를 완전히 충족. **성격이 다르니 같은 근거로 쓰지 않는다** — CH-3 쌍은 4분 간격·같은 주입 데이터라 **결정성**을 보이고, **AP-1 쌍은 18시간 간격·다른 주입이라 실행 간 분산을 실제로 쟀다(값 0)**. 나머지는 전부 N=1 |
| 누적 비용 | 약 **$30.2** (회차당 $0.28~**$2.35**; 자연어 11회 중 7회가 직전 최고를 경신 — $1.1146 → $1.1353 → $1.3628 → $1.3892 → $1.9451 → $2.3392 → $2.3496 → $2.2704 → **$2.3546**(CH-3 회차2, 최고) → $1.7765(IN-2) → $1.8058(IN-1). **CH-1·CH-2·CH-3 네 회차가 같은 창을 공유**한다) |
| findings | NF 12건 · DF 1건 · **AE 7건** ([AE-07](findings/ae-07-rca-v0-ap2-round2-eval.md) — 도구 결함 ③④ 등재). **🔴 미등재 3건**: 모델 라벨 파싱 버그(AP-1 회차2, 수정 완료) · **로그 이중 적재 ⑤**(AP-3 회차2 신규) · **`C` 진동**(AP-3 회차2) |

> **🔴 자연어 2회차가 델타 예측을 연속으로 반증했다.** AP-1 회차 2는 *"+25는 전량 Phase 4
> (코드 인지)"* 를 깨고 코드 인지 없이 +15를 냈고, AP-2 회차 2는 *"+25는 전량 Loki 두 결함"* 을
> 깨고 두 결함을 고쳤는데 **−10**이 났다. **공통 원인은 결함을 셋 이상 놓친 것**이지
> 에이전트 능력이 아니다 — 예측을 세울 때 *"이 신호가 실제로 도달하는가"* 를 끝까지
> 따라가지 않았다. AP-2 회차 2에서 그것을 실측으로 두 건 잡았다(라인 필터 · Tempo skip).
>
> **AP-3 회차 2에서 처음으로 예측이 살아남았다.** 회차 1의 *"이미 만점이라 v0.1 델타가 올릴
> 자리가 없다"* 가 그대로 유지됐다(로그는 들어왔고, 점수는 천장이었다). **차이는 예측의
> 성질이다** — 앞의 둘은 *"무엇이 점수를 올린다"* 는 **인과 주장**이었고 AP-3는 *"천장에
> 닿아 있다"* 는 **산술 관찰**이었다. **인과 주장을 세울 때만 신호 도달을 끝까지 따라가야 한다.**

**부적합률은 67% → 33%로 내려갔지만, 실패의 모양이 바뀌었다.** 앵커 *부적합*(자의 품질 문제,
유형 A·B·C)은 AP-3에서 0이 됐는데 — 만점 요건 4개가 전부 **실행 결과 신호**(예외 지문·span
구조·롤백 이벤트)였다 — AP-2에서 **앵커 부재**(자의 생성 타이밍 문제, 유형 D)라는 새 실패가
나왔다. **STATUS가 "⚠️ 앵커 실측 박제 필요"로 정확히 표시하고 있었는데도 주입이 실행됐다.**
대응이 다르다: A·B·C는 체크리스트로, **D는 하네스 실행 게이트로** 막아야 한다(아래 ②).

**그래도 유효 5회는 전부 N=1이라 baseline은 아직 성립하지 않는다.** AU-4가 그 위험을 못박는다 —
같은 v0인데 두 실행이 75 vs ~100으로 25점 벌어졌다(장황본이 Redis GET으로 캐시 만료를 특정,
간결본은 놓침). **AP-3의 100점도 같은 이유로 인용할 수 없다** — out 11,070의 장황본이고,
원인이 단일 트레이스에 예외 지문으로 자백된 v0 최적 문항이다. 같은 v0의 out 편차는
2,657(IN-2)~11,070(AP-3)로 **4.2배**다.

원인은 세 유형이고 뿌리는 하나다 — **앵커를 "무엇이 관측되어야 하는가"로 썼고 "실제로 무엇이
관측되는가"를 확인하지 않았다.**

| 유형 | 회차 | |
|---|---|---|
| A. 전개 갈래 미상정 | CH-1 회차1 · AU-2 | 조건에 따라 신호가 안 나오는데 한 갈래만 적음 |
| B. 에이전트 입력 범위 무시 | CH-2 · AU-4 | 앵커가 요구한 신호가 수집 채널에 없음 |
| C. 코드 독해를 실측으로 착각 | AU-4 | `Duration.ofSeconds(3)` 상수 → "3s timeout" 요건. 실제는 23.5ms refused |

**절차 개정**: baseline 채록을 앵커 작성의 **입력**으로 만든다 — 정상 상태에서 실제로 무엇이
관측되는지 먼저 보고, **거기서 존재를 확인한 신호로만** 만점 요건을 구성한다.
이 순서였다면 **4건 중 3건이 막혔다.** 체크리스트와 근거는
[채점 대장](scoring/README.md#앵커-부적합률-67--원인-분류와-절차-개정).

**추적 지표로 삼는다**: v1 앵커 67% → 개정 후 목표 0%. 다음 3회차에서 안 떨어지면 절차가
아니라 루브릭(4항목 고정 배점) 자체를 의심해야 한다.

> 주입 절차도 같은 곡선을 탔다 — CH-1 회차0(라벨 버그 + 트리거 미도달) · CH-1 회차2(트리거를
> 원복 3초 전 발사) · AU-2 회차0(프로브 오염)으로 **3번 무효를 거쳐** 안정됐다.
> 앵커도 첫 사이클이라는 점은 감안하되, **떨어지는 것을 보여야** 학습 곡선이라 부를 수 있다.

### ①-c. 남은 문항 3개 (+ 부하 트랙 2개) — 실행 준비 상태

> **AU-1(auth CPU 기아)과 IN-3(커넥션 풀 고갈)은 이 표에서 뺐다.** 둘 다 부하가 있어야
> 성립하므로 **content·chat·auth 통합 부하 테스트 트랙**으로 묶어 나중에 함께 돌린다
> (아래 ①-d).

| 문항 | 만드는 장애 | 확인하려는 것 | 앵커 | 사용자 영향 | 지금 가능? |
|---|---|---|---|---|---|
| ~~**AP-1**~~ | 댓글 201자 → varchar(200) 위반 500 | 인프라 정상일 때 "왜 **이 요청만**" 수렴 | v2 박제 | 없음 | ✅ **회차 1 완주·75** |
| ~~**AP-3**~~ | 중복 해시태그 → `uk_feed_hashtag` 유니크 위반 500 | 같은 API·같은 `DataIntegrityViolationException`을 **예외 원문**으로 가름 (길이 vs 유니크). AP-1과 쌍 | v1 박제 | 없음 | ✅ **회차 1 완주·100** ([ap-3/](ap-3/README.md)) |
| ~~**AP-2**~~ | 팔로우 목록 `size` 미기본값 → `size+1` 언박싱 NPE → 500 | **정답이 로그에만 있는 문항** — v0.1 도구 델타 **+25점** 측정 지점 (AP-1과 대조군) | v1·**v2 박제** | 없음 | ✅ **회차 1 완주·채점 유보**(델타 baseline 60 확보) ([ap-2/](ap-2/README.md)) |
| ~~**CH-3**~~ | Mongo 다운 **20~25초** (드라이버 서버 셀렉션 대기 상한 30초 안쪽), 트리거 주입 직후 | **에러 span이 0인 장애** — 유일한 신호가 "자식으로 설명되지 않는 공백". 지연↔유실 구별. CH-1과 쌍 | **v2 박제** (`add5aff`) | 없음 (지연 흡수) | 🔴 **회차 1·2 완주 · 각 4 / 100**([ch-3/round-1.md](ch-3/round-1.md)) — 탐색이 주입을 **1초 차이로** 잘라냈다(결함 ⑫·⑬·⑭). **§8.1 첫 완전 충족 N=2**(평균 4 ± 0). [B-17·B-18·B-19](round-3/README.md) 후 **주입 없이 재조사** |
| **IN-1** | Redis 다운 | 3서비스의 **상이한 증상을 단일 근원으로 수렴** — 최고 난도 | v1 | 3서비스 영향 | ⚠️ 스케줄러 `@Observed` 전제 확인 필요 |
| **AU-3** | JWT 시크릿 드리프트 | 401은 4xx라 **trace에 error가 안 남는다** — 메트릭+로그로만 도달 | v1 | **전 사용자 401** | ❌ **도구 수정 전 금지** (아래) |

**AU-3는 현재 도구로 도달 채널이 0이다.** 이 문항의 유일한 경로가 401 rate 메트릭과
`{service_name="content-service"} |= "JwtFilter"` 로그인데, 전자는 `metric-queries`에 없고
후자는 Loki 셀렉터 결함으로 막혀 있다. AU-2는 hikaricp·gc가 파드와 함께 사라져 *우연히*
메트릭 신호가 됐지만, AU-3는 content가 살아 있고 401만 는다 — **우회로가 없다.**
RUNBOOK 권장 순서도 AU-3를 맨 마지막에 둔다(전 사용자 영향).

**권장 순서**: ~~AP-1~~ ✅ → ~~AP-3~~ ✅ → ~~AP-2~~ ✅ → **(도구 수정 + 재조사 + 문항당 N≥2)** → IN-1 → AU-3

> **여기서 신규 문항보다 도구 수정과 N≥2가 앞선다.** 문항 8개를 열어놨는데 **인용 가능한
> 점수는 여전히 0개**다. 문항을 더 늘리면 §8.1의 N≥2를 채우기가 그만큼 어려워진다.
> 그리고 **AP-2가 v0.1 델타 +25점짜리 측정 지점을 열어놨다** — 도구 수정 후 같은 traceId로
> 재조사하면 주입 0회로 델타가 나온다. AU-3의 실행 금지도 같은 수정으로 풀린다.

### ①-d. 부하 테스트 트랙 (별도, 나중에)

**IN-3**는 부하가 있어야 성립하므로 별도 트랙으로 둔다. (원래 **AU-1**과 묶여 있었으나
AU-1은 **문항 보류**됐다 — 아래.)

| 문항 | 만드는 장애 | 확인하려는 것 | 함께 검증되는 것 |
|---|---|---|---|
| ~~**AU-1**~~ 🚫 | ~~auth CPU 기아(느려짐)~~ | **보류(2026-07-28)** — 앵커는 v2로 성립하나 **CPU limit 50m가 3초를 넘길 보장이 없다.** 안 넘으면 타임아웃도 fallback도 없어 증상 자체가 안 난다. **주입 강도 → 증상이 비선형이라 §8.1의 N≥2를 쌓을 수 없다** | — |
| **IN-3** | 커넥션 풀 고갈 | "DB가 느림"과 "커넥션을 못 얻음" 구별 — **span 없는 공백** 읽기 + P0 알람 실발화 | **NF-10** · **[NF-11](findings/nf-11-feed-scroll-n-plus-one.md)**(수정 완료) |

**AU-1 보류로 NF-10 검증 경로를 다시 짜야 한다.** AU-4에서는 connection refused(23.5ms)가
timeout(3s)보다 빨라 커넥션 점유가 오히려 짧아졌다 — **auth가 *죽을* 때가 아니라 *느려질* 때만**
드러나기 때문이다. AU-1이 그 "느려질 때"를 만드는 문항이었다.

**대안: auth를 느리게 만드는 수단을 CPU 기아에서 바꾼다.** 필요한 것은 "auth가 3초 넘게
응답하지 않는 상태"일 뿐이고, CPU limit은 그걸 만드는 **여러 수단 중 재현성이 가장 나쁜 것**이다.
강도 → 증상이 선형인 수단(예: auth 앞단 지연 주입, 또는 IN-3 부하로 auth 자체를 포화)으로
바꾸면 같은 검증을 재현 가능하게 얻는다. **문항 재설계로 다룰 것** — 지금 결론 내지 않는다.

사전 예측(주입 전에 박아둘 것): `size=10` 기준 요청당 커넥션 점유가 약 227ms → **3,000ms+**
(3s timeout 포함)로 뛰고, 동시 요청이 풀 크기를 넘는 순간 `hikaricp_connections_pending`이
0에서 상승한다. NF-11 수정 이후이므로 N+1 몫(~33ms)은 이미 빠져 있다.

## ② 바로 다음 할 일

전체 개선안은 **[v0.1-plan.md](v0.1-plan.md)**, 변경 대기열은 회차별로
**[round-2/](round-2/README.md)** · **[round-3/](round-3/README.md) ← 지금 여기** 에 있다. 아래는 요약.

> **AU-4 회차 1에서 나온 결함(NF-09·NF-10)은 지금 고치지 않는다.** 회차 1을 baseline으로
> 두고 회차 2 직전에 적용해야 델타가 성립한다. 그리고 **변경군 A(앱 계측)와 B(조사 도구)를
> 섞지 않는다** — B는 주입 없이 기존 traceId 재조사로 검증되므로 먼저 하고, A는 회차 2
> 주입으로 검증한다. 섞으면 점수가 올라도 원인을 못 가린다.

완료된 것 (2026-07-27):

- [x] **블라인드 오염 차단** — `claude -p`가 레포 `CLAUDE.md`를 컨텍스트로 읽던 문제.
      `ClaudeCliLlmClient`가 중립 임시 디렉터리에서 CLI를 띄우도록 수정하고 프로브로 검증
      (`31e2a85`). AU-4 회차 1이 격리 적용 후 첫 조사다
- [x] **앵커 v2/v3 개정** — CH-1·CH-2·IN-2·AU-2 v2, AU-4 v3, AP-1 선박제
- [x] **AU-2·AU-4 회차 1 완주** + 채점 + findings 등재(NF-09·NF-10)

다음 (주입 없이 되는 것부터):

0-α. **🔴 탐색 구조 개편 — Incident를 1급 개념으로** (2026-07-29 신설, [round-3 §4.1-b](round-3/README.md))

   **결함 15**: 탐색 출력이 `(창 1 + 서비스 + traceId 1)`이라 **창 안에 장애가 둘이면 표현할
   방법이 없다.** 증거는 이번 회차다 — 서로 다른 장애를 묻는 CH-1·CH-2 두 질문에
   **같은 traceId를 답했다**(`6a6988a1…`, CH-1 것). CH-2 조사가 받은 후보 3건이 전부 CH-1 것이었다.

   **의존 관계상 순서가 강제된다. B-20 델타를 먼저 잰 뒤 하나씩 얹는다.**

   - **B-20 Incident Clustering** ← 나머지 셋 전부의 전제이자 **이 묶음 최우선**.
     시간 근접 신호를 `Incident[]`로 묶고 **복수 유지**. 기존 **B-19를 흡수**한다
     (버린 구간 기록 = Incident가 여럿일 때의 특수한 경우)
   - **B-23 검증 패스** — 고른 Incident가 질문 증상과 맞는지 재확인. **싸다**(컨텍스트 수백 자)
   - **B-21 질문 기반 재랭킹** — ⚠️ **어휘 사전을 코드에 박지 않는다**(블라인드 위반). 매칭은 LLM에
   - **B-22 Top-2 조건부 수집** — **마지막.** 비용 최대 2배($2.35 → ~$4.7)라 조건부 필수이고,
     **B-20 없이 넣으면 같은 Incident를 두 번 가져올 뿐이다**

   **B-20 델타는 주입 불필요** — 07-29 04:58~05:15 데이터가 보존기간 안이면 같은 두 문안으로
   재조사한다. **성공 기준: 두 조사가 서로 다른 traceId를 고르는 것.**

0. **🔴 `chaos.sh` 실행 게이트 신설** — `<ID> run` 전제에 "해당 문항 `answer.md`의 채점 앵커 표가
   비어 있지 않을 것"을 넣는다. 사람의 체크리스트가 **두 번 연속** 새어 나갔다
   (AP-3 SoT 미커밋 · **AP-2 앵커 부재 → 채점 불가 1회 발생**)
0.5 **🔴 AP-2 회차 2가 연 것 — 이게 지금 1순위다** (전부 주입 불필요):

   - **ⓐ 깨끗한 변경군 B 델타** — 회차 1 traceId `6a680b56067d9e2387043740be2cb115`로
     `POST /investigate` 1회. 회차 2의 −10에는 입력 모델 변경이 섞여 있어 **도구 수정분만
     남기려면 입력 모델을 고정**해야 한다. 선행 확인: 07-28 01:52Z 데이터가 **보존기간 안**인가
   - **ⓑ 🔴 수집기: traceId 없으면 서비스+창으로 Tempo 검색** (결함 ④) — 지금은 통째로 skip해
     `trace: 0B / 0 spans`가 된다. **CH-2·AU-2도 같은 경로**라 세 문항이 한 번에 풀린다.
     `Scope`가 traceId를 선택값으로 설계된 취지(strategy.md §4.5)가 수집 단계에서 안 지켜지고 있다
   - **ⓒ 🔴 Loki 예외 본문 도달 경로** (결함 ③) — 라인 필터가 스택트레이스를 배제한다.
     **traceId 단위 전량 조회**가 정답(에이전트가 조치 1로 제안한 그것).
     스택 포함 정규식(`|~ "ERROR|WARN|Exception|\tat "`)은 무관한 스택까지 끌어오므로 차선
   - **ⓓ 수집 실패 문구 수정** — 현재 *"트레이스가 생성되지 않는 장애다"* 가 오해를 부추겨
     에이전트가 **OTLP 파이프라인 결함 가설을 지어냈다**(후보 3). 사실은 "에러 트레이스만 검색했다"
   - **ⓔ 어셈블 컨텍스트에 탐색 선정 이유 동봉** — 분석 단계가 탐색의 창 축소를 모르고
     *"조회 창이 질문과 불일치"* 라며 **자기 탐색을 반박**했다
   - **ⓕ 앵커 정정(다음 회차부터)** — AP-2 조치에 **갈래 ㄴ 복원** · 탐색에 **중간 구간 신설**

0.6 **🔴 회차 3 대기열로 이관 → [docs/round-3/](round-3/README.md)**

   AP-3·AU-2 회차 2가 더한 항목(결함 ④ 범위 정정 · ⑤ 로그 이중 적재 · ⑥ 탐색 이유 미전달 ·
   ⑦ 401 쿼리 · 앵커 개정 5건)은 **한 곳에서 관리한다** — 같은 표가 두 곳에 살면 갈라진다.

   여기서 기억할 것은 셋뿐이다.

   - **🔴 최우선은 B-9(결함 ④)이고, 최적 측정 지점은 AU-2다.** 감점 24점 **전부**가 이 결함에
     걸려 있고 **신호의 존재를 채점자가 실측 확인**했다(`6a6957956058…` = 27 span 전부 content ·
     Redis GET ×4 · **auth 호출 span 0건**). **AP-2보다 깨끗하다** — AP-2는 ③·④가 섞였고 AU-2는 ④ 단독
   - **⚠️ 수정 범위를 정정했다** — 위 0.5-ⓑ의 *"(창 + 서비스) 기준"* 으로는 **AU-2가 안 풀린다.**
     탐색이 `services: ["auth-service"]`로 좁혔고 그게 정답이라, content 트레이스는 여전히 범위 밖이다.
     **"창 기준 전 서비스"** 여야 한다
   - **🆕 변경군 C(프롬프트)를 신설했다** — 에이전트가 *"장애 서비스 밖"* 을 스스로 안 본다.
     **단 B 델타를 먼저 잰다** — 지금은 신호가 0바이트라 지시를 줘도 볼 것이 없고,
     같이 넣으면 기여를 못 가린다

   - **ⓚ findings 등재 3건 밀림** — 모델 라벨 파싱 버그 · 로그 이중 적재 ⑤ · `C` 진동
   - **ⓝ 주입 사양 준수 게이트** — AU-2를 2회 돌린 것이 "재시작 루프" 오귀인을 유도했다.
     `chaos.sh`가 RUNBOOK 사양(주입 횟수·유지 시간)을 벗어나면 경고하도록

1. **🔴 도구 수정(변경군 B) → traceId 재조사** — **주입 0회로 첫 델타**가 나온다.
   **⚠️ B-1·B-2는 완료됐고 AP-2 회차 2로 검증했다 — 결과는 예측(+25)과 반대인 −10이었다.**
   아래 대조군 설계는 그 반증을 반영해 다시 짜야 한다(위 0.5-ⓐ).
   **AP-2가 최적 측정 지점**(+25점 전량이 이 수정에 걸림 — 셀렉터와 `logfmt`를 따로 고치면
   기여도까지 분리된다). **baseline은 이미 확보돼 있다: v1 60 / v2 65** — 회차 2를
   **두 앵커로 모두 채점**해 델타를 두 번 낸다([프로토콜](ap-2/round-1.md#델타-baseline으로서의-이-회차--회차-2-채점-프로토콜)).
   **AP-1을 대조군으로 함께 재조사**할 것 — 델타가 같은 +25인데 레버가 코드 인지라,
   AP-2만 오르고 AP-1은 그대로여야 진단이 맞은 것이다.
   [round-2/](round-2/README.md)의 B-1~B-6:
   - Loki 셀렉터 `{app=~...}` → `{service_name=~"...-service"}` (**6회 연속** 로그 0건 원인)
   - `errorWarnQuery`의 `| logfmt | level=~` → 라인 필터 `|~ "ERROR|WARN"` (평문 Logback이라
     `level` 필드가 안 생긴다 — 셀렉터와 **별개 원인**)
   - metric-queries에 `up` · `kafka_consumergroup_lag` · `kafka_brokers` · `mongodb_up` 추가
   - **🔴 B-4 수집 창 확대 — 우선순위 상향.** AU-2에서 복구가 창 **밖** 1분 15초였던 것에 더해,
     **창(트레이스 ±120초 ≈ 4분)이 `rate([5m])` lookback보다 짧다**는 게 드러났다. 인접 스텝이
     95% 겹쳐 곡선이 평평해지고, **주입이 창보다 먼저 시작되면 평시 대조군이 창 안에 없다** —
     "평시 대비 급증"을 요구하는 앵커(AU-3 ⓒ · CH-2 lag)가 구조적으로 달성 불가가 된다.
     임시로 AU-3 401 rate를 `[1m]`으로 낮춰 뒀다. 근본 대응은 **선행/후행 창 별도 수집**
   - 어셈블 컨텍스트에 span 절대 시각 명시 (`-120s`는 **어셈블러 버그가 아니라 LLM 앵커링** —
     AE-04 정정, AE-05 참조)
2. **에이전트 개선 — 마찰 제거형** (전부 주입 불필요, 기존 traceId로 검증):
   - **부재 신호 대조표**를 어셈블러가 생성 — 에이전트가 CH-2·AU-2·AU-4에서 *스스로* 하던 일
   - **baseline 트레이스 동봉** — AU-4에서 결정적이었는데 에이전트는 symptom만 봤다.
     §8.2 입력 패키징이 원래 요구하는 것
   - 리포트 **구조화 출력** — 채점 자동화로 회차 확보 비용을 낮춘다 (지금 N=1이 최대 병목)
3. **문항당 N≥2 채우기** ← **지금 최우선**. CH-1·IN-2·**AP-3** 각 1회씩만 더 돌리면
   **최초의 인용 가능한 점수**가 나온다. **AP-2는 회차 2가 곧 회차 1**이다(앵커 v1이 이제
   박제됐으므로 다음 회차부터 채점 성립). 신규 문항보다 우선한다.
   **AP-3 회차 2가 특히 값싸다** — 인프라 무접촉·원복 없음이고, 회차 1이 out 11,070 **장황본**
   이었으므로 회차 2가 간결본이면 AU-4에서 본 thoroughness 분산을 **두 번째 문항에서 재현**하게
   된다(같은 v0의 실행 간 편차가 문항 고유가 아님을 입증)
4. **AP-2·AP-3 SoT 역동기화** — 두 문항 모두 toy-content `answer.md`가 **구 문항 그대로**다
   (AP-2 = 대용량 업로드, AP-3 = 이모지 charset). 교체본·앵커는 서버 `~/chaos`와 이 레포
   `docs/ap-2`·`docs/ap-3`에만 있다. 채점은 성립시켰으나 두 기록이 갈라져 있다
5. **앵커 절차 개정** — 만점 요건은 **baseline 실측으로 존재를 확인한 신호만**.
   앵커 부적합 4건 중 3건이 이걸로 막혔을 것 ([채점 대장](scoring/README.md) 결함 ⑩).
   **AP-3가 보탠 단서**: 코드 독해로 쓴 앵커라도 요건이 **실행 결과 신호**(예외 지문·span 구조)면
   부적합이 안 났다 — "무엇을 읽었는가"보다 "검증되게 썼는가"가 결정적

5.5 **🔴 앵커를 루브릭 v3 항목으로 재배치** ([rubric-v3.md](scoring/rubric-v3.md)) —
   **회차 2 주입 전 선행 필수.** v1 `근본 원인 40`이 원인 지목·메커니즘/귀속·**영향 판정**
   셋을 한 칸에 담고 있다(13문항 전수 확인). CH-1 회차 2는 **감점 20 전액이 영향 오판**인데
   원인은 정확히 맞혔다 — 다른 능력이 한 점수에 묶여 무엇이 부족한지 안 보인다.
   v3: 원인 40 / 근거 25 / **탐색 15(신설)** / **영향 10(분리)** / 오귀인 5 / 조치 5.
   **재배치는 요건 문언을 바꾸지 않고 위치만 옮기는 작업** — 문언을 함께 고치면 채점 무효다.
   **정상 문항 1건도 함께 실행할 것**(주입 불필요 · 오귀인 변별력 0을 푸는 유일한 방법).
6. **미실행 2문항 주입 전 검증** — 실행 대상 12문항 앵커는 전부 성립하지만
   **AU-3·IN-1은 체크리스트가 남아 있다**(각 `answer.md` 말미).
   통과 전 주입은 **유형 C 재발**이다:
   - **AU-3**: 서명 불일치와 만료가 로그에서 **구별되는 문구인지** 미확인. 안 되면 ⓑ 폐기.
     **주입 없이 확인된다** — 잘못된 토큰과 만료된 토큰으로 각각 한 번씩 호출
   - **IN-1**: Redis 다운 시 Lettuce span이 **에러로 남는지 아예 사라지는지**(사라지면 ①을
     "부재 신호"로 교체) · **`websocket.active_users`가 다운 중에도 유지되는지**(④의 전제 —
     CONNECT 핸들러가 예외로 세션을 못 만들면 함께 떨어져 ④가 무너진다)
   - ~~**CH-3**~~ ✅ **주입 실행 완료** (2026-07-29, 다운 23초로 사양 적중).
     남은 것은 주입이 아니라 **도구**다 — [B-17·B-18](round-3/README.md) 후 **주입 없이 재조사**
   - 🚫 **AU-1은 문항 보류** — 앵커는 성립하나 **증상 재현이 노드 부하에 좌우**된다.
     CPU limit 50m가 3초를 넘길 보장이 없고, 안 넘으면 증상 자체가 안 난다.
     **주입 강도 → 증상이 비선형이라 N≥2를 쌓을 수 없다**

백로그:

- **🔴 Phase 4.5 — 장애 탐색(triage) 신설** ([strategy.md](strategy.md#phase-45--장애-탐색triage-어젯밤-오류-찾아줘에-답한다)).
  **지금 v0는 RCA 에이전트가 아니라 RCA 설명기다** — `RcaController`가 `@NotBlank traceId`를
  요구하고, Loki·Mimir 시간창이 트레이스에서 파생돼(`TimeWindow.fromTrace`) traceId 없이는
  쿼리를 만들 수 없다. 실무 조사는 알람·증상에서 시작하고 **traceId는 사람이 찾아낸
  중간 산출물**이다. 붙일 것은 앞단뿐이고 클라이언트는 이미 다 있다(Tempo `status=error`
  검색 · Loki 광역 `errorWarnQuery` · Mimir `up`/rate 이상 구간).
  **baseline은 무효가 되지 않는다** — 탐색은 traceId 입력을 *대체*가 아니라 *추가*하므로
  기존 진입점과 분석 측정은 그대로 살아 있다. 지켜야 할 것은 **탐색 정확도를 분석 점수와
  분리 채점**하는 것 하나뿐이다.
  **주입 없이 평가된다** — 과거 9회의 정답이 이미 있다(주입 시각 `timeline.log` · 실제
  traceId 회차 리포트 · 원인 `answer.md`). "이 시각 무렵 이상 찾아줘"로 회고 평가하고,
  정상 구간을 섞으면 **C7 오탐 대조군까지 이 트랙에서 해결**된다.
  **선행 확인: Grafana Cloud 보존기간에 07-26~28 데이터가 남아 있는가.** 없으면 회고 평가 불가.
  AU-3가 이 한계의 실증이다(전역 장애인데 입력이 요청 1건 → 하네스 우회 장치 필요).
- **AP-2 앵커 선박제** — 실행 전에 v2 원칙으로. AP-1(`38e01f0`)이 선례.
- **AU-4 v5 앵커** — ⓒ`작성자 익명 전환`은 **질문에 적힌 증상**이라 근거가 될 수 없다(유형 E).
  회차 1(75)이 v3로 채점됐으므로 **회차 2부터** 적용하고, AP-2처럼 **두 앵커로 이중 채점**해
  앵커 민감도를 분리한다
- ~~**AU-1 준비**~~ — **문항 보류(2026-07-28)**. 다만
  [NF-10](findings/nf-10-content-db-connection-held-during-external-call.md)(외부 호출 중 DB
  커넥션 점유) 검증은 **다른 문항으로 옮길 수 있다** — IN-3(커넥션 풀 고갈)이 같은 지표
  (`hikaricp_connections_pending`)를 쓰고 **부하로 재현되므로 강도 조절이 선형**이다.
  CPU 기아 대신 **IN-3에 외부 호출 지연을 겹치는 변형**을 검토할 것
- **`measure_AU_2`·`measure_CH_2`에 `loki_count`/`prom`/`tempo_search` 보강** (toy-content) —
  AU-2 evidence가 HTTP 프로브 2건뿐이라 소급 조회로 메워야 했다. `measure_AU_4` 수준으로
- **`chaos.sh` 프로브 정규식을 접두사 기반으로** — `user_fallback`이 `대체 사용자|fallback`을
  찾는데 코드는 `사용자 목록 조회 실패`를 찍어 **0건 오보고**됐다 ([NF-09](findings/nf-09-user-fallback-no-traceid.md))
- N2(피드 목록)/N3(채팅 fan-out) 정답지 큐레이션
- 컨슈머 예외 삼킴을 findings에 기록 (NF-02와 같은 계열)
- api-sweep 재실행으로 500 결함 3군 회귀 확인 (수정되면 200 전환)

**해소됨** — Loki 라벨 두 값 공존 여부 실측 완료(2026-07-27): `service_name`만 존재하고
`app` 라벨은 **없다**. "과거 조사에서 로그가 잡히긴 했다"는 기록은 착오였고, 실제로는
**6회 전부 로그 0건**이었다. 셀렉터 수정은 위 ②-1로 이관.

## ③ 문서 지도 (뭘 보려면 어디로)

> **이 파일은 "지금 뭘 할 차례인가"만 담는다.** 프로젝트 전체를 한 번에 파악하려면
> **[docs/README.md](README.md) — 종합 문서**를 보면 된다 (정의·측정 체계·결과·문서 지도 전부).
> 포트폴리오·면접용 서술은 **[portfolio.md](portfolio.md)** 에 따로 뽑아 두었다.

| 알고 싶은 것 | 문서 |
|---|---|
| **프로젝트 전체를 한 문서로** | [README.md](README.md) — 종합 문서 (여기서 다른 문서로 뻗는다) |
| **포트폴리오에 쓸 기술 의사결정** | [portfolio.md](portfolio.md) — OTel vs Brave · 단일 패스 · 평가 체계 등 8건 |
| 전체 계획과 왜 이 순서인가 | [strategy.md](strategy.md) |
| 관측 파이프라인 구성·한계 (실측) | [monitoring.md](monitoring.md) |
| 왜 그렇게 결정했나 (수치 포함) | [decisions/](decisions/README.md) — ADR 6건 |
| 찾아낸 문제들 (정답지 겸용) | [findings/](findings/README.md) — NF 12건 + DF 1건 + AE 6건(블라인드 조사 평가) |
| **회차별 §8 점수와 판정 근거** | [scoring/](scoring/README.md) — 채점 대장. 조사를 돌렸으면 여기에 회차를 추가한다 |
| ↳ 장애별 **항목 점수표** | [scoring/summary.md](scoring/summary.md) — 회차마다 4항목 점수 + 한 줄 판정 |
| ↳ 장애별 **앵커 요건표** | [scoring/anchors.md](scoring/anchors.md) — 무엇을 만점으로 보는가 + 채널 감사 |
| **토큰·비용은 어떤 수치로 재나** | [measurement.md](measurement.md) — 측정 기준. 개선 지표 정의·통제 변수·차트 사양 |
| **회차 1 토큰 실측 (포트폴리오용)** | [round-1-input-tokens.md](round-1-input-tokens.md) — 기준선·오버헤드 분해·검증 |
| **다음에 뭘 고칠 건가 (측정 기반)** | [v0.1-plan.md](v0.1-plan.md) — 감점·비용 원인별 개선안 + 검증 가능한 예측 5건 |
| **회차 2에 적용할 변경** (완료분 포함) | [round-2/](round-2/README.md) — 변경군 A(앱)/B(도구) 분리, 실행 체크리스트, 전후 검증 기준 |
| **🔴 회차 3에 적용할 변경 (지금 여기)** | [round-3/](round-3/README.md) — 결함 ③~⑮ 수정안 · **변경군 C(프롬프트)** · **§4.1-b 탐색 구조 개편(B-20~B-23)** · 앵커 개정 보류 목록 · 예측 표 |
| **장애별 회차 기록** (원인대조·스샷 traceId·리포트) | [ch-1/](ch-1/README.md) · [ch-2/](ch-2/README.md) · [ch-3/](ch-3/README.md) · **[in-1/](in-1/README.md)** · [in-2/](in-2/README.md) · [au-2/](au-2/README.md) · [au-4/](au-4/README.md) · [ap-1/](ap-1/README.md) · [ap-2/](ap-2/README.md) · [ap-3/](ap-3/README.md) |
| 에이전트 리포트 실물 | `reports/` (조사별 .md/.json) · [sample-report.md](sample-report.md) · [sample-review-report.md](sample-review-report.md) |
| 실행 도구 | `scripts/api-sweep.sh` (읽기 순회) · `scripts/api-write-flow.sh` (업로드→피드→댓글) |
| chaos 하네스 | 서버 `~/chaos` (이 레포 밖) — 시나리오·evidence·채점 |

## ④ 활동 로그 (최신이 위)

- **2026-07-29 (AP-1 회차 2 · **실행 2** — **90점 · 편차 0** · **실행 간 분산을 잰 첫 쌍**)**:
  주입 08:51:47.463Z(250자 댓글 1건), 조사 08:54:14Z 스윕 → 08:56:58Z 리포트.
  문안은 **회차 2와 문자 그대로 동일**. [ap-1/round-2b.md](ap-1/round-2b.md) ·
  [채점 대장](scoring/README.md#ap-1-회차-2--실행-2--90--100--81-첫-완전-충족--편차-0)

  **① 회차 2가 지정한 실험의 답이 나왔다.** 회차 2 기록이 *"같은 자연어 입력으로 한 번 더 →
  **90 근처 = 구성 효과** · 75 근처 = 분량/실행 편차"* 라고 적어뒀는데, **90이 나왔고
  항목별로도 30·30·20·10으로 전부 같다.**

  **② 통제 변수 다섯이 고정됐다** — 문안(문자 그대로) · 자(4항목 anchors.md v2, 회차 2와 동일) ·
  입력 모델 · 도구(`src/`·`prompts/` 변경 0건) · 주입 스펙. **그리고 주입이 별개다**
  (18시간 간격·다른 데이터). **CH-3 쌍(4분 간격·같은 데이터)이 못 잰 실행 간 분산을
  이 쌍이 실제로 쟀고, 값이 0이다.** → 인용 가능한 점수가 2개가 됐고 **성격이 다르다**:
  CH-3 = 결정성, AP-1 = 실행 간 분산.

  **③ ⚠️ 회차 1(75) → 회차 2(90)의 +15는 여전히 미분리다.** 이번이 지운 것은 **"실행 운"**
  하나뿐이고 입력 모델·Loki 수정·앵커 v1→v2 셋은 그대로다.
  **"AP-1이 90이다"는 인용 가능, "자연어 진입이 +15를 만들었다"는 여전히 불가.**

  **④ 분량 가설이 부분 반증됐다** — 이번 out **6,472**로 회차 2(7,159) 대비 **−9.6%**,
  컨텍스트도 74KB vs 90KB인데 **점수가 같다**. 단 AU-4 사례는 **3.5배**(2,980 ↔ 10,511)였다.
  **"±10%로는 안 움직인다"까지가 확인된 것**이고 임계는 미측정.

  **⑤ 같은 30점인데 추론 경로가 갈렸다.** 회차 2는 검증 부재를 **1순위·확신도 높음**으로
  확정했고, 실행 2는 **2순위·확신도 중간**이되 *"검증이 있었다면 **INSERT span 자체가
  생성되지 않았어야 함**"* 이라는 **부재 논증**으로 도달했다. 둘 다 조치에서 `TEXT`/컬럼 확장을
  내 **앱/DB 책임 분리를 못 닫은 것도 같다**.

  **⑥ 탐색 분산은 못 쟀다** — 두 실행 다 후보가 **1건**(창의 유일한 에러 트레이스가 정답).
  **후보가 여럿인 문항(CH-2 3건 · IN-1 8건)에서 N=2를 잡아야** 탐색 분산이 나온다.
  `C` = 26,576으로 **7회 연속 동일**(약 8시간) · 비용 **$1.0209로 자연어 회차 중 최저**.

- **2026-07-29 (IN-1 회차 1 — 자연어 진입 · **98점** · **복합 장애를 한 근원으로 수렴**)**:
  주입 Redis 다운 **07:28:00~07:31:00Z(3분)**, 조사 08:05:17Z 스윕 → 08:09:46Z 리포트.
  질문은 증상 3건 나열("① 로그인이 느리다 ② 친구가 오프라인으로 보인다 ③ 작성자 이름이 이상하다").
  [in-1/round-1.md](in-1/round-1.md) ·
  [채점 대장](scoring/README.md#in-1-회차-1--98--100--자연어-진입--복합-장애를-한-근원으로-수렴-81-평균-제외)

  **① IN-1 첫 실행 — 문항 커버리지 9개로.** 98/100(원인 40 · 근거 25 · 탐색 15 · 영향 10 ·
  오귀인 5 · **조치 3**). **채점자 판정 2건**(영향·조치, 범위 93~100).

  **② 앵커가 *"탐색 실패가 오귀인으로 번지는 유일한 문항"* 이라 못박은 함정을 통과했다.**
  질문이 증상을 따로 주므로 한 서비스만 파면 auth를 근원으로 지목하기 쉬운데
  (Redis가 죽으면 캐시 미스로 auth 직행이 늘어 **auth가 느려 보인다**),
  **탐색이 content + chat 둘 다 골랐고 auth는 후보에도 안 올렸다.** 처리도 정확했다 —
  *"**지연은 오류를 남기지 않으므로** … '정상 확정'이 아니라 **관측 신호 없음**으로 둔다"*.
  **무죄 추정조차 하지 않았다.**

  **③ 교차 서비스성을 밀리초로 입증했다** — 같은 Redis 대상 같은 메시지가
  content `qnxk6` 16:27:34.185 / content `scw7k` .234 / chat `w7bf7` .255에 거의 동시 시작.
  *"서로 다른 노드의 pod가 동시에 실패 → pod/노드 측이 아닌 **Redis 서버 측 사건**"*.

  **④ 지연을 정량으로 논증했다** — 호출 횟수와 응답시간의 선형 관계를 표로 만들었다
  (10회 → **20,238ms** / 8회 → 16,138ms / 8회 → 16,268ms), 로그 간격도 실측(**≈2.006~2.011초**).
  **"Redis가 죽어서 느리다"가 아니라 "호출당 2초 × N회라서 정확히 이만큼 느리다"** 를 보였다.

  **⑤ 🔴 감별 메트릭 미수집이 다섯 번째 — 이번엔 `redis_up`.** 에이전트가 후보 1의 반증
  데이터로 *"`up{job=redis}`는 exporter 스크레이프 성공만 뜻하고 **`redis_up`은 수집되지 않았다**"*
  고 정확히 적고 조치 3으로 요청했다. **채점자 실측 — `redis_up`은 Mimir에 있고 07:28~07:31에
  정확히 0이다.** AU-2·CH-2·CH-1·IN-2에 이어 **다섯 번 다 "데이터소스엔 있고 목록엔 없고
  에이전트가 정확히 요청했다"** 이고, **다섯 번 다 감점은 없었다**(다른 경로로 도달).
  → **점수 결함이 아니라 확신도·서술 품질 결함**이다.

  **⑥ 결함 15의 진단이 한 회차 만에 대조로 확인됐다.** IN-2에서 `chars × 0.524`가 처음으로
  **−10.7%** 과대추정했는데(errwarn 147.5KB, 거의 동일한 WARN 반복), 이 회차는 **+2.6%/+6.6%**로
  정상이다(errwarn 46.9KB, 혼합). **계수가 어긋나는 조건을 "반복 비중"으로 특정할 수 있다.**
  `C` = 26,576으로 **6회 연속 동일**.

  **⑦ 사용자 경로가 200이라 에러 검색에 안 걸렸다** — 피드 16~20초가 전부 `200`이고 스윕이
  잡은 에러 8건은 스케줄러 태스크였다. **로그 채널이 없었으면 CH-3처럼 됐을 것**이다(결함 12와 같은 뿌리).

- **2026-07-29 (IN-2 회차 2 — 자연어 진입 · **100점** · **메커니즘 예측이 처음으로 적중**)**:
  주입 Kafka 다운 **06:28:15~06:31:30Z(약 3분)**, 조사 06:34:53Z 스윕 → 06:38:23Z 리포트.
  "최근 1시간 안에 댓글 알림이 안 왔다는 제보가 있다". [in-2/round-2.md](in-2/round-2.md) ·
  [채점 대장](scoring/README.md#in-2-회차-2--100--100--자연어-진입--메커니즘-예측이-처음으로-적중-81-평균-제외)

  **① 100/100** (원인 40 · 근거 25 · 탐색 15 · 영향 10 · 오귀인 5 · 조치 5). 판정 **0건**.

  **② 🔴 회차 1의 오선정이 해소됐다.** IN-2는 구 회차·재실험 **둘 다** "토픽 부재"를
  "브로커 다운" 위에 올려 *"하위 원인 감별 실패(N=2 재현)"* 로 기록된 문항이다.
  회차 2는 1순위를 **"Kafka 브로커 단일 노드 다운"** 으로 확정했고, 감별 근거로
  **`up{job="kafka"}` 0 전환 + `kafka_brokers` 시계열 결손**을 인용했다.
  **같은 예외 문구**(`Topic ... not present in metadata after 60000 ms`)를 회차 1은
  *토픽이 없다*로, 회차 2는 *브로커에 못 붙었다*로 읽었다 — **차이는 추론이 아니라 옆에 놓인 메트릭이다.**

  **③ round-2 대기열의 예측이 문자 그대로 적중했다** — *"근본 20 → 40. **B-3의 `kafka_brokers`
  시계열 부재**가 '토픽 부재 vs 브로커 다운' 감별의 결정타"*. **AP-1·AP-2의 델타 예측이 연속
  반증된 뒤 처음**이고, 차이는 예측을 *"무엇이 오른다"* 가 아니라 **"어떤 신호가 어떤 판단에
  쓰인다"** 로 적은 것이다. ⚠️ 총점 80 → 100은 자가 달라(v1 4항목 → v3 6항목) **인용 금지** —
  인용 가능한 것은 **메커니즘 확인**이다.

  **④ 결함 4 다섯 번째 재현 — 이번엔 자력 대체.** baseline `6a699d62…`(주입 **29초 전**)의
  `publish user.notifications`가 **14.32ms 성공**(36 spans)인데 못 받았다.
  앵커 B가 요구한 *"정상 발행이 몇 ms인지"* 가 창 안에 있었다. 에이전트는 `max.block.ms`
  상수 지식으로 메워 **감점 없음**(AP-3와 같은 자력 대체).

  **⑤ 🔴 `chars × 0.524`가 처음으로 과대추정했다(−10.7%).** 이 회차 `errwarn`이 **147,513B**로
  압도적이고 Kafka 프로듀서 WARN의 거의 동일한 반복이다(253건). **반복 텍스트는 토크나이저가
  0.524보다 효율적으로 압축한다** — 계수가 혼합 텍스트 기준이라 로그 지배 회차엔 안 맞는다.
  **[측정 기준](round-1-input-tokens.md)에 조건을 달아야 한다.**
  `C` = 26,576으로 **5회 연속 동일**. 비용 $1.7765로 **6회 연속 최고치 경신이 멈췄다**.

- **2026-07-29 (CH-3 회차 1·2 — 자연어 진입 · **각 4 / 100** · 🏆 **§8.1 첫 완전 충족 N=2**)**:
  주입 Mongo 정지 **약 23초**(05:22:15.45~05:22:38.32Z · 사양 20~25초 적중), 트리거 트레이스
  `6a698e07c0a70c241a89278e13affb6e`. 조사 05:45:40Z·05:50:09Z **두 번**,
  문안은 **문자 그대로 동일** — "최근 1시간 안에 앱이 잠깐 버벅였다는 얘기가 있어요. 뭔가 있었는지 봐줘".
  [ch-3/round-1.md](ch-3/round-1.md)(공통 SoT) · [round-2.md](ch-3/round-2.md) · [채점 대장](scoring/README.md)

  **① 문항은 의도대로 재현됐다.** 트리거 트레이스가 교과서적이다 — `receive` 23,458ms 중
  **첫 Mongo `insert`까지 22.29초 공백**, 자식 전부 타임라인 오른쪽 끝, **에러 span 0건**,
  `receive` span **1개**(재시도·DLQ 미발동), 첫 insert 시각이 **Mongo 복구 시각과 일치**.

  **② 🔴 그런데 에이전트가 그 구간에 도달하지 못했다.** 두 회차 모두 04:57~05:2x로 좁혀
  **CH-1(Mongo 5분)·CH-2(chat 컨슈머 공백)** 를 분석했다 — 같은 창의 다른 두 주입이고,
  각각 이미 **100점**을 받은 사건이다. **CH-3 언급 0회.**
  회차 2는 창 끝이 **05:22:00**으로, 주입 시작(05:22:15.45)을 **15.45초 차이**로 비껴갔다.

  **③ 4 / 100** (원인 0 · 근거 0 · **탐색 4** · 영향 0 · 오귀인 0 · 조치 0). 앵커는 박제됐고(`add5aff`, 주입보다
  **13시간 12분** 앞섬) 만점 요건 4요소가 전부 실측 트레이스에 존재한다. **막은 것은 탐색 채널이다.**
  채점자가 3채널을 전부 직접 쿼리해 확정했다 — Tempo `{status=error}` **0건**(에러 span이 없다) ·
  Loki `ERROR|WARN` 창 전체 **1건**(CH-1은 54건 피크) · Mimir `mongodb_up` **step 5m에 한 샘플도 안 걸림**.
  **주입은 스윕 창 안에 있었다. 창이 아니라 해상도가 막았다.**

  **④ 🔴 결함 ⑫·⑬·⑭ 신설, 그리고 ⑬은 새로운 축이다.** ⑫ = 스윕에 지연 채널 부재
  (`trace-query`가 `{status=error}` 단독) · ⑬ = **스윕 메트릭 step 5m이 짧은 딥을 삼킨다** ·
  ⑭ = **탐색이 단일 패스라 약신호 구간을 버린다**(→ B-19).
  **탐색 원본(`reports/raw/scan-1785300609-*`) 확인 결과 05:22를 가리키는 신호는 Loki 05:25 버킷의
  `1` 한 칸뿐이었고 바로 옆이 54였다. 좁힌 창 끝은 주입 1초 앞(05:22:00 ↔ 05:22:01)이다.**
  지금까지 탐색 커버리지 논의는 전부 *채널 종류*(trace/log/metric)였는데
  **시간 해상도가 커버리지를 결정한 사례는 처음이고 채널 감사표에 그 열이 없다.**
  주입이 짧을수록 걸리는데 **실운영의 장애는 대개 짧다.**
  수정안 → [round-3 B-17·B-18·B-19](round-3/README.md), **주입 없이 이 데이터로 재조사 가능**.

  **⑤ 🏆 §8.1을 완전히 충족하는 최초의 회차 쌍 — 평균 4 ± 0.** 같은 문항 · 같은 자 ·
  같은 입력 모델(문안 문자 그대로 동일) · N=2 · **편차 0**. **이 프로젝트 최초의 인용 가능한
  점수이고 그 값이 4다.** AU-4의 N=2는 입력 모델과 분량이 함께 바뀐 것이었다.
  ⚠️ 단 4분 간격·같은 주입 데이터라 **실행 간 분산은 이 쌍으로 측정되지 않는다.**

  **⑤-b 🔴 최초 판정을 뒤집었다.** 처음 "채점 불가(도구 요건 미충족)"로 적었는데, 그것은
  **앵커에도 루브릭에도 없는 범주**를 채점자가 만든 것이고 CH-2 회차 2 정정이 금지한 행위다.
  **요건은 충족한 만큼 주고, 0점 칸은 게이트가 아니라 예시다.** 그리고 루브릭 v3가 탐색 15를
  신설한 이유가 정면으로 이것이다 — **탐색 실패로 분석이 성립하지 않는 것은 측정 실패가 아니라
  측정 결과다.** 도구 결함은 **점수의 원인이지 면제 사유가 아니다.**

  **⑥ 앵커 감사에서 모순 1건.** anchors-v2의 §탐색 채널 감사는 CH-3를 *"Tempo 지연 기준 ·
  ⚠️ error로는 안 걸림"* 으로 적어 놓고, Ⅳ. 앵커 건전성 표에서는 **✅ 전 항목 도달 가능**으로
  분류했다. **후자가 틀렸다** — CH-2·AP-2·AU-3와 같은 ⚠️ 군이다. 정정은 [round-3 §5 ㅇ](round-3/README.md)로 보류.

  **⑦ B-16의 예측은 방향이 맞고 층이 틀렸다.** *"CH-3가 곧 걸린다 — 로그 채널이 통째로 비어서
  온다"* 고 적었는데 실제로는 **그 앞 단계인 탐색에서 막혔다.**
  **결함을 예측할 때 어느 단계에서 걸리는지까지 지목해야 한다.**

  **⑧ 에이전트는 결론을 과장하지 않았다.** 회차 2가 스스로 *"'앱이 버벅였다'는 증상과 이
  트레이스의 인과는 **관측 데이터로 확정할 수 없음**"*, *"**사용자 제보 시각을 재확인**"* 이라 적었다.
  `C` = **26,576이 7회 연속 동일**(01:07Z~05:54Z).

- **2026-07-29 (CH-1 회차 2 — 자연어 진입 · **100점** · **실패가 해소된 첫 사례** · 회차 재번호)**:
  주입 `docker stop mongodb` **약 5분**(`mongodb_up` 0 = 04:58:45~05:03:30Z), 트리거 04:59:13Z,
  **도착 05:04:18.6Z(5분 5.6초·유실 0)**. 조사 05:39:13Z 스윕 → 05:43:11Z 리포트.
  "최근 1시간 안에 댓글 알림이 안 왔다는 제보가 있어요. 확인해줘".
  [ch-1/round-2.md](ch-1/round-2.md) ·
  [채점 대장](scoring/README.md#ch-1-회차-2--100--100--자연어-진입--루브릭-v3--실패가-해소된-첫-사례-81-평균-제외-n1)

  **① 100/100** (원인 40 · 근거 25 · 탐색 15 · 영향 10 · 오귀인 5 · 조치 5). 판정 1건(영향, 반대 시 95).
  탐색은 **Top-1 자력 선정**(후보 4건 중 주입 트레이스) · 창 04:57~05:22Z가 주입·복구·**도착**을 전부 덮었다.

  **② 🔴 실패가 재현되지 않고 해소된 첫 사례.** AU-4가 *"실패는 항목 단위로 재현된다"* 를 보였다면
  CH-1은 그 반대다. 회차 1의 감점 20 **전액이 4) 영향 오판**(*"미발송(유실) 상태"*)이었는데,
  회차 2는 *"'영구 유실'은 근거가 약하다"* 로 뒤집고 재처리 성공을 **≈05:04:17로 추정**했다
  (실제 05:04:18.629 — **오차 2초**). ⚠️ **총점 80 → 100은 자가 바뀐 것**(4항목 → 6항목)이라 인용 금지.

  **③ ✅ round-2 대기열(B-1·B-2·B-3)의 첫 양(+) 델타.** AP-2에서 −10을 냈던 **같은 수정군**이다.
  회차 1은 Loki 0건, 회차 2는 45,754B. **결정타는 B-3(kafka lag 메트릭 추가)** —
  도착 로그가 INFO라 안 왔는데 `notification-recovery` 컨슈머그룹 lag **1→0**으로 동치 도달했다.
  **델타의 부호는 수정의 옳고 그름이 아니라 그 문항의 정답이 어느 채널에 있는지가 정한다.**

  **④ 🔴 결함 11 신규 — 도착 근거가 수집 채널에 원리적으로 없다.** `errorWarnQuery`가
  `ERROR|WARN`뿐이라 `[Kafka] DLQ 알림 재처리 성공`(INFO)이 창 안에 실재했는데 안 실렸다.
  **4) 영향 판정 만점 요건이 구조적으로 도달 곤란**하고, **CH-3가 곧 정면으로 걸린다**
  (에러 로그가 하나도 없는 문항) → [round-3 B-16](round-3/README.md).

  **⑤ 결함 ④가 4문항 연속 재현 끝에 첫 미발생.** 탐색이 traceId를 자력 선정했기 때문이지
  고쳐져서가 아니다 — **에러 트레이스가 있는 6문항에서는 잠복**한다.
  B-9 측정 지점을 AU-2로 유지해야 할 근거가 늘었다. 반면 **`scale 0` 오추정은 5회 연속** 재현.

  **⑥ ⚠️ CH-2 회차 2와 같은 창이다.** 주입이 6분 간격으로 연달아 났고(Mongo 5분 → chat `scale 0` 10분)
  문안만 달리해 두 번 돌렸다. **두 100점을 독립 표본으로 세지 않는다.**
  회차 3부터 **단독 주입으로 조건을 분리**해야 §8.1의 N이 쌓인다.

- **2026-07-29 (CH-2 회차 2 — 자연어 진입 · **100점** · 복합 창에서 계층 분리)**:
  조사 05:23:30Z 스윕 → 05:27:57Z 리포트. "최근 1시간 안에 채팅 알림이 안 온다는 문의가
  여러 건 들어왔다". [ch-2/round-2.md](ch-2/round-2.md) ·
  [채점 대장](scoring/README.md#ch-2-회차-2--100--100--자연어-진입--복합-창에서-계층-분리-81-평균-제외)

  **① 100/100** (원인 40 · 근거 25 · 탐색 15 · 영향 10 · 오귀인 5 · 조치 5). 판정 0건.
  **CH-2 최초의 실제 점수**다(회차 1은 앵커 부적합 → 델타 불성립).

  **② 창 안에 원인이 둘이었다.** ① `mongodb_up` 0 (**04:58:45~05:03:45, 5분**)
  ② chat `spec_replicas` 0 (**05:05:15~05:15:15, 불가용 10분** = CH-2). **6분 간격 연속.**
  에이전트는 섞지 않고 **근본(Mongo) → 확대(컨슈머 부재 9분) → 증폭(DLQ도 같은 Mongo 의존)** 으로
  층을 세웠고 **앵커 여섯 요건을 전부 충족**했다. **CH-2 단독보다 어려운 문항이었고 통과했다.**

  **③ 🔴 채점자 정정 — "채점 불가(유형 F)"로 판정했다가 같은 날 뒤집었다.**
  근거로 든 *"이상 트레이스가 존재하지 않는다"* 는 앵커의 **산문**이지 채점 문언이 아니었다.
  실제 요건(*"S = lag 상승 구간 **또는** WS 급락 구간"* 택일)은 충족돼 있었다.
  **앵커에 없는 잣대를 끌어온 것이라 그 판정이 §8상 무효였다.**
  더 근본적으로 — **운영에서 장애는 겹친다. 겹친 창을 못 재는 자가 문제지 겹친 채록이 문제가 아니다.**
  → **유형 F 철회.** 대신 둘을 남겼다: **채점자 규율("앵커의 산문과 채점 문언을 구별한다")** 과
  **회차 조건 항목("창 안 원인 개수" 필수 기록 — 단독 창과 복합 창을 N=2로 묶지 않는다)**.

  **④ 회차 2 대기열 예측 적중** — *"CH-2 → B-3로 lag 추가 → 채점 성립"*. lag 곡선이 만점의
  핵심 근거가 됐다. `websocket_active_users`는 전 구간 0이라 못 썼으나 **앵커가 인정한
  메트릭 고고학**(파드 시계열 소멸·출현 교차)으로 대체됐다.

  **④ 에이전트의 근거가 정교했다** — *"같은 호스트 IP의 redis:6379는 정상(0.6ms) → 호스트/네트워크
  장애가 아닌 **mongod 프로세스 단독 다운**"*, *"`up{job=mongodb}`(exporter)는 1 → mongod만 죽었고,
  이는 후보 1과 배치되는 게 아니라 **'호스트 전체 장애' 해석을 반증**하는 값"*,
  30.01초 반복을 `serverSelectionTimeout` 상수와 대조.

  **⑤ 파드 소멸 원인 오판이 네 회차 연속.** chat 소멸을 probe 실패로 추정했는데 05:05:15는
  `scale 0`이다(04:59:15 이탈은 probe가 맞아 **절반만 틀렸다**). AU-2와 같은 계열이고
  같은 두 메트릭이 수집 목록에 없다. **에이전트는 이번에도 그 쿼리를 조치로 요청했다.**

  **⑥ 신규 도구 결함 — 스윕이 깨진 행을 반환한다.** 후보 첫 행이 `durationMs = 2,889,849,755`
  (약 33일) · `startTimeUnixNano` → 1970. **Tempo `/api/search`가 그대로 내려주는 값**임을
  실측 확인(딥 페치는 28 spans·57.75ms로 멀쩡). 이번엔 해가 없었으나 **B-9으로 후보를 N건
  모으면 정렬 기준이 필요해지므로 그 전에 방어해야 한다**(round-3 B-15 신설).

  **⑦ 계측**: 컨텍스트 **256,131자**·out **17,822**·**$2.3392**로 전부 최고 경신.
  **자연어 6회가 모두 직전 최고를 갈아치웠다.** 로그가 처음으로 큰 비중(errwarn 45.7KB) —
  결함 ⑤(이중 적재) 수정의 비용 효과가 이 문항에서 가장 크다.
  `C` = 26,576으로 **4회 연속 동일**.

- **2026-07-29 (AU-4 회차 2 — 자연어 진입 · **88점** · **실패가 항목 단위 N=2로 재현**)**:
  주입 `scale 0` **11분 유지**(02:44:40~02:55:40Z · available 13분), 조사 04:02:37Z 스윕 →
  04:05:29Z 리포트. "최근 **2시간** 안에 피드에 작성자 이름이 이상하게 나온다는 제보가 있다".
  [au-4/round-2.md](au-4/round-2.md) · [채점 대장](scoring/README.md#au-4-회차-2--88--100--자연어-진입--루브릭-v3--실패가-n2로-재현-81-평균-제외)

  **① 88/100** (원인 30 · 근거 25 · 탐색 15 · 영향 10 · 오귀인 3 · 조치 5). 판정 1건(영향, 반대 시 83).
  창 표현만 "1시간 → 2시간"으로 열었다 — 주입이 조사보다 1시간 4분 앞서 있었다(문안 템플릿 `<시각>` 자리).

  **② 🔴 이 프로젝트 최초의 항목 단위 재현.** **회차 1도 회차 2도 원인 30**이고 부분점 문언까지
  같다(*"auth 다운 확정, **캐시 만료라는 발현 조건** 미언급"*). 그사이 **입력 모델(traceId → 자연어)과
  출력 분량(2,980 → 7,982, 2.7배)이 모두 바뀌었는데도** 같은 실패가 났다.
  **회차 1의 진단(*"간결본 누락 — 분량이면 풀린다"*)이 반증됐고, 남은 레버는 프롬프트(변경군 C)뿐이다.**
  **§8.1의 N≥2가 재려던 것이 "점수 평균"이 아니라 "실패의 재현성"임을 보여준 첫 사례**이고,
  **총점이 비교 불가여도 항목은 비교된다**는 것도 함께 확인됐다.

  **③ ⚠️ 총점 75 → 88을 개선으로 인용하지 않는다.** 자가 4항목 → 6항목이고, 특히 **2) 근거는
  앵커 요건이 3종 → 2종으로 줄었다**(익명 전환 제거=유형 E · baseline 대조를 탐색으로 이동).

  **④ ✅ round-2 대기열의 예측이 문자 그대로 적중했다** — *"fallback 로그는 `log.error`라
  `errorWarnQuery`로 도달 가능. **A-2 없이도 보인다.** A-2의 값어치는 도달 가능성이 아니라
  **귀속 정확도**"*. 로그는 도달했고 `traceId=NONE`도 그대로인데, 에이전트가 **밀리초 시각
  상관**으로 귀속을 스스로 복원했다.

  **⑤ 🔴 결함 ④가 네 번째 문항에서 재현 — 이번엔 원인 항목을 막았고, traceId를 골랐는데도 막혔다.**
  채점자 실측: baseline(02:43:24) `http get` **446.44ms 성공**(36 span) ↔ symptom(02:54:38)
  **25.73ms `Connection refused`**(28 span). **둘 다 Redis GET ×4 뒤 auth 호출** = 캐시는 주입 전부터
  만료 상태였고, baseline이 있으면 "**auth 다운 + 캐시 만료**의 결합"이 직접 논증된다.
  **수정안에 "창 안 후보 트레이스를 N건까지 수집"을 추가해야 한다** — "창+서비스"로는 안 풀린다.

  **⑥ 탐색은 증상 문안이 가리키는 쪽을 따라간다.** AU-2("로그인" = 상류) → auth 하나,
  AU-4("작성자 이름" = 하류) → **content + auth 둘 다**. **질문에 없는 쪽은 스스로 안 본다** —
  C-1의 근거가 하나 더 늘었다.

  **⑦ 창 크기가 비용을 지배한다.** 좁힌 창 25분 → 컨텍스트 **203,219자**(AU-2의 1.8배) ·
  **$1.9451**로 최고 경신. **자연어 5회가 전부 직전 최고를 갈아치웠다.**
  `C` = 26,576으로 **3회 연속 동일**(01:07Z·01:42Z·04:05Z) — 진동은 시간 단위이지 3시간 안에선 안정.

- **2026-07-29 (AU-2 회차 2 — 자연어 진입 · **76점** · **결함 ④ 예측이 처음으로 적중**)**:
  주입 `scale --replicas=0` **2회**(01:29:30~01:32:30 · 01:33:30~01:36:30, 각 약 3분),
  조사 01:38:33Z 스윕 → 01:42:10Z 리포트. "최근 1시간 안에 로그인이 안 된다는 문의가 몰렸다".
  [au-2/round-2.md](au-2/round-2.md) · [채점 대장](scoring/README.md#au-2-회차-2--76--100--자연어-진입--루브릭-v3--결함--예측-적중-81-평균-제외-n1)

  **① 76/100 — AU-2 최초의 실제 점수** (원인 30 · 근거 18 · **탐색 15** · 영향 5 · 오귀인 5 · 조치 3).
  회차 1은 앵커 부적합으로 점수가 없어 **델타는 성립하지 않는다.** 채점자 판정 **0건**.

  **② 🔴 AP-2 회차 2의 결함 ④ 예측이 적중했다.** 그 회차는 *"CH-2 · **AU-2** · AP-2 세 문항이
  전부 이 경로를 탄다"* 고 못박았다. **감점 24점이 전부 content 축**이고, 그 정답 신호는
  **Tempo에 멀쩡히 있었다** — 장애 한복판(01:29:57Z) 피드 읽기 `6a6957956058…`가
  **27 span 전부 content-service · Redis `GET` ×4 · auth 호출 span 0건 · 200 · 55ms**.
  `{ name =~ "http get /user.*" }`도 창 전체 **0건**. 앵커 ⓑ("user-service 호출 span 없음")가
  **문언 그대로 존재**했는데, traceId 미선정 → Tempo 통째 skip으로 **정상 트레이스까지 버려졌다.**
  **AP-1·AP-2 예측과 다른 점: 이번엔 신호의 존재를 먼저 확인했다.** 단 *"span 없음 → 캐시 히트"* 를
  에이전트가 연결하는지는 **미측정**이고, 그것이 수정 후 재조사의 시험 내용이다.

  **③ 탐색 15/15 — 메트릭 채널이 처음으로 시험받고 통과했다.** rubric-v3 §3이 AU-2를
  "에러로 안 잡힘 — 메트릭" 문항으로 분류한 근거가 실증됐다. 선정 이유 6줄 중 **4줄이 `up` 시계열**,
  **traceId를 지어내지 않았고**(AP-2 회차 2에 이어 두 번째), 창 안의 **AP-3 주입 에러 트레이스를
  자력 기각**했다. B(대조군)도 *"chat·content의 up은 전 구간 연속"* 으로 앵커 문언 그대로.
  **AP-2 회차 2에서 채점자 판정으로 처리했던 논점이 이번엔 앵커 문언만으로 결정됐다.**

  **④ 🔴 5) 오귀인 5/5인데 원인을 틀렸다 — 항목 설계 한계.** 에이전트는 auth 부재를
  **"재시작 루프 → OOMKill/liveness probe"** 로 봤고, 채점자가 `restarts_total=0` ·
  `spec_replicas 1→0→1→0→1`로 **반증**했다(실제는 의도적 scale 0/1). 그런데 AU-2 앵커의
  오귀인 요건이 *content·ingress*만 보므로 **감점 사유가 없다.** 앵커에 없는 잣대로 깎으면
  §8상 무효라 그대로 5점을 줬고, **개정 후보로 등재**했다. **"맞는 계층에서 틀린 메커니즘"은
  C7(정상 문항)로도 안 잡히는 별개 실패 모드다.**

  **⑤ 세 번째로 반복되는 패턴 — 에이전트는 다음 수를 알고 도구가 실행해 주지 않는다.**
  조치 3이 `kube_pod_container_status_restarts_total` · `kube_deployment_spec_replicas`를
  정확히 지목했고 **채점자가 그대로 실행해 답을 얻었다**(AP-2 조치 1의 재현).
  두 메트릭 다 데이터소스엔 있고 **수집기 쿼리 목록엔 없다.**

  **⑥ ⚠️ 주입이 2회였다(사양은 1회) — 그것이 오귀인을 유도했다.** pod가 3개 찍혀
  "재시작 루프"로 읽혔다. 1회였다면 후보 2가 안 나왔을 개연성이 크다. **다음 회차는 1회 고정.**
  덤: **B-4를 운으로 피했다** — 마지막 복구(01:36:30)가 조사(01:38:33)에 2분밖에 안 떨어져
  창에 담겼다. 1회 주입으로 돌아가면 회차 1의 복구 오판이 다시 걸린다.

  **⑦ 계측**: `C` = **26,576**으로 AP-3 회차 2와 **동일**(35분 간격) — 진동은 시간 단위이지
  분 단위가 아니다. 분석 `out 11,775`로 **지금까지 최대**(장황본 표본인데 76). 비용 **$1.3892**로
  최고치 경신 — **자연어 4회가 모두 직전 최고를 갈아치웠다**($1.1146 → $1.1353 → $1.3628 → $1.3892).

- **2026-07-29 (AP-3 회차 2 — 자연어 진입 · **100점** · `C` 진동 확정 · 결함 ⑤ 신규)**:
  주입 **00:56:32.321Z**(KST 09:56:32) 1건, 조사 01:03:43Z 스윕 → 01:07:21Z 리포트.
  traceId 없이 자연어 한 줄("최근 1시간 안에 피드 작성이 실패했다는 제보가 있다")로 완주.
  [ap-3/round-2.md](ap-3/round-2.md) · [채점 대장](scoring/README.md#ap-3-회차-2--100--100--자연어-진입--루브릭-v3-81-평균-제외-n1)

  **① 전 항목 만점 100/100** (원인 40 · 근거 25 · 탐색 15 · 영향 10 · 오귀인 5 · 조치 5).
  탐색이 **주입 트레이스를 자력으로 집었고**(`6a694fc0…`, Top-1 적중), 분석이
  `Duplicate entry '151-174' for key 'tb_feed_hashtags.uk_feed_hashtag'`를 span error·Loki 양쪽에서
  축자 인용했다. **동시성 오귀인을 구조적으로 반증**했고(`151`·`174` 둘 다 이 트랜잭션이 생성),
  조치로 "정규화 후 `distinct`" + `DataIntegrityViolation`→409를 냈다 — 앵커 만점 문언 그대로.
  **채점자 판정 1건**(4) 영향, 반대 판정 시 95).

  **② 🔴 회차 1(100)과 나란히 두지 않는다.** 자가 다르다 — 회차 1은 v1 4항목, 회차 2는 v3
  6항목이고 **탐색 15 + 영향 10(25점)이 회차 2에만 있다.** 숫자가 같아 "변화 없음"으로 읽히는
  것이 가장 위험하다. **§8.1의 사각지대도 드러났다** — 같은 문항 2회인데 자와 입력 모델이
  함께 바뀌어 N=2가 성립하지 않는다. §8.1은 "같은 (문항 × 자 × 입력 모델) N회"로 읽어야 한다.

  **③ 🔴 오버헤드 `C`가 되돌아왔다 — 상수 아님의 결정적 증거.**
  07-28 **26,626** → 07-29 00:1x~00:4xZ **21,247/21,567/21,331** → 이 회차 **26,576**(앱 프로브)
  · **26,603**(독립 프로브 4회, 편차 0, Δ27). **뒤 두 구간은 CLI 2.1.220으로 동일 버전**이다.
  클라이언트가 안 바뀌었는데 +24.7% 움직였다가 복귀 = **단조 드리프트가 아니라 진동**이고
  원인은 클라이언트 밖(서버가 내려주는 툴 정의)에 있다. 그리고 ② 경로(`chars × 0.524`)가
  **그날 프로브 값을 매번 지지한다**(이번 +3.9%/+5.0%) — "회차마다 재라"는 규칙의 독립 검증.
  부수: 같은 프로브를 레포 cwd에서 돌리면 32,747(**+6,144, +23%**) = **CLAUDE.md·`.claude/`가
  실제 로드되는 분량**. 샌드박스가 막고 있는 오염이 처음으로 수치화됐다.

  **④ 결함 ④ 재현 · ⑤ 신규 · ③ 미발현.**
  ④ 스윕이 `{status=error}`만 검색해 **정상 피드 생성 200이 앞뒤로 2건 실재했는데**
  (`6a694fb4…` 00:56:20 · `6a694ffd…` 00:57:33, 채점자 실측) 도달 불가 — AP-2에선 traceId를
  못 골랐고 여기선 **대조군을 못 만들었다**. 증상만 다르고 뿌리는 하나.
  ⑤ **신규** — `loki-error-warn`과 `loki-trace-id`가 **같은 4줄**을 각각 반환하는데 병합 시
  dedup이 없어 컨텍스트에 8줄로 실린다(≈5%). 에이전트는 `totalEntriesReturned: 4`로 고유
  줄 수를 정확히 셌다.
  ③ 라인 필터 결함은 **이 문항에선 미발현** — 예외 원문이 헤더 줄 자체에 있다(AP-1과 동일).

  **⑤ 앵커 구조 결함이 두 회차 연속 판정을 강요했다.** AP-2 회차 2는 3) 탐색에서, 이 회차는
  4) 영향에서. **3구간(만점/중간/0)뿐인 항목 중 배점이 큰 것**(탐색 15 · 영향 10)에 중간 구간을
  신설해야 한다 — 특정 항목이 아니라 anchors-v2.md 전반의 문제다.

  **⑥ AP-3의 델타 예측만 반증되지 않았다(3문항 중 처음).** 회차 1의 *"이미 만점이라 올릴
  자리가 없다"* 는 인과 주장이 아니라 **산술 관찰**이었기 때문이다. 단 v3에서 천장이 25점
  높아졌으므로 **v1 자에 한해** 유효했던 것으로 기록한다.

  **⑦ 운영 메모**: `CLAUDE_CLI_PATH`가 비어 있어 `ProcessBuilder`가 `claude`를 못 찾고
  조사가 500으로 죽었다(Windows는 `claude.cmd`). `.env`에 절대경로를 박아 해결.

- **2026-07-29 (AP-2 회차 2 — 자연어 진입 · 53점 · **예측 +25가 나오지 않았다**)**:
  주입 07-28 15:35:35~15:36:36Z(61초), 조사 15:43:16Z 시작. traceId 없이 자연어 한 줄
  ("최근 1시간 안에 팔로우 목록이 안 열린다는 제보가 있다")로 완주.
  [ap-2/round-2.md](ap-2/round-2.md) · [채점 대장](scoring/README.md#ap-2-회차-2--53--100--자연어-진입--루브릭-v3-첫-적용-81-평균-제외-n1)

  **① 🔴 v0.1 델타 예측 +25가 반증됐다.** 회차 1은 *"+25가 전량 Loki 셀렉터 + `logfmt`
  두 결함에 걸려 있다"* 고 못박았다. **두 결함은 실제로 고쳐졌고 로그도 4줄 들어왔는데**,
  같은 자(v1)로 재면 **60 → 50 (−10)**, v2로는 **65 → 60 (−5)**. 두 델타가 같은 방향·비슷한
  크기라 앵커 개정의 산물이 아니다. **AP-1 회차 2에 이어 델타 예측이 두 회차 연속 빗나갔다.**

  **② 원인은 결함이 둘이 아니라 넷이었다 — 셋째·넷째를 실측으로 잡았다.**
  ③ **라인 필터가 스택트레이스를 배제한다** — Logback이 스택을 줄마다 별개 Loki 엔트리로
  보내는데 그 줄엔 `ERROR`/`WARN`이 없다. `|~ "ERROR|WARN" |= "NullPointer"` → **0줄**(채점자
  실측). 헤더 줄엔 `handleAllException` 메서드명뿐이라 **"로그가 들어온다"와 "예외를 읽을 수
  있다"가 갈렸다** — AP-1에선 예외가 헤더 줄에 있어 안 드러났다.
  ④ **traceId 미선정 시 수집기가 Tempo를 통째로 건너뛴다** — 트레이스는 **멀쩡히 있었다**
  (`http get /user/{userid}/following`, 4 span, `status=UNSET`). 스윕은 `{status=error}`만
  검색하고 심층 수집은 traceId가 없어 skip. **에이전트가 "판단 불가"로 꼽은 엔드포인트 정체가
  거기 있었다.** **CH-2·AU-2도 같은 경로를 탄다.**

  **③ 그러나 −10을 변경군 B 델타로 인용하면 안 된다.** 프로토콜은 같은 입력 모델로 두 번
  재는 것을 전제했는데 입력 모델이 traceId → 자연어로 함께 바뀌었다(= 결함 ④로 트레이스 증거
  전량 상실). **깨끗한 델타는 주입 없이 지금 잴 수 있다** — 회차 1 traceId
  `6a680b56067d9e2387043740be2cb115`로 `POST /investigate` 1회. **다음 순번.**

  **④ 탐색은 AP-1 회차 2보다 어려웠고 더 잘했다.** 같은 1시간 창에 **AP-1 회차 2의 주입
  (15:05Z 에러 트레이스)이 오답으로 남아 있었는데** "쓰기 경로라 증상과 불일치"로 기각하고,
  **traceId를 지어내지 않았다**(이 문항은 에러 트레이스가 존재하지 않는다).
  strategy.md §4.5의 정정(*후단은 traceId가 아니라 `(창+서비스+신호 종류)`*)의 첫 실전 확인.

  **⑤ 오귀인이 12회 만에 처음 만점을 놓쳤다(3/5) — 문항이 아니라 자를 바꿔서.**
  v3가 넣은 **"3: 후보로 올리되 자력 반증"** 구간이 즉시 변별했다. AP-2 회차 1은 **같은 행동**
  으로 v1에서 20/20 만점이었다. **rubric-v3의 예측 효과가 실측으로 확인된 첫 사례**
  (단 F10 "없는 원인 날조"는 여전히 C7 정상 문항으로만 시험된다).

  **⑥ 앵커 결함 2건 — 다음 회차부터 적용.** ⓐ **조치 갈래 ㄴ이 재배치에서 소실됐다.**
  AP-2 v2가 이중 감점을 막으려 만든 문언인데 anchors-v2.md로 옮겨지며 사라졌고, **이 회차
  조치 0점이 그 산물**이다 — 에이전트는 갈래 ㄴ 만점짜리 행동을 실제로 했다(조치 1 =
  "레벨 무관 전체 로그 조회", 채점자가 실행해 NPE 원문 확인). STATUS 5.5가 재배치를
  "문언을 바꾸지 않고 위치만 옮기는 작업"으로 규정했는데 문언이 하나 사라진 것이다.
  ⓑ **탐색에 중간 구간이 없다**(15/8/0) — **채점자 판정 1건**을 써야 했다(반대 판정 시 46).

  **⑦ 계측: AP-1 회차 2가 남긴 공백이 메워졌다.** 탐색 단계도 `contextChars`가 기록돼
  두 경로 추정이 다 나온다(격차 +3.9% / +6.3%, AP-1 회차 2의 +7.0%보다 좁다).
  **`C`는 조사 당일 프로브 3회로 실측** = **21,331**(문서 §2.1의 21,247/21,567과 +84).
  컨텍스트 **≈ 68,491 ▓추정** · **$1.1353 · E2E 175.0s(11회 중 최고 비용)**.
  **모델 라벨 정상** — AP-1 회차 2의 `reportedModel()` 파싱 버그 수정이 실전 확인됐다.
  분석 out 8,773 → 8,423(**−4%**)으로 **분량 변수는 이번에 통제됐다** — 델타 −10을 분량으로
  설명할 수 없다.

- **2026-07-29 (AP-1 회차 2 — 자연어 진입 첫 실전 조사 · 90점 · 계측 결함 1건)**:
  탐색 단계를 **실제 Grafana Cloud에 처음 돌렸다.** traceId 없이 자연어 한 줄
  ("최근 1시간 안에 댓글 작성이 실패했다는 제보가 있다")로 완주.
  [ap-1/round-2.md](ap-1/round-2.md) · [채점 대장](scoring/README.md#ap-1-회차-2--90--100--자연어-진입-첫-조사-81-평균-제외)

  **① 탐색이 정답 트레이스를 자력으로 찾았다.** 1시간 창 → `15:03~15:10:53Z`(8분)·
  `content-service`·traceId `6a68c522…` — **실제 주입 트레이스와 일치.** 선정 근거로
  로그 버킷 겹침·auth/chat span 부재·인프라 정상을 인용했다(**부재를 근거로 쓴 사례가
  탐색 단계에서도 나왔다**). 단 이 문항은 **1시간 창에 에러 트레이스가 1건뿐**이라 탐색
  난도 최저다 — CH-2·AU-2처럼 트레이스가 생성되지 않는 문항이 진짜 시험이다.

  **② 채점 90 (근본 30 · 근거 30 · 오귀 20 · 조치 10), 앵커 v2 · 채점자 판정 0건.**
  회차 1은 판정 2건이 필요했는데 이번엔 앵커 문언 그대로 채점됐다 — **"판정 개입 없이 같은
  결과가 나온다"던 §8.2 개정의 첫 실증.** 감점 10은 앱/DB 책임 분리 미완 하나
  (후보3 "스키마 부족"·조치8 "TEXT 확장"으로 갈래를 열어둠).

  **③ 🔴 예측 두 개가 반증됐다.** 회차 1 델타 추적표의 *"개선 여력 +25는 전량 Phase 4
  (코드 인지)"* 와 anchors.md의 *"AP-1은 대조군이라 그대로여야 AP-2 로그 델타가 분리된다"* 가
  **둘 다 틀렸다** — 코드 인지 없이 +15가 났고 그것도 "안 오른다"던 근본·조치에서 났다.
  **AP-2 델타의 대조군 설계를 다시 짜야 한다.**

  **④ 그러나 이 90을 §8.1 평균에 넣지 않는다.** 입력 모델·Loki 수집(0건→9줄)·
  **출력 분량(2,719→7,159, 2.6배)**·앵커(v1→v2)가 동시에 바뀌었다. AU-4가 이미 *같은 v0의
  두 실행이 분량만으로 25점* 벌어진 사례를 남겼으므로 가장 그럴듯한 설명은 분량이다.
  summary.md를 **Ⅰ(입력 모델 v0) / Ⅱ(자연어+탐색)** 두 표로 갈랐다.

  **⑤ 계측 결함 — 모델 라벨 오기록(수정 완료).** 리포트에 `claude-haiku-4-5`로 찍혔으나
  **실제 본답변은 `claude-opus-5`가 맞다.** CLI 응답 `modelUsage`에 **모델이 둘** 들어오는데
  (본답변 + CLI 보조 작업용 소형 모델) `reportedModel()`이 `fieldNames().next()`로 첫 키를
  집었다. 요청 모델이 목록에 있으면 그것을 쓰도록 고쳤다. **하마터면 "모델 고정이 깨졌다"고
  오진해 회차를 폐기할 뻔했다** — 앵커 감사의 규칙(*반증 근거도 실측이어야 한다*)이 계측에도
  적용된 사례. 토큰·비용은 무관하다(최상위 `usage`가 요청 모델 실측).
  **findings 등재 필요(NF 신규).**

  **⑥ 토큰 기록 기준을 CLAUDE.md에 고정했다** — [round-1-input-tokens.md](round-1-input-tokens.md)를
  먼저 읽을 것. 총 `in` 인용 금지 · 오버헤드 26,626 분리 · `contextTokens` 없으면 두 경로
  (`in−26,626`, `chars×0.524`)로 추정하고 둘 다 적기 · 비용은 API 환산 추정치로 표기.
  이번 회차는 `ANTHROPIC_API_KEY` 공백으로 **실측이 없어 추정(≈60,844 tok)** 이고,
  **탐색 단계는 `contextChars` 자체를 기록하지 않아** 검산 경로가 없다(계측 공백).
  `RCA_CLAUDE_CLI_MODEL=claude-opus-5`를 `.env`·`.env.example`에 명시로 고정.

- **2026-07-28 (탐색 단계 구현 · 리포트에 관측 증거 수록)**: Phase 4.5를 문서에서 코드로 옮겼다.
  **입력이 traceId에서 자연어로 넓어졌다** — `POST /diagnose {"question":"어젯밤에 댓글 알림이 안 왔어요"}`.

  **① 파이프라인이 4단계가 됐다.** 창 파싱(결정적) → 스윕(코드가 고정 목록) → 선정(LLM) →
  심층 수집·분석(**기존 v0 경로 그대로**). 신설 `triage` 패키지 6개 + `collector/Scope`.
  - **`Scope(window, services, traceId?)`가 탐색↔분석 인터페이스다.** traceId는 **필수가 아니다** —
    strategy.md §4.5의 정정(CH-2·AU-2는 이상 트레이스가 아예 생성되지 않는다)을 타입으로 못박았다.
    `Collector.collect(Scope)`가 traceId 없으면 Tempo 조회와 traceId 로그 쿼리를 건너뛰고,
    **건너뛴 사실을 실패 목록에 남겨** 모델이 부재를 근거로 쓰게 한다.
  - **`TimeExpressionParser`는 LLM이 아니라 코드다.** 창을 LLM에 맡기면 같은 질문이 회차마다
    다른 창을 만들어 재현이 깨지고, 탐색 채점의 "T 시간창" 항목을 분석과 분리해 잴 수 없다.
    "어젯밤"→어제 18:00~오늘 06:00(KST) 등 결정적 파싱 + 명시적 from/to 우선 + 상한 클램프.
  - **스윕은 집계 쿼리만 쓴다** — Tempo `/api/search`(신설) · Loki `count_over_time` ·
    Mimir `up`/`mongodb_up`/`kafka_brokers`/`lag`/`ws_users`. 창이 12시간이어도 응답이 작다.
    3채널인 근거는 앵커 감사(Tempo 에러 검색만으로는 12문항 중 6문항 미도달).
  - **선정 실패가 조사를 멈추지 않는다.** 계획 JSON을 못 읽으면 스윕 창 전체로 분석하고
    그 사실을 `notes`에 남긴다 — 탐색이 틀렸을 때도 *실제로 고른 범위*로 분석을 채점해야 한다.

  **② 리포트에 관측값 원문을 실었다** (`Evidence` + `EvidenceExtractor`).
  **모델의 서술만 남기면 나중에 그 서술이 맞았는지 확인할 수 없다** — 회고와 채점은 원문 위에서만
  성립한다. duration 상위 span(시각 포함) · **로그 원문** · 메트릭 시계열의 min/max/last와
  **값이 0이던 구간** · `reports/raw/` 접두사. 탐색 절에는 **고르지 않은 트레이스 후보까지** 남긴다
  ("다른 걸 골랐어야 했나"를 판단하려면 그때 무엇이 보였는지가 있어야 한다).

  **③ baseline은 무효가 되지 않는다.** `POST /investigate`는 그대로고, 내부적으로
  `Scope.ofTrace(traceId)`로 위임될 뿐 **분석 코드가 같다.** `RcaServiceFlowTest` 무수정 통과로 확인.
  **테스트 32개(신규 18개) 전부 통과.**

  **④ 아직 점수가 없다.** 구현·단위 검증까지이고 **실제 Grafana Cloud 실행도, 채점도 0회**다.
  다음은 과거 9회의 ground truth(`timeline.log` 주입 시각 · 회차 리포트 traceId · `answer.md`)로
  **주입 없이 회고 평가** — "이 시각 무렵 이상 찾아줘"에 실제 구간이 뜨는지가 재현율이고,
  정상 구간을 섞으면 **C7 오탐 대조군까지 같은 트랙에서 해결**된다.
  선행 확인은 그대로 남아 있다: **Grafana Cloud 보존기간에 07-26~28 데이터가 있는가.**

  **⑤ 참고한 사례** — Grafana Sift(체크 카탈로그를 창+네임스페이스로 스코프) ·
  Grafana Assistant 하네스(가설 해소 게이트 · 증거 원문 인용 · **결과로 채점, 경로는 안 봄** ·
  **단순 구조가 멀티에이전트를 이겼다**) · Netflix Edgar(트레이스 등뼈 + analyzer 모듈) ·
  우아한형제들(복구가 원인 규명보다 우선 → 조치를 완화/제거/예방 3층으로).
  **아직 안 넣은 것**: 가설 해소 게이트, 조치 3층 강제, `service.version` 태깅(배포 원인 가설).

- **2026-07-28 (앵커 v2 신설 — 입력 모델을 traceId에서 자연어로)**: 회차 2부터 에이전트가
  **탐색까지 하도록** 입력을 바꾸기로 하고, 그에 맞춰 채점을 새로 짰다. **기존
  `answer.md`·[anchors.md](scoring/anchors.md)는 한 글자도 고치지 않았다** — 회차 1까지의
  점수가 그 자로 매겨졌고 §8.2가 구버전 삭제를 금한다.
  - **[루브릭 v3 개정](scoring/rubric-v3.md)** — §0(입력 모델 v0→v1) 신설, **§3 탐색 15점을
    실전 기준으로 확정**(T 시간창 · S 대상 선정 · B 대조군), §5 적중률을 end-to-end로,
    §9 이행계획 갱신. **질문 문안 규칙**을 못박았다 — 컴포넌트명·에러코드·영향 판정 결과 금지.
  - **[앵커 v2](../../toy-content/docs/chaos/anchors-v2.md) 12문항** — 6항목 재배치 + 문항별
    질문 문안 박제 + 탐색 요건. **실제 변경은 셋뿐**이고 나머지 만점 요건은 문언 그대로 옮겼다:
    ①영향 판정 분리 ②baseline·다건 대조 **복원(단 `근거`가 아니라 `탐색`으로)** ③질문에 적힌
    증상 제거(AU-4 ⓒ).
  - **②의 근거**: v0에서 *"입력이 traceId 1개라 구조적 달성 불가"*를 사유로 뺀 요건들
    (AU-4 baseline 대조 · IN-2 baseline producer span · AU-3 401 전역)이 있었는데,
    **자연어 입력에서 그 사유가 소멸**했다. 위치를 근거가 아니라 탐색으로 둔 것은
    *가져오는 것*(탐색)과 *대조하는 것*(근거)이 다른 능력이기 때문 — 이중 감점 방지.
  - **부수 발견 두 건이 Phase 4.5 설계를 바꾼다.**
    ① **Tempo 에러 검색만으로는 12문항 중 6문항을 못 찾는다** — CH-2·CH-3·AU-2·AU-3·AP-2·IN-3.
    그중 **CH-2·AU-2·AP-2는 정답 신호가 트레이스에 아예 없다**(컨슈머 사망·ingress 차단·`exception=none`).
    ② 따라서 **[strategy.md](strategy.md) 초안의 "대표 traceId를 뽑아 넘김"이 반증됐다** —
    후단은 `(시간창 + 대상 서비스 + 신호 종류)`를 받아야 한다. strategy.md에 정정 반영.
  - **질문 문안 설계에서 걸린 것**: AP-1↔AP-3, CH-1↔IN-2, AU-4↔IN-1③이 **같은 사용자 증상**을
    갖는다. 문안을 구별해 쓰면 답을 알려주는 셈이라 **의도적으로 같게 뒀다** — 예외 원문·
    영향 판정으로만 갈린다. 그리고 **CH-1의 제보("알림이 안 왔다")는 틀린 제보**다(실제로는
    DLQ 경유 10분 내 도착) — 제보 검증이 4) 영향 판정이 재는 능력이다.
  - **회차 2의 차단 요인이 앵커에서 진입점으로 옮겨갔다** — 채점 준비는 끝났고
    `RcaController`의 `@NotBlank traceId`가 남았다.

- **2026-07-28 (루브릭 v3 제정 · Phase 4.5 신설 · AU-3 하네스 개편)**:
  **"자연어만 받고 장애를 찾아야 진짜 RCA 에이전트"**라는 지적에서 시작해 측정 범위 자체를 다시 짰다.

  **① 제품 경계를 확인했다 — v0는 RCA 에이전트가 아니라 RCA 설명기다.**
  `RcaController`가 `@NotBlank traceId`를 요구하고, 더 근본적으로 **Loki·Mimir 조회 시간창이
  트레이스에서 파생된다**(`Collector`: `TimeWindow.fromTrace` → `queryRange`). traceId 없이는
  쿼리 자체를 못 만든다. 그런데 **실무에서 장애가 나면 traceId를 아는 사람이 없다** — 사람이
  시간대·에러 조건으로 뒤져 찾아내고, **그 뒤지는 행위가 원인 분석의 절반**이다.
  → **[strategy.md Phase 4.5](strategy.md) 신설.** 붙일 건 앞단뿐이고 클라이언트는 이미 다 있다.
  **정정**: 처음엔 "입력 모델을 바꾸면 기존 baseline이 무효"라고 적었으나 **틀렸다** —
  탐색은 traceId 입력을 *대체*가 아니라 *추가*하므로 기존 진입점과 분석 측정은 그대로 산다.
  **그리고 탐색은 주입 없이 평가된다** — 과거 9회의 정답(주입 시각·traceId·원인)이 이미 있어
  회고 평가가 가능하고, 정상 구간을 섞으면 **C7 오탐 대조군까지 같은 트랙에서 해결**된다.
  선행 확인 필요: **Grafana Cloud 보존기간에 07-26~28 데이터가 남아 있는가.**

  **② [루브릭 v3](scoring/rubric-v3.md) 제정 — 회차 2부터 v1과 병행 채점.**
  v1의 구조적 결함 둘이 실측으로 확인됐다.
  **(a) `근본 원인 40`이 세 가지를 한 칸에** — 원인 지목·메커니즘/귀속·**영향 판정**
  (13문항 전수 확인). **CH-1 회차 2는 감점 20점 전액이 영향 오판**인데 원인은 정확히 맞혔다.
  **(b) `오귀인 20`은 척도가 아니다** — 9회 전부 만점이라 실질 만점이 80점이었다.
  원인은 항목이 아니라 **정상 상황을 한 번도 안 준 문항 세트**에 있다.
  → **원인 40 / 근거 25 / 탐색 15(신설) / 영향 10(분리) / 오귀인 5 / 조치 5.**
  원칙은 **"어떻게든 원인을 맞히는 것이 첫째, 경로는 묻지 않는다."**
  종합 지표는 점수가 아니라 **원인 적중률(Top-1)** — §0 북극성과 같다.
  **[rubric-v2](scoring/rubric-v2.md)(소항목 14개)는 보류** — 좋은 설계지만 현재 병목(N=1)을
  채점 비용으로 악화시킨다. 단 **v2의 `A4 영향 판정` 분리 판단은 승계**했다(진단은 v2의 것).

  **③ AU-3 하네스 개편 — 트리거가 없었다.** `measure_AU_3`가 인증 API를 **1회** 부르는 게
  전부여서 ⓐ 401 rate 급증이 안 생기고 ⓑ traceId를 건질 수 없었다(응답에 traceId가 없고
  401은 `status=error`가 아니라 Tempo 기본 검색에도 안 걸린다).
  → `trigger_AU_3` 신설(초당 1회 × 90초) · `inject_AU_3`가 **주입 전 평시 트래픽 60초** 선행
  (수집 창 ±120초 안에 대조군 확보) · **traceparent 직접 주입으로 traceId 사전 확정** ·
  baseline 게이트 2종(전파 채택 여부 · 서명 실패 로그 문구) · rate lookback `[5m]`→`[1m]`.
  **정정**: "fire 중반 traceId를 쓰라"는 틀렸다 — 롤아웃이 길면 warm이 창 밖으로 밀린다.
  `au3_pick`이 `trace_time < warm_end + 120`으로 계산해 고르고, 불가능하면 경고한다(로직 검증 완료).
  부수: **RUNBOOK ④에 v2에서 반증된 서술**("401은 trace로 못 찾는 문항")이 남아 있어 정정 —
  그대로 두면 채록자가 trace를 안 떠 증거가 유실된다.

  **④ [대외용 보고서](scoring/report.md) 작성** — 장애별 상황·채점 항목·결과를 한 문서로.
  §1.1에 **제품 경계를 명시**했다(설명 엔진이지 탐색 엔진이 아님, 지금 안 붙이는 이유).
  §5.2에 "측정 범위가 절반"을 인용 유보 사유로 추가.

- **2026-07-28 (앵커 전수 감사 — 실행 대상 12/12 성립 · AU-1 보류)**: [anchors.md](scoring/anchors.md)에 문항별
  요건표 + 채널 감사를 신설하고, ❌로 묶여 있던 **IN-1·AU-1을 코드 실측으로 재판정해 해제**했다.
  **둘 다 앵커가 아니라 *내 감사가* 틀렸던 경우다.**
  **IN-1**: "세 신호가 정의돼 있지 않다"고 썼으나 **이미 계측돼 있었다** — Lettuce 커맨드 span이
  content·chat 양쪽에 수동 등록(`TracingConfig:29`·toy-chat `ObservabilityConfig:46`)돼 있고,
  `Redis 캐시 조회 실패`가 **두 서비스 동일 문구 `log.error`**(`UserCacheStore:51`·
  `UserInfoCacheService:50`)다. `errorWarnQuery`가 라인 필터라 **같은 실패가 `service_name`만
  다르게 뜨는 것**이 곧 "공통 의존성 하나"의 증거 — traceId 없이 도달한다.
  **AU-1**: `3s 잘림`을 "반증됐다"고 뺐는데, 근거로 든 AU-4 실측(23.5ms refused)은 **auth 다운**
  케이스다. AU-1은 **살아서 느린 것**이라 타임아웃이 정상 전개고, `TIMEOUT = Duration.ofSeconds(3)`은
  실재한다(`ExternalUserApiClient:30`). **다른 장애의 실측을 옮겨 쓴 오류.**
  → **규칙 신설**: 요건을 "도달 불가"로 뺄 때의 **반증 근거도 실측이어야** 한다.
  **진짜 결함은 v1에서 ✅로 통과시켰던 쪽에 있었다 — 유형 E(증상을 근거로 요구) 신설.**
  AU-1의 `작성자 익명 표시`는 관측 신호가 아니라 **질문에 적힌 증상**이라 인용하면 순환논증이다
  (제거). 같은 요건이 **AU-4 v3 ⓒ에도 있으나 회차 1(75)이 그 자로 채점돼 §8.2상 소급 수정 불가** —
  v5로 회차 2부터 적용하고, 회차 1 근거 경로 점수가 **위로 치우쳤다는 한계를 함께 밝힌다**.
  **뺀 요건**: IN-1 스케줄러 skip(cron이 매시 정각인데 수집 창은 ±120초 → 주입 시각의 우연에
  걸림, 유형 B) · AU-1 fallback 로그(도달은 되나 **어느 가설도 죽이지 못해** 변별력 0).
  **IN-1에 WS 프레즌스 경로 추가(④)**: `ClusterPresenceStore`가 **쓰기는 삼키고 읽기는 던진다** —
  쓰기(`:55` `:81` `:115`)는 `log.error`만 남기고, 읽기(`:100` `:132` `:165` `:206`)는 try/catch가
  없어 STOMP 핸들러까지 전파돼 `❌ [STOMP CHATROOM SUBSCRIBE] Error`로 찍힌다. 증상은
  **"접속은 되는데 남들 눈엔 오프라인"**. 여기에 `websocket.active_users`가
  **`SimpUserRegistry`(로컬 세션) 기반이라 Redis 다운에도 유지**되므로(`WebSocketMetricsConfig:30`)
  *"세션은 멀쩡한데 프레즌스만 깨졌다"*가 **chat 앱 자체 장애를 죽인다**.
  **단 필수 요건이 아니라 동치 대안으로 넣었다** — 채점은 "프레즌스를 봤는가"가 아니라
  "Redis 실패가 두 서비스 이상에서 동시에 관측됨을 보였는가"로 한다(§8.2 충분 근거 원칙).
  **AU-1 문항 보류 결정**: 앵커는 v2로 성립하지만 **증상 재현이 노드 부하에 좌우된다** —
  CPU limit 50m가 3초를 넘길지 보장되지 않고, 안 넘으면 타임아웃도 fallback도 없어 증상이
  안 난다. **주입 강도와 증상이 비선형이라 §8.1의 N≥2와 상성이 나쁘다.** 문항 선정의
  결론이므로 **앵커 부적합률에는 넣지 않는다.** 문항은 접어도 **여기서 나온 규칙 둘
  (유형 E · 반증도 실측으로)은 남는다.**
  **부수 발견 — `rate([5m])`의 창 문제**: 수집 창이 트레이스 ±120초(≈4분)라 lookback 5분이
  **창보다 길어** 인접 스텝이 95% 겹치고 곡선이 평평해진다. AU-3 401 rate를 `[1m]`으로 낮췄고,
  근본 대응인 **B-4(수집 창 확대)를 우선순위 상향**했다 — "평시 대비 급증"을 요구하는 앵커
  (AU-3 ⓒ·CH-2 lag)가 **주입이 창보다 먼저 시작되면 구조적으로 달성 불가**다(AU-2 결함 ⑦과 동근).

- **2026-07-28 (측정 계측 개편)**: 포트폴리오용 토큰 지표를 설계하다 **계측 결함 3건**을 찾아
  수정했다. [measurement.md](measurement.md) 신설(측정 기준 고정).
  **① 모델이 고정돼 있지 않았다** — `ProcessBuilder`에 `--model`이 없어 CLI 기본값으로 돌았다.
  회차마다 다른 모델이 채점됐을 수 있고 토크나이저도 달라 토큰 비교까지 무너진다.
  `claude-opus-5`로 고정하고 응답의 `model`·`num_turns`를 리포트에 기록. **`num_turns > 1`이면
  "도구 없는 단일 패스" 전제가 깨진 것**이라 WARN을 남긴다.
  **② 캐시 3분해가 합산에 묻혀 있었다** — `cacheReadTokens`/`cacheCreationTokens`를 `LlmResult`·
  리포트에 병기. cache read는 정가의 1/10이라 같은 `in`도 비용이 열 배 갈린다.
  **③ `chars/4` 추정이 3.8배 과소였다**(16,341 vs 실측 61,528) — 필드 제거하고
  **`coverage.contextTokens`(count_tokens 실측)로 대체**. `metricsBytes`·`promptChars`도 추가해
  컨텍스트의 미분류 11%를 없앴다. 부수: `TokenCounter` 신설(API 키 없으면 −1, 추정치 미생성).
  **폐기한 방법**: `in − C` 뺄셈 — C의 회귀 추정이 95% CI [965, 23,345], 한 점 제거 시 2.6배
  진폭이라 성립하지 않는다. **구성 검증은 C ≈ 30,000을 지지**하며 회귀(12,155)를 기각한다.
  프로브 스크립트 `scripts/cli-probe.ps1` 신설(C 실측 + 캐시 경계 판별).
  **과거 7회의 점수 기록은 그대로 유효하나, 모델 미기록이라 토큰 차트의 before로는 못 쓴다.**

- **2026-07-28**: **AP-2 회차 1 완주 · 채점 불가(앵커 미박제 — 신규 유형 D).**
  주입은 서버에서 실행돼 있었고(01:52:22Z), Tempo에서 팔로우 트레이스 4건을 찾아 확보 —
  `/api/user/1/following` **200(2,928ms)** → **500(137ms)** → `/followers` **500(142ms)** →
  **200(40ms)**. **소요시간은 판별 신호가 아님을 실측으로 확인**(정상이 실패보다 느리기도 하다).
  **§8.2 문제 발견**: AP-2 앵커가 박제돼 있지 않았다 — 로컬 toy-content `answer.md`는 구
  "대용량 업로드" 문항 그대로이고 STATUS도 "draft (실측 후 박제)"였는데 **주입이 먼저 실행됐다.**
  조사 답안을 보기 전에 trace·Loki **실측만으로 앵커 v1을 작성**해 [ap-2/](ap-2/README.md)에
  박제하고(체크리스트 5개 전부 통과), 회차 1은 **실측 회차**로 두고 앵커는 **회차 2부터** 적용.
  블라인드 조사 **$0.4199 · 131.1s · 입력 4 span/5,475B(9회 중 최소)**. 참고 점수 **60**
  (근본 20·근거 20·오귀 20·**조치 0**).
  **핵심 발견 — 정답이 로그에만 있는 첫 문항.** NPE 원문(`... because "this.size" is null`)·
  `FollowCondition.java:25/45` 스택·`handleAllException`이 전부 Loki에만 있고, trace는
  4 span에 `exception=none`이라 **예외 존재 자체를 감춘다**. 게다가 그 로그 레벨이 **WARN**이라
  `| logfmt | level=~` 결함까지 걸린다 — **셀렉터와 logfmt 두 결함을 다 뚫어야 도달**.
  **v0 상한 예측 80이 실측 60으로 반증**됐고, 그 실측이 다시 **앵커 결함 4건**을 드러내
  **v2로 개정**했다(중간 구간 15 신설 · 조치 갈래 ㄴ 신설 · "DB 미진입" 문언 교체 · "지목"
  정의 성문화). **v2 참고 재채점 65**(v1 60) — 개정의 알맹이는 +5가 아니라 **부호가 갈린 것**이다:
  근거 −5(중간 구간이 없어 후하게 줬던 것 교정), 조치 +10(**같은 실패를 근본 원인과 조치에서
  두 번 벌하던 이중 감점 제거**). 두 오류가 우연히 상쇄돼 총점만 비슷해 보였다.
  **v2 개정은 답안을 읽은 뒤 이뤄졌으므로 v2 단독 델타는 근거가 약하다** — 회차 2를
  v1·v2 **두 앵커로 모두 채점**해 교차 확인한다.
  **채점 규칙 신설**: "채점 불가"를 **불가(유형 A·B·C, baseline까지 잃음)**와
  **유보(유형 D, 절대 점수만 잃고 델타는 유효)**로 나눴다 — AP-2 회차 1을 통째로 버리면
  **v0.1 도구 델타를 잴 baseline이 사라져 주입을 한 번 더 해야 했다.**
  **델타 예측 정정: v1 +40 → v2 +25** — 줄어든 이유는 도구가 나아져서가 아니라 **자가
  정확해졌기** 때문이다. "+40"은 인용하지 않는다. AP-3(0)·AP-1(+25, 코드 인지)·AU-4(+25,
  프롬프트)와 레버가 갈리고, **AP-1과는 크기가 같고 레버가 달라 서로의 대조군**이 된다.
  **에이전트 진전**: `totalLinesProcessed: 0`을 "필터링 0건이 아니라 스캔 대상 자체가 0건 =
  파이프라인 공백"으로 진단하고 **그 공백을 이유로 1순위 확신도를 중간으로 낮췄다** —
  AU-2(결함 특정)에서 **확신도 조정 반영**으로 한 단계.
  **부산물**: auth-service **JDBC 미계측** 확인(`secured request` 93.76ms가 완전 블랙박스,
  정상 트레이스도 동일) — 문항 설계의 "DB span 부재를 근거로"를 무효화한 원인.
  **절차 보강 결정**: `chaos.sh <ID> run` 전제에 "앵커 표 비어 있지 않을 것" 게이트 신설
  (사람 체크리스트가 두 번 연속 샜다).
- **2026-07-28**: **AP-3 회차 1 완주 · 채점 100 — 최초의 만점이자 앵커 부적합 0의 첫 회차.**
  서버 `~/chaos`에서 주입 실행(01:25:04Z), Tempo 검색으로 에러 트레이스 확보
  (`6a6804f072fa431c691e39138e8f5e36`, 23 spans, 218ms). **문항 성립** —
  `Duplicate entry '148-173' for key 'tb_feed_hashtags.uk_feed_hashtag'`로 500,
  앞뒤 피드 생성은 200(01:24:59 · 01:26:05)이라 **부분 장애 대조군까지 확보**.
  블라인드 조사 **$0.5547 · 162.9s · out 11,070 tok(8회 중 최대, 장황본)**.
  에이전트는 성공 insert(`generated-keys=212`)와 실패 insert가 **같은 `connection` span 하위**임을
  근거로 동시성·재시도를 반증하고, `select tb_hashtags` **row-count 0→1** 대조로 중복 생성 경로를
  재구성했다. 조치로 **정규화 후 distinct + `DataIntegrityViolation`→409 매핑**을 둘 다 제시 —
  실제 근본 원인인 `trim().toLowerCase()` 정규화를 **관측만으로 처방**했다.
  채점 **100**(근본 40·근거 30·오귀인 20·조치 10). **근본 40은 경계 판정** — dedup 구멍이
  1순위가 아닌 2순위라 AP-1 선례로는 20~30이나, 확신도 중간 + "1번을 장애로 승격시킨 층위"라는
  **계층 명시**가 AU-4 파일럿 선례에 더 가깝다고 판단. 자기 일치도 검사 대상으로 표시.
  **관측 2건**: ① 리포트가 "같은 이름이 두 번"이라 단정했으나 실제는 `coffee`/`COFFEE`가
  정규화로 같아진 것 — 바인드 파라미터 미기록을 스스로 지목해 놓고 본문에선 단정(앵커 문언에
  정규화가 없어 감점 불가, rubric-v2 변별 지점). ② Loki에 `DataIntegrityViolationException`
  예외 체인 전문이 traceId와 함께 실재했으나 셀렉터 결함으로 0건 — **trace가 이미 충분 집합**
  이라 이 문항에서 **v0.1 Loki 수정이 점수를 올릴 자리는 없다**(AU-4와 같은 결론).
  **절차 결함 발견**: STATUS가 SoT로 지목한 `toy-content/.../AP-3/answer.md`는 **로컬 toy-content에
  반영돼 있지 않다**(구 이모지 문항 그대로, RUNBOOK §6·chaos.sh도 미교체, `uk_feed_hashtag`
  검색 0건). 교체본은 서버 `~/chaos`에만 존재 — 채점은 rca-agent `docs/ap-3/README.md` 요약표
  (`536c007`, 채록 9시간 31분 전 커밋)로 성립시켰고 선박제 요건은 충족하나 역동기화 필요.
- **2026-07-28**: **IN-2 회차 1을 CLI 격리 재실험으로 덮어씀 + 재채점 80.** 구 회차 1(07-26)은
  격리(`31e2a85`) 도입 전 조사라 오염 가능성으로 폐기. 재주입(Kafka **7분 21초** 다운, 트리거를
  주입 +5분 55초로 미뤄 `metadata.max.age.ms`(5분) 경과시킴)으로 producer `send()`가
  `max.block.ms` **60초를 꽉 채우고** `Topic user.notifications not present in metadata after 60000 ms`
  → 재시도·outbox 없어 **영구 유실** 재현(traceId `6a677e99`, `rca-cli-sandbox` 격리 실행 확인).
  조사 **$0.2976·49.4s**(구 회차 $0.92·123s의 절반, out 8,266→2,657 tok). 채점 **80**
  (근본20·근거30·오귀20·조치10) — **토픽 부재를 브로커 다운 위에 랭킹**하는 구 회차의 감별 실패가
  그대로 재현(**N=2 재현성 시사**, 단 구 회차 오염으로 §8.1 평균 인용엔 못 씀). 다만 구 회차의
  핵심 오독(`peer.service` 클러스터 ID를 "연결 성립" 근거로 읽음)은 **이번엔 안 함** — 근거 질은
  개선, 랭킹 결과 동일이라 점수 20. **부가 발견: IN-2 유실은 다운 5분(메타데이터 만료) 초과
  여부로 갈린다** — 80~100초만 죽인 3회 시도는 producer가 레코드를 버퍼에 쥐고 있다 복구 후
  발행(18.7s·26.2s·94.5s **지연 도착**, 유실 아님). Tempo 스샷 3장 추가(장애 1·정상 2).
  round-1 문서·[채점 대장](scoring/README.md) 갱신, 커밋 `754e983` 푸시.
- **2026-07-28**: **AP-3 문항 교체 (이모지 charset → 중복 해시태그)**. 구 AP-3(4바이트 이모지)를
  실행했으나 이모지 댓글이 **200**으로 통과 → **불성립**(테이블 utf8mb4). Tempo 실측으로 확인:
  14:59~15:00 댓글 POST 3건(baseline-ascii/emoji/symptom-ascii) 전부 200·insert 깨끗·에러 트레이스
  0건 · Loki charset 0건. 하네스 범위(요청 1건, 인프라·스키마 무접촉)로는 재현 불가한 degenerate
  문항이라 **AP-1의 짝 역할을 실제 재현되는 결함으로 대체**. toy-content 코드 탐색으로 앱 계층 500
  후보 3건 발굴(중복 해시태그 유니크 위반 / 상품 attachmentFileInfos NPE / 피드 null 해시태그 NPE),
  코드 4곳 직접 확인 후 **#1 중복 해시태그** 선정 — AP-1과 같은 API·같은
  `DataIntegrityViolationException`인데 지문이 `Duplicate entry ... uk_feed_hashtag`(유니크)로
  `Data too long`(길이)과 갈린다. **앵커 v1 동결**(toy-content `scenarios/AP-3/answer.md`),
  RUNBOOK §6 AP-3·요약표·FEED_ID 전제 교체, **`chaos.sh` 실행 로직도 교체**(`measure_AP_3`/
  `inject_AP_3`/신규 `feed_create`: 업로드→카테고리→피드생성, 이모지 제거, 구문검사 통과),
  `docs/ap-3/` 회차 폴더 신설.
  **다음**: 서버 `~/chaos`(= `toy-content/docs/chaos/` 배포본)에 **동기화** → `./chaos.sh AP-3 run`
  → 500 + `Duplicate entry ... uk_feed_hashtag` 확인 → traceId 확보 → rca-agent 조사 → §8 채점. 원인:
  `FeedService.findOrCreateHashtag` 정규화(`trim().toLowerCase()`)로 `coffee`·`COFFEE`가 같은
  Hashtag를 반환하는데 `createFeed`가 dedup 없이 두 `FeedHashtag`를 cascade insert.
- **2026-07-28**: **AP-2 재설계** — 구 "대용량 업로드"는 폐기. 실 업로드가 toy-auth의
  presigned URL + S3 직접 방식이라 content 로컬 업로드 경로가 **비대표**였고, "대용량이 앱을
  통과하며 계층에서 막힌다"는 전제가 실 구조와 불일치. 대신 순회 자연발생 500(DF-01 #2)을
  근인 확정해 문항화: **팔로우 목록 `size` 미기본값 → `FollowCondition.limit()`의 `size+1`
  언박싱 NPE → 500** (read-path, DB 미진입, following/followers 단일 근원). AP-1/AP-3(DB write
  제약 위반)과 다른 계열이고 **DB 오귀인 함정**이 핵심. RUNBOOK §6 AP-2·chaos.sh·answer.md
  교체(앵커는 실측 후 박제 — DRAFT). 부산물: **[NF-12](findings/nf-12-fileservice-windows-path-separator-hardcoded.md)**
  등재(FileService `"\\"` 하드코딩 — 구 AP-2 게이트가 잡아낸 이식성 버그, 비대표 경로라 미수정) ·
  보안 관찰 2건(toy-auth `application-local.yml`의 AWS 키는 gitignore로 **미커밋**이나 워킹트리
  평문, DB password `Wjdals12!`는 **히스토리 `f27d320`에 잔존**).
- **2026-07-28**: **AP-1(75)·AU-4 재실행(75) 채점 + 채점 규칙 3건 개정.** AP-1: 250자 댓글
  → varchar 위반 500, 채점자 판정 2건을 **충분 근거 원칙(§8.2)으로 일반화**(신호 개수 아닌
  증거력, 로그↔span 동치 대체 인정). AU-4: 구 회차 폐기 후 재주입·재실행, 앵커 "3s
  timeout"→"Connection refused" 정정(폐기 회차 실측 근거). **간결본(out 2,980)이 캐시 만료를
  놓쳐 75 — 폐기 장황본(out 10,511)이면 ~100. 같은 v0 두 실행 25점 차 = §8.1 N≥2 실증.**
  규칙 개정: 시간 분리 폐지 · 충분 근거 원칙 · [rubric-v2](scoring/rubric-v2.md) 제안. 기존
  au-4 스크린샷 재사용(모양 동일). AP-1 sweep에서 500 결함 3군·게이트웨이 라우팅 이상 발견,
  chaos.sh `tag` unbound 버그 수정.
- **2026-07-27 (저녁)**: **AU-4 회차 1 완주** — auth 22분 51초 다운 + 캐시 만료.
  **갈래 A(fallback 정상)**: 피드 200 유지, 작성자 10명 익명 저하. cwd 격리 적용 후 첫 조사.
  **$0.7089 · 165.5s — 6회 중 최저 비용**.
  **앵커가 틀리고 에이전트가 맞은 첫 사례** — 앵커는 "3s timeout"을 만점 요건으로 요구했으나
  실측은 **23.5ms connection refused**였고 에이전트가 "타임아웃이 아니라 TCP RST"로 정정했다.
  `.timeout(3s)`는 대기 상한이라 RST에는 발동하지 않는다. 채점된 3항목 **70/70 전부 만점**.
  **앵커 결함 ⑩ 확정**: 앵커를 코드 상수만 보고 쓴 것이 원인 — `fallback 로그` 요건도
  `.onErrorReturn()`이 에러를 흡수해 **코드에 로그 자체가 없다**. AE-05에서 "앵커 작성 전
  실측 선행"을 교훈으로 적고 반복했다. v3 정정 필요.
  **신규 발견 2건**: ① fallback 무음 저하 — 로그 0건·`http_client_requests` 메트릭 부재·
  HTTP 200·지연 오히려 **단축**(0.27s→0.17s)이라 **어떤 알람도 못 울린다**.
  ② content 읽기 경로에서 DB 커넥션 점유 중 외부 HTTP 호출(NF-01 계열, 에이전트가 먼저 지적).
  누계: 조사 6회 / 유효 채점 2회 / **앵커 부적합 4회**.
- **2026-07-27 (오후)**: **AU-2 회차 1 완주** — 소급 채록(`measure_AU_2`가 Loki·Tempo·Mimir를
  하나도 안 찍어 Grafana API로 직접 확보) → 블라인드 조사($0.8357, 209.2s) → 채점.
  **성과**: 트레이스가 원리적 무신호(baseline·symptom 모두 65 spans·동일 구성·`user-service`
  span 없음)인데 **메트릭 3계열 동시 단절 + 다른 서비스 연속 대조**만으로 auth 다운에 도달.
  같은 `/24` 대역 content 파드 정상을 근거로 노드 장애까지 스스로 배제.
  Loki `totalStreams: 0`을 근거로 **자기 셀렉터 결함을 원인까지 특정**.
  **한계**: 복구 판정 오판 — 원복 01:22:24인데 신 파드 첫 스크레이프가 01:25:28이라
  수집 창(~01:24:13) **밖**이었다(결함 7: window padding 120s 부족).
  **정정 2건**: ① AE-04의 "-120s는 어셈블러 버그" → **틀림**. `ContextAssembler`에 시각 변환
  코드가 없다(31행 조회창 출력이 유일) — LLM 앵커링이 맞고 AE-03 원가설이 옳았다.
  ② AU-2 v2 앵커의 "로그 침묵" 요건 폐기 — auth는 평시에도 로그 0건이라 변별력이 없고,
  실제 신호는 재기동 버스트(01:23:04~ 118줄)다. **앵커 작성 전 소급 채록이 선행돼야 한다.**
  누계: 조사 5회 / 유효 채점 2회 / **앵커 부적합 3회**.
- **2026-07-27**: **§8 블라인드 채점 최초 실시** — AE-01·02·03이 "채점 대기"로 방치돼 있던 것을
  앵커(`scenarios/*/answer.md`) 기준으로 채점하고 [채점 대장](scoring/README.md) 신설.
  CH-1 회차3 **80**, IN-2 회차1 **80**, CH-1 회차1 **채점 불가**. **§8.1상 전부 N=1이라 인용 불가** —
  "baseline 확보"는 아직 성립하지 않는다. **앵커 결함 4건** 발견(개정은 §8.2에 따라 다음 회차부터):
  ① CH-1 앵커가 단일 전개만 가정(경계 안/밖 두 갈래인데 회차1이 채점 불가가 된 원인)
  ② 2단계 앵커라 부분 충족 표현 불가(만점→부분점 낙폭 20점) ③ 오판과 미판단을 구분 못 함
  ④ 앵커가 요구하는 baseline 대조를 v0 입력이 제공하지 않음.
  **CH-2 기록 부재 확인** — STATUS가 실제보다 앞서 있었다(정정함).
  AU-2 회차 1 채록(01:20Z, 2분 24초): 로그인 200→503, content 200 유지·작성자 실명·지연 +32.8ms =
  캐시 히트로 흡수. 회차 0을 무효화한 프로브 오염(t2 size 미지정 500)은 재발하지 않음.
  부수: 레포 로컬 실행 복구(JDK 21 데몬 고정, `.env` 로더 래퍼), Loki `errorWarnQuery`의
  `| logfmt` 파싱 결함 신규 발견(셀렉터와 별개 — 평문 Logback에 `level` 필드 없음).
- **2026-07-26 (오후)**: **블라인드 조사 4회 완주** — CH-1 회차1(지연 24.7s, AE-01)·회차2(구 회차3, DLQ 복구, AE-02)·CH-2(컨슈머 정지, AE-04)·IN-2(Kafka 유실, AE-03). 회차별 기록 [ch-1/](ch-1/README.md)·[ch-2/](ch-2/README.md)·[in-2/](in-2/README.md)(장애상황·Loki/Tempo 발췌·원인대조·스샷 traceId + 리포트 원문). 신규 findings: NF-07(지연↔유실 경계=드라이버 30s timeout), NF-08(DLQ trace 단절→영향 오판). **4회 관통 패턴 3건**: ① 영향 판정 정확도 = trace 연속성(끊기면 오판), ② -120s 시각 밀림은 어셈블러 버그로 확정(raw ns 병기로 입증), ③ 부재 신호(사라진 메트릭·침묵 로그) 활용 불가. AU-2 조사는 오염 — content 피드 500이 auth가 아니라 **`/feeds/scroll` size 미지정 NPE**(잠복 실버그, toy-content `1e7df3f`로 수정)였고 chaos.sh t2가 size 없이 호출해 CH-1~IN-2 내내 조용히 500 통과. AU-4(auth 다운+캐시 만료) 시나리오 신설.
- **2026-07-26 (오전)**: 하트비트 span 필터 추가·푸시(`0ba282b`, Brave SpanHandler drop). Mongo
  계측 동작 실측 확인(`hello` span 배포 시점부터 생성 — 단 고아 트레이스 노이즈 발견이
  필터의 계기). 댓글 흐름 부모-연결 검증은 배포 후 댓글 부재로 대기. STATUS.md 신설.
- **2026-07-25**: CH-1 1차 실행 분석 — 증상 0의 원인 규명(주입 16s < 타임아웃 체인, 그리고
  **컨슈머 예외 삼킴**이 근본 원인). toy-chat 4커밋: Kafka 재시도/DLQ 관측 로그, 예외 삼킴
  제거+DLQ 무한 재시도 팩토리, Mongo 계측 중복 제거+contextProvider 보완. DLQ 과거 적재
  11+2건 발견. Mongo 미계측을 monitoring.md 한계 4번으로 기록.
- **2026-07-24**: 리뷰 모드 구현·실검증($1.23, 신규 발견 3건). 실서비스 GET 순회 79회 →
  **500 고정 결함 3군**(DF-01) + 게이트웨이 라우팅 이상(NF-05) 발견. write-flow 스크립트
  (업로드→피드→댓글 = N1 트레이스 생성기) + sweep-assets/ 폴더.
- **2026-07-23**: strategy를 2트랙 평가(C-트랙 chaos / N-트랙 정상)로 재설계. findings
  체계 신설(NF-01~06, DF-01) — N1 정답지 확정. docs 구조화(ADR 6건, monitoring.md).
- **2026-07-22**: Grafana Cloud 연동 3/3 검증(Loki 테넌트 오류 격리, Mimir /api/prom 경로
  수정). claude-cli 토큰 회계 수정(in=2→42,774). 리포트 .md+.json 이중 저장.
  kafka_consumergroup_lag 실존 확인(ADR-005).
- **2026-07-21**: 실전 조사 1호 성공 — dispatch 996ms 중 994ms 미계측 갭 특정, 사람이
  코드로 FCM 동기 호출 확정. 앱 메모리 실측(RSS ~200MB).
- **2026-07-20**: v0 에이전트 완성(수집→조립→LLM 1회→통보). 패키지 구조화, 측정 하네스,
  프롬프트 파일화, Docker. OTel vs Brave 결정 문서화.
