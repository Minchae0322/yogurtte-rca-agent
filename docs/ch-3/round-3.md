# CH-3 회차 3 — **85 / 100** · 🏆 **4 → 85, B-17(지연 채널)이 문항을 열었다**

> **회차 1·2는 각 4점이었고 사유가 *"에러 검색 단독이라 지연 장애를 못 본다"* 였다.**
> 이번 회차는 **같은 문안·같은 문항**에 지연 채널이 켜진 상태로 돌렸고,
> **탐색이 정답 트레이스를 후보로 올려** 분석이 공백을 22.5초로 정량화했다.

## 한눈 요약

| | |
|---|---|
| **문항 정답** | Mongo 20~25초 미가용 → 드라이버 서버 셀렉션 대기 → **대기 중 복구로 첫 시도가 예외 없이 성공** → 지연 도착·유실 0·재시도/DLQ 미발동 |
| **입력** | 자연어 한 줄 — *"최근 1시간 안에 앱이 잠깐 버벅였다는 얘기가 있어요. 뭔가 있었는지 봐줘"* (**회차 1·2와 문자 그대로 동일**) · **창 미지정** |
| **주입** | **2회 관측** — 14:41:38Z · 15:33:42Z (각 약 22~26초). 조사는 15:35:30Z 시작이라 **두 번째 주입 2분 뒤** |
| **탐색 결과** | `"최근 1시간"` → **14:35:30~15:35:30Z 자력 파싱(EXACT)** · 후보 7개 중 **6개 선택** · 대상 `chat-service`·`content-service` |
| **에이전트 파악 원인** | 1순위(확신도 **높음**) *"MongoDB 순간 중단이 **2회** 발생 → chat-service 알림 처리가 매회 **약 22~26초 정지**"* |
| **§8 채점** | **85 / 100** — 원인 30 · 근거 25 · **탐색 15** · 영향 5 · 오귀인 5 · 조치 5. 앵커 [anchors-v2 CH-3](../../../toy-content/docs/chaos/anchors-v2.md) `add5aff` **무수정** |
| **모델** | `claude-opus-5` · `num_turns=1` |
| **비용·시간** | 탐색 $0.4538 · 54.9s + 분석 $1.5145 · 172.1s = **$1.9682 · E2E 237.0s** |
| **리포트 전문** | [round-3-rca-report.md](round-3-rca-report.md) · [json](round-3-rca-report.json) |

> **⚠️ 이 회차 앞에 같은 문안으로 돌린 시행 하나가 있었다** — 그때는 창이 주입 시각을 덮었는데도
> **CH-3 신호가 세 채널 모두 무신호**여서 **채점 불가**로 처리했다(주입 무흔적).
> 그 시행에서 나온 결함 2건은 §5에 보존한다. **이 문서는 재주입 후의 유효 회차다.**

---

## 1. 회차 1·2와 무엇이 달라졌나 — 도구가 문항을 열었다

| | 회차 1·2 (각 **4점**) | **회차 3 (85점)** |
|---|---|---|
| 스윕 트레이스 쿼리 | `{ status = error }` **단독** | **+ `{ duration > 3s && status != error }`**(B-17) |
| 후보에 오른 것 | 에러 트레이스 4건(전부 CH-1·CH-2 것) | **지연 트레이스 10건 — 정답 2건 포함** |
| 좁힌 창 | 04:57~05:20Z — 주입(05:22:01) **밖** | 14:35:30~15:35:30Z — 주입 **2건 모두 안** |
| CH-3 산출물 | **0** | **1순위 원인으로 특정** |

**정답 트레이스가 지연 채널로 올라왔다** — 채점자가 리포트 `triage.traceCandidates`에서 확인:

```
slow  15:33:42.861  <root span not yet received>   23,772ms  6a70b4d6fec0e31f   ← 2차 주입
slow  14:41:25.157  content-service http post .../comments  38,887ms  6a70a895a7050d84  ← 1차 주입
```

**전부 `channel = slow`다.** 에러 채널로는 한 건도 안 잡혔고(CH-3는 예외가 안 난다),
[B-17](../round-3/README.md)이 없었다면 회차 1·2와 같은 결과였다.

---

## 2. 채점 — 앵커 v2 (anchors-v2.md CH-3 · 루브릭 v3)

| # | 항목 | 배점 | 점수 | 한 줄 | 회차 1·2 |
|---|---|---:|---:|---|---:|
| 1 | 원인 적중 | 40 | **30** | Mongo 미가용 + 대기 위치·시간까지. **"대기 중 복구로 첫 시도 성공"은 미언급** | 0 |
| 2 | 근거 | 25 | **25** | 공백 정량화(22.5초) + 위치(첫 insert 직전) + 업스트림 200/57ms 대조 **3요건** | 0 |
| 3 | 탐색 | 15 | **15** | T EXACT · **S 지연 채널로 정답 2건** · B 정상 API duration 대조 | 4 |
| 4 | 영향 판정 | 10 | **5** | 지연은 확정, **유실 아님·재처리 불필요를 명시하지 않음** | 0 |
| 5 | 오귀인 | 5 | **5** | 풀·GC를 **수치로** 배제 + Kafka lag을 결과로 정확히 배치 | 0 |
| 6 | 조치 | 5 | **5** | 가용성 확인 + 계측 보강 + 구조 개선 | 0 |
| | **합계** | **100** | **85** | 판정 0건 | **4** |

---

### 1) 원인 적중 — **30 / 40**

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **40** | Mongo 미가용 + **첫 Mongo 작업 직전** 대기 + **대기 중 복구로 첫 시도 성공** |
| **30** | **대기는 특정, 복구 타이밍 미언급** |
| 20 | "느렸다"까지 |
| 0 | 앱·Kafka 지목 |

**이번 리포트**

| 요건 | 판정 | 리포트가 쓴 것 |
|---|:-:|---|
| Mongo 미가용 | 충족 | *"MongoDB(172.31.46.124:27017) 순간 중단이 2회 발생"* + `InterruptedAtShutdown` · `Connection refused` 축자 |
| **첫 Mongo 작업 직전 대기** | 충족 | *"`process-notification`은 14:41:38.298에 시작했는데 **첫 `insert toychat`은 14:42:00.841에야 실행** → 약 **22.5초가 Mongo 대기**"* |
| 드라이버 대기 기전 | 충족 | *"`Waiting for server to become available … Remaining time: 29927 ms. Selector: WritableServerSelector, topology description: {type=UNKNOWN}`"* |
| **대기 중 복구로 첫 시도 성공** | **미충족** | 첫 insert가 실행된 사실은 적었으나 **그것이 "예외 없이 첫 시도에 성공"이라는 판정을 하지 않았다.** 재시도·DLQ 미발동도 명시하지 않았고, 조치 6에서 오히려 *"리스너의 재시도/DLQ 경로 동작을 **확인**"* 으로 남겼다 |

**→ 30점.** 40 문언의 세 요소 중 둘을 했고, 30 문언(*"대기는 특정, 복구 타이밍 미언급"*)에
**그대로 해당**한다. 판정이 아니라 문언 일치다.

> **회차 1·2는 0점이었다** — CH-3에 대한 진술이 아예 없었다.

---

### 2) 근거 — **25 / 25**

**채점 기준**: 25 = *"공백 **정량화**(총 시간 − 자식 합) + 공백 **위치**(첫 Mongo 작업 직전) + 업스트림 정상 대조"*

| 요건 | 판정 | 리포트가 쓴 것 |
|---|:-:|---|
| 공백 정량화 | 충족 | `receive` span **25,750ms**(14:41:38.294→14:42:04.045) ↔ 자식 첫 insert까지 **22.5초** · 2차 사건도 `receive` **23,772ms** / 대기 **22.6초** |
| 공백 위치 | 충족 | *"첫 `insert toychat`(user_notifications)은 14:42:00.841에야 실행"* — 첫 Mongo 작업 직전임을 시각으로 특정 |
| 업스트림 정상 대조 | 충족 | *"content-service … `status=200`, 14:41:25.157→14:41:25.214 = **57ms**"* · 2차 사건 **50ms** |
| 재현성 | 문언 이상 | *"동일 패턴 재현"* — **두 사건을 같은 지문으로 묶고** *"두 사건 간격이 약 52분으로 **재발성**이 있다"* 까지 |

**→ 25점.** 18 문언(*"정량화만"*)과 갈리는 지점이 위치·대조인데 **둘 다** 했다.

**결정적 관측 하나 더** — *"`mongodb_up` 241점 **전 구간 1**, `up{job=mongodb}`도 전 구간 1 —
익스포터 관점에서는 다운이 전혀 안 잡혔다"* 고 **스스로 적었다.**
앵커가 CH-3를 *"메트릭 해상도로는 못 잡는 문항"* 으로 설계한 그 사실을 에이전트가 역으로 짚은 것이고,
조치 3에서 *"**다운을 놓치는 알람은 지금 신뢰할 수 없다**"* 로 이어갔다.

---

### 3) 탐색 — **15 / 15**

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **15** | **S = 200 성공이면서 duration 이상한 트레이스**(지연 채널) + B(정상 구간 동일 API duration 대조) |
| 4 | 창이 20~25초 구간을 못 덮음 |
| 0 | 엉뚱한 구간 |

**이번 리포트**

| 요건 | 판정 | 근거 |
|---|:-:|---|
| **T** 시간창 | 충족 | 창 미지정 → `"최근 1시간"` **EXACT** 파싱 → `14:35:30~15:35:30Z`가 **주입 2건 모두 포함** |
| **S** 지연 채널 | 충족 | 후보 트레이스 10건이 **전부 `slow` 채널**이고 그중 `6a70a895…`(38,887ms)·`6a70b4d6…`(23,772ms)가 **분석이 인용한 정답 트레이스**다. INC-7은 *"`<root span not yet received>` 23,772ms(slow 채널)"* 로 2차 주입을 통째로 후보화했다 |
| **B** 정상 대조 | 충족 | 같은 API의 정상 응답 **57ms / 50ms**를 대조로 인용(2) 절) |
| 후보 처리 | 문언 이상 | **7개 중 6개 선택** + 선정 이유에 *"어느 쪽이 제보된 버벅임인지 **확정 불가하여 둘 다 남긴다**"* — 두 사건을 임의로 하나로 좁히지 않았다 |

**→ 15점.** 회차 1·2의 4점 사유(*"창이 20~25초 구간을 못 덮음"*)가 **완전히 해소**됐다.

> **좁힌 창이 스윕 창 전체(60분)와 같다.** 앵커 T 기준이 *"과도하게 넓은 창은 그 자체로 감점하지
> 않는다. 단 창이 넓어 **후보가 정답 아닌 것으로 채워졌다면** S에서 깎인다"* 인데,
> 후보가 **정답으로 채워졌으므로** 감점 사유가 아니다. 사건이 52분 간격 2건이라 합집합이 넓어진 것이다.

---

### 4) 영향 판정 — **5 / 10**

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **10** | **지연이지 유실 아님** + 재처리 불필요 |
| **5** | **유보** |
| 0 | **"장애 없음"으로 결론** · 또는 유실 판정 |

**이번 리포트**

| 요건 | 판정 | 리포트가 쓴 것 |
|---|:-:|---|
| 지연임 | 충족 | *"알림 처리가 매회 약 22~26초 정지"* · *"알림/푸시 도착 22~26초 지연"* |
| **유실 아님 명시** | **미충족** | *"유실"* 이라는 단어가 **0회**다. lag이 *"14:42:30 이후 0으로 회복"* 이라고 적었으나 **그것을 "유실 없음"으로 판정하지 않았다** |
| **재처리 불필요** | **미충족** | 조치 6에서 오히려 *"재시도/DLQ 경로 동작을 **확인**"* 으로 남겼다 |
| 0점 회피 | 충족 | *"장애 없음"* 으로 결론짓지 않았고 유실 판정도 하지 않았다 |

**→ 5점.** 10 문언의 두 요소를 **둘 다 문장으로 닫지 않았고**, 0 문언에는 해당하지 않는다.
5 문언(*"유보"*)이 정확하다.

**리포트가 유보한 이유를 스스로 밝혔다** — *"사용자가 말한 '버벅임'이 **어느 화면/API인지 특정
불가**"* · *"chat-service의 `http_server_requests_seconds_*` 시계열이 수집되지 않아 실제 사용자
요청 지연 분포를 확인할 수 없다"*. **CH-1 회차 4에서 도착을 초 단위로 확정했던 것과 대비된다** —
거기서는 `DLQ 재처리 성공` INFO가 컨텍스트에 있었고, 여기서는 **재시도가 발동조차 안 해서
"성공했다"고 말해 줄 로그가 없다.** 문항 구조상 **부재를 근거로 판정해야 하는 자리**다.

---

### 5) 오귀인 없음 — **5 / 5**

**채점 기준**: 5 = *"풀·GC·Kafka 지표 배제"*

| 요건 | 판정 | 리포트가 쓴 것 |
|---|:-:|---|
| 풀 배제 | 충족 | *"커넥션 풀 고갈은 **없었다**: `hikaricp_connections_active` min 0 / **max 1**, `pending`은 chat/content/auth 전 파드 **전 구간 0**"* |
| **GC 배제** | 충족 | *"GC로는 설명 안 됨: `rate(jvm_gc_pause_seconds_sum[5m])` chat-service **max 0.00192(≈1.9ms/s)**, major GC는 전 구간 0. 즉 공백을 GC 정지로 돌릴 수 없다"* |
| Kafka 배제 | 충족 | lag을 **원인이 아니라 결과**로 배치 — *"파급: lag이 14:41:30에 max 26까지 올랐다가 14:42:30 이후 0으로 회복"* |
| 오판 회피 | 충족 | 파드 교체(후보 2)·트랜잭션 전파(후보 3)를 올리되 확신도 중간·*"독립 원인이라기보다 1·2를 사용자 체감으로 키운 요인"* 으로 종속 관계 명시 |

**→ 5점.** 앵커가 요구한 3종을 **전부 수치로** 닫았다.

---

### 6) 조치 — **5 / 5**

**채점 기준**: 5 = *"가용성 확인 · 재처리 불필요 명시 · 계측 추가 중 **둘 이상**"*

| 요건 | 판정 | 리포트가 쓴 것 |
|---|:-:|---|
| 가용성 확인 | 충족 | 조치 1 — mongod 로그·`systemctl status`·`journalctl`로 **두 시각 기준** shutdown 사유 특정 + *"52분 간격 재발이므로 조회창을 6~24시간으로 넓혀 **주기성**"* |
| 계측 추가 | 충족 | 조치 3 — *"mongodb_exporter가 왜 `mongodb_up=1`을 유지했는지 점검"* · 조치 4 — `http_server_requests`·`restarts_total`·Mongo 연결 메트릭 재수집 |
| 재처리 불필요 명시 | 미충족 | 4)와 같은 사유 |
| 구조 개선 | 문언 이상 | 조치 7 — *"Kafka 리스너에서 **JDBC 트랜잭션 안에 Mongo·FCM 외부 호출을 두지 않도록 분리**(현재 커넥션 25.7초 점유)"* · 조치 8 — 헬스 indicator를 readiness에서 분리 |

**→ 5점.** *"둘 이상"* 요건을 **가용성 확인 + 계측 추가**로 충족했다.

---

## 3. 토큰 — `█ 실측`

`coverage.overheadTokens = 21,309`.

| 단계 | 총 `in` | 문자 | ① `in − 21,309` | ② `chars × 0.524` | 격차 |
|---|---:|---:|---:|---:|---:|
| 탐색 | 48,881 | 51,006 | **27,572** | 26,727 | +3.2% |
| 분석 | 131,401 | 206,928 (컨텍스트 206,352 + 프롬프트 576) | **110,092** | 108,430 | +1.5% |
| **합계** | **180,282** | | **≈ 137,664** | | |

- **비용 $1.9682** — 오늘 회차들 중 가장 싸다(CH-1 회차 4 $2.99 · AU-3 $1.90 · AP-3 $1.29).
- **분석 `out`이 11,490으로 최대다** — 두 사건을 각각 복원하느라 분량이 늘었다.
- `num_turns=1` 유지.

## 4. 수집 실패 1건 — B-13 여섯 회차 연속

```
Metric 'sum(rate(http_server_requests_seconds_count{application="content-service", status="401"}[1m]))'
returned no series in this window; skipped.
```

리포트가 *"인증 관련 가설은 **판정 불가**"* 로 공백을 명시했다. 처방은 [round-4 B-29](../round-4/README.md).

## 5. 직전 시행(주입 무흔적)에서 나온 결함 — 보존

> 이 회차 앞에 **같은 문안**으로 한 번 더 돌렸고, 창이 보고된 주입 시각을 덮었는데도
> 세 채널 모두 무신호여서 **채점 불가**로 처리했다(`mongodb_up` step 10s로 0인 지점 0 ·
> 지연 트레이스 0건 · Mongo 장애 로그 0줄 · 트래픽은 전부 4~12ms 정상).
> **CH-3는 트리거 요청이 다운 20~25초 구간에 겹쳐야만 흔적이 남는다** — 그 시행은 겹치지 않았고,
> 재주입한 이번 회차는 **2건 모두 겹쳐서** 관측에 남았다. 주입 절차의 민감도가 실측된 셈이다.

**신규 결함 2건** (그 시행에서 문안의 `<시각>`을 *"30분쯤 전"* 으로 채웠을 때 발견 — 문안이 달라 미채점):

1. **🔴 `TimeExpressionParser`가 `"N분쯤 전"`을 못 읽는다** → `FALLBACK` 24시간.
   `"최근 N시간"`·`"어젯밤"`·절대 시각은 되는데 **분 단위 상대 표현 어휘가 사전에 없다.**
   → [round-4 B-31](../round-4/README.md) 등재
2. **창 파싱 실패의 비용 — 탐색이 분석보다 비싸진 첫 사례.**
   24시간 창에서 후보 **28개**, **탐색 `in` 254,002 tok · $2.6642**(분석 $2.4393),
   총 **$5.1035**(프로젝트 최고). 이번 회차(1시간 창)는 후보 7개 · 탐색 **$0.4538**이다.

## 6. 다음 회차로 넘기는 것

1. **🔴 1) 원인 40점의 남은 10점이 어디 있는지 분명해졌다** — *"대기 중 복구로 첫 시도 성공"* 이다.
   에이전트는 **첫 insert 시각까지 짚고도** 그것을 *"재시도 없이 성공"* 으로 판정하지 않았다.
   4)의 *"재처리 불필요"* 와 **같은 뿌리**다 — 둘 다 **부재(재시도 로그가 없음)를 근거로 삼는 판정**이고,
   현재 컨텍스트에는 *"재시도가 없었다"* 를 말해 주는 신호가 없다.
   → **[B-16](../round-3/README.md)의 부재 신호 대조표**가 이 자리를 정확히 겨냥한다.
2. **CH-3 N=2가 아직 아니다** — 이번이 v3 자로 첫 유효 표본이고, 회차 1·2의 4점은 **도구가 다르다**.
   같은 도구로 2차 실행이 필요하다.
3. **`mongodb_up`이 다운을 놓쳤다** — 에이전트가 조치 3으로 스스로 지적했다.
   B-18(메트릭 해상도)의 후속으로 **exporter 스크랩 주기**를 볼 것.

## 🔴 회차 1·2와 평균 내지 않는다 (§8.1)

| | 회차 1·2 (N=2, 4 ± 0) | **회차 3 (N=1, 85)** |
|---|---|---|
| 스윕 채널 | 에러 검색 단독 | **+ 지연 채널(B-17)** |
| 도구 | 회차 2 상태 | + B-9·10·11·15·17·18·20·25·26·28·30 |
| 자 | `add5aff` · v3 | **동일** |

**자는 같고 도구가 다르다 — 이것이 B-17의 델타다.** 다만 **N=1이라 §8.1상 인용 보류**다.
