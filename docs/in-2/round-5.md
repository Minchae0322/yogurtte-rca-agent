# IN-2 회차 5 — **98 / 100** (판정 1건) · 🔴 **창에 주입이 둘인데 서로의 무고함까지 증명했다**

## 한눈 요약

| | |
|---|---|
| **실제 원인** | Kafka 브로커 다운 **정확히 3분** — `up{job="kafka"}` 0 구간 **04:47:20~04:50:20Z**(채점자 실측, 10초 해상도). `AFTER_COMMIT` 발행이 `max.block.ms` 60초를 소진하고 실패 → **알림만 조용히 유실, 댓글은 커밋** |
| **입력** | **traceId 없음 · 창 미지정.** 자연어 한 줄 — *"최근 1시간 안에 댓글 알림이 안 왔다는 제보가 있다. 원인을 조사해줘"* (**회차 2~4와 문자 그대로 동일**) |
| **탐색 결과** | 1시간 스윕 → 좁힌 창 **04:25:00~05:04:55Z(40분)** · 대상 content + chat · 장애 후보 **8건 중 6건 선택**(INC-2·4·5·6·7·8) |
| **에이전트 파악 원인** | 1순위(확신도 **높음**) *"Kafka 브로커 다운(04:47:15~04:50:15) → 발행 60초 블로킹 후 만료·폐기"*. 2순위(중간) **Redis 다운(= IN-1 주입)** — *"제보된 댓글 알림과는 다른 알림 경로"*. 3순위(낮음) 컨슈머 적체 — *"데이터상 반증됨, 사실상 배제"* |
| **§8 채점** | **98 / 100** — 원인 40 · 근거 25 · 탐색 15 · 영향 10 · **오귀인 3** · 조치 5. **판정 1건** — 5) 반대 시 **100** |
| **앵커** | [anchors-v2.md IN-2](../../../toy-content/docs/chaos/anchors-v2.md) — 블록 최종 수정 `add5aff` **2026-07-28T16:09:54Z**, 주입 2026-08-06T04:47:20Z → **8일 12시간 앞선 박제**, 워킹트리 무수정 |
| **모델** | `claude-opus-5` · `num_turns=1` · 라벨 정상 |
| **비용·시간** | 탐색 $0.2138 · 59.0s + 분석 $0.9291 · 136.8s = **$1.1429 · E2E 203s** |
| **에이전트 보고서 전문** | [round-5-rca-report.md](round-5-rca-report.md) |

> ### 이 회차의 핵심 — **두 주입이 한 창에 있었고, 둘을 갈랐을 뿐 아니라 서로 무고함을 증명했다**
>
> | 주입 | 구간 | 리포트 처리 |
> |---|---|---|
> | **IN-1 Redis 다운**(직전 회차) | 04:32:5x ~ 04:36 | **후보 2로 분리** — *"제보된 '댓글 알림'과는 다른 알림 경로다(`BATTLE_ITEM_COMMENT` 발행 경로는 Kafka, 이쪽은 배틀 마감 알림 스케줄러)"* |
> | **IN-2 Kafka 다운**(이번) | **04:47:20 ~ 04:50:20** | **후보 1** — 발행 span 60,016ms · `max.block.ms` 60,000ms 대조 |
>
> **그리고 결정적 한 줄** — *"04:48:35 댓글 트레이스에서는 `[user-cache] 캐시 HIT - userId: 1,
> elapsed: 4ms`, redis `GET` span **0.628ms**로 **정상** → **후보 2는 후보 1의 원인이 아니다**
> (시간·경로 모두 분리)"*.
>
> **Redis가 죽어 있지 않았음을 Kafka 장애 트레이스 안에서 증명했다.** 두 장애가 겹칠 때
> *"시각이 다르다"* 로 끝내지 않고 **문제의 트레이스 내부에서 다른 후보의 정상성을 관측**한 것은
> 이 프로젝트에서 처음이다.
>
> 🔴 **"토픽 부재" 함정도 피했다.** 예외 지문이 축자로 `Topic user.notifications **not present in
> metadata** after 60000 ms`인데, **이 문구가 회차 1과 회차 4 실행 A를 오답으로 끌고 갔던 그것**이다.
> 이번엔 1순위를 *"브로커 다운"* 으로 세우고, 탐색 단계에서 이미
> *"`kafka_consumergroup_lag` 이상 0건은 **정상 근거로 쓰지 않음** — 브로커가 죽은 구간에는 lag
> 메트릭 자체가 수집되지 않아 0으로 보일 수 있음"* 이라고 못박았다.

---

## 채점자 실측

| 쿼리 | 실측 | 무엇을 확정하나 |
|---|---|---|
| `up{job="kafka"}` 04:44~04:54Z **step 10s** | 04:47:10 = 1 → **04:47:20 ~ 04:50:20 = 0** → 04:50:30 = 1 | 주입 **정확히 3분**. 리포트의 *"04:47:15~04:50:15"* 와 **오차 5초** |
| `kafka_brokers` | 04:47:00 = 1 → **04:47:30~04:50:00 결측** → 04:50:30 = **0** → 04:51:00 = 1 | *"다운 시 0이 아니라 시리즈가 사라진다"*(관측 기록 2026-07-26)가 그대로 재현 |
| `kafka_consumergroup_lag{topic="user.notifications.dlq"}` | 전 구간 **0 / -1** | **DLQ 유입 0** — 유실 확정, 재시도 경로 없음 |
| 발행 시도 구간 | 04:48:35.955 ~ 04:49:35.971 | **다운 구간에 완전히 포함** |

**T 판정에 쓰는 값**: 주입 04:47:20 ~ 04:50:20Z. 좁힌 창 **04:25:00~05:04:55Z**가 전부 포함한다.

> **스크레이프 10초 변경(02:49Z)의 첫 관측 이득이다.** 3분짜리 장애라 30초로도 잡혔겠지만,
> 리포트가 다운 구간을 **±5초로 특정**했다. 30초 간격이었다면 경계 오차가 ±30초다.
> **점수에는 영향이 없다** — 기록으로만 남긴다.

---

## 채점 — 앵커 v2 (anchors-v2.md IN-2 · 루브릭 v3 6항목)

| # | 항목 | 배점 | 점수 | 한 줄 |
|---|---|---:|---:|---|
| 1 | 원인 적중 | 40 | **40** | 브로커 다운 → 발행 60초 만료·폐기 · **"토픽 부재" 함정 회피** |
| 2 | 근거 | 25 | **25** | producer span 60,016ms ↔ `max.block.ms` 60,000ms + 컨슈머 부재 + 댓글 200 공존 |
| 3 | 탐색 | 15 | **15** ⚠️ | S = producer span error(INC-7) + 메트릭 두 지문 · B는 **부재 신호**로 충족 |
| 4 | 영향 판정 | 10 | **10** | 알림만 유실 · **자동 복구 불가** 명시 + 수동 복구 범위·방법 |
| 5 | 오귀인 | 5 | **3** (판정) | 200 오판 없음 · chat 미지목이나 **컨슈머를 랭킹 3번에 유지** |
| 6 | 조치 | 5 | **5** | Kafka 복구 + **미도착분 자동 복구 불가 인지** + 발행 실패 삼킴 제거 |
| | **합계** | **100** | **98** | 5) 반대 시 **100** |

---

### 1) 원인 적중 — 40 / 40

**채점 기준**

| 점수 | 요건 |
|---|---|
| 40 | **브로커 다운** → 발행 실패, 재시도 경로 없음 |
| 30 | 발행 실패는 정확하나 **하위 원인 감별 실패** |
| 20 | "알림이 안 간다"까지 |
| 0 | content API·chat 지목 |

**이번 리포트**

| 요건 | 판정 | 리포트가 쓴 것 |
|---|---|---|
| 브로커 다운 지목 · 1순위 | **충족** | *"**Kafka 브로커 다운**(04:47:15~04:50:15 UTC) → content-service의 `user.notifications` 발행이 60초 블로킹 후 만료·폐기"*, 확신도 **높음** |
| 발행 실패 기전 | **충족** | `notification-publish` `durNs=60021881000`, `error="Send failed"`, `STATUS_CODE_ERROR` → 자식 `publish user.notifications`(`SPAN_KIND_PRODUCER`) `durNs=60016108000` |
| **재시도 경로 없음** | **충족** | *"재시도 증거 없음(동일 key='7' 재발행 로그·span 없음), DLQ로도 안 감 — `kafka_consumergroup_lag{consumergroup=notification-recovery, topic=user.notifications.dlq}`가 전 구간 0/-1"* |
| **하위 원인 감별** | **충족** | 아래 참조 |

**→ 40점.** 30점 문언(*"하위 원인 감별 실패"*)과 갈리는 지점이 이 문항의 함정이다.

> 🔴 **예외 문구가 오답을 유도하는 문항이다.** 지문이 축자로
> `Topic user.notifications **not present in metadata** after 60000 ms` 인데,
> **회차 1과 회차 4 실행 A가 이것을 읽고 *"토픽 자체가 없다"* 로 갔다**(회차 4 실행 A는 그 한 항목으로 −10).
> 이번 리포트는 같은 문구를 인용하면서도 원인을 **브로커 다운**으로 세웠고,
> 근거를 클라이언트·인프라 양쪽에 걸었다 — *"`up{job=kafka}` **0인 구간 04:47:15Z~04:50:15Z**,
> `kafka_brokers` 결측 … 발행 시도 구간(04:48:35.955 → 04:49:35.971)이 **브로커 다운 구간에 완전히 포함**된다"*.
>
> **그리고 자기 근거의 약점까지 스스로 짚었다** — *"`up{job=kafka}`·`kafka_brokers`는 둘 다
> `instance=infra-server`의 **동일 exporter**에서 나온다. **브로커 다운인지 exporter 다운인지 이 두
> 시리즈만으로는 분리되지 않는다.** 다만 클라이언트 측 `Topic … not present in metadata`는
> 브로커/메타데이터에 실제로 도달하지 못했음을 **독립적으로** 뒷받침한다."*
> **IN-1 회차 5에서 `up{job=redis}` 모순을 잡아낸 것과 같은 종류의 자기검증**이고, 이번엔 그것을
> 다른 독립 채널로 **닫았다.**

---

### 2) 근거 — 25 / 25

**채점 기준**

| 점수 | 요건 |
|---|---|
| 25 | producer span error(**60,060ms를 `max.block.ms`와 대조**) + **chat span 부재를 배제 근거로** + 댓글 200 공존 |
| 18 | 부재 신호 미활용 |
| 10 | span error만 |

**이번 리포트**

| 요건 | 판정 | 리포트가 쓴 것 |
|---|---|---|
| producer span error | **충족** | `publish user.notifications` `SPAN_KIND_PRODUCER`, `messaging.system=kafka`, **60,016ms**, `error="Topic … not present in metadata after 60000 ms."` |
| **`max.block.ms` 대조** | **충족** | *"실측 대기 **60,016ms** vs 상한(에러 메시지에 명시된 메타데이터 대기 한도 = Kafka 프로듀서 **`max.block.ms` 60,000ms**) → **만료됨**"* |
| **컨슈머 부재를 배제 근거로** | **충족** | 후보 3에서 *"컨슈머 span·로그가 수집되지 않았다"*, *"호출 그래프에 엣지가 없고(`user.notifications`는 **publish 엣지만** 존재)"*, DLQ lag 0/-1 |
| **댓글 200 공존** | **충족** | *"`status=200`, `outcome=SUCCESS`, **59.9ms로 정상 종료**. 로그도 `[battle] 댓글 작성 완료 - commentId: 327` — **댓글 자체는 DB 커밋됨**(`jdbc.row-affected=1`, `generated-keys=327`, connection span에 `commit` 이벤트)"* |

**→ 25점.** 18점 문언(*"부재 신호 미활용"*)의 반대다.

> **부재 신호를 두 방향으로 썼고, 그중 하나는 자기 근거를 약화시키는 쪽이었다** —
> *"`kafka_consumergroup_lag` … 전 파티션 전 구간 0 — '브로커 장애면 lag이 튀어야 하지 않나'로
> 읽힐 수 있으나, **발행 자체가 실패해 메시지가 들어가지 않았으므로 lag 0은 오히려 이 후보와
> 정합적이다**"*. 반대로 후보 3에서는 *"lag 0은 '컨슈머가 잘 따라잡았다'와 '메시지가 애초에
> 없었다'를 **구분하지 못한다** — 데이터 부족"* 이라 적었다. **같은 0을 두 방향에서 읽었다.**

---

### 3) 탐색 — 15 / 15 · ⚠️ 앵커 예시 미수행

**채점 기준**

| 점수 | 요건 |
|---|---|
| 15 | **S = producer span error** + B(**정상 구간 producer span과 대조** — 정상 발행이 몇 ms인지) |

**이번 리포트**

| 요건 | 판정 | 근거 |
|---|---|---|
| **S** producer span error | **충족** | INC-7 = *"04:48:35~04:49:35, root span 미수신 상태의 **60,021ms error 트레이스**"* 를 후보로 세우고 **선택**. 분석이 그것을 1순위 근거로 인용 |
| **S** 메트릭 채널 | 문언 이상 | INC-5(`up` 1→0, 0 구간 명시) · INC-8(`kafka_brokers` 1→0)을 **둘 다** 선택하고 *"동일 장애의 다른 지문이라 함께 선택"* 이라 사유를 적음 |
| **T** 시간창 | **충족** | 창 미지정 → *"최근 1시간"* EXACT → 좁힌 창 04:25~05:04:55Z가 주입 04:47:20~04:50:20을 **포함** |
| **B** 대조군 | **충족(부재 신호)** | lag 0의 이중 해석 · DLQ 0/-1 · 컨슈머 엣지 부재 · *"exporter 두 시리즈로는 분리 안 됨"* 을 근거의 한계로 명시 |
| 후보 처리 | 문언 이상 | 8건 중 2건을 **기각 사유와 함께** 버림 — INC-1(chat, IN-1 잔여) *"Kafka 단절 창이 아닌 04:30~04:40대이고 규모도 작아"*, INC-3(`/feeds/scroll` 16초) *"알림 발행·전달 경로와 엔드포인트가 다르고"* |

**→ 15점.**

> ⚠️ **앵커가 예시로 든 B(정상 구간 producer span 3ms와 대조)는 이번에도 안 했다 — 네 회차 연속이다.**
> 창에 정상 발행이 **한 건도 수집되지 않았다**(호출 그래프의 `kafka/user.notifications` publish가 1회뿐).
> 리포트는 그것을 **복구 확인 절차로 미뤘다** — *"신규 댓글 1건을 테스트로 작성해 `publish
> user.notifications` span이 `STATUS_CODE_OK` / **수십 ms 내** 종료되는지 확인(장애 시 60,016ms·ERROR였다)"*.
> rubric §3이 B를 *"정상 구간 대조 **또는** 부재 신호 활용"* 으로 규정하므로 감점하지 않는다.

> 🔴 **대표 `traceId`가 또 다른 Incident 것이다** — 후단에 넘어간 것은 `6a740e7c…`
> (**IN-1의 스케줄러 트레이스**)이고, 정답인 60초 발행 트레이스 `6a741223…`가 아니다.
> INC-7이 선택돼 수집은 됐고 분석이 자력으로 골라 써서 감점이 없었다.
> **CH-3 회차 5에 이어 세 번째 발현** → B-20 우선순위 유지(아래 ⓑ).

---

### 4) 영향 판정 — 10 / 10

**채점 기준**

| 점수 | 요건 |
|---|---|
| 10 | **알림만 10분 내 미도착 + 복구 불가**(재시도 경로 없음) + **API는 정상** |
| 5 | 미도착은 맞으나 복구 가능성 미판단 |
| 0 | "지연 도착"으로 오판 · 또는 "장애 없음" |

**이번 리포트**

| 요건 | 판정 | 리포트가 쓴 것 |
|---|---|---|
| 알림만 미도착 | **충족** | *"댓글은 저장(200 응답), **알림만 소실** → 제보 증상과 정확히 일치"* |
| **복구 불가(재시도 경로 없음)** | **충족** | *"**자동 복구는 불가**: DLQ(`user.notifications.dlq`) lag이 전 구간 0/-1로 실패 메시지가 **적재되지 않았다**. 예외가 `NotificationEventListener`에서 **로깅만 되고 소실**됐다"* |
| API는 정상 | **충족** | 200 · `outcome=SUCCESS` · 59.9ms · 커밋 확인 |
| 지연 도착 오판 | **회피** | 최종 상태를 *"**실패 후 폐기**"* 로 단정 |

**→ 10점.** 5점 문언(*"복구 가능성 미판단"*)과 갈리는 지점을 넘어, **복구 범위와 방법까지** 갔다 —
*"`tb_battle_item_comment`에서 `created_at BETWEEN 04:47:15Z AND 04:50:15Z` 범위를 조회 → 각 댓글의
수신 대상에게 `user.notifications` 재발행"*.

> **그러면서 범위의 한계를 스스로 못박았다** — *"관측된 소실 알림은 **1건(userId=7)뿐**이다.
> 다운 구간 3분 동안의 총 소실 건수는 트레이스 10건 샘플만으로는 **판정 불가**"*, 그리고
> *"복구 범위 확정 전 선행 필요: 04:47~04:50 구간의 전체 발행 실패 건수를 로그(`알림 발행 실패` 카운트)로
> 먼저 집계할 것"*. **CH-1과 갈리는 지점**(그쪽은 10분 내 도착)을 정확히 반대편에서 짚었다.

---

### 5) 오귀인 — 3 / 5 · **판정**

**채점 기준**

| 점수 | 요건 |
|---|---|
| 5 | 200을 "정상"으로 오판 안 함 + **chat 미지목** |
| 3 | **후보로만** |
| 0 | chat 확정 |

**이번 리포트**

| 요건 | 판정 | 리포트가 쓴 것 |
|---|---|---|
| 200을 "정상"으로 오판 안 함 | **충족** | 200·`SUCCESS`·59.9ms를 인용하면서 **그 안의 비동기 span이 60초 실패**임을 갈랐다. *"댓글은 저장, 알림만 소실"* |
| chat 확정 | **없음** | chat-service를 원인으로 지목하지 않음. 탐색에서 INC-1(chat ERROR/WARN)을 **명시 기각** |
| **컨슈머를 랭킹에 유지** | **부분** | 후보 3 *"컨슈머(`notification-processors`) 적체·처리 실패 — **데이터상 반증됨, 사실상 배제**"*, 확신도 **낮음** |

**→ 3점.** 5점 문언(*"chat 미지목"*)과 갈리는 지점이 **랭킹에 남겼는가**인데, 제목에 배제를 박고
확신도를 낮음으로 두었어도 **원인 후보 랭킹 3번**에 있다.

> **판정 — 반대로 읽으면 5점(총 100).** 지목이 아니라 **배제 목적의 기재**이기 때문이다.
>
> **채점자는 전례를 따랐다.** 같은 배치가 IN-2에서 두 번 3점이었다 —
> 회차 2 실행 2(*"랭킹 3번 + 배제 목적으로 기재"*)와 회차 3 실행 1. 반대로 회차 3 실행 2는
> **chat을 랭킹이 아니라 "데이터 부족" 절에** 두어 5점을 받았다.
> **판단은 세 실행 모두 같았고 갈린 것은 배치 형식뿐**이다.
>
> 🔴 **이 항목이 IN-2에서 다섯 번째로 갈렸다.** 앵커 5)의 구간에 *"후보로 올린 뒤 자력 반증"* 의
> 자리가 없는 것이 원인이고, 이미 [채점 대장](../scoring/README.md)에 결함으로 등재돼 있다.
> **앵커 개정 후보로 승격한다**(아래 ⓒ) — 배치 형식이 점수를 정하는 것은 능력을 재는 것이 아니다.

---

### 6) 조치 — 5 / 5

**채점 기준**

| 점수 | 요건 |
|---|---|
| 5 | Kafka 복구 + **미도착분 복구 불가 인지** |
| 3 | 복구만 |

**이번 리포트**

| 요건 | 판정 | 리포트가 쓴 것 |
|---|---|---|
| Kafka 복구 | **충족** | 복구 확인 1(`up{job=kafka}`·`kafka_brokers` 1 유지) + 재발 방지 3(*"`up{job=kafka} == 0` 및 `absent(kafka_brokers)` 1분 지속 시 페이지"*) |
| **미도착분 복구 불가 인지** | **충족** | *"**자동 복구는 불가**"* 를 절 제목 수준으로 명시하고 사유(DLQ 미적재 · 예외 삼킴)까지 |
| 근본 처방 | 문언 이상 | 1 *"**발행 실패를 삼키지 말 것** — 실패 시 아웃박스 적재 또는 `user.notifications.dlq` 강제 produce로 전환(**DLQ 토픽과 `notification-recovery` 컨슈머 그룹은 이미 존재하는데 사용되지 않고 있다**)"* · 2 *"`max.block.ms` 60초는 과다 — 브로커 다운 시 알림 스레드가 요청당 60초 점유"* |

**→ 5점.** 3점 문언(*"복구만"*)과 갈리는 지점이 **복구 불가 인지**인데, 그것을 짚은 뒤
**왜 불가인지**(DLQ 경로가 존재하는데 안 쓰인다)까지 관측으로 확인했다.

> `absent(kafka_brokers)`를 알람식으로 제안한 것은 관측 기록의
> *"`kafka_brokers`는 다운 시 0이 아니라 **부재** — `kafka_brokers < 1` 류 알람은 영원히 안 울린다"*
> (2026-07-26)와 **정확히 같은 처방**이다. 그 문서를 본 적 없는 상태에서 나왔다.

---

## 회차 대조 — 다섯 항목은 굳었고 5) 오귀인만 흔들린다

| # | 항목 | 2 실행1 | 2 실행2 | 3 실행1 | 3 실행2 | 4 실행A | 4 실행B | **5** |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| 1 | 원인 | 40 | 40 | 40 | 40 | **30** | 40 | **40** |
| 2 | 근거 | 25 | 25 | 25 | 25 | 25 | 25 | **25** |
| 3 | 탐색 | 15 | 15 | 15 | 15 | 15 | 15 | **15** |
| 4 | 영향 | 10 | 10 | 10 | 10 | 10 | 10 | **10** |
| 5 | 오귀인 | 5 | **3** | **3** | 5 | 5 | 5 | **3** |
| 6 | 조치 | 5 | 5 | **3** | 5 | 5 | 5 | **5** |
| | **합계** | **100** | **98** | **96** | **100** | **90** | **100** | **98** |

**일곱 실행에서 2)·3)·4)가 한 번도 안 움직였다.** 1)이 흔들린 것은 회차 4 실행 A 한 번뿐이고
그 원인은 **프롬프트 변경(C-6)** 으로 귀속이 닫혔다. **5)는 다섯 번째 진동**이고 전부
*"컨슈머/chat을 어디에 적었는가"* 라는 **배치 형식**이다.

**⚠️ 회차 2·3·4와 평균 내지 않는다.** 도구 세대(B-34 로그 접기 · B-35 트레이스 압축)와
관측 스택(스크레이프 30 → 10초)이 다르다.

---

## 토큰 (§2)

| 단계 | 총 `in` | overhead | **컨텍스트 토큰** | 표기 |
|---|---:|---:|---:|---|
| 탐색 | 33,264 | 29,913 | **3,351** | **`█ 실측`** |
| 분석 | 96,532 | 29,913 | **66,619** | **`█ 실측`** |
| **합계** | 129,796 | | **69,970** | |

- `overheadTokens` **29,913** — 이 회차 프로브 실측.
- 🔴 **같은 날 네 회차의 `C` 산포가 832 tok(2.8%)로 이번이 최대다** —
  CH-3 **29,081** → IN-1 **29,401** → CH-2 **29,426** → IN-2 **29,913**.
  프로브 자체 오차 ±320 tok(1.5%)을 **2.6배 넘는다.** §2.2의 *"상수가 아니다"* 가
  **하루 안에서도** 성립함을 네 표본으로 확인한 첫 사례다. **회차마다 재는 것 외에 방법이 없다.**
- `num_turns=1` · `llmModel=claude-opus-5`.
- 수집 원본: trace 77,179B / 78 spans(창 안 후보 10건) · **logs errwarn 149,739B** ·
  traceId 11,938B · **metrics 244,339B**(창 40분). 분석 컨텍스트 **119,031자**.
- 비용 $1.1429는 **API 환산 추정치**다.

> **창이 40분으로 이번 네 회차 중 가장 넓다**(CH-2 20분 · CH-3 22.3분 · IN-1 20분).
> 후보를 6개 선택해 합집합이 벌어진 결과이고, 메트릭이 244KB로 따라 올랐다.
> **CH-1 회차 3에서 확인된 "후보 다선택 → 창 확대 → 컨텍스트 폭증" 경로와 같은 모양**이지만,
> 이번엔 후보 다선택이 **두 주입을 갈라내는 데 실제로 쓰였다.**

---

## 다음 회차로 넘기는 것

### ⓐ 로그 149,739B — B-39 절삭 확인 (IN-1과 동일 · 우선)

IN-1 회차 5(149,710B)과 **거의 같은 크기**다. 두 회차가 같은 창(04:25~)을 공유하므로
**같은 원본으로 한 번에 확인**할 수 있다. `log-limit` 1000줄에 걸렸다면 창 뒤쪽
(= Kafka 복구 구간 04:50~)이 잘렸을 것이고, **이번엔 점수에 영향이 없었지만 CH-1 회차 5의 감점 사유**였다.
**주입 불필요** — `reports/raw/scan-1785990300-*`로 오프라인 확인.

### ⓑ 대표 `traceId` 오배정 — 세 번째 발현 (B-20)

| 회차 | 넘어간 traceId | 정답 traceId |
|---|---|---|
| CH-3 회차 5 | CH-2의 515,150ms | Mongo 24,073ms |
| **IN-2 회차 5** | **IN-1의 스케줄러 2,003ms** | **Kafka 발행 60,021ms** |

두 번 다 분석이 자력으로 정답 트레이스를 찾아 써서 감점이 없었다. **그러나 AU-2 회차 2
실행 2는 같은 구조에서 −22점이 났다.** → [round-3 §4.1-b B-20](../round-3/README.md) 우선순위 유지.

### ⓒ 🔴 앵커 5) 개정 승격 — "배제 목적의 기재"에 자리를 준다

IN-2 5) 오귀인이 **일곱 실행 중 다섯 번째로 갈렸고, 갈린 이유가 전부 배치 형식**이다.

| 배치 | 점수 | 회차 |
|---|---:|---|
| 랭킹 밖("배제된 후보"·"데이터 부족" 절) | **5** | 2 실행1 · 3 실행2 · 4 실행A · 4 실행B |
| **랭킹 안 + 배제 명시** | **3** | 2 실행2 · 3 실행1 · **5** |

**판단은 일곱 실행 모두 같았다** — chat/컨슈머를 원인으로 지목한 적이 없다.
**앵커 5)의 3점 구간(*"후보로만"*)이 "지목했으나 확신 없음"과 "배제하려고 적었음"을 구분하지 못한다.**

- 개정안: 만점 요건에 *"**또는 후보로 올린 뒤 자력 반증**"* 을 추가하고, 3점을
  *"후보로 올리되 **반증 근거 없이** 남김"* 으로 좁힌다.
- **적용은 다음 회차부터**(§8.2). 이번 채점은 현행 문언으로 했다.
- ⚠️ **점수를 올리려는 개정이 아니다** — 이 구간이 **다섯 번 진동해 §8.1의 N=2 판정을 좌우해 왔다**.
  능력이 아니라 서술 형식을 재고 있다.

### ⓓ 하루 안에서 `C` 산포 832 tok — §2.2 재확인 (문서 갱신)

같은 날 같은 CLI·모델로 네 번 쟀는데 **29,081 / 29,401 / 29,426 / 29,913** 이 나왔다.
**최대 산포 832 tok(2.8%)로 프로브 오차 ±320의 2.6배**다.
[round-1-input-tokens §2.2](../round-1-input-tokens.md)가 *"하루 만에 −20% 움직였다"* 로 적어 둔 것을
**같은 날 네 표본으로 재확인**한 첫 사례이므로 그 문서에 표본을 추가한다.
**실무 결론은 변함없다 — 회차마다 재고, 다른 회차의 값을 가져다 쓰지 않는다.**

### ⓔ 잔여 — B-13 401 쿼리 결측 다섯 회차 연속

이번에도 no series. 결론에 영향 없었고 리포트도 *"이를 지지하는 다른 근거도 없음"* 으로 정리했다.
