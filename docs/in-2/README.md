# IN-2 — Kafka 다운 × 알림 발행 경로 (회차별 기록)

toy-content `docs/chaos/` RUNBOOK의 IN-2 문항. CH-1(소비측 장애)과 대비쌍 — 같은 "알림 안 와요" 증상이 발행측에서 나면 **재시도·DLQ가 받아줄 수 없어 조용한 영구 유실**이 된다는 걸 실측하는 문항이다. 문항 전체 결과 종합은 toy-content `docs/chaos/RESULTS.md`, 폴더 구성 방식은 [`../ch-1/README.md`](../ch-1/README.md)와 동일.

## 회차 인덱스

| 회차 | 일시(UTC) | 입력 | 다운 | 결말 | RCA 조사 | 문서 |
|---|---|---|---|---|---|---|
| 1 (재실험) | 07-27 15:45:58~15:53:19 (7분 21초) | traceId 직접 | Kafka `docker stop` | 트리거 API 200, 알림 1건 영구 유실 (재시도·outbox 없음) · CLI 격리 하 조사 | O | [round-1.md](round-1.md) |
| 2 (실행 1) | 07-29 06:27:57~06:31:04 (3분 7초) | **자연어** | 동일 | 동일 — 댓글 200(`generated-keys=173`), `userId=7` 알림 1건 영구 유실 | O | [round-2.md](round-2.md) |
| 2 (**실행 2**) | 07-29 12:45:48~12:48:52 (3분 4초) | 자연어 (**같은 문안**) | 동일 | 동일 — 댓글 200(`commentId=212`), `userId=7` 알림 1건 영구 유실 | O | [round-2-run2.md](round-2-run2.md) |
| 3 (실행 1) | 08-04 **00:48:06~00:51:06 · 01:20:36~01:23:36** (한 창에 2회) | 자연어 (**같은 문안**) | 동일 | 동일 — 댓글 200(`commentId=254`·`256`), `userId=7` 알림 **2건** 영구 유실 | O | [round-3.md](round-3.md) |
| 3 (**실행 2**) | 동일 데이터 재조사 (**창 고정**) | 자연어 (**같은 문안**) + `from`/`to` | 동일 | 동일 | O | [round-3.md](round-3.md) |
| **5** | **08-06 04:47:20~04:50:20 (정확히 3분)** | 자연어 (**같은 문안**) | 동일 | 동일 — 댓글 200(`commentId=327`), `userId=7` 알림 1건 영구 유실 · **창에 IN-1 Redis 주입이 함께 있었다** | O | [round-5.md](round-5.md) |

> ## 🔴 회차 5 (2026-08-06): **98 / 100** · 창에 주입이 둘인데 서로의 무고함까지 증명했다
>
> [round-5.md](round-5.md). **한 창에 IN-1 Redis(04:32:5x~04:36)와 IN-2 Kafka(04:47:20~04:50:20)가
> 같이 있었다.** 리포트는 후보 1/후보 2로 갈랐을 뿐 아니라 **문제의 트레이스 안에서 다른 후보의
> 정상성을 관측**했다 — *"04:48:35 댓글 트레이스에서는 `[user-cache] 캐시 HIT - userId: 1, elapsed: 4ms`,
> redis `GET` span **0.628ms**로 정상 → **후보 2는 후보 1의 원인이 아니다**"*.
> **두 장애가 겹칠 때 시각 분리로 끝내지 않고 내부 관측으로 닫은 것은 이 프로젝트에서 처음이다.**
>
> 🔴 **"토픽 부재" 함정을 피했다** — 지문이 축자로 `Topic user.notifications **not present in metadata**
> after 60000 ms` 인데 **이것이 회차 1과 회차 4 실행 A를 오답으로 끌고 간 그 문구**다.
> 이번엔 1순위를 *"브로커 다운"* 으로 세우고 탐색 단계에서 이미
> *"`kafka_consumergroup_lag` 이상 0건은 **정상 근거로 쓰지 않음** — 브로커가 죽은 구간에는
> lag 메트릭 자체가 수집되지 않아 0으로 보일 수 있음"* 이라 못박았다.
> **자기 근거의 약점도 스스로 짚고**(*"`up{job=kafka}`·`kafka_brokers`는 동일 exporter라
> 브로커 다운인지 exporter 다운인지 분리되지 않는다"*) 클라이언트 지문으로 독립적으로 닫았다.
>
> | # | 항목 | 2 실1 | 2 실2 | 3 실1 | 3 실2 | 4 A | 4 B | **5** |
> |---|---|---:|---:|---:|---:|---:|---:|---:|
> | 1 | 원인 | 40 | 40 | 40 | 40 | **30** | 40 | **40** |
> | 2 | 근거 | 25 | 25 | 25 | 25 | 25 | 25 | **25** |
> | 3 | 탐색 | 15 | 15 | 15 | 15 | 15 | 15 | **15** |
> | 4 | 영향 | 10 | 10 | 10 | 10 | 10 | 10 | **10** |
> | 5 | 오귀인 | 5 | **3** | **3** | 5 | 5 | 5 | **3** |
> | 6 | 조치 | 5 | 5 | **3** | 5 | 5 | 5 | **5** |
> | | 합계 | 100 | 98 | 96 | 100 | 90 | 100 | **98** |
>
> **일곱 실행에서 2)·3)·4)가 한 번도 안 움직였다.** 1)이 흔들린 것은 회차 4 실행 A(C-6) 한 번뿐이다.
> 🔴 **5) 오귀인은 다섯 번째 진동이고 전부 배치 형식이다** — 랭킹 밖에 적으면 5, **랭킹 안에
> 배제 명시로 적으면 3**. **판단은 일곱 실행 모두 같았다**(chat/컨슈머를 원인으로 지목한 적이 없다).
> **앵커 개정 승격** — 만점에 *"또는 후보로 올린 뒤 자력 반증"* 을 넣고 3점을 *"반증 근거 없이 남김"* 으로
> 좁힌다. 반대 판정 시 **100**.
>
> 부수: 대표 traceId가 **또 다른 Incident 것**(IN-1 스케줄러 2,003ms ↔ 정답 Kafka 60,021ms) — **B-20 세 번째** ·
> 앵커 예시 B(정상 producer span 대조)는 **네 회차 연속 미수행**(창에 정상 발행 0건) ·
> 🔴 같은 날 `C` 산포 **832 tok(2.8%)** 로 최대(29,081 / 29,401 / 29,426 / **29,913**).
> ⚠️ **회차 2·3·4와 평균 내지 않는다**(도구 세대 · 관측 스택 상이).

> ## 🔴 회차 4 (2026-08-04): **90 → 100** · 고치려던 규칙이 다른 자리를 깎았다
>
> **프롬프트만 바꿨다.** 코드·수집·창 무변경, 회차 3 실행 2와 **같은 관측**을 창 고정 재조사.
>
> | # | 항목 | 회차3 실1 | 회차3 실2 | **실행 A**<br>(C-4+5+6) | **실행 B**<br>(C-4+5) |
> |---|---|---:|---:|---:|---:|
> | 1 | 원인 | 40 | 40 | **30** 🔴 | **40** ✅ |
> | 5 | 오귀인 | **3** | 5 | **5** ✅ | **5** |
> | 6 | 조치 | **3** | 5 | **5** ✅ | **5** |
> | | 합계 | 96 | 100 | **90** | **100** |
>
> **C-5·C-6는 겨냥한 자리를 열었는데(5)·6) 각 3→5), C-6가 건드리지 않기로 한 1)을 −10 시켰다.**
> 1순위가 *"토픽 부재"* 로 뒤집혔고 — **IN-2 회차 1의 실패 모드** — 그건 채점자 실측으로
> **반증된 주장**이다(그 로그 2줄은 브로커 재기동 순간 · 주입 84초 전 같은 토픽 3ms 발행 성공 ·
> lag 파티션 0~5 전 구간 존재).
>
> **기전**: C-6가 반증된 후보를 랭킹에서 빼자 **빈 칸을 경쟁 가설이 아니라 인과 사슬 분해로
> 채웠고**, 확신도 순 정렬이 *로그에 문구가 직접 찍힌 틀린 쪽*을 1위로 올렸다.
> **1)은 직전 다섯 실행 전부 40이라 분산이 아니다.**
>
> **C-6 한 줄만 빼서 확인했다 — 1)이 40으로 복귀하고 100이 됐다(실행 B).**
> 그리고 **5)는 C-6 없이도 5점**이다 → C-6는 얻는 것 없이 −10만 만든 변경이라 **폐기 확정**.
>
> 가장 얄궂은 부분: 리포트가 **감별을 후보 2 본문에 써 놓고 랭킹만 반대로 놓았다** —
> 이 회차가 고치려던 문제와 정확히 같은 모양이다. → [round-4.md](round-4.md)

> ## 🔴 회차 3 (2026-08-04): **96 / 100 → 평균 98 ± 2 · §8.1 인용 가능**
>
> **아무것도 안 고쳤는데 4점이 움직였다.** 실행 1에서 못 받은 4점은 **5) 오귀인 2 + 6) 조치 2**로
> 둘 다 판정 항목이었고, **변경 없이 다시 돌리자 둘 다 만점**이 됐다.
> 입력은 사실상 동일하다 — 컨텍스트 **374,186자 ↔ 374,106자**, 같은 창·모델·후보 선택.
> **바뀐 것은 샘플링뿐이다.**
>
> | 항목 | 회차2 실1 | 회차2 실2 | 회차3 실1 | 회차3 실2 | 편차 |
> |---|---:|---:|---:|---:|---:|
> | 1)원인 · 2)근거 · 3)탐색 · 4)영향 | 40·25·15·10 | 40·25·15·10 | 40·25·15·10 | 40·25·15·10 | **0** |
> | **5) 오귀인** | **5** | **3** | **3** | **5** | **2** |
> | **6) 조치** | **5** | **5** | **3** | **5** | **2** |
> | 합계 | 100 | 98 | **96** | **100** | — |
>
> **네 실행에서 1)~4) 편차가 0이다.** 흔들리는 것은 배점 5점짜리 두 항목뿐이고 둘 다
> *"판단은 맞는데 어디에 썼는가"* 를 잰다. **측정되는 능력은 안정적이고 흔들리는 것은 형식이다.**
>
> **한 창에 같은 장애가 두 번 들어왔고 두 실행 다 한 원인으로 묶었다** — *"실패한 두 트레이스의
> publish 구간이 각각 `up{job=kafka}`=0 두 구간에 **정확히 포함**됨"*.
> 창 앞부분의 IN-1 Redis 잔여는 실행 1이 *"같은 IP 공통 호스트"* 로 상위 원인 후보에 세운 뒤
> 자력 반증했고, 실행 2는 **시각 대조로 "별개 장애"라고 잘라냈다**(00:44 ↔ 00:49/01:21).
>
> **브로커 다운 확정 N=4 · IN-2↔CH-1 영향 변별 N=4 · 오염된 창 자력 분리 N=3.**
>
> ⚠️ **회차 2(99 ± 1)와 총점을 나란히 두지 않는다** — 도구가 8커밋 바뀌었고 관측 조건도 다르다
> (창 안 주입 2건 + IN-1 잔여). 실행 2는 창 고정이라 **3) 탐색의 T가 미시험**이다.
>
> 🔴 **결함 ① (실행 1) — 반증된 주장**: *"토픽 자체가 없음"* 의 근거 로그 2줄은 **브로커
> 재기동 순간(01:23:39)의 과도 상태**이고, 주입 84초 전 같은 토픽으로 3ms 발행·소비가
> 정상 성립했다(채점자 실측). 조치 2건이 헛짚었다. **신호 부재가 아니다** — 복구는
> `up` 0→1(01:24:06Z) · `kafka_brokers` 재개(01:24:11) · 탐색 후보 `up 0 → 1` 로 세 채널에
> 다 찍혔고 메트릭 쪽은 리포트가 인용까지 했다. **채널 간 시각 대조 부재**가 기전이고,
> **실행 2는 같은 로그에 자력 반증을 붙였다**(lag 시리즈 존재 → 토픽 소멸 아님).
>
> 🔵 **결함 ② (실행 2) — 타임아웃 설정 오명명**: 60초를 `delivery.timeout.ms`로 지칭했다.
> 실제는 `max.block.ms`이고 **실행 1은 맞게 썼다.** 두 실행이 서로의 결함을 하나씩 갖고 있다.
>
> 🔵 **결함 ③ (공통) — 탐색 후보 INC-6의 0 구간이 폭 0**(`01:24:18Z ~ 01:24:18Z`, 실제는
> `01:20:36~01:23:36Z`). 5분 샘플링 해상도 탓이고 **두 실행 다 분석이 원본에서 자력 정정**했다.
> 후보만 보는 구성에서는 2차 다운이 사라진다 → 변경군 B.

> **회차 2 §8.1 성립 (2026-07-29): 100 / 98 → 평균 99 ± 1 · 인용 가능.**
> 다섯 항목 편차 0이고 갈린 하나가 **5) 오귀인**인데, 점수가 아니라 **후보 배치 형식**에서
> 갈렸다 — 실행 1은 chat을 "배제된 후보(참고)" 절에, 실행 2는 랭킹 3번에
> *"지지 근거 약함, 배제 목적으로 기재"* 로 올렸다. 판단 내용은 같다.
>
> **실행 2는 더 어려운 조건이었다** — 탐색 창(12:02~13:02Z)에 주입이 **셋**(CH-2 · CH-3 · IN-2)
> 이고 그중 CH-2는 **같은 사용자 증상**을 낸다(lag 25 적체). 좁힌 창을 12:44부터 잡아
> 둘을 잘라내고 IN-2만 남겼다. **AU-2 실행 2(같은 오염 조건에서 오답 선정, −22점)의 대조 사례.**

> **회차 2 실행 1 결과: 100 / 100** — anchors-v2 · 루브릭 v3 6항목. 채점자 판정 **0건**.
>
> **🔴 회차 1의 오선정이 해소됐다.** IN-2는 구 회차·재실험 **둘 다** "토픽 부재"를 "브로커 다운"
> 위에 1순위로 올려 *"하위 원인 감별 실패(N=2 재현)"* 로 기록된 문항이다.
> **회차 2는 1순위를 "Kafka 브로커 단일 노드 다운"으로 확정**했고, 감별 근거로
> **`up{job="kafka"}` 0 전환 + `kafka_brokers` 시계열 결손**을 인용했다.
>
> **[round-2 대기열](../round-2/README.md)의 예측이 문자 그대로 적중했다** —
> *"근본 20 → 40. **B-3의 `kafka_brokers` 시계열 부재**가 '토픽 부재 vs 브로커 다운' 감별의 결정타"*.
> AP-1·AP-2의 델타 예측이 연속 반증된 뒤 **처음으로 예측한 메커니즘이 그대로 작동했다.**
>
> ⚠️ **총점 80 → 100은 개선으로 인용하지 않는다**(자가 v1 4항목 → v3 6항목).
> 인용 가능한 것은 **메커니즘 확인**이다 — 점수 축이 아니라 행동 축.

> 구 회차 1(07-26 09:14Z, 5분 17초, traceId `6a65d039…`)은 CLI cwd 격리 도입(07-27) 전 조사라
> 오염 가능성으로 폐기하고 위 재실험으로 덮어썼다. 사유·격리 확인은 [round-1.md](round-1.md) 상단 참조.

## 스크린샷 공통 팁

- Grafana 시간대는 KST(UTC+9). 주입 창 = **KST 07-28 00:45:58 ~ 00:53:19**. 트리거 = **00:51:53 KST**.
- 이 문항의 그림 세 장: ① Tempo 장애 트레이스(`6a677e99…` 60,020ms error span + chat 부재), ② Loki `알림 발행 실패` ERROR + producer WARN 스팸, ③ `kafka_brokers` 그래프의 **선이 끊기는 구간**(0이 아니라 부재 — 그 공백 자체가 스샷 포인트).
- **유실 vs 지연 갈래 주의**: 다운이 5분(메타데이터 만료)을 못 넘으면 producer가 버퍼에 쥐고 있다 복구 후 발행 성공(지연 도착). 유실은 트리거 후 60초+ 다운 유지 + 5분+ 경과라야 난다.
