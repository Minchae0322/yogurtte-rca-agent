# CH-1 회차 2 · **2차 실행** — **95 / 100** · 🏆 **§8.1 충족 (평균 97.5 ± 2.5)**

> **[round-2.md](round-2.md)(실행 1, 100점)와 짝이다.** 문안·자·입력 모델·도구가 전부 같고
> **주입만 다르다**(04:58Z ↔ 11:54Z, 약 7시간 간격). AP-1 쌍과 같은 성격 —
> **실행 간 분산을 실제로 잰** 표본이지 같은 데이터 재조사가 아니다.
>
> 🔴 **갈린 항목은 4) 영향 하나뿐이고, 그것이 실행 1에서 ⚠️ 채점자 판정이었던 바로 그 항목이다.**

## 한눈 요약

| | |
|---|---|
| **실제 원인** | `mongodb_up`=0 **11:54:40~11:59:10Z**(약 5분) → 컨슈머 재시도 **4회 × ~31초** 소진 → **DLQ 적재**(11:57:16Z) → 복구 후 **재처리 성공 11:59:16.9Z**. **지연 약 4분, 유실 0** |
| **입력** | traceId 없음. 자연어 한 줄 — *"최근 1시간 안에 댓글 알림이 안 왔다는 제보가 있어요. 확인해줘"* (**실행 1과 문자 그대로 동일**) |
| **탐색 결과** | 스윕 11:04:31~12:04:31Z → **좁힌 창 11:53:00~12:04:31Z** · 대상 **`chat-service` + `content-service`** · traceId `6a69ea2063240c0918077d8cbd8c859c` **자력 선정(후보 8건 중 Top-1)** |
| **에이전트 파악 원인** | 1순위(확신도 **높음**) *"MongoDB(mongod) 프로세스 다운 → chat-service 알림 문서 저장 실패"*. 2순위(높음) *"재시도 창(약 2분) < 장애 지속(약 5분) → DLQ 이관"*. 3순위(**낮음**) *"DLQ 재처리 이후 최종 발송 도달 여부 미확인 — 데이터 부족"* |
| **§8 채점** | **95 / 100** — 원인 40 · 근거 25 · 탐색 15 · **영향 5** · 오귀인 5 · 조치 5 |
| **모델** | `claude-opus-5` · `llmTurns=1` · 라벨 정상 |
| **비용·시간** | 탐색 $0.4586 · 67.1s + 분석 $1.4262 · 173.1s = **$1.8848 · E2E 240.3s** |
| **리포트 전문** | [round-2b-rca-report.md](round-2b-rca-report.md) |

> ### 🔴 이 회차의 핵심 — **수집 채널이 실패 절반만 주는데도 유실 단정을 거부했다**
>
> 도착 근거인 `[Kafka] DLQ 알림 재처리 성공`(11:59:16.9Z)은 **INFO · `traceId=NONE`** 이라
> `errorWarnQuery`(`ERROR|WARN`)에도 `traceIdQuery`에도 안 걸린다. **채점자가 수집 원본을
> 직접 검사해 확인했다** — `재처리 실패`는 1건 들어왔고 **`재처리 성공`은 0건**이다.
>
> **즉 에이전트는 DLQ 이야기의 실패 절반만 받았다.** 그런데도 *"영구 유실이라고 단정할 수
> 없다"* 로 정확히 멈췄다(근거: DLQ lag가 11:59:30Z에 `0` 복귀). **결함 11/B-16이 미수정인
> 상태에서 나온 감점 5점**이고, 신호의 부재는 채점자가 실측으로 확인했다.

---

# 1. 주입 실측 (채점자 · 에이전트 비노출)

| 시각(UTC) | 사건 | 근거 |
|---|---|---|
| 11:54:40 | `mongodb_up` **0** (첫 0 샘플) | Mimir step 30s |
| 11:55:42 | 재시도 **1회차** 실패 `topic=user.notifications partition=3 offset=1004` | Loki |
| 11:56:13 / 11:56:44 / 11:57:16 | **2·3·4회차** — 간격 **~31초** | Loki |
| **11:57:16.011** | **`[KAFKA-DLQ] 발행: user.notifications -> user.notifications.dlq (partition=3 offset=1004)`** cause `MongoTimeoutException … Connection refused` | Loki (ERROR) |
| 11:57:17.108 | `[KAFKA-RETRY] 재시도 소진 - recoverer 처리 완료` | Loki (ERROR) |
| 11:57:47 | `DLQ 알림 재처리 실패 (1분 후 재시도)` | Loki (ERROR/WARN) |
| 11:58:47 | DLQ 재처리 재시도 | Loki (**INFO**) |
| **11:59:16.923** | **`[Kafka] DLQ 알림 재처리 성공: userId=7, type=BATTLE_ITEM_COMMENT`** | Loki (**INFO · traceId=NONE**) |
| 11:59:10 | `mongodb_up` 마지막 0 샘플 → 복구 | Mimir |

**재시도 간격 ~31초 = `serverSelectionTimeoutMS` 30.0s + 오버헤드.** 앵커 2)의
*"각 30.0s를 `serverSelectionTimeoutMS`와 대조"* 요건이 실측으로 성립한다.

**갈래 B 확정** — 다운 ≥5분이라 재시도 창(약 2분)을 넘겼고 DLQ를 경유했다.

---

# 2. 채점 — 앵커 v2 (anchors-v2.md CH-1 갈래 B · 루브릭 v3 6항목)

| # | 항목 | 배점 | 점수 | 한 줄 |
|---|---|---:|---:|---|
| 1 | 원인 적중 | 40 | **40** | Mongo 다운 → 저장 실패 → 재시도 소진 → DLQ 적재까지 메커니즘 전부 |
| 2 | 근거 | 25 | **25** | 예외 원문 + 30.0s 대조 + **DLQ producer span 동일 트레이스** + Kafka·발행측 배제 |
| 3 | 탐색 | 15 | **15** | 주입 트레이스 **Top-1 자력 선정** · 창이 주입~복구~도착 전부 포함 |
| 4 | 영향 판정 | 10 | **5** | 유실은 **부정**했으나 **도착을 확정하지 못했다** — 도착 신호가 수집 채널 밖 |
| 5 | 오귀인 | 5 | **5** | Kafka·풀·GC를 수치로 배제 · 댓글 200을 "장애 없음"으로 오판 안 함 |
| 6 | 조치 | 5 | **5** | DLQ 적재분 수동 재처리 + 재발 방지 5건 |
| | **합계** | **100** | **95** | 감점 5 전액이 4) 영향 |

---

### 1) 원인 적중 — **40 / 40**

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **40** | Mongo 다운 → 컨슈머 실패 → 재시도 소진 → **DLQ 적재**까지 메커니즘 |
| 30 | Mongo 다운 확정, **DLQ 경로 미언급** |
| 20 | "chat이 이상하다"까지 |
| 0 | Kafka·content 지목 |

**이번 리포트**

| 요건 | | 리포트가 쓴 것 |
|---|:-:|---|
| Mongo 다운 | ✅ | 1순위 *"MongoDB(mongod) 프로세스 다운"* **확신도 높음** |
| 컨슈머 실패 | ✅ | *"chat-service가 알림을 MongoDB에 저장하는 단계에서 실패"* + `Inserting Document … in collection: user_notifications` 인용 |
| 재시도 소진 | ✅ | *"재시도 창(약 2분) < 장애 지속(약 5분) 불일치"* — 4회 시각 전부 명시 |
| **DLQ 적재** | ✅ | 2순위 표제에 *"DLQ로 이관"* · `[KAFKA-DLQ] 발행` 축자 인용 |

**→ 40점.** 30점 문언과 갈리는 지점이 *"DLQ 경로 미언급"* 인데, DLQ를 **2순위 후보의 표제로
올리고** 발행 로그·producer span·lag 이동까지 붙였다. 요건 이상이다.

**요건 밖 가산은 없지만 기록한다** — *"`Connection refused`는 포트에서 리스닝하는 프로세스가
없음을 뜻한다"* 로 **호스트 장애 ↔ mongod 프로세스 장애**를 갈랐고, 같은 호스트
`172.31.46.124`의 Redis가 같은 시각 0.5ms로 성공한 것을 대조군으로 썼다.

---

### 2) 근거 — **25 / 25**

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **25** | Mongo 예외 → 재시도 N회(각 30.0s를 `serverSelectionTimeoutMS`와 대조) → **DLQ producer span을 한 트레이스로 연결** + Kafka·발행측을 관측으로 배제 |
| 18 | 연결은 했으나 **배제 논증 없음** |
| 10 | DLQ 로그만 |
| 0 | 근거 없음 |

**이번 리포트**

| 요건 | | 리포트가 쓴 것 |
|---|:-:|---|
| Mongo 예외 | ✅ | `Timed out while waiting for a server that matches WritableServerSelector … caused by {java.net.ConnectException: Connection refused}` **축자** |
| 재시도 N회 | ✅ | 4회, 각 시각 명시 (11:55:42 / 11:56:13 / 11:56:44 / 11:57:16) |
| **30.0s ↔ `serverSelectionTimeoutMS`** | ✅ | *"각 시도 정확히 약 30.0초 … = Mongo 서버 선택 타임아웃 30초"* · 조치 6에서 `serverSelectionTimeout` 축소 제안 |
| **DLQ producer span 동일 트레이스** | ✅ | *"span `publish user.notifications.dlq`(SPAN_KIND_PRODUCER)도 11:57:16.030Z에 존재"* + 체인 `publish`(content) → `receive`(chat, partition=3 offset=1004) → `process-notification` `STATUS_CODE_ERROR` |
| Kafka·발행측 배제 | ✅ | `kafka_brokers=1` · `up{job="kafka"}=1` 전 구간 + `[Kafka] 알림 발행 성공: … partition=3, offset=1004` |

**→ 25점.** 18점 문언과 갈리는 지점이 *"배제 논증 없음"* 인데, Kafka를 **메트릭 2종 + 발행
성공 로그**로 배제했고 발행측(content)이 정상임을 span으로 확정했다.

---

### 3) 탐색 — **15 / 15**

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **15** | S = **컨슈머 Mongo 실패 error 트레이스 또는 `mongodb_up`=0 구간** + B(정상 구간 컨슈머 처리 대조) |
| — | T(시간창)는 §3 공통. B는 루브릭 §3상 *"정상 구간 대조 **또는** 부재 신호 활용"* |

**이번 리포트**

| 요건 | | 리포트가 한 것 |
|---|:-:|---|
| T 시간창 | ✅ | 11:53:00~12:04:31Z — 주입(11:54:40) · 복구(11:59:1x) · **도착(11:59:16.9)** 전부 포함 |
| S 대상 선정 | ✅ | traceId `6a69ea2063…` = **DLQ 발행 ERROR 로그의 바로 그 트레이스**. 후보 8건 중 **Top-1 자력 선정** |
| S 서비스 | ✅ | `chat-service` + `content-service` — 소비측·발행측 둘 다 |
| B 대조군 | ✅ | ① lag 시간 대조(partition 3: 11:55:30~11:57:15 `1` → `0`) ② `up{job=mongodb/redis/node-infra}` 전 구간 `1` ③ **부재 신호 활용** |
| 오탐 | ✅ | 후보에 **AU-3 게이트 트레이스**(`6a69e01b…`, 500)가 섞여 있었으나 **고르지 않았다** |

**→ 15점.** 선정 이유가 정확하다 — *"chat-service 스팬만 30초 타임아웃으로 실패하기 시작한
11:55:43Z부터 … 제보된 증상과 시각이 일치하는 유일한 구간"*.

**B에서 실행 1보다 나은 점.** 실행 1은 메트릭 공백을 *"파드 부재"* 로 읽었다가 분석 단계에서
자기반박했는데, 이번 실행은 **애초에 오독하지 않고** 기전까지 붙였다 —
*"액추에이터 헬스 엔드포인트가 30초 블로킹되어 스크랩이 타임아웃된 것과 시간대가 일치"*.

---

### 4) 영향 판정 — **5 / 10** 🔴 **실행 1과 갈린 유일한 항목**

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **10** | **DLQ 경유 재처리로 10분 내 도착** — 제보("안 왔다")를 관측으로 **뒤집음** |
| **5** | 도착 여부 판단 **유보** |
| 0 | **"유실"로 판정** |

**이번 리포트**

| 요건 | | 리포트가 쓴 것 |
|---|:-:|---|
| 유실로 판정했나 | ✅ 아님 | *"**영구 유실이라고 단정할 수 없다.**"* (근거: DLQ lag 11:59:30Z에 `0` 복귀) |
| **도착을 확정했나** | ❌ | 3순위 표제가 *"DLQ 재처리 이후 최종 발송 도달 여부 **미확인**"* · 확신도 **낮음** |
| 도착 시각 | ❌ | 제시 없음. *"성공 로그는 확보되지 않았다"* |
| 유보를 명시했나 | ⚠️ | 조치 1이 *"**실제 유실인지 지연 배달인지 판정**"* 을 요구 — 스스로 미결로 남겼다 |

**→ 5점.** 10점 문언과 갈리는 지점은 **"도착"의 확정**이다. 실행 1은 도착 시각을
**≈05:04:17로 추정(오차 2초)** 해 제보를 뒤집었지만, 이번 실행은 *"오프셋 커밋 ≠ 알림 발송
성공"* 이라며 **명시적으로 유보**했다. 5점 문언(*"도착 여부 판단 유보"*)에 정확히 해당한다.

> **⚠️ 판정 아님 — 이번엔 문언이 명확하다.** 실행 1의 10점은 채점자 판정이었으나
> (*"유보가 남아 5점 문언에 걸릴 여지가 있으나…"*), 이번 리포트는 **후보 표제 자체가
> "미확인"** 이고 확신도가 **낮음**이라 재량 여지가 없다.

**🔴 감점의 원인은 도구다 — 그러나 면제 사유가 아니다.**
도착 근거 `[Kafka] DLQ 알림 재처리 성공`(11:59:16.9Z)은 **INFO · `traceId=NONE`**.
채점자가 **수집 원본을 직접 검사**했다:

| 수집 파일 | `재처리 실패` | `재처리 성공` |
|---|---:|---:|
| `…-loki-error-warn.json` | **1건** | **0건** |
| `…-loki-trace-id.json` | 0건 | **0건** |

**실패는 들어오고 성공은 안 들어온다** — `errorWarnQuery`가 `ERROR|WARN`이라 INFO 성공 로그를
자르고, `traceIdQuery`는 `traceId=NONE`이라 못 잡는다. **에이전트는 DLQ 이야기의 실패 절반만
받았다.** [CH-3 선례](../ch-3/round-1.md)대로 **도구 결함은 점수의 원인이지 면제 사유가 아니다.**

**그래도 기록할 것: 편향된 입력에서 과잉 결론을 내지 않았다.** 실패 로그만 받았으면
*"유실"* 로 기울기 쉬운데(회차 1이 정확히 그렇게 틀렸다), lag 곡선을 근거로 **유실을 부정**했다.

---

### 5) 오귀인 — **5 / 5**

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **5** | Kafka·풀·GC를 **수치로** 배제 + 댓글 200을 "장애 없음"으로 오판 안 함 |
| 3 | 후보로만 올리고 자력 반증 |
| 0 | 무관 컴포넌트 확정 |

**이번 리포트**

| 요건 | | 리포트가 쓴 것 |
|---|:-:|---|
| Kafka 배제 | ✅ | `kafka_brokers=1` · `up{job="kafka"}=1` 전 구간 + 발행 성공 로그 |
| 풀 배제 | ✅ | *"content-service·auth-service의 Hikari active/pending 모두 0"* |
| GC 배제 | ✅ | *"GC pause 최대 0.00027초 수준"* |
| 댓글 200 오판 | ✅ 아님 | *"댓글 저장·이벤트 발행(content)까지는 정상이었고"* — 정상 구간을 **범위 한정**에 썼지 "장애 없음" 근거로 쓰지 않았다 |

**→ 5점.** 창에 **AU-3 게이트의 500 트레이스**가 섞여 있었는데도 JWT·auth를 원인 후보로
올리지 않았다.

---

### 6) 조치 — **5 / 5**

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **5** | Mongo 복구 + **DLQ 적재분 처리 방침** + 재발 방지 |
| 3 | 복구만 |
| 0 | 무관 조치 |

**이번 리포트**

| 요건 | | 리포트가 쓴 것 |
|---|:-:|---|
| Mongo | ✅ | 조치 4 — `journalctl -u mongod` · OOM killer · 디스크로 **종료 원인** 추적 (조사 시점엔 이미 복구돼 "복구" 자체는 무의미) |
| **DLQ 적재분 처리** | ✅ | 조치 2 — *"`user.notifications.dlq` partition=0 offset=13 메시지를 수동 재처리/재발행"* **오프셋까지 특정** |
| 재발 방지 | ✅ | 조치 6~10 — 재시도 창 확대 · DLQ 최대 시도/알람 · `mongodb_up==0` 알람 · **헬스 30초 블로킹이 만든 스크랩 결측 교정** |

**→ 5점.** 조치 10에서 *"chat-service가 `content` 데이터소스를 참조하는 설정 자체도 의도된
것인지 확인"* 이라며 **문항 밖 구조 문제**까지 제기했다(요건 밖, 감점·가점 없음).

---

# 3. 🏆 §8.1 충족 — CH-1 회차 2 구성 **97.5 ± 2.5**

| 항목 | 실행 1 | **실행 2** | 편차 |
|---|---:|---:|---:|
| 1 원인 | 40 | **40** | 0 |
| 2 근거 | 25 | **25** | 0 |
| 3 탐색 | 15 | **15** | 0 |
| **4 영향** | **10** ⚠️ | **5** | **5** |
| 5 오귀인 | 5 | **5** | 0 |
| 6 조치 | 5 | **5** | 0 |
| **합계** | **100** | **95** | **5** |

**평균 97.5 · 최대편차 ±2.5 → §8.1의 ±10 한계 안이므로 인용 가능.**

**통제 변수 다섯이 고정됐고 주입만 다르다** — 문안(문자 그대로 동일) · 자(앵커 v2 + 루브릭 v3) ·
입력 모델(자연어) · 도구(B-1·B-2 적용 상태, B-16 미적용) · 모델(`claude-opus-5`).
주입은 04:58Z ↔ 11:54Z로 **약 7시간 간격의 별개 주입**이라, AP-1 쌍처럼
**실행 간 분산을 실제로 잰** 표본이다.

## 🔴 4) 영향이 또 갈렸다 — **세 번째다**

| 문항 | 실행 1 | 실행 2 | 갈린 항목 |
|---|---:|---:|---|
| AP-3 회차 2 | 100 | 95 | **4) 영향** (10 → 5) |
| **CH-1 회차 2** | **100** | **95** | **4) 영향** (10 → 5) |

**AP-3와 완전히 같은 모양이다** — 다섯 항목 편차 0, 4) 영향에서만 5점, 총점 97.5 ± 2.5.
[round-3 ㄷ·ㄹ](../round-3/README.md)이 *"실행 간 분산 전량이 이 항목에서 나왔다"* 고 적은 것이
**두 번째 문항에서 재현**됐다. 우연으로 보기 어렵다.

**다만 두 문항의 원인은 다르다.** AP-3는 *"롤백은 확정했으나 영향 범위를 유보"* 라 앵커 문언의
해상도 문제였고, **CH-1은 유보의 원인이 도구다** — 도착 신호가 수집 채널에 물리적으로 없다.
**AP-3는 앵커 개정(중간 구간 신설)으로, CH-1은 B-16으로 풀린다.** 같은 증상에 처방이 다르다.

---

# 4. 측정치

| | 탐색 | 분석 | 계 |
|---|---:|---:|---:|
| in (tok) | 46,759 | 122,236 | 168,995 |
| **컨텍스트 (in − C)** | **25,450** | **100,927** | **126,377** |
| out (tok) | 4,663 | 11,836 | 16,499 |
| 비용 | $0.4586 | $1.4262 | **$1.8848** |
| 시간 | 67.1s | 173.1s | **240.3s** |

- **`C` = 21,309 `█ 실측`** (그 회차 프로브). CLI 2.1.220.
- `contextChars` 184,283 · `cacheRead` 18,133 · `cacheCreation` 104,101 · `llmTurns=1`.
- 수집: trace 30,369B / 28 spans(무절삭) · errorWarn 41,142B · traceId 23,495B · metrics 89,085B.
- 수집 실패 1건 — content 401 rate(**11회 연속**, 결함 7).

> ⚠️ **실행 1과 토큰을 직접 비교하지 않는다.** 실행 1의 `C`는 **26,576**이었고 이번은 **21,309**로,
> 같은 날 안에서 −5,267 움직였다([결함 8 `C` 진동](../round-3/README.md)). 컨텍스트 토큰은
> 각 회차 프로브로 뺐으므로 개별 값은 유효하지만, **차이를 개선/악화로 읽으면 안 된다.**

---

# 5. 다음 회차로 넘기는 것

| | 항목 | 근거 |
|---|---|---|
| 1 | **B-16 최우선 상향** — 도착·성공 신호(INFO)를 수집 채널에 넣는다 | 이 회차 감점 **5점 전액**이 여기 걸렸고, 신호의 부재를 채점자가 **수집 원본 검사로 확정**했다 |
| 2 | 결함 11의 두 번째 축 — **`traceId=NONE` 로그** | DLQ 컨슈머 스레드 로그에 traceId가 없어 `traceIdQuery`로도 못 잡는다. **AU-3 결함 18과 같은 뿌리** |
| 3 | 앵커 4) 영향에 **중간 구간** 신설 검토 | *"유실 부정 + 도착 미확정"* 이 5점(단순 유보)과 같은 칸에 들어간다. 이 리포트는 **관측으로 유실을 부정**했는데 판단 유보와 동점이다 |
| 4 | 결함 7 — 401 rate 쿼리 **11회 연속** 빈 시리즈 | B-13 |

**주입 없이 되는 것**: 1·2·3 전부. 이 회차 데이터가 보존기간 안이면 B-16 적용 후 같은 문안으로
재조사해 **4) 영향이 10으로 오르는지**가 곧 B-16의 델타다.
