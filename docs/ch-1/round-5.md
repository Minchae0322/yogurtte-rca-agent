# CH-1 회차 5 · **실행 2 — 100 / 100 (판정 0건)** · 🏆 §8.1 N=2 성립 (93 ± 7)

> **실행 1은 86, 실행 2는 100.** 문안·앵커·도구·모델이 같고 **같은 주입 데이터**를 봤는데 14점이 갈렸다.
> 🔴 **원인은 능력이 아니라 채널이다** — 두 실행 다 로그 error/warn 채널이 **1000줄에서 잘렸고**(B-39),
> 실행 2만 **traceId 채널이 정답 traceId를 잡아** 도착 신호가 살아 들어왔다.
> 실행 1의 문서·리포트 원본은 삭제됐다(2026-08-05) — 점수와 채널 감사 수치는 이 문서와 채점 대장에만 남는다.

## 한눈 요약

| | |
|---|---|
| **실제 원인** | MongoDB 다운 → chat 컨슈머 저장 실패 → 재시도 **4회**(각 30초) → **DLQ 적재** → **재처리 성공 07:57:43.993** (발행 07:53:38.61 → **지연 4분 5초 · 유실 0**) |
| **입력** | 자연어 *"최근 1시간 안에 댓글 알림이 안 왔다는 제보가 있어요. 확인해줘"* — 실행 1과 **문자 그대로 동일** · 창 미지정 |
| **주입** | `mongodb_up=0` **07:53:26~07:58:26Z**(step 30s) · 실행 1과 **같은 주입 데이터** |
| 🔴 **창 오염** | **실행 2 창에만 장애가 하나 더 들어왔다** — chat-service 파드 부재 **08:14~08:23**(`qwjfl` 마지막 샘플 08:13:57 → 신규 `dnbs9` 08:23:57), `notification-processors` p3 **lag 0→25→0**. CH-2형 사건이고 CH-1 주입과 무관하다 |
| **탐색 결과** | `"최근 1시간"` 자력 파싱(EXACT) · 좁힌 창 **07:35:00~08:28:31Z(53.5분)** · 대상 `content`+`chat` · 후보 13개 중 **10개 선택** |
| **§8 채점** | **100 / 100 · 판정 0건** — 원인 40 · 근거 25 · 탐색 15 · **영향 10** · 오귀인 5 · 조치 5 |
| **모델·토큰** | `claude-opus-5` · `num_turns=1` · 🔴 `overheadTokens = -1`(프로브 실패) → 토큰은 `▓ 추정`뿐 |
| **비용·시간** | 탐색 $0.2352 · 61.0s + 분석 $2.0700 · 390.8s = **$2.3052 · E2E 456s** |
| **보고서 전문** | [round-5-rca-report.md](round-5-rca-report.md) · [json](round-5-rca-report.json) |

## 채점 — 앵커 v2 (anchors-v2.md CH-1 · 루브릭 v3)

| # | 항목 | 배점 | **실행 2** | 실행 1 | 한 줄 |
|---|---|---:|---:|---:|---|
| 1 | 원인 적중 | 40 | **40** | 40 | Mongo → 실패 → 재시도 4회 → DLQ → 재처리까지 **offset 단위로** |
| 2 | 근거 | 25 | **25** | 18 | **DLQ 발행 로그 원문** + 재시도 4회 각 30초 상한 대조 + Kafka 수치 배제 |
| 3 | 탐색 | 15 | **15** | 15 | T EXACT · S = `mongodb_up`+lag+댓글 에러 · B = 정상 인프라 대조 |
| 4 | 영향 판정 | 10 | **10** | 5 | *"최종 상태: 성공(총 4분 5초 지연)"* + 푸시 `success=1` `messageId` |
| 5 | 오귀인 | 5 | **5** | 3 | Kafka·풀·Redis·MySQL·auth를 **수치로** 배제 · 401은 *"후보 아님"* 으로 격하 |
| 6 | 조치 | 5 | **5** | 5 | 피해별 복구 가능성 표 + FCM 재발송 + 재발 방지 6항목 |
| | **합계** | **100** | **100** | **86** | **평균 93 ± 7 — §8.1 인용 가능**(±10 이내) |

### 1) 원인 40 · 2) 근거 25

한 traceId(`6a72ec02…`) 안에서 전 경로를 시각으로 잇는다:

> 발행 `07:53:38.614` → 실패 1회차 `07:54:08.649` → 2회차 `07:54:39.714` → 3회차 `07:55:10.734` →
> 4회차 `07:55:41.754` → `DLQ 발행: user.notifications -> user.notifications.dlq (partition=3 offset=1090)`
> → DLQ 1회차 실패 `07:56:12.593`(*1분 후 재시도*) → **DLQ 재처리 성공 `07:57:43.993`**

- **상한 대조**: `Remaining time: 29999 ms` = 30초 상한 ↔ span 실측 `30.009 / 30.002 / 30.002 / 30.002초` → **전부 만료**, `재시도 소진 - recoverer 처리 완료`
- **Kafka·발행측 배제**: `kafka_brokers=1` · `up{job=kafka}=1` 전 구간 · 발행 자체는 성공
- 부수: JDBC `connection` span이 **재시도 4건 모두 `rollback`, 성공 건만 `commit`**

**→ 실행 1이 18점이던 두 사유(재시도 N 미확정 · Kafka 배제 없음)가 둘 다 해소됐다. 25점.**

### 4) 영향 10 — **결함 11이 처음으로 "도구가 살려서" 해소됐다**

> *"최종 상태: **성공 (총 4분 5초 지연)**"* · offset 1098은 푸시까지 `success=1`
> (`messageId=…1785918156682743`) · *"이 장애만으로는 '안 왔다'가 아니라 '4분 늦게 왔다'"*

제보를 관측으로 뒤집었고 **10분 내 도착**을 초 단위로 확정했다.

### 5) 오귀인 5 · 6) 조치 5

- 배제 집합: `hikaricp active/pending` 전 구간 0 · `up{redis|kafka|mongodb}`=1 · Redis `KEYS` 최대 86.6ms · auth 정상
- 창 안의 401(07:35~07:45, 시크릿 드리프트 잔재)을 **"보조 관측, 후보 아님"** 으로 격하하고 *"401 메트릭 미수집 → 규모 판정 불가"* 를 명시 — **지어내지 않았다**
- 조치는 **피해 4종별 복구 가능/불가 표** + 컨슈머 다중화 + 셀렉션 타임아웃 단축 + lag·`mongodb_up`·푸시 실패 알람 신설

### 🔴 창에 들어온 다른 장애 둘도 갈라냈다 (앵커 밖 · 가점 없음)

| 사건 | 리포트 판정 |
|---|---|
| **파드 부재 08:14~08:23** | 1순위로 올려 **트레이스로 지연 정량화** — offset 1098 발행 `08:14:09.565` → 수신 `08:22:17.125` = **8분 8초**. 적체 25건 전량 소비돼 **유실 아님** |
| **FCM `INTERNAL` 1건** (offset 1105) | *"재시도 없이 폐기 → 실제 미전달"* · **14초 전 같은 토큰이 `success=1`** 이라 토큰 문제가 아닌 일시 5xx로 갈랐다 |

**CH-1 주입분은 도착했고, 진짜 미전달 1건은 다른 원인이었다** — 이 분리가 4)의 만점을 지탱한다.

## 🔴 14점 차의 기계적 원인 — 채널 감사 (채점자 실측)

| | 실행 1 | **실행 2** |
|---|---|---|
| error/warn 채널 | 1000줄 · **07:31:40~07:54:34에서 절삭** | 1000줄 · **07:35:04~07:55:24에서 절삭** |
| 그 안의 `DLQ 발행` / `재처리 성공` | **0 / 0** | **0 / 0** |
| traceId 채널 | 32,546B · 헬스체크 트레이스 | **77,519B · 132줄 — `DLQ 발행` 1 · `재처리 성공` 1 · `알림 처리 실패` 4** |
| 4) 영향 | 5 | **10** |

**B-39(`limit=1000` + `direction=forward`)는 두 실행 다 발생했다.** 갈린 것은 **traceId 채널이
어느 트레이스를 잡았느냐**다 — 실행 1은 헬스체크 트레이스를, 실행 2는 알림 경로 트레이스를 잡았다.

> **그래서 이 100점은 도구가 고쳐져서 나온 것이 아니다.** 같은 결함을 안고 **운이 반대로 붙은 것**이고,
> [B-39](../round-6/README.md)와 B-16(도착 신호 명시 수집)이 필요한 이유가 오히려 강해졌다.
> **±7의 편차 자체가 "채널 선택이 점수를 정한다"는 증거다.**

## 토큰 — 🔴 `▓ 추정`만 가능

`overheadTokens = -1`(프로브 실패, 실행 1은 29,426 실측). 두 실행의 토큰 축은 **비교 불가**다.

| 단계 | 총 `in` | 문자 | ② `chars × 0.524` `▓ 추정` |
|---|---:|---:|---:|
| 탐색 | 35,148 | 8,252 | ≈ 4,324 |
| 분석 | 199,180 | 328,240 | ≈ 171,998 |

- 수집: trace **189,638B / 203 spans(상위 span만)** · errwarn 294,428B · traceId 77,519B · metrics 332,440B · 컨텍스트 **327,435자**
- **창이 53.5분으로 실행 1(44.4분)보다 넓다** — 파드 부재 사건이 창에 들어오면서 후보가 13개로 늘었다.
  비용도 $1.84 → **$2.31**.

## 다음 회차로 넘기는 것

1. 🔴 **B-39가 1순위임이 재확인됐다** — 두 실행 다 절삭이 걸렸고, 점수는 traceId 채널의 우연이 갈랐다.
2. **B-16(도착 신호 명시 수집)** — 채널 운에 맡기지 않으려면 이것이 정답이다.
3. **창 오염 기록** — 실행 2 창의 파드 부재(08:14~08:23)는 CH-1 주입이 아니다.
   §8.1 평균은 유지하되(문안·자·도구 동일), **AU-2 실행 2·AU-3 회차 5에 이어 세 번째 오염 사례**다.
4. **프로브 실패가 두 회차 중 한 번꼴로 난다** — 토큰 축을 잃는다. AU-2 회차 5에서 제안한
   **CLI 버전 기록·불일치 시 무효 표시**를 올린다.
