# CH-1 회차 3 — **93 / 100** · 자는 회차 2와 **같다** · 🔴 **결함 11 재현**

## 한눈 요약

| | |
|---|---|
| **실제 원인** | MongoDB(`172.31.46.124:27017`) 다운 → chat-service `user.notifications` 컨슈머의 알림 저장 실패 → 재시도 3회 소진 → **DLQ 적재**. 복구 후 재처리로 도착 |
| **입력** | **traceId 없음 · 창도 없음.** 자연어 한 줄 — *"최근 1시간 안에 댓글 알림이 안 왔다는 제보가 있어요. 확인해줘"* (회차 2·2b와 문자 그대로 동일) |
| **주입** | 2026-08-03 **14:24:14Z** 셧다운 → `mongodb_up=0` **14:24:39~14:29:24Z**(약 4분 45초) · 갈래 B(≥5분급 → DLQ) |
| **탐색 결과** | `"최근 1시간"` → **13:36:09~14:36:09Z 자력 파싱** · 대상 `chat-service` · **후보 13개 중 7개 선택** · `mongodb_up` 후보(INC-9) 선택 |
| **에이전트 파악 원인** | 1순위(확신도 **높음**) *"MongoDB 셧다운/거부로 알림 저장 실패 → **재시도 3회 소진 후 DLQ 유입**"*. 2순위(중간) 복구 후 파티션 3 컨슈머 정지. 3순위(낮음) auth 호출 거부 — **"별개 장애"로 명시** |
| **§8 채점** | **93 / 100** — 원인 40 · 근거 25(판정) · 탐색 15 · **영향 5**(판정) · 오귀인 3(판정) · 조치 5. 앵커 [anchors-v2 CH-1](../../../toy-content/docs/chaos/anchors-v2.md) `add5aff` **무수정** |
| **모델** | `claude-opus-5` · `num_turns=1` · 라벨 정상 |
| **비용·시간** | 탐색 $0.5150 · 45.0s + 분석 $2.2683 · 111.2s = **$2.7833 · E2E 170.1s** — **프로젝트 최고 비용**(직전 최고 $2.5740, CH-3 2b) |
| **에이전트 보고서 전문** | [round-3-rca-report.md](round-3-rca-report.md) · [json](round-3-rca-report.json) |

## 통제 변수 — 자는 같고 도구만 다르다

| | 회차 2 구성 (실행 1·2) | **회차 3** | |
|---|---|---|---|
| 질문 문안 | *"최근 1시간 안에 댓글 알림이 안 왔다는 제보가 있어요. 확인해줘"* | **문자 그대로 동일** | 충족 |
| 시간창 | 질문에서 파싱 | **동일**(창 미지정 — T가 시험됨) | 충족 |
| 자(앵커) | `anchors-v2.md` CH-1 (`add5aff`) | **동일 커밋 · CH-1 구역 워킹트리 무수정** | 충족 |
| 루브릭 | v3 6항목 | **동일** | 충족 |
| 도구 | 회차 2 상태 | **+ B-9·10·11·15·17·18·20·25·26·28 + DEBUG 제외** | **다름** |
| 주입 | Mongo 다운 ≥5분 | **동일 사양 · 신규 주입**(4분 45초) | 충족 |

**AP-3 회차 3과 같은 성격이다** — 자가 같으므로 **도구 델타로 읽을 수 있다.**

## 채점 — 앵커 v2 (anchors-v2.md CH-1 · 루브릭 v3)

| # | 항목 | 배점 | 점수 | 한 줄 | 회차 2 / 2b |
|---|---|---:|---:|---|---:|
| 1 | 원인 적중 | 40 | **40** | Mongo 다운 → 재시도 3회 소진 → **DLQ 유입** 문언 그대로 | 40 / 40 |
| 2 | 근거 | 25 | **25** (판정) | 예외 원문 · 재시도 3회 · **30초를 셀렉션 타임아웃과 대조** · Kafka 배제. DLQ는 트레이스가 아니라 **메트릭으로** 입증 | 25 / 25 |
| 3 | 탐색 | 15 | **15** | T 자력 파싱 · **S = `mongodb_up` 후보 선택**(앵커 택일 요건) · B = 정상 파티션 대조 | 15 / 15 |
| 4 | 영향 판정 | 10 | **5** (판정) | **도착을 확인 못 했다** — 도착 신호가 컨텍스트에 도달하지 않음(결함 11 재현) | 10 / 10 |
| 5 | 오귀인 | 5 | **3** (판정) | Kafka는 수치 배제, **풀·GC는 미배제**. auth 건은 *"별개 장애"* 로 명시 후 반증 | 5 / 5 |
| 6 | 조치 | 5 | **5** | Mongo 원인 확인 + **DLQ 적재분 처리 방침** + 재발 방지(타임아웃·프로브 분리) | 5 / 5 |
| | **합계** | **100** | **93** | 2) 반대 시 86 · 4) 반대 시 88 · 5) 반대 시 95 | 100 / 95 |

---

### 1) 원인 적중 — **40 / 40**

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **40** | Mongo 다운 → 컨슈머 실패 → 재시도 소진 → **DLQ 적재**까지 메커니즘 |
| 30 | Mongo 다운 확정, DLQ 경로 미언급 |
| 20 | "chat이 이상하다"까지 |
| 0 | Kafka·content 지목 |

**이번 리포트**

| 요건 | 판정 | 리포트가 쓴 것 |
|---|:-:|---|
| Mongo 다운 | 충족 | 서두에서 *"chat-service가 알림을 저장하는 MongoDB(172.31.46.124:27017)가 14:24~14:29 UTC 동안 다운"* |
| 컨슈머 실패 | 충족 | *"`user.notifications` 컨슈머가 저장에 실패"* · 스택으로 `UserNotificationService.saveNotification:82` 특정 |
| 재시도 소진 | 충족 | *"**재시도 3회 소진**"* + `[config] user-notification 처리 실패 1회차/2회차/3회차` 원문 |
| **DLQ 적재** | 충족 | 1순위 제목이 *"재시도 3회 소진 후 **DLQ 유입**(직접 원인)"* |

**→ 40점.** 30 문언(*"DLQ 경로 미언급"*)과 갈리는 지점이 DLQ인데 **1순위 제목에 명시**했다.
회차 2·2b와 **세 번째 재현**이다.

---

### 2) 근거 — **25 / 25** (채점자 판정)

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **25** | Mongo 예외 → 재시도 N회(각 30.0s를 `serverSelectionTimeoutMS`와 대조) → **DLQ producer span을 한 트레이스로 연결** + Kafka·발행측을 관측으로 배제 |
| 18 | 연결은 했으나 **배제 논증 없음** |
| 10 | DLQ 로그만 |
| 0 | 근거 없음 |

**이번 리포트**

| 요건 | 판정 | 리포트가 쓴 것 |
|---|:-:|---|
| Mongo 예외 | 충족 | `MongoNodeIsRecoveringException: Command failed with error 11600 (InterruptedAtShutdown)` → 이후 `AnnotatedConnectException: Connection refused` **전환까지** 축자 |
| 재시도 N회 | 충족 | `1회차/2회차/3회차: topic=user.notifications partition=3 offset=1045` |
| **30.0s ↔ 셀렉션 타임아웃 대조** | 충족 | *"`secured request` span이 전부 **정확히 30.00~30.01초**… 헬스체크가 **Mongo 서버 셀렉션 타임아웃 30초를 그대로 먹고 있음**"* + 조치 5에서 `serverSelectionTimeoutMS` 명시 |
| **DLQ를 한 트레이스로 연결** | **대체 경로** | 트레이스가 아니라 **DLQ 전용 토픽 lag**으로 입증 — *"`kafka_consumergroup_lag{consumergroup=notification-recovery, topic=user.notifications.dlq}` 이 **14:27:39~14:30:24 구간에만 1**, 그 외 0"* |
| Kafka·발행측 배제 | 충족 | *"Kafka 자체는 정상 — `up{job=kafka}=1`, `kafka_brokers=1` 전 구간 상수"* |

**→ 25점 (판정).** 18 문언과 갈리는 지점이 *"배제 논증"* 인데 **했다.**
남은 것은 DLQ 입증 **경로**이고, 트레이스 대신 **DLQ 전용 토픽의 lag이 그 구간에만 1**이라는 것은
같은 사실(적재됐다)을 담은 **동치 근거**다. §8.2 충분 근거 원칙이고, CH-2 앵커가 이미
*"또는 동등한 대체 경로(**메트릭 고고학 인정**)"* 로 같은 취지를 명문화하고 있다.

> **⚠️ 반대 판정(18) 시 총점 86.** 반대 논거는 CH-1 앵커에는 CH-2와 달리 *"동등한 대체 경로"*
> 문구가 **없다**는 것이다. 다만 그것은 앵커 간 서술 차이이지 요건의 실질 차이로 보기 어렵고,
> §8.2가 전 문항 공통 원칙이므로 인정 쪽을 택했다.

---

### 3) 탐색 — **15 / 15**

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **15** | T 정확 + **S = 컨슈머 Mongo 실패 error 트레이스 또는 `mongodb_up`=0 구간** + B(정상 구간 컨슈머 처리 대조) |
| 12 / 8 / 4 / 0 | §3 공통 |

**이번 리포트**

| 요건 | 판정 | 근거 |
|---|:-:|---|
| **T** 시간창 | 충족 | **창을 주지 않았다.** `"최근 1시간"` → `13:36:09~14:36:09Z` 자력 파싱, 주입(`14:24:14Z`)·복구(`14:29:24Z`) **모두 포함** |
| **S** 대상 선정 | 충족 | **INC-9 `mongodb \| mongodb_up` 선택** — 앵커의 택일 요건 *"또는 `mongodb_up`=0 구간"* **그대로**. 함께 **INC-8 `chat-service \| ERROR/WARN`**(컨슈머 실패 로그 버킷)도 선택 |
| **B** 대조군 | 충족 | *"다른 파티션(0,1,2,4,5)은 **전 구간 0**"* · *"`13:36:09Z~14:25:24Z` 0 → 장애 중 상승"* — 정상 구간 대조 자력 확보 |

**→ 15점.** 앵커가 택일로 열어 둔 두 경로 중 **메트릭 경로를 정확히 골랐다.**

> **⚠️ 대표 traceId는 빗나갔다.** 탐색이 지목한 `6a70a49244908ca8f15be0b4d7a168b5`는
> **에러가 없는 `secured request` 4-span 트레이스**다(채점자가 Tempo에서 직접 확인).
> 앵커 S가 *"또는 `mongodb_up`=0 구간"* 을 열어 뒀고 그것을 골랐으므로 요건은 충족이지만,
> **대표 선정 자체는 이번에도 근거가 약하다** — AP-1 회차 3이 남긴 같은 지적의 두 번째 재현이다.

> **후보를 7개 고르자 창 축소가 무효화됐다.** 합집합 창이 **스윕 창 전체(60분)** 와 같아졌고,
> 지목 traceId가 **20건**으로 늘어 분석 컨텍스트가 **363,268자**(프로젝트 최대)가 됐다.
> 아래 토큰 절 참조.

---

### 4) 영향 판정 — **5 / 10** (채점자 판정) · 🔴 결함 11 재현

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **10** | **DLQ 경유 재처리로 10분 내 도착** — 제보("안 왔다")를 관측으로 뒤집음 |
| **5** | **도착 여부 판단 유보** |
| 0 | **"유실"로 판정** |

**이번 리포트**

| 요건 | 판정 | 리포트가 쓴 것 |
|---|:-:|---|
| 도착 확인 | **미충족** | *"**DLQ 처리 결과**: `notification-recovery` 랩이 1→0으로 돌아온 것만 보이고, **재처리 성공/사용자 전달 여부를 확인할 로그가 없음**"* |
| 유실 확정 | **안 함** | *"판단 불가한 항목(**데이터 부족**)"* 절로 분리. 유실이라 쓰지 않았다 |
| 부수 | — | 후보 2에서 *"복구 이후에 발생한 댓글 알림은 아직 소비되지 않은 상태"* 라고 **미전달 지속**을 주장 — 0점(유실 판정)에 가까워지는 서술이다 |

**→ 5점 (판정).** 도착을 **명시적으로 유보**했으므로 5 문언 그대로다.
**반대 판정(0) 시 총점 88** — 후보 2의 *"미전달이 계속되는 원인"* 을 유실 판정으로 읽는 경우다.

> **🔴 채점자 실측 — 도착은 실재했고, 신호가 컨텍스트에 도달하지 않았다.**
>
> | 시각 | 레벨 | 로그 |
> |---|---|---|
> | 14:26:16 | ERROR | `[kafka] 알림 처리 실패: userId=7, type=BATTLE_ITEM_COMMENT` |
> | 14:28:50 | **INFO** | `[kafka] DLQ 알림 재처리: userId=7` |
> | 14:29:20 | ERROR | `[kafka] DLQ 알림 재처리 실패 (1분 후 재시도)` |
> | **14:30:20** | **INFO** | **`[kafka] DLQ 알림 재처리 성공: userId=7`** ← 주입 후 **약 6분**, 앵커의 *"10분 내"* 충족 |
>
> **네 줄이 전부 같은 traceId(`6a70a4cbf41848fcfa14ba00fe4a02f8`)를 달고 있다.**
> 그런데 리포트 `evidence.logSamples` 60줄 중 이 traceId 줄은 **4줄뿐이고 전부
> `Connection refused` ERROR**다. `재처리 성공`·`알림 처리 완료`·`WebSocket 알림 전송 완료`는
> **0건** — 즉 **INFO 도착 신호가 컨텍스트에 실리지 않았다.**
>
> **결함 11의 두 번째 재현이다.** 회차 2가 *"도착 근거가 INFO라 수집 채널 밖 → 4) 만점이
> 구조적으로 도달 곤란(B-16)"* 이라고 적은 그대로다. **DEBUG 제외 변경으로 traceId 채널이
> INFO를 통과시키게 됐지만**, 이 트레이스가 **지목 20건에 들지 못해** 그 채널이 걸리지 않았고
> ERROR/WARN 채널만 4줄을 건졌다. **B-16이 여전히 필요하다.**

---

### 5) 오귀인 없음 — **3 / 5** (채점자 판정)

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **5** | **Kafka·풀·GC를 수치로 배제** + 댓글 200을 "장애 없음"으로 오판 안 함 |
| 3 | 후보로만 올리고 자력 반증 |
| 0 | 무관 컴포넌트 확정 |

**이번 리포트**

| 요건 | 판정 | 근거 |
|---|:-:|---|
| Kafka 수치 배제 | 충족 | *"`up{job=kafka}=1`, `kafka_brokers=1` 전 구간 상수"* |
| **풀·GC 수치 배제** | **미충족** | `hikaricp_*`는 *"14:32:39Z까지만 존재"* 라는 **결측 서술로만** 등장하고 배제 논증이 아니다. **GC 지표는 언급 자체가 없다** |
| 무관 컴포넌트 미지목 | 충족 | auth 건을 후보 3으로 올리되 제목에 *"댓글 알림 경로와는 **무관해 보이는 별개 장애**"* 라 쓰고, *"호출 그래프 어디에도 chat-service → auth-service 엣지가 없음"* 등 **반증 3건** + 확신도 낮음 |

**→ 3점 (판정).** 5점 문언이 요구한 3종(Kafka·풀·GC) 중 **하나만** 수치로 배제했다.
3점 문언(*"후보로만 올리고 자력 반증"*)은 후보 3의 처리와 정확히 일치한다.
**반대 판정(5) 시 총점 95** — 근거는 *"Mongo 원인이 확정적이라 풀·GC를 배제할 필요가 없었다"* 이고,
회차 2·2b는 같은 자리에서 5점이었다.

> **회차 2 대비 유일한 실질 하락 지점이다.** 회차 2는 *"Kafka·풀·GC 전부 수치로 배제"* 였다.

---

### 6) 조치 — **5 / 5**

**채점 기준**

| 점수 | 요건 |
|---:|---|
| **5** | Mongo 복구 + **DLQ 적재분 처리 방침** + 재발 방지 |
| 3 | 복구만 |
| 0 | 무관 조치 |

**이번 리포트**

| 요건 | 판정 | 리포트가 쓴 것 |
|---|:-:|---|
| Mongo 복구 | 충족 | 조치 1 — shutdown 사유(수동 재시작/OOM/디스크) 확인 + 프로세스 상태 확보 |
| **DLQ 적재분 처리 방침** | 충족 | 조치 4 — *"`user.notifications.dlq`의 **offset 1045**(userId=7) 메시지가 재처리되어 실제 저장·전달됐는지 Mongo 컬렉션에서 직접 조회. **미저장이면 DLQ 재처리 트리거**"* |
| 재발 방지 | 충족 | 조치 5 — *"`serverSelectionTimeoutMS`/`connectTimeoutMS`를 프로브 타임아웃보다 짧게, **liveness/readiness 프로브가 Mongo 의존 헬스에 묶이지 않도록 분리**"* |

**→ 5점.** 부가로 조치 2가 **현재진행형 영향**(파티션 3 랩 23)을 최우선으로 올렸고,
조치 6이 auth 건을 별건 추적으로 분리했다.

---

## 회차 2 구성과의 대조 — 자가 같아 도구 델타로 읽힌다

| 항목 | 회차 2 | 회차 2b | **회차 3** |
|---|---:|---:|---:|
| 1 원인 | 40 | 40 | **40** |
| 2 근거 | 25 | 25 | **25** |
| 3 탐색 | 15 | 15 | **15** |
| 4 영향 | 10 | 10 | **5** |
| 5 오귀인 | 5 | 5 | **3** |
| 6 조치 | 5 | 5 | **5** |
| **합계** | **100** | **95** | **93** |

**네 항목이 재현됐고 갈린 둘이 4)·5)다.**

- **4)는 도구 문제다** — 도착 신호가 실재하는데 컨텍스트에 안 왔다(위 실측). 회차 2는
  그 신호에 도달해 *"오차 2초"* 로 짚었다. **같은 문항에서 도구가 후퇴한 것처럼 보이지만,
  회차 2는 창이 좁아 traceId 채널이 걸렸고 이번엔 후보 7개로 지목이 20건이 되며 밀렸다.**
- **5)는 서술 범위 문제다** — 풀·GC 배제를 이번엔 안 썼다.

**즉 이번 회차의 −7은 능력 저하가 아니라 ⓐ 도착 신호 미도달 ⓑ 배제 서술 축소이고,
ⓐ의 원인이 후보 다중 선택 → 창 미축소 → 지목 폭증이라는 사슬이다.**

## 토큰 — `█ 실측` · **프로젝트 최대 컨텍스트**

`coverage.overheadTokens = 21,629`(AP-3 회차 3·AU-3 회차 2와 동일).

| 단계 | 총 `in` | 문자 | ① `in − 21,629` | ② `chars × 0.524` | 격차 |
|---|---:|---:|---:|---:|---:|
| 탐색 | 56,823 | 64,220 (컨텍스트 62,821 + 프롬프트 1,399) | **35,194** | 33,651 | +4.6% |
| 분석 | 211,000 | 363,844 (컨텍스트 363,268 + 프롬프트 576) | **189,371** | 190,654 | −0.7% |
| **합계** | **267,823** | | **≈ 224,565** | | |

- **분석 컨텍스트 363,268자 — 프로젝트 최대**(직전 AP-1 회차 3의 282,951자).
  **B-25(메트릭 요약)를 켜고도** 이 크기다.
- **비용 $2.7833으로 최고 경신**(직전 $2.5740).
- 원인은 **후보 7개 동시 선택**이다 — 합집합 창이 스윕 창 전체(60분)와 같아져 창 축소가
  무효화됐고, 지목 traceId가 **20건**·수집 트레이스 **20건 / 104 spans / 98,114 B**가 됐다.

| 채널 | 바이트 |
|---|---:|
| 트레이스 | 98,114 B / 104 spans / 20건 |
| 메트릭 | 요약 적재(B-25) |
| 로그 | ERROR/WARN + traceId 채널 |

- `num_turns=1` 유지 · `llmModel=claude-opus-5` 정상.

## 수집 실패 1건 — B-13 **네 회차 연속**

```
Metric 'sum(rate(http_server_requests_seconds_count{application="content-service", status="401"}[1m]))'
returned no series in this window; skipped.
```

에이전트가 조치 6으로 *"왜 수집되지 않는지도 함께 점검"* 을 냈다.
[round-4 B-29](../round-4/README.md)가 이미 이 진단을 정정해 뒀다(라벨 오류가 아니라 무트래픽).

## 다음 회차로 넘기는 것

1. **🔴 B-16(도착·성공 신호 수집)이 여전히 필요하다 — 근거가 두 번째로 나왔다.**
   도착 로그가 **INFO**이고 후보 트레이스와 **같은 traceId를 달고 있는데도** 도달하지 못했다.
   처방 갈래 가(**traceId 단위 전량 조회**)가 DEBUG 제외로 절반 구현됐으나,
   **그 traceId가 수집 대상에 들어야만** 작동한다. → 스윕이 찾은 후보 트레이스의 traceId를
   **수집 상한과 무관하게 로그 조회 대상에 넣는** 방향을 검토한다.
2. **🔴 후보 다중 선택이 창 축소를 무효화한다 — 신규 결함.**
   후보 7개를 고르자 합집합 창 = 스윕 창이 되어 컨텍스트가 **363,268자**로 불었고,
   지목 20건이 상한을 채워 **정답 트레이스가 밀렸다**(위 1번과 같은 뿌리).
   → 합집합이 스윕 창의 N%를 넘으면 경고하거나, 후보별 창을 유지한 채 수집을 분할한다.
3. **대표 traceId 선정 근거 부재 — 두 번째 재현**(AP-1 회차 3에 이어). 이번에도 **에러 없는
   트레이스**가 대표가 됐다. 앵커가 택일을 열어 둬 3)은 만점이었지만 다음에도 그러리란 보장이 없다.
4. **CH-1 회차 3 2차 실행** — N=1이라 §8.1상 인용 보류. 4)·5)가 갈린 항목이라 관전 지점이 분명하다.

## 🔴 이 점수를 회차 2와 평균 내지 않는다 (§8.1)

| | 회차 2 구성 (N=2) | **회차 3 (N=1)** |
|---|---|---|
| 도구 | 회차 2 상태 | **+ B-9·10·11·15·17·18·20·25·26·28 + DEBUG 제외** |
| 자 | `anchors-v2` CH-1 `add5aff` | **동일** |
| 주입 | 07-29 | **08-03 신규** |

**자는 같지만 도구가 다르므로 별도 구성이다.** 회차 2 구성은 97.5 ± 2.5로 인용 가능하고,
이 회차는 **N=1이라 인용 보류**다.
